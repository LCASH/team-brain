# Daily Log: 2026-05-13 (lcash)

## Sessions

## Memory Maintenance

### Session (09:32) [value-betting-scanner]

(Background agents completed - see results above)

**Assistant:****Context:** Architecting and shipping per-soft-book pick lineage for the v3 value-betting scanner to decouple slow books from fast ones.

**Key Exchanges:**
- Discussed why global "fresh state" model breaks when adding 4th+ soft book with different latencies — each (market, soft_book) should be its own async time series
- Inventoried 43 live books: 13 AU soft bookies + 6 prediction markets flowing via OpticOdds SSE; only Bet365 uses own scraper
- Confirmed tracker already had per-book keying (`_generate_pick_id` includes `soft_book_id`) — half the architecture was pre-built
- Discovered `captured_at = push_time` was intentional as a "scraper alive" heartbeat, needed to split into two concerns

**Decisions Made:**
- **16 books get own pick lineage**: 10 AU softs (365, 900-903, 907-911) + 6 prediction markets (950, 970-971, 980-982); thin books (Dabble, PickleBet, Unibet AU, Surge, BetIT) deferred
- **No pair-freshness gate needed**: sharps stream real-time via SSE, so only soft-side freshness matters
- **Phased rollout**: 0 (captured_at fix) → 1 (diff cache) → 2 (ALL_SOFT_TARGETS) → 3 (latency dashboard) — shipped all 4 in one session
- Created `ALL_SOFT_TARGETS = SOFT_BOOK_IDS | PREDICTION_MARKET_BOOK_IDS` as the canonical iteration set

**Lessons Learned:**
- `datetime.utcnow()` on a non-UTC host (Eve is AEST +10) produces naive datetimes that `.timestamp()` interprets as local time → 10-hour offset. Fix: use `datetime.now(timezone.utc)` or explicit UTC conversion
- The push loop was firing `store.change_event` every 5s regardless of whether odds changed — "phantom freshness" that made the tracker grind through no-op cycles. Diff cache keyed on `market_key` eliminates this
- Prediction markets have dual-use in the system: sharp-substitute anchor (Crypto Edge theory) AND soft target — per-book lineage handles both without special-casing

**Action Items:**
- Verify Eve restart completed and `captured_at` values are now correct (UTC-aware)
- Confirm push-loop diff cache is logging skip counts properly
- Validate coverage.html latency panel renders with real data
- Commit all changes across `startup.py`, `server/tracker.py`, `ev_scanner/models.py`, `coverage.html`
- Monitor per-book refresh latency metrics over first 24h to baseline each book's natural cadence

### Session (10:02) [value-betting-scanner]

**Context:** Shipping 4-phase "temporal honesty" overhaul to the v3 value-betting push loop (Bet365 scraper → tracker pipeline), plus dashboard latency panel and prediction-market coverage expansion.

**Key Exchanges:**
- Deployed all 4 phases: (0) Bet365 captured_at uses real wizard parse time, (1) push loop skips unchanged markets via diff cache, (2) tracker iterates ALL_SOFT_TARGETS (16 books including prediction markets), (3) coverage.html per-book latency panel
- Verified captured_at ages are now 10–15s (was 0s — gate was a no-op previously)
- Investigated and fixed three bugs discovered during live verification

**Decisions Made:**
- **Diff cache key must include `line`**: `make_market_key()` deliberately drops `line` for engine cross-line matching, but this causes alt lines (e.g., Over-25.5 vs Over-26.5) to flap-overwrite each other. Fixed by keying on `(fixture_id, market_key, line)`.
- **Added `is_main` to diff key**: MLB v4 wizard emits both O/U records (`is_main=True`) and milestone records (`is_main=False`) at the same `(player, prop, side, line)` with different odds — collide without this dimension.
- **First-wins dedupe for sub-market collisions**: MLB wizard exposes sub-markets ("anytime hit" / "first-inning hit") that the parser collapses into one prop_type, producing 3 different odds (1.714, 4.75, 20.0) for the same logical key. Pragmatic fix: first-wins in push loop + WARNING log for upstream visibility. Upstream parser disambiguation deferred.
- **Selective commit strategy**: startup.py had parallel Betr/TAB/TabTouch work mixed in; reset to HEAD, re-applied only push-loop changes, committed, then restored parallel work via stash.

**Lessons Learned:**
- `datetime.utcnow().timestamp()` on an AEST host returns a timestamp 10h off — Eve runs AEST, so naive-UTC interpretation was silently producing stale timestamps. Must use timezone-aware conversion.
- When reusing internal utility functions (like `make_market_key`) for a new purpose (diff caching), verify what dimensions they intentionally drop — those omissions may be fine for the original use case but create flap in the new one.
- MLB v4 wizard data has sub-market granularity that the current parser flattens; any downstream consumer that assumes uniqueness at `(player, prop, side, line)` will see phantom churn. This is a known upstream gap.
- Live verification on real data (not just unit tests) caught all 3 bugs — each was invisible in isolation but obvious under production load.

**Action Items:**
- Commit Bug 3 dedupe fix (first-wins + WARNING log) — code is deployed but not yet committed
- Commit TRACKER_TEMPORAL_HONESTY.md progression doc (~270 lines)
- **Near-term**: upstream parser-level disambiguation for MLB sub-markets (anytime vs first-inning vs specific-inning props)
- **Medium-term**: `book_capture_events` table for persistent latency tracking, alerting on stale books
- Verify MLB push counts stabilize after dedupe fix (initial observations show 185 collisions deduped cleanly)

### Memory Flush (10:38) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (11:32) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (12:07) [value-betting-scanner]

**Context:** Continued temporal-honesty implementation on the value-betting scanner — verified shipped work, created forward roadmap, and executed Sprint 1 (per-book lineage verification, collision WARNING throttle, MLB sub-market parser fix).

**Key Exchanges:**
- Five queued wakeup tasks confirmed as already shipped in commits `85d0a98` (temporal honesty foundation) and `b21268c` (MLB sub-market dedupe + progress doc)
- Forward roadmap created: 5 sprints prioritized by ROI, recommended weekly cadence
- Sprint 1.1: 9 of 16 books producing picks; 7 missing due to theory config gaps (not code bugs)
- Sprint 1.3: Root-caused MLB sub-market collision — wizard packs Hits/Singles/Doubles/Triples into one EV section, `S2` field carries actual stat but was being flattened to `prop_type="Hits"`

**Decisions Made:**
- Build infrastructure during quiet game windows ("net ready to cast")
- Sprint order: close temporal-honesty loops → verify lineage → AU direct scrapers
- MLB parser fix: extract actual stat from wizard `S2` field (e.g. "Over 0.5 Singles") as canonical `prop_type` instead of using EV-section name
- Collision WARNING throttled to once/60s/sport (Sprint 1.2) — keeps visibility without log spam

**Lessons Learned:**
- MLB wizard `S2` field is the disambiguator for sub-markets (Hits vs Singles vs Doubles vs Triples) — the EV section header collapses them all under "Player Hits"
- 7 books with zero picks traced to theory `soft_books` config, not missing scraper data — 5 MLB books (907, 909, 911, 980-982) simply not listed in theory soft_books arrays
- OpticOdds already exposes Singles/Doubles/Triples as separate prop_types (288/288/145 markets) — the parser just needed to match

**Action Items:**
- **Pending user decision:** UPDATE 5 MLB theory rows in Supabase to add AU books (907, 909, 911) + prediction markets (980, 981, 982) to `soft_books`
- AFL/NRL need theories built from scratch (separate workstream, out of scope for current sprint)
- Verify post-deploy collision count drops to near-zero and Singles/Doubles/Triples appear as own prop_types on Bet365
- Sprint 2.1 next: per-book CLV materialized view + dashboard panel (~1 day)
- Sprint 4.1: land AU direct scrapers (Betr, TAB, TABtouch) sitting on disk

### Session (13:25) [value-betting-scanner]

**Context:** Value betting scanner v3 — Sprint 1 (pipeline integrity) and Sprint 4.1 (AU scrapers deployment)

**Key Exchanges:**
- Traced why 7 of 16 books produced zero picks: 5 MLB books missing from theory `soft_books` lists, 910 Betr too thin, AFL/NRL have zero theories
- Diagnosed MLB wizard sub-market collision: S2 field carries actual stat (Hits/Singles/Doubles/Triples) but parser was collapsing all under EV-section name "Hits"
- Post-fix: collisions dropped 88% (93→11 keys, 222→30 dupes); new prop_types (Singles, Doubles, Triples, Batter Walks) now flowing for Bet365

**Decisions Made:**
- Extract actual stat from wizard S2 field as canonical `prop_type` instead of using EV-section header — fixes sub-market flattening
- AU scrapers (Betr/TAB/TabTouch) gated behind `ENABLE_BETR`/`ENABLE_TAB`/`ENABLE_TABTOUCH` env vars (default off) — safe to ship without active AU games
- MLB theory `soft_books` update (add 907, 909, 911, 980-982) deferred to user decision — config change, not code

**Lessons Learned:**
- 229 "data anomalies" on coverage page were actually sub-stat collisions (e.g., Triples odds 26.0 appearing as Hits @ 26.0) — parser fix dissolved them to just 2 genuine outliers
- Milestone records (`is_main=False`) cause residual collisions (11 keys) — separate concern from main prop_type fix
- One-shot debug dump to `/tmp/vb_debug/` was effective diagnostic pattern: deploy, wait for collision, inspect raw wizard body, then remove

**Action Items:**
- Pending user call: UPDATE MLB theory `soft_books` in Supabase to include AU books (907, 909, 911) and prediction markets (980, 981, 982)
- AFL/NRL theories need separate workstream (zero theories exist)
- Toggle `ENABLE_BETR`/`ENABLE_TAB`/`ENABLE_TABTOUCH` when AU games resume
- Remaining 11 milestone collisions to investigate (low priority)
- Committed as `8de9a90`, tagged `tracker-temporal-honesty-v1`

