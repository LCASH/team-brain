# Daily Log: 2026-05-13 (lcash)

## Sessions

## Memory Maintenance

### Session (09:32) [value-betting-scanner]

(Background agents completed - see results above)

**Assistant:****Context:** Architecting and shipping per-soft-book pick lineage for the v3 value-betting scanner to decouple slow books from fast ones.

**Key Exchanges:**
- Discussed why global "fresh state" model breaks when adding 4th+ soft book with different latencies — each (market, soft_book) should be its own async time series
- Inventoried 43 live books: 13 AU soft bookies + 6 prediction markets flowing via OpticOdds SSE; only Bet365 uses own scraper
- Confirmed tracker already had per-book keying (`_generate_pick_id` includes `soft_book_id`) — half the architecture was pre-built
- Discovered `captured_at = push_time` was intentional as a "scraper alive" heartbeat, needed to split into two concerns

**Decisions Made:**
- **16 books get own pick lineage**: 10 AU softs (365, 900-903, 907-911) + 6 prediction markets (950, 970-971, 980-982); thin books (Dabble, PickleBet, Unibet AU, Surge, BetIT) deferred
- **No pair-freshness gate needed**: sharps stream real-time via SSE, so only soft-side freshness matters
- **Phased rollout**: 0 (captured_at fix) → 1 (diff cache) → 2 (ALL_SOFT_TARGETS) → 3 (latency dashboard) — shipped all 4 in one session
- Created `ALL_SOFT_TARGETS = SOFT_BOOK_IDS | PREDICTION_MARKET_BOOK_IDS` as the canonical iteration set

**Lessons Learned:**
- `datetime.utcnow()` on a non-UTC host (Eve is AEST +10) produces naive datetimes that `.timestamp()` interprets as local time → 10-hour offset. Fix: use `datetime.now(timezone.utc)` or explicit UTC conversion
- The push loop was firing `store.change_event` every 5s regardless of whether odds changed — "phantom freshness" that made the tracker grind through no-op cycles. Diff cache keyed on `market_key` eliminates this
- Prediction markets have dual-use in the system: sharp-substitute anchor (Crypto Edge theory) AND soft target — per-book lineage handles both without special-casing

**Action Items:**
- Verify Eve restart completed and `captured_at` values are now correct (UTC-aware)
- Confirm push-loop diff cache is logging skip counts properly
- Validate coverage.html latency panel renders with real data
- Commit all changes across `startup.py`, `server/tracker.py`, `ev_scanner/models.py`, `coverage.html`
- Monitor per-book refresh latency metrics over first 24h to baseline each book's natural cadence

