# Daily Log: 2026-06-01 (lcash)

## Sessions

## Memory Maintenance

### Memory Flush (00:37) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (01:09) [superwin]

**Context:** Overnight monitoring of SuperWin VPS value-betting infrastructure; discovered and fixed a bug in the Tier 2.5 cookie-mint code.

**Key Exchanges:**
- TAB/TabTouch dropping to 0 was confirmed as normal end-of-day racing pattern (22:50 AEST), not a regression
- Cron-stop at 13:00 UTC only disabled 4/9 bookies (bet365, betr, boostbet, pointsbet) — 5 remained enabled; noted as intentional config change, not our bug

**Decisions Made:**
- Used the quiet overnight period to audit Tier 2.5 cookie-mint code that had never fired in production

**Lessons Learned:**
- **Tier 2.5 cookie-mint was a no-op in production**: `page.goto` with `wait_until="domcontentloaded"` fires before Akamai JS sets cookies, so `cookie_jar` was always empty and the `if cookie_jar:` guard silently skipped. Fix: poll for `AKA_A2` cookie up to 8s before harvesting.
- **SSH exit code 255 ≠ real failure**: deploy task reported 255 but service was active and code was deployed — the SSH session dropped during the trailing `sleep 5`. Always verify with a follow-up `systemctl is-active` check.

**Action Items:**
- Validate Tier 2.5 fix actually fires at 9am AEST cron restart (~23:00 UTC) when real racing data is available

### Session (01:57) [superwin]

**Context:** Overnight monitoring of SuperWin scraper on VPS; identified and fixed a bug in Tier 2.5 cookie-mint code.

**Key Exchanges:**
- Noticed Tier 2.5 cookie-mint never fired in production despite being deployed
- Root-caused: cookies harvested immediately after `domcontentloaded` event, before Akamai JS finishes setting `AKA_A2` cookie — making the `if cookie_jar:` check always false
- Deploy reported exit code 255 but was actually successful (SSH session timeout artifact during trailing `sleep 5`)

**Decisions Made:**
- Fix: poll for `AKA_A2` cookie up to 8s before harvesting (was instant/no-op before)
- Real validation deferred to 9am AEST cron restart (~23:00 UTC) when racing data exists

**Lessons Learned:**
- **Tier 2.5 cookie-mint timing bug:** `wait_until="domcontentloaded"` fires before JS-set cookies exist. Must poll/wait for target cookies to appear, not just assume they're ready after navigation completes.
- SSH exit code 255 doesn't necessarily mean the remote command failed — it can be the SSH session dropping after the work completed (especially with idle timeouts)
- Gap identified: no automated verification that Tier 2.5 actually fires in production (silent no-op went unnoticed)

**Action Items:**
- Verify Tier 2.5 fires correctly after 9am AEST cron restart with real racing data
- Consider adding alerting/logging when Tier 2.5 is consistently skipped (detect silent no-ops)

### Session (02:56) [superwin]

**Context:** Overnight passive monitoring of SuperWin after deploying Tier 2.5 cookie-mint v2 fix

**Key Exchanges:**
- Background deploy task reported exit code 255 failure, but was actually successful — the 255 was an SSH session timeout artifact during the trailing `sleep 5`. Verified service active and fix markers present.
- Captured the Tier 2.5 cookie-mint v1 → v2 lesson into memory (timing bug: was firing before Akamai JS finished setting `AKA_A2` cookie, fix polls up to 8s)

**Lessons Learned:**
- SSH session drops causing exit code 255 on background tasks are cosmetic — always verify actual service state rather than trusting the exit code
- Discord "alerts" can be false positives — the 14 alerts in 24h were actually regular heartbeat POSTs (HTTP 204), not real alerts

**Action Items:**
- Verify Tier 2.5 fires correctly with real racing data at 9am AEST cron restart (~23:00 UTC)

### Session (03:28) [superwin]

**Context:** Overnight passive monitoring of SuperWin crash watcher on VPS

**Key Exchanges:**
- Captured Tier 2.5 cookie-mint v1→v2 lesson into knowledge base memory, noting timing-bug specifics and a verification gap
- Ran a quiet-period health snapshot to check for creeping issues before the 9am AEST cron restart
- Health check confirmed: snapshot file present (36MB, .bak rotated), 14 Discord "alerts" were actually regular heartbeat POSTs (HTTP 204), no real crashes, memory & disk stable

**Lessons Learned:**
- SSH drops every ~15 min overnight are routine behavior for the resilient watcher — always reconnects and rearms successfully
- Discord heartbeat POSTs returning 204 can look like alerts in log counts but are normal

**Action Items:**
- Confirm Tier 2.5 fires with real data after the 9am AEST cron restart

### Memory Flush (03:59) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (05:02) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (05:34) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (06:06) [superwin]

**Context:** Monitoring SuperWin crash watcher during overnight dormant period (cron-stop active)

**Key Exchanges:**
- Investigated stale data warnings; confirmed all 9 racing bookies `enabled=False` in DB — cron-stop working as intended
- Verified Tier 2.5 Akamai cookie-mint path still returns HTTP 200, 30 races, single `AKA_A2` cookie after SSH drops

**Lessons Learned:**
- Freshness alarm doesn't differentiate "intentionally off overnight" from "broken" — stale warnings during cron-stop window are expected, not actionable
- Should check cron-stop state before investigating stale data during known dormant hours (avoid false-alarm drills)

### Memory Flush (06:42) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (07:13) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (07:28) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (07:46) [superwin]

**Context:** Diagnosing why some bookies stayed enabled overnight when the 13:00 UTC stop-cron should have disabled them, plus planning day-to-day reliability tracking for the v3 value-betting scanner.

**Key Exchanges:**
- TAB's low race count (80 vs 420 discovered) traced to COLD_CHUNK=80 cap per cycle combined with DISCOVERY_INTERVAL=3600 — full coverage takes hours to ramp
- Forensic finding: all 9 bookies' `updated_at` = 21:26 UTC May 31 (the manual restart overwrote timestamps), so cron's actual effect is unverifiable
- The `racing_schedule.sh` Python output parser has been broken since creation — escaped quotes in shell heredoc cause SyntaxError every iteration, making all POST responses invisible in logs
- The cron DID fire at 13:00 UTC (proven by `/var/log/superwin_schedule.log`) but we can't confirm which POSTs succeeded

**Decisions Made:**
- Root cause of "bookies slipping" = schedule script is unobservable (broken parser hides HTTP responses)
- Fix priority: (1) make cron observable, (2) add state-dump after cron, (3) rewrite /diagnose for day-to-day use, (4) create reliability log MD, (5) fix TAB COLD_CHUNK regression

**Lessons Learned:**
- Shell heredocs with embedded Python f-strings silently break — use shell-native parsing for cron scripts
- Supabase `UPDATE WHERE slug=$2` silently no-ops on slug mismatch (no error surfaced)
- `updated_at` as forensic evidence is useless if manual restarts overwrite it
- The schedule script covers all 9 bookies (CLAUDE.md was outdated saying only 3)

**Action Items:**
- Fix Python parser in `racing_schedule.sh` so each POST logs `slug: enabled=X (HTTP NNN)`
- Add final state-dump (GET /admin/bookies) at end of each cron run
- Rewrite `/diagnose` skill for day-to-day reliability focus (odds freshness, race counts, WS health, schedule-cron audit)
- Create `docs/day-to-day-reliability.md` (or similar) with dated entries per /diagnose run
- Fix TAB COLD_CHUNK=80 regression (discovery finds 420 but only 80 fetched per cycle)
- Update CLAUDE.md references that say cron only manages 3 bookies

### Session (08:22) [superwin]

**Context:** Debugging value-betting scanner bookie reliability — cron observability, /diagnose rewrite, and TAB race coverage investigation

**Key Exchanges:**
- Investigated why some bookies (tab, tabtouch, betfair, sportsbet) stayed `enabled=true` overnight when the 13:00 UTC stop-cron should have disabled them
- Discovered the cron's Python output parser has been broken since day one (escaped quotes → SyntaxError), making every cron run unobservable
- Fixed `racing_schedule.sh` on VPS — new parser logs `slug: HTTP 200, enabled=True/False` per iteration + final state-dump
- Built and deployed lean `/diagnose` skill (replacing 1600-line deep-check version), seeded first reliability log entry
- Deep-dived TAB "regression" — discovered `/status` race_count only reflects last-batch-size, NOT catalogue presence. TAB actually has 824 races in catalogue (healthy), not the 0-40 shown in /status

**Decisions Made:**
- Cron observability fix deployed immediately — next 13:00 UTC stop is the first auditable run
- Old 1600-line SKILL.md moved to `DEEP_CHECKS.md` as reference; lean 5-probe `/diagnose` is now primary
- TAB code changes (COLD_CHUNK tuning, DISCOVERY_INTERVAL) **NOT deployed** — the "regression" was a false alarm from a misleading metric
- Pending decision: fix only the diagnostic to use catalogue-presence, OR also normalise the `race_count` field in tab/sportsbet/tabtouch adapters so /status tells the truth

**Lessons Learned:**
- `/status` race_count is inconsistent across bookies: betfair/neds/boostbet report catalogue-total, but tab/sportsbet/tabtouch report last-batch-size — this created a false regression signal
- The cron schedule script was effectively a black box — Python heredoc inside bash shell escaping silently broke JSON parsing, hiding all POST outcomes
- Always verify the metric source before acting on a "regression" — the catalogue had 824 TAB races while /status showed 0

**Action Items:**
- Fix `/diagnose` to query `/odds` endpoint for real per-bookie catalogue presence instead of trusting `/status` race_count
- Decide whether to also normalise `race_count` in tab/sportsbet/tabtouch adapter code (affects Discord watcher + dashboard)
- Betfair over-counting flagged (1304 vs expected ~400-600 AU+NZ daily) — needs investigation
- Sportsbet: 0 push clients still unexplained
- Pointsbet: 35% baseline failure rate (79/228 race detail calls failing)
- Verify next 13:00 UTC cron produces clean audit log entry

### Session (08:53) [superwin]

**Context:** Building Phase 1 of the WarRoom→Venom automated betting pipeline — settings library, UI components, and DB schema

**Key Exchanges:**
- Built a settings library (`app/src/lib/settings/`) with types, merge logic, migration helpers, and 42 test cases
- Created three UI components: SettingsEditor (per-account modal), TagSettingsEditor (per-tag modal), EffectiveSettingsPanel (merged read-only view in WarRoom)
- Applied three new jsonb columns to live Supabase: `betting_accounts.settings`, `betting_accounts.edge_config`, `tags.settings` + GIN indexes
- Regenerated `database.types.ts` from live schema, removed all `(supabase as any)` casts
- Ran programmatic round-trip verification: settings write, edge_config write, GIN containment query (Venom's hot path), tags.settings write — all passed

**Decisions Made:**
- `edge_config` is a precomputed per-edge eligibility map (`{slug: boolean}`) — Venom reads it on every pick for <500ms without joins
- Tag settings merge strategy: arrays INTERSECT with account settings, account wins on scalar conflicts
- Phase 2 should bias toward tag-first flows (tag settings are the operator-time sink)

**Lessons Learned:**
- Supabase `Json` type has a strict index signature that TypeScript interfaces don't satisfy — need boundary casts
- `ADD COLUMN IF NOT EXISTS` migrations are safe to re-run and can be applied directly via Supabase SQL editor for speed

**Action Items:**
- Phase 2: WarRoom preparer (autonomous service deriving `edge_config` from tags+settings+edge_schedule) — needs detailed plan expansion
- Plan files: `~/.claude/plans/okay-here-goes-a-lucky-waterfall.md` (master), `~/.claude/plans/automated-betting/02-phase-2-warroom-preparer.md` (outline)
- Commit `36a904a` on local main, 55 ahead of origin — push when ready
- Pre-existing in-flight changes still in working tree (settings.json, gitignore, SuperWinApp.tsx, 6 untracked files)

### Session (10:45) [server]

**Context:** Building WarRoom→Venom pipeline for automated betting system (TAKEOVER project), specifically Phase 1 (DB schema + settings UI) completed and Phase 2 (WarRoom preparer) started

**Key Exchanges:**
- Phase 1 migrations applied to live Supabase: `betting_accounts.settings`, `betting_accounts.edge_config`, `tags.settings` (jsonb columns + GIN indexes)
- Programmatic round-trip verification confirmed all writes/reads work, including GIN containment query (Venom's hot path)
- Phase 2 compute function built and tested (31 tests pass), but rule logic is based on assumptions that need validation

**Decisions Made:**
- Tag settings are the operator-time sink — Phase 2 WarRoom preparer UX should bias toward tag-first flows and merge-attribution debugging
- Phase 1 committed as `36a904a` (55 commits ahead of origin/main), pre-existing in-flight work preserved in working tree
- Stopped and course-corrected on Phase 2 after discovering edge_tags in DB ("low balance", "low blance", "Priming") don't match assumed eligibility model

**Lessons Learned:**
- Edge tags in the DB are operational markers (warnings/flags), NOT positive eligibility signals — rule 6 ("must have edge_tag") was wrong
- Don't assume DB semantics from column names — validate against real data first (the 582ms snapshot sweep was good practice, but rule interpretation was guesswork)
- The `account_edge_tags` table has a typo: "low blance" (#10b981) alongside "low balance" (#f59e0b)

**Action Items:**
- **BLOCKING:** User needs to answer 5 Phase 2 design questions before compute rules can be finalized:
  1. What does `account_edge_tags` membership mean for eligibility?
  2. Which `edge_schedule.edge_key` entries gate which `superwin_bookie_modes`?
  3. Are all 6 current modes horse_racing only?
  4. Should `status='priming'` accounts be eligible?
  5. What's the actual "account ready to bet" gate beyond status/promotional_status?
- Plan files for reference: `~/.claude/plans/okay-here-goes-a-lucky-waterfall.md` (master), `~/.claude/plans/automated-betting/02-phase-2-warroom-preparer.md` (detail)

### Session (11:20) [value-betting-scanner]

**Context:** Multi-issue investigation and fix session for the v3 value-betting scanner, covering tracker sentinel verification, MLB fixture resolution, and code hygiene commits.

**Key Exchanges:**
- Phase 1 (tracker.py sharp-alt sentinels): Investigated missing sentinel alerts — turned out to be SSH/grep transients, not actual missing code. All 6 sentinels confirmed present on Eve across 3 consecutive runs, byte-for-byte matching working tree.
- Phase 2 (Athletics fixture canon): Diagnosed that only "Oakland Athletics vs Yankees" failed fixture resolution out of 14 games. Root cause: 2025 MLB team rename from "Oakland Athletics" to just "Athletics". MLB Stats API confirmed. Fixed with `_TEAM_ALIASES` dict + `_canonicalize_fixture` helper in resolver.py.
- Phase 3 (Betr MLB 0% stable-ID): Documented as expected behavior in healthcheck skill note, added `skip_no_player` threshold for future monitoring.
- Phase 4/5 (diff-key collisions, phantom-EV path 2): Deferred per plan — no data loss from collisions, phantom-EV root cause documented but not capped.

**Decisions Made:**
- Added symmetric alias approach for team renames (stores both forms in accepted set) — future-proofs against bookie vs API name drift
- Deferred collision instrumentation (Issue 4) until evidence shows data loss
- Deferred phantom-EV path 2 fix (POISSON_PROPS missing MLB count props) per standing direction
- Committed 4 focused commits instead of one monolith: resolver fix, MLB market_id fix, engine one-sided fallback gate, alt-rollout cluster

**Lessons Learned:**
- SSH/grep health checks can produce transient false negatives — need multiple consecutive reads before flagging sentinel issues
- 2025 MLB rename ("Oakland Athletics" → "Athletics") broke fixture matching; bookies lag behind official API naming
- Alt-rollout code was live on Eve but uncommitted in git for 4 days — alignment gap between deploy and source control
- Healthcheck skill updated with 5 lessons-learned from today's debugging

**Action Items:**
- Verify Athletics resolution on next MLB resolver cycle (hourly) — expect `skip_no_player` to drop ~60 picks/cycle
- AU bookie adapter changes, name canon audit, PM Edge theories remain uncommitted in separate workstreams
- Today's plan doc + skill update potentially still uncommitted — user was asked if they want those committed separately

### Session (11:50) [superwin]

**Context:** Designing the v2 `betting_accounts.settings` JSON schema for Venom automated betting gating

**Key Exchanges:**
- Analyzed three existing tables (`superwin_bookie_modes`, `edge_schedule`, `account_edge_tags`) and determined none are suitable for Venom's "should this account fire this edge" gating — each serves a different purpose (VA shift planning, legacy mirrors, etc.)
- Landed on consolidating all gating logic into a single `settings` JSONB column on `betting_accounts`, eliminating reliance on scattered tables
- Proposed three allocation models (pure derived / allocation table / hybrid) — user chose to skip all of them and just beef up the settings JSON directly

**Decisions Made:**
- **Single column, single source of truth** — all Venom eligibility lives in `betting_accounts.settings`, no tag-based merging, no `edge_schedule` reads
- **`venom_enabled` master switch** at top level — one boolean to pull an account from the entire pipeline
- **Explicit opt-in model** — edges not listed in the `edges` map are NOT enabled (safe default for new SuperWin edges)
- **`defaults` block + per-edge override** pattern — DRY config where each edge entry inherits from defaults and can override any field
- **No bookie keys in JSON** — `bookmaker_id` on the row already scopes to bookie
- **Dropped `tags.settings` overlay merging** from Phase 1 — tags revert to free-form notes
- **Edge key = SuperWin slug directly** — no composite keys needed since slugs are already bookie-scoped

**Lessons Learned:**
- `superwin_bookie_modes` only had 6 rows vs 25 actual edges — would have silently ignored 19 edges if Phase 2 relied on it
- `edge_schedule` labels reveal it's VA shift planning ("Tab Dupe Lambo", "Boosts Identification"), not bet-firing gating
- The "closest working precedent" for account-edge allocation was `mug_strategy_groups.account_ids[]` — but even that's manual operator memory

**Action Items:**
- Resolve open design questions A–E (time-to-jump per-edge, stake method per-edge, enabled:false+reason pattern, multiple time windows, per-edge cooldowns)
- Once locked: rewrite TypeScript types, migration function, SettingsEditor UI, bump to schema_version 2
- Future: "copy settings from another account" button as replacement for bulk tag-based config
- Future: address `account_personas` separately (realism concern, not operations)

### Memory Flush (12:24) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (12:55) [superwin]

**Context:** Designing the automated betting system's v2 settings schema and planning the handoff of remaining phases to Jay.

**Key Exchanges:**
- Evaluated 4 architectures for hit-counting at dispatch (Postgres query, Redis, in-memory counter, watchdog flag). Concluded **in-memory counter in Venom + watchdog for visibility** is optimal — sub-microsecond lookups, zero race risk, no new infra.
- Settings stores only `max_hits_per_day` (config), never counts. Counts live in Venom RAM, seeded from `bet_log` on startup, with 60s watchdog reconciliation.
- Decided on clean break (option A) from v1 schema — v1 was 1 day old with zero real data, so in-place replacement over backwards-compatible coexistence.

**Decisions Made:**
- **In-memory counter architecture locked:** dict keyed by `(account_id, slug)`, per-key asyncio lock, decrement on bookie failure, watchdog as monitor not gate.
- **v2 schema shipped** (commit `f120d0e`): `venom_enabled` master toggle + `defaults` block + dynamic `edges` map keyed by SuperWin slug. Removed merge library, tag editor, preview panel.
- **Phases 3-6 handed off to Jay:** User confirmed remaining phases (session refresher, Venom dispatcher, live deployment, bookie expansion) are Jay's domain. Handoff doc written at `~/.claude/plans/automated-betting/00-jay-handoff.md`.

**Lessons Learned:**
- v2 schema design made Phase 2 (WarRoom preparer) mostly obsolete — settings IS the source of truth Venom reads directly, collapsing ~2 weeks of planned work into Phase 4.
- Bookie HTTP roundtrip (200-1000ms) dwarfs all other dispatch latency; obsessing over sub-ms counter speed is the wrong focus — contention/race behavior matters more.
- In-memory counter pattern enables free extensions: per-edge cooldowns, daily P&L tracking, bookie-wide rate limiting, backpressure signalling to SuperWin.

**Action Items:**
- Decide where Jay's handoff doc should live (TAKEOVER dev branch, Notion, or separate repo)
- Get access to Jay's repo/project for contract surface validation
- Decide: push 56 local commits to TAKEOVER-dev now or wait?
- Decide: quick v2 operator validation before Jay starts, or skip?
- `edge_config` column cleanup — drop now or defer?

### Memory Flush (13:27) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (14:00) [superwin]

**Context:** Deep architecture walkthrough of TAKEOVER's Venom dispatch engine — how picks flow from SuperWin through RAM cache to bookie HTTP placement across 1000+ accounts.

**Key Exchanges:**
- Detailed end-to-end flow documented: SuperWin pick → Venom cache lookup → eligibility check → bookie HTTP fire → bet log write
- Watchdog reconciliation architecture: 3-layer cache consistency (Realtime push ~100ms, pulsing watchdog every 30s, full reload on boot/disconnect)
- Theoretical throughput analysis for 1000 accounts firing on one pick: ~400ms median, ~1s for 99%, Venom logic adds <10ms

**Decisions Made:**
- 1-phone-per-account is the correct topology — sharing phones serializes bets and becomes the bottleneck
- Only one DB write in the hot path (edge_bet_logs INSERT after bookie confirms) — settings are read-only from RAM
- Batched INSERTs for bet logs (~30-80ms for 1000 rows) to avoid saturating Supabase connection pool

**Lessons Learned:**
- Bookie HTTP latency (250-500ms) is the irreducible floor — everything else is optimized away
- Per-IP rate limits don't apply because each account routes through a unique phone/SIM/IP
- The real ceiling isn't throughput — it's bookie detection tolerance (betting patterns, frequency). Personas, mug bets, and rotation matter more than speed
- DO droplet CPU only becomes a bottleneck at ~10,000+ concurrent dispatches; Supabase write pool at ~5,000 concurrent INSERTs
- Python GIL is not a factor because asyncio dispatch is I/O-bound

**Action Items:**
- Consider per-bookie token bucket throttling for graceful rate-limit handling
- Real-world account distribution matters more than theoretical max — ~150 accounts per bookie, not 1000

### Memory Flush (14:09) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (14:13) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (14:14) [superwin]

**Context:** Diagnosing VB scanner alert (false alarm) and attempting to revive the bet365 racing scraper on Eve

**Key Exchanges:**
- VB scanner alert at 11:05 was a **false alarm** — v3 restarted at 11:04:33, ping-watcher caught the ~60s startup gap. Eve fully healthy: 3972 markets, both orchestrators cycling
- Bet365 racing scraper (`bet365_stream.py`) has been **dead for ~7 days** — 10,080 supervisor crash loops trying to connect to CDP port 39191 where no Chrome is listening
- The "next-to-jump → venue → race tab walk" architecture Luke described is **already built and shipped** (commit 1209170, May 22) — `meeting_tab_walk` discovers ~11 meetings via WS, hash-navs to each venue, real mouse-clicks each race tab to trigger PM market subscriptions
- AdsPower profile `k19yb91n` (bet365 racing) is in a **zombie state** — API reports "Active" but no Chrome process exists. The two working profiles are VB scanner's NBA + MLB on chrome_146; racing profile is pinned to **chrome_142** which may be the regression
- API-based zombie recovery failed repeatedly — AdsPower returns success + new port but Chrome never actually spawns

**Decisions Made:**
- Don't restart AdsPower daemon over SSH — would kill healthy VB scanner profiles
- Need manual intervention at Eve's desktop GUI to open the k19yb91n profile, then restart supervisor remotely

**Lessons Learned:**
- AdsPower zombie recovery via API doesn't always work — the documented fix (from 2026-05-18) was proven on a different profile; k19yb91n on chrome_142 has a different failure mode
- Profile pinned to older chrome version (142 vs 146) may cause silent launch failures
- Bet365 scraper was dead 7 days without anyone noticing — needs a dedicated alerting check in the watcher

**Action Items:**
- Luke needs to manually open profile `k19yb91n` via AdsPower GUI on Eve's desktop
- Once Chrome is alive, restart `bet365_stream.py` supervisor and monitor first meeting-tab-walk cycle
- Consider bumping VB watcher from 3 consecutive failures (~90s) to 5 (~150s) to suppress restart noise
- Investigate whether k19yb91n should be migrated from chrome_142 → chrome_146
- Add bet365 racing scraper liveness check to the watcher/alerting so 7-day outages don't go unnoticed again

### Session (14:29) [value-betting-scanner]

Done — all three deliverables are now written to `brain/findings/`:

| # | File | Status |
|---|------|--------|
| 1 | `2026-06-01-nba-picks-model-readiness.md` | ✅ Complete |
| 2 | `2026-06-01-mlb-picks-model-readiness.md` | ✅ Complete |
| 3 | `2026-06-01-trail-capture-infrastructure-audit.md` | ⚠️ Rate limited — needs retry |
| 4 | `2026-06-01-model-training-subset-spec.md` | ✅ Complete |
| 5 | `2026-06-01-resolver-label-integrity-audit.md` | ⚠️ Rate limited — needs retry |

**Key findings so far:**

**NBA (55K picks):**
- Data quality is excellent — all entry-time features 100% populated
- Post-resolution columns 95-97% filled
- Trail depth varies wildly (0-200+ snapshots) — needs investigation
- `avg_ev` 35% NULL is the only meaningful gap
- Ready for modeling after leakage scrub

**MLB (needs verification):**
- Similar schema, likely same quality patterns
- CO markets and Stats-API lag are MLB-specific concerns
- Batter/pitcher splits need checking

**Training subset spec (Agent 4):**
- Anchored at 2026-05-26 tracker decouple boundary
- 30+ column trust matrix with leakage flags
- Purged k-fold with 3h NBA / 4h MLB embargo windows
- Binary, multi-class, and regression label definitions

I'll re-deploy agents 3 and 5 once the rate limit clears — they cover trail-capture infrastructure and resolver/label integrity, both critical for clean training labels.

### Session (15:04) [superwin]

**Context:** Deep statistical validation of SuperWin edge-pick theories — falsifying overfit signals, confirming real edges, and deploying a 5-agent quant lens fan-out.

**Key Exchanges:**
- User corrected that run-2nd-3rd is a promo where placing = win (bonus turnover economics), not a pure loss — changed the scoring methodology
- Full fan-out of 5 quant-lens agents (microstructure, ML/leakage, probability/calibration, derivatives/hedging, statistics/regime) hunted 20 candidates, verified 9, confirmed 7

**Decisions Made:**
- **Picks are not independent** — effective sample size is ~40 days, not 27k picks. All prior CIs were ~26× too tight. Day-as-unit t-test is the honest methodology going forward.
- **Confirmed edge:** superpicks@tabtouch + cash-multi@tab flat stake (day-t ≈ 3.0)
- **Deploy Gate 1:** skip odds < 4 — kills the favorite bleeder + anti-chalk variance concentrator (−721u day). Day-t 2.78, inverse is significant bleeder.
- **Deploy Gate 3:** run-2nd-3rd only if ev_pct ≥ 10 — flips −6.7% → +32–65% OOS, monotonic
- **Deploy sizer:** bk_sz/lay_sz ∈ [2,4) → stake 1.5–2×
- **Probationary:** Monday hard-abort + weekend stake-up (only 6 Mondays of data)

**Lessons Learned:**
- Two methodology errors created the entire false-theory edifice: (a) using settlement-time ltp/bsp as staking signals = leakage; (b) treating correlated picks as independent = massively overconfident CIs
- The favorite-longshot bias is **inverted** in this dataset — longshots realize above implied WR, favorites below. "Long favorites" isn't just variance, it's negative-EV.
- ML meta-labels were strictly dominated by one hand-set odds threshold — five quant lenses confirmed every clever overlay was leakage or overfit
- OL_ltp "single most valuable signal" was 100% suspension-LTP artifact — proven dead with live detection-time data
- Confirmation Score (+40% ROI headline) collapsed to +5.2% OOS with flagship cells inverting
- run-2nd-3rd settlement bug (places booked as −1.0) contaminated all pre-05-29 data for that mode
- Full back+lay hedging is a leakage tautology (back price built from leaky ev_pct field)

**Action Items:**
- Re-confirm Gate 1 and regime filters after ~10 more holdout days accumulate (currently only ~3 holdout days / 6 Mondays)
- Memory files updated: edge-pick-theories.md banner + edge-research Learnings Catalog
- Bonus turnover economics for run-2nd-3rd live outside the pick ledger — separate tracking needed to measure full strategy ROI

### Session (15:08) [Knowted admin]

**Context:** Planning and structuring a 70-minute workshop talk on Claude Code for a founder audience (June 9th event, co-hosted with Dani)

**Key Exchanges:**
- User provided raw block structure for the talk; assistant shaped it into timed segments with gap tracking
- User corrected multiple assumptions: no "recent win" story in the opening, Antigravity IDE isn't needed (just VSCode), and GitHub needs its own explainer block
- OpenClaw section reframed: the angle is "people waste money on Mac Minis when a VPS does the same thing cheaper" — then pivot to Claude Code
- User's core positioning line for Claude Code over OpenClaw: **"quality — meaning speed and ease to outcome"**

**Decisions Made:**
- Removed Antigravity as a dedicated section — replaced with generic "use any IDE, VSCode is fine" (~3 min)
- Added a standalone GitHub explainer block (~7 min) — audience is non-technical founders
- No personal "speed-to-outcome" story — user explicitly refused this for the opening
- Demo options narrowed to three: (a) content analyzer/generator, (b) AEO/SEO blog agent (already built), (c) LinkedIn inbox manager — decision still pending
- Working rule established: assistant must prompt user for content, not generate it independently

**Lessons Learned:**
- User pattern: gives bullet-point outlines and expects assistant to structure/organize, NOT to write the actual words or invent stories
- User gets frustrated when assistant tries to produce polished content instead of asking questions — "prompt me, stop trying to make it yourself"

**Action Items:**
- User needs to pick which demo to commit to (a/b/c) and get a GitHub repo ready — this is a blocker
- Still need user's answers on: founder must-know basics (Block 2), GitHub explainer in their words (Block 4), local folder gotchas (Block 5), VPS setup details (Block 6), actual safety rules and stories (Block 7)
- Pre-work email to attendees needed 48hr before (install checklist)
- Backup screen recordings needed for: auth flow, scraper/demo run, deploy to VPS
- VA to produce audience follow-along worksheet and "Claude Code Setup Guide for Founders" takeaway PDF
- Confirm with Dani whether session is recorded

### Session (15:40) [value-betting-scanner]

I'll wait for the 4 research agents to complete before giving you results. They're running now:

1. **NBA book sharpness** - Brier scores, CLV, calibration per book
2. **MLB book sharpness** - Same metrics + re-testing prior claims against fresh data  
3. **Devig method bake-off** - mult vs add vs power vs shin across sports/props
4. **First-mover trail analysis** - Which book leads sharp consensus moves (new capability from our trail data)

I'll consolidate findings as they come back.

