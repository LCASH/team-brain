# Daily Log: 2026-05-26 (lcash)

## Sessions

## Memory Maintenance

### Memory Flush (08:17) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (08:49) [value-betting-scanner]

**Context:** Ran full v3 value-betting pipeline healthcheck to verify data cleanliness and system health.

**Key Exchanges:**
- Full healthcheck sweep returned overall GREEN status
- Eve v3 running 16h, 9301 markets, pulse 85125 (rising)
- Cache performing well (6.6s cold, 0.006-0.011s warm)
- MLB-dominant market split normal for May (8323 MLB / 926 NBA)

**Decisions Made:**
- Data is trustworthy for analysis — confirmed all lifecycle stages (intake → tracking → resolving → CLV calc) functioning correctly
- Trustworthy columns identified: `sharp_clv_pct_true`, `peak_ev`/`peak_at`, `optic_clv_pct`, raw `trail_entries`
- Naive `clv_pct` column still misleads on bet365 line reshuffles (known artifact, not corruption)

**Lessons Learned:**
- MLB pre-game sharp tracking p50 down to 1.6 min (was 16.7 min) — confirms recent fix working
- Two known yellows are operational housekeeping not data threats: MLB overlay drops 77% (canonicalization), MLB resolver stale 192 (draining naturally)
- Historical picks pre-fix have known biases largely addressed by side-encoding backfill + sharp-trail decoupling

**Action Items:**
- MLB canonicalization pattern still causing 77% overlay drops — same as yesterday, eventual fix needed
- MLB resolver stale count (192) continuing to drain — monitor but no action required

### Session (08:54) [TAKEOVER]

**Context:** Discord Brain project — reviewing pipeline output quality, packaging for friend onboarding, and running an Army research query on sportsbet.io.

**Key Exchanges:**
- Synth output for Betr (AU) reviewed: recency-led TL;DR working correctly, 2026-Q2 content properly surfaced, historical demoted to timeline
- FUD detector upgraded in nuance — caught synth over-extrapolating ("low-to-medium" instead of rubber-stamping), pushing back on trend-claims from post-gub frustration
- Bundled `discord-brain-private.zip` (208 MB) for friend: full source, backfilled DB (815k messages, 743 channels, 11 servers), token, build transcript, onboarding doc
- Army search on `sportsbet.io` correctly returned zero results and bailed early (~$0.25 vs $5+)

**Decisions Made:**
- Include token + full DB in private bundle (friend needs complete working system, not a clean-room setup)
- Ship build transcript (`build-session.jsonl`) so friend can understand architectural decisions
- Two bundles maintained: private (208 MB, secrets+DB) and public-safe (67 KB, code-only)

**Lessons Learned:**
- Army vocab agent correctly disambiguates similar entities (`sportsbet.io` crypto book vs `sportsbet` AU bookie) via distinct_meanings and safe/noisy alias split — prevented false positive worker spawns
- Pipeline's early-bail on zero triage hits saves ~$4.75 per null-result query — working as designed
- Indexed corpus (AU matched-bet + US sharp/DFS) has zero crypto-betting coverage; sportsbet.io gap is a coverage gap, not a pipeline failure
- FUD detector matured: now catches synth overreach instead of always rubber-stamping — validates the auditor architecture

**Action Items:**
- Friend onboarding: should replace token with his own to avoid ToS risk (same token, different IP/UA)
- Consider joining crypto-betting Discord communities if sportsbet.io coverage is desired
- Check 13 unticked servers in account for possible crypto-related communities

### Session (09:21) [superwin]

**Context:** Adding a new "sb-normal" (Sportsbet Normal Odds) racing edge to SuperWin and discovering a critical timezone bug blocking all Sportsbet merges.

**Key Exchanges:**
- User requested a new edge "sb-normal" that shows Sportsbet normal (unboosted) opportunities, and wanted to verify the system dynamically picks it up everywhere
- Audited entire codebase for hardcoded slug references — found them in scoring.py, resolver.py, and CommandFeed.tsx but none block sb-normal
- Edge inserted successfully and appeared in `/api/v1/edges` but produced 0 opportunities
- Deep investigation revealed only 1/188 sportsbet races had Betfair pairing vs 116/117 for TAB/TabTouch/betr

**Decisions Made:**
- Used `bookies: ["sportsbet"]` array filter (not `allowed_bookies` — skill doc had wrong field name, now fixed)
- Set min_ev: 1 for sb-normal (aggressive threshold to start)
- Place-market: false (win only), so `is_place_edge` hardcoding doesn't affect it

**Lessons Learned:**
- **Critical timezone bug:** Sportsbet adapter was using UTC date for `meeting_date` while all other bookies (TAB, TabTouch, Betfair, Betr) use AEST date. Since `canonical_id` includes meeting_date, Sportsbet races could NEVER merge with Betfair — making the entire bookie useless for value scanning. Fix: explicitly convert to `AEST = timezone(timedelta(hours=10))` (Brisbane has no DST)
- The criteria field for bookie filtering is `bookies` (array), NOT `allowed_bookies` — skill doc was wrong
- `scoring.py` has per-slug confidence floors and penalties (racing-value gets high-odds/mid-liq/tabtouch penalties) — new edges get defaults only
- `resolver.py:is_place_edge` is hardcoded to only `racing-mult` — any future place edge MUST be added there or picks stay PENDING forever
- Dynamic discovery works well: EdgesView, CommandFeed dropdown, scanner, REST/SSE/WS all auto-discover from DB

**Action Items:**
- Verify sb-normal opps start flowing after adapter re-cycles with timezone fix (~2 min)
- After backtest data accumulates, consider adding sb-normal-specific scoring penalties in scoring.py
- Updated add-racing-mode skill doc with: correct `bookies` field, bookie-restricted pure-odds template, and new §4b "Known hardcoded-slug references" table
- Consider whether other adapters have similar timezone issues (unlikely since TAB/TabTouch/Betr were already working)

### Session (10:09) [value-betting-scanner]

**Context:** Value betting scanner healthcheck + making resolver/backtest context-aware of recent fix cascade + diagnosing manual take system health

**Key Exchanges:**
- Full system healthcheck came back 🟢 GREEN — Eve running 16h, 9301 markets, sharp trail p50 down to 1.6 min (was 16.7 min pre-fix)
- User asked to make backtest/resolver "context aware based on absolutely everything" — led to comprehensive doc + code updates across 9 files
- Manual take system health audit revealed one real gap: `sharp_clv_pct_true` is NULL on all 5 resolved manual picks

**Decisions Made:**
- Created era-boundary table in 3 places (resolver.md, findings doc, MEMORY.md) to serve as single source of truth for which data is trustworthy from which date
- Backtest/calibration script docstrings now warn against mixing pre/post-fix MLB rows
- Pushed all changes to origin/main (commit `1d98f8f`)

**Lessons Learned:**
- Manual picks with `triggered_by='manual_topdown'` don't have a matching row in `nba_optimization_runs`, so resolver can't find sharp weights to compute `sharp_clv_pct_true` — falls through to naive soft CLV
- Dashboard fallback chain (`sharp_clv_pct_true` → `optic_clv_pct` → `synthetic_clv_pct` → `clv_pct`) means manual picks still display something, just not the devigged truth
- Two known yellows (MLB overlay drops 77%, MLB resolver stale 192) are operational housekeeping not data integrity issues — stale count is draining naturally

**Action Items:**
- Fix `sharp_clv_pct_true` NULL on manual picks — either add a `manual_topdown` row to optimization_runs with default sharp weights (~5 LOC) or add a resolver fallback for unrecognised theories
- Consider OO backfill cron for `optic_clv_pct` on manual picks
- MLB canonicalization pattern still causing 77% overlay drops (known, not regression)

### Session (10:43) [superwin]

**Context:** BoostBet bookie onboarding recon — WebSocket-first live browser investigation via AdsPower CDP

**Key Exchanges:**
- Estimated BoostBet onboarding effort based on Sportsbet lessons: best case 6hrs (WS/JSON), middle case 1.5 days (HTML scraping), worst case 3-5 days (Kasada + AU-IP block)
- User established rule: **"we always optimise for websocket"** — this reorders recon priority for all future bookie onboarding
- Static recon of boostbet.com.au completed: scanned 25 JS bundles, probed common WS/API endpoints — zero `wss://` references found
- Initiated live browser recon via AdsPower profile `k19yb91n` with Playwright CDP WebSocket monitor attached

**Decisions Made:**
- **WebSocket-first is a hard rule** for bookie onboarding — not optional. Recon always starts with WS presence test before falling back to REST/HTML
- **Static recon insufficient for WS determination** — lazy-loaded WS connections only visible via live DevTools observation
- Effort estimate for BoostBet: **~1 day most likely** (80% confidence no WS → Sportsbet-pattern HTML scraping), with 15% chance of hidden WS that would reduce to ~6hrs

**Lessons Learned:**
- BoostBet infra: **Next.js SSR + CloudFront + Cloudflare** (not Akamai despite initial JS lib match)
- BoostBet REST API fully private — `/api`, `/api/v1/*`, `/api/racing`, `/api/markets` all return 404. Data likely served via Next.js SSR fetch to an internal path
- BoostBet has a **promo/boost product** (strings found in JS bundles) — potential edge source if onboarded
- Reusable assets from Sportsbet onboarding (May 23-26): adapter skeleton, Playwright browser-bootstrap pattern, per-race chunked fetcher, Venom payload contract, dual-BookieOdds gotcha (now caught by CHECK 18a)
- Common VPS error pattern documented: `42703 column does not exist` = code-vs-schema mismatch in `_persist_picks`

**Action Items:**
- Live WS monitoring session in progress — user navigating BoostBet to detect lazy WS connections
- If WS confirmed: build streaming adapter (~6hrs)
- If no WS: fall back to Sportsbet-clone HTML scraping (~1 day)
- Consider TabTouch Kambi WS wiring as backlog candidate (currently HTTP+proxy but Kambi WS exists)

### Session (11:13) [superwin]

**Context:** Deployed and verified the racing-run-2nd-3rd-sportsbet refund-bonus edge end-to-end, then diagnosed a settlement scanner gap causing 500 stuck picks.

**Key Exchanges:**
- Full implementation shipped: scanner criteria (min_field_size, EV formula with bonus_conversion), resolver (PLACE_REFUND outcome, position_settlement branch), persistence (ev_trail with pl_lay + bn_cv), backtest endpoint, dashboard qual_rate display
- Manual EV scan against live catalogue confirmed formula is correct — no picks during Tasmanian morning meets because sportsbet prices are shorter than Betfair lay; EV only goes positive when sportsbet drifts long on metro-Saturday mid-priced runners in 10+ fields
- First real sb-run-2nd-3rd pick landed at +6.5% EV with both new DB columns populated (bf_place_lay=1.15, bonus_conversion_assumed=0.80)
- Discovered 500 picks stuck unsettled across 109 races — settlement scanner only tracks markets it's actively subscribed to at suspension time

**Decisions Made:**
- INSERT path gated on `bonus_conversion_assumed is not None` so non-refund edges aren't affected by DDL timing — removable later if we want bf_place_lay on all edges
- Edge inserted disabled, flipped to enabled only after DDL confirmed — safe rollout pattern
- Expected volume for refund-bonus edge: 5-15 picks/day, concentrated in Saturday metro + Wednesday night metro blocks

**Lessons Learned:**
- Settlement scanner has a blind spot: markets that close BEFORE a service restart get no race_results saved — no retroactive scan exists. Multiple restarts during racing hours (3-4 today for deploys) compound this into hundreds of orphan picks
- Restarts during racing hours (9am-11pm AEST) should be minimised; deploy changes outside that window when possible
- Betfair place liquidity is thin on early-morning Tasmanian meets — run-2nd-3rd edge needs metro cards with deep place books to fire

**Action Items:**
- Write a backfill scanner that re-fetches results via Betfair `listMarketBook` REST for races jumped in last N hours after any restart
- Resolve today's 500 stuck picks with a one-shot backfill
- Reduce restart frequency during racing hours — batch code changes into single deploys outside 9am-11pm AEST window
- Monitor first PLACE_REFUND settlement to confirm resolver PnL math (WIN → +(bookie_odds−1), PLACE_REFUND → −0.20, LOSE → −1)

### Memory Flush (11:14) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (11:45) [superwin]

**Context:** Onboarding BoostBet as a new sportsbook adapter in the superwin value-betting scanner (Phase 1 odds, then full day-card upgrade)

**Key Exchanges:**
- Initial adapter used `next-to-jump` endpoint — capped at 90 races (rolling window), only 54 AU/NZ races vs TAB's 197
- User requested full day-card coverage ASAP — triggered hunt for better endpoints
- Found two critical endpoints via browser network capture: `GET /v1/racing/race-card/today/A` (full day card, 447 specs) and `POST /v1/racing/event-races-by-ids` (batch fetch by ID, 20-ID limit per request)
- Final coverage: 440 races (today + tomorrow), 195/195 TAB overlap (100%), plus 30 extra NZ/tomorrow races

**Decisions Made:**
- Use `race-card/today/A` + `tomorrow/A` for discovery instead of `next-to-jump` — gets full day card + next day's early card
- Batch POST chunked at 20 IDs with asyncio.gather (~11 parallel requests for 213 races) — respects API limit while maximizing throughput
- No auth, no proxy, no browser needed for any endpoint — keeps adapter simple and fast
- Tiered cadence: hot ≤60min → 3s, warm ≤4h → 9s, cold → 45s

**Lessons Learned:**
- BoostBet's `next-to-jump` is a rolling ~6-8hr window capped at 90 — inadequate for full-day coverage
- The Race constructor must use canonical model field names, not flat-model names (caused early smoke test failure)
- `POST /v1/racing/event-races-by-ids` has a hard 20-ID batch limit — exceeding silently truncates
- BoostBet actually covers MORE races than TAB when including tomorrow's card (especially harness: 44 vs 15)
- Cumulative race_count tracking is essential — per-cycle count (9) is misleading vs true coverage (440)

**Action Items:**
- Phase 2: Investigate boost product endpoint (`/v1/benefits/dividend-boosts`) — auth-gated, needs AdsPower cookie or username/password flow
- Phase 2: Capture actual bet-placement commit payload (`/v1/betting/place-bet` POST body)
- Let adapter bake in production for a day before Phase 2 work
- Commits: `LCASH/superwin@1a25db2` (Phase 1) and `LCASH/superwin@3c29314` (day-card upgrade)

### Session (12:16) [superwin]

**Context:** Debugging and fixing the v3 value-betting settlement pipeline — picks stopped settling due to queue starvation and Betfair subscription limits.

**Key Exchanges:**
- Traced the "settlement cliff" to 2026-05-21 when cumulative orphan picks exceeded 200, completely starving the resolver's `get_unsettled_picks` query (ASC + LIMIT 200 pattern from commit `00b6827` shipped 2026-05-19)
- Fix 1 shipped: flipped resolver to `detected_at DESC` + added orphan VOID sweep (>72h). Results within 90s: today's unsettled dropped from 39→6, historical 4,431 draining at ~100/min
- Investigated residual 1-3 races/day failing (e.g. Angle Park G2 invisible to scanner while G1 settled fine). Root cause: Betfair streaming API hard-caps at 200 markets per connection
- User correctly called that 200 was Betfair's limit, not ours — confirmed via live test: `SUBSCRIPTION_LIMIT_EXCEEDED, max allowed: 200`

**Decisions Made:**
- Ship Fix 1 (DESC ordering + orphan sweep) immediately — smallest change, biggest unblock
- User pushed back on "Layer 2 = Betfair gaps" framing — correctly identified it's our architecture losing data, not Betfair failing to provide it. The subscription pipeline drops races that don't fit in the 200-slot window
- Decided to gather failure examples first before fixing, which revealed venue aliases were a red herring (fuzzy matching works, picks were just queue-starved)

**Lessons Learned:**
- `_build_subscription_list` sorts by start_time ASC and fills first 200 WIN+PLACE pairs. Morning races that haven't formally CLOSED hold slots into the afternoon, blocking late-arriving races from ever subscribing
- Betfair auth response shows `connectionsAvailable: 8` — real capacity ceiling is 8×200 = 1,600 markets via multi-connection sharding
- The `max_markets=200` comment in `betfair.py:283` was misleading — it referenced the REST `listMarketBook` batch cap, but the same 200 limit applies to streaming subscriptions (for different reasons)
- 95% of the 4,431 "stuck" picks were pure queue starvation, not settlement logic failures

**Action Items:**
- Decide between Option B (smarter eviction within single 200-market connection, ~15 lines) vs Option A (multi-connection sharding, 2+ streams) — recommendation is B first
- Option B: evict markets with start_time >90 min ago to free slots for imminent races, re-subscribe each cycle instead of waiting for CLOSED swap
- Monitor Fix 1 overnight to confirm stability before stacking subscription fix
- Files changed: `server/app/db/supabase.py:668-720`, `server/app/edges/resolver.py:42-93`

### Session (12:35) [superwin]

**Context:** Parallel recon on Pointsbet + Neds bookmakers, then building Neds WS adapter for SuperWin scanner

**Key Exchanges:**
- Ran wide-net CDP WS recon on both Pointsbet and Neds simultaneously — both confirmed WS streams
- Pointsbet uses **Azure SignalR** (`wss://push.au.pointsbet.com/client/?hub=signalrhub`), JWT-gated (3600s TTL), full day-card at `/api/racing/v4/meetings` (151KB)
- Neds uses **Socket.IO v4** (`wss://push.neds.com.au/socket.io/`), session-ID auth, GraphQL at `/gql/router`
- User chose Neds first; initial WS investigation wrongly concluded WS was in-play only
- **User challenged the conclusion** ("I see Neds odds moving crazy") — recon proved user correct

**Decisions Made:**
- Build Neds WS-first adapter (not REST polling) after confirming pre-race odds ARE pushed via WS
- Neds adapter est. ~6 hrs (Socket.IO pattern reuses TAB template); Pointsbet est. ~1-1.5 days (first SignalR adapter)

**Lessons Learned:**
- **Wrong subscribe channel = wrong conclusion.** `racing/livemarketupdated` is in-play only. The actual pre-race odds channel is `pricing/prices` with market_id subscriptions — pushed 137 frames in 90s (~1.5/sec). Always capture the browser's actual subscribe payloads before concluding a WS doesn't carry data.
- Neds Socket.IO subscribe protocol uses **per-market subscriptions**, not a global firehose: `{handler:"pricing", method:"prices", market_ids:[...]}`, `{handler:"racing", method:"marketupdated", ids:[...]}`, `{handler:"racing", method:"entrantupdated", ids:[...]}`, `{handler:"racing", method:"racecard-status", id:...}`
- Neds race discovery: paginated POST `/v2/racing/search` with page_size cap of 20 (same as BoostBet pattern). Full AU+NZ catalogue = 243 races, ~28s to paginate.
- "In-play" in AU racing = race has jumped (gates opened). Very different from pre-race fixed odds. Most SuperWin edges fire pre-race, so in-play-only WS would be nearly useless.
- **Trust domain experts (the user)** — they see odds moving on the site daily. When real-world observation contradicts a technical conclusion, re-investigate before doubling down.

**Action Items:**
- Parse Neds price payload shape and win/place market type mapping (in progress)
- Build Neds Socket.IO WS adapter using TAB template
- Register `neds-normal` + any boost edges in Supabase
- Pointsbet SignalR adapter queued as next bookie after Neds ships
- Update `/onboard-bookie` skill with Neds subscribe protocol learnings (per-market vs global subscribe patterns)

### Session (12:52) [superwin]

**Context:** Building and shipping a new Neds sportsbook adapter for the value-betting scanner (superwin project)

**Key Exchanges:**
- Decoded the full Neds protocol: Socket.io v4 websocket endpoint, REST API structure, price_type_id mappings for win/place markets
- Verified price_type_id mapping via DOM probe (win 5.00 → place 2.00, confirming 1+(W-1)/4 formula)
- Built complete adapter with paginated REST search, AU+NZ filtering, win/place price decoding

**Decisions Made:**
- Ship REST-based adapter first, defer WebSocket streaming to follow-up phase (too risky to ship both at once)
- REST parser designed to support both REST and WS price-key formats so price cache can be shared when streaming lands later
- Supabase row inserts deferred to user (write access denied to Claude)

**Lessons Learned:**
- Neds REST discovery endpoint `/v2/racing/search` only returns facets at `page.size=1` — need to probe `today`/`next-to-jump`/GraphQL variants for full data
- Price type UUIDs: Win = `940b8704-...`, Place = `7cf3eea6-...` — verified empirically, not from docs
- Entity-normalized REST model (races/markets/entrants/prices all keyed by UUID) — different pattern from other bookies
- Needed user to open a race tab twice during protocol verification — recommend pairing for new bookie reverse-engineering

**Action Items:**
- User needs to run Supabase INSERT statements for `bookies` and `edges` tables (3 rows: neds bookie, nd-normal edge, racing-run-2nd-3rd-neds edge) — column names guessed from BoostBet pattern, verify schema first
- Deploy via `/deploy`, then verify with `journalctl -u superwin -f | grep neds`
- Follow-up: implement WS streaming layer using Socket.io v4 (`wss://push.neds.com.au/socket.io`) with 4 subscribe channels per race
- Follow-up: probe alternative REST discovery endpoints (`today`, `next-to-jump`, GraphQL)
- Commit: `dbd7332` — files: `server/bookies/neds.py`, `server/app/adapters/neds.py`, `server/app/adapters/registry.py`
- Protocol notes saved to `.claude/memory/neds-protocol.md` for future sessions

### Session (13:46) [superwin]

**Context:** Onboarding two new bookmaker adapters (Neds + Pointsbet) into the Superwin value-betting scanner — full build from protocol reverse-engineering through to production deployment.

**Key Exchanges:**
- Neds protocol was fully reverse-engineered in a prior context window (Socket.io v4 WS endpoint, REST discovery, price_type_id mapping for win/place). Memory file saved at `.claude/memory/neds-protocol.md` to bridge sessions.
- Neds adapter built, smoke-tested locally (152 AU+NZ races), committed (`dbd7332`), Supabase rows inserted, deployed — 100% venue match on first production cycle.
- Pointsbet adapter built immediately after, smoke-tested (19 hot-tier races initially, scaling to 76+), committed (`9a48535`), deployed — also 100% venue match.

**Decisions Made:**
- **REST-first, WS deferred:** Both adapters use paginated REST discovery; WebSocket streaming layers deferred to a follow-up phase. REST adapters already support WS price-key formats so the price cache can be shared when streaming lands.
- **Supabase schema learned the hard way:** `bookies` table uses `poll_interval`/`discovery_interval` (not `poll_interval_seconds`), requires `is_canonical` and `needs_browser` booleans, `config` is JSONB. `edges` table scopes to bookies via `criteria.bookies` array (no FK).
- **Edge configuration pattern:** Each bookie gets a `*-normal` edge (value mode) and a `racing-run-2nd-3rd-*` edge (refund-as-bonus mode with `bonus_conversion: 0.80`, `min_field_size: 8`).

**Lessons Learned:**
- Neds price_type_id mapping verified via DOM probe: Win=`940b8704-...`, Place=`7cf3eea6-...` — confirmed by 5.00 win / 2.00 place matching 1+(W-1)/4 formula.
- `Runner not in scaffold` log lines from Pointsbet are benign — just runners the bookie has that Betfair doesn't (NZ suffix horses, late scratchings).
- Pointsbet `PERF YELLOW` warnings on initial warmup are transient — normalise once hot tier populates after first warm sweep.
- Both bookies hit 100% venue match on first cycle — no `/link-venues` manual cleanup required.

**Action Items:**
- Wire WS streaming layer for Neds (protocol fully documented in `.claude/memory/neds-protocol.md`) for sub-second tick freshness
- Wire WS streaming layer for Pointsbet (similar follow-up)
- Monitor Pointsbet perf metrics after warmup stabilises

### Session (14:20) [superwin]

**Context:** Debugging and fixing Betfair streaming subscription limits and runner-number mapping bugs in the value betting scanner.

**Key Exchanges:**
- Confirmed Betfair's hard cap is 200 markets per stream connection, with 8 connections available (1,600 total capacity)
- Chose Option B (smarter eviction within single 200) over Option A (multi-connection sharding) — fixes the symptom with minimal risk
- Audited settlement accuracy: PnL/result fields are correct, but BSP/LTP/CLV are wrong on races with scratchings (~42% of historical races)

**Decisions Made:**
- Tiered subscription priority: SUSPENDED (Tier 0, never evicted) → imminent ±90min (Tier 1) → upcoming → stale evicted. Re-subscribe threshold of 3 to prevent churn
- Fixed cloth-number bug at source (`_cloth_number()` in `server/bookies/betfair.py:359-365`) by parsing saddle from runner name prefix (e.g. "5. Lady Nova" → 5) rather than relying on `sortPriority` which compresses to sequential indices when runners scratch
- Ship source fix over band-aid parse-from-name at the race_results writer level

**Lessons Learned:**
- `_cloth_number()` fallback to `sortPriority` silently breaks when greyhound scratchings compress the sequence — the runner NAME from Betfair is the reliable source of truth for cloth/box number
- Audit that compared pick.bsp to race_results.runner_map[number].bsp gave false 200/200 pass — both sides had the SAME wrong number, so they matched. Audits must compare on NAME, not number, to catch mapping bugs
- PnL is status-derived (WIN/LOSE) and immune to runner-number mapping errors; BSP/CLV are lookup-derived and vulnerable
- Betfair `connectionsAvailable: 8` means multi-connection sharding is available if 200-cap becomes insufficient

**Action Items:**
- Backfill Phase A: fix ~4,400 race_results rows by parsing saddle from runner name
- Backfill Phase B: re-resolve ~10k settled picks to correct BSP/LTP/CLV using fixed race_results
- Dry-run backfill on 50 races first (before/after comparison) before applying at scale
- Monitor next races with scratchings to confirm forward-fix works end-to-end
- Consider Option A (multi-connection sharding) if daily AU+NZ markets routinely exceed 400

### Session (14:54) [superwin]

**Context:** Diagnosing SuperWin VPS after deploying TAB stream stability fix (heartbeat/cache-reuse)

**Key Exchanges:**
- TAB stream held 4+ minutes with zero disconnects — first time since the regression (prior pattern was flapping every 52s–432s)
- Odds confirmed mutating in catalogue (Hamilton R4 Bow Ribbon 2.7→2.9 in 30s)
- Scheduled 20-min follow-up check to confirm stream holds long-term
- Proposed dual WS hot/standby architecture for truly zero-downtime TAB streaming

**Decisions Made:**
- Dual WS hot/standby is the recommended next step for zero user-visible TAB downtime (2× WS connections, deduplicate by `(prop_id, timestamp)`, ~2hr implementation)
- Wait for 20-min stability confirmation before building it
- Disk cleanup triggered automatically at 82% → freed 96M journals → 81%

**Lessons Learned:**
- Cache-reuse window extension (now 300s) means any future reconnect is ~5s blink instead of 60-90s cold REST freeze — this alone fixes "frozen odds" symptom even if flaps still occur
- Dual WS hot/standby is the canonical pattern for "this connection MUST stay up" — stagger connections by 30s, deduplicate by highest-seen timestamp per prop
- TAB may rate-limit by IP if running two WS connections — mitigation is routing second through different egress

**Action Items:**
- 20-min follow-up check to confirm TAB stream stability
- Build dual WS hot/standby if stream holds (or build it regardless since it eliminates the problem class entirely)
- Monitor edge resolver — 149 picks stuck across 29 races, down from 500/109, still catching up

### Memory Flush (15:24) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (15:56) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (15:56) [value-betting-scanner]

**Context:** Ensuring the value-betting resolver, backtest, and manual-take systems are fully context-aware of recent infrastructure fixes and verifying pick grading correctness.

**Key Exchanges:**
- Made backtest/resolver "context-aware" of the full 2026-05-22→2026-05-26 fix cascade (sharp/soft decouple, OO boundary widen, era boundaries) — updated 9 files including brain docs, Python docstrings, MEMORY.md, and a new era-boundary finding doc
- Verified manual-take system health: 195 total picks, 161 resolved, grading working correctly on same path as auto picks
- Audited 20 resolved manual picks: 20/20 results correct, 20/20 CLV formula correct — grading math is sound
- Identified `sharp_clv_pct_true` is NULL for all manual picks — structural gap (no theory weights exist for `manual_topdown` trigger), not a bug
- Updated `/VB-V3-Healthcheck` skill with new manual-pick CLV/result verification block, pass criteria, failure mode documentation

**Decisions Made:**
- Era boundary table stored in 3 synced locations (resolver.md, finding doc, MEMORY.md) so all reading surfaces have it
- `sharp_clv_pct_true` NULL for manual picks treated as INFO not FAIL in healthcheck — fix path is ~5 LOC (add `manual_topdown` theory row)
- Manual pick verification added as permanent healthcheck section (green = 20/20+20/20, red = ≥2 mismatches)

**Lessons Learned:**
- Resolver has 3 hard gates preventing early resolution: staleness check, fixture-completion (OO `/fixtures` status=completed), per-date stats availability — all fail closed
- Player matching identity: normalized `player_name` + `fixture_name` (must be in completed set) + `prop_type` → stat key mapping + `side`/`line` comparison
- The 1h OO boundary widen fix (commit 79d2214) was critical — without it, games starting exactly at boundary timestamps were excluded from completed-fixtures queries
- MLB uses MLB Stats API for player stats (not OO player-results which had 30-40% error rate per 2026-04-28 finding)
- `peak_ev` NULL on 7/20 manual picks is expected — thin-trail picks taken close to tip naturally have fewer trail rows

**Action Items:**
- Ship `manual_topdown` theory row in optimization_runs table so `sharp_clv_pct_true` populates for manual picks (~5 LOC fix, not yet done)

### Session (15:56) [value-betting-scanner]

**Context:** Debugging frozen EV picks on the `/nba` theory picks page (port 8803) while topdown dashboard showed live odds movement.

**Key Exchanges:**
- Friend reported sharp book odds moving on topdown but EV column staying static on `/nba` — not a delay issue, a complete freeze
- Root cause traced through 3-layer chain: VPS proxy hardcoded `"pulse": 0` → browser falsy check `if (data.pulse)` never fires for 0 → `computeEVPicks()` memo cache key `(0,0)` always matches → EV never recomputes for the lifetime of the tab

**Decisions Made:**
- Browser-side cache bust (`_evPicksCache.pulse = -1` after every poll/SSE frame) is the load-bearing fix — applied to both SSE and polling branches
- VPS server emits `int(time.time() * 1000)` instead of hardcoded 0 — defense-in-depth so the falsy guard is harmless even if cache-bust line is ever removed
- Chose NOT to fix by forwarding upstream pulse (mini PC returns bare list, no pulse in envelope) — generating a monotonic timestamp server-side is cleaner

**Lessons Learned:**
- JavaScript falsy check on `0` is a silent footgun — `if (data.pulse)` skips when pulse is literally zero. Use `!== undefined` for numeric fields that can legitimately be 0
- Memoization caches keyed on externally-provided values can pin forever if the provider is broken — always have an explicit bust mechanism alongside the key-comparison path
- VPS proxy was building its own response envelope and discarding upstream metadata — proxy layers that reconstruct instead of forwarding are a recurring source of data loss
- topdown was unaffected because it renders straight from `markets[]` with no pulse-keyed cache — simpler rendering path was accidentally more resilient

**Action Items:**
- `MAX_SOFT_ODDS_AGE_S = 3600` still loosened from scraper outage workaround — stale Bet365 odds feed picks for up to 60 minutes in normal operation (noted but not fixed this session)
- VB-V3-Healthcheck skill updated with pulse cache-bust marker check (§4) and symptom-table entry distinguishing this from the picks-overlay lag fix
- EV refresh is now ≤3-5s (polling) or sub-second (SSE), down from ∞ (never refreshed without hard reload)

### Memory Flush (15:57) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (16:27) [superwin]

**Context:** Debugging TAB (bookmaker) WebSocket stream architecture for zero-downtime coverage in the value betting scanner, testing dual hot/standby worker approaches

**Key Exchanges:**
- Verified dual Worker A/B TAB stream architecture mechanically works (both connect, subscribe, feed catalogue without double-counting)
- Ran three sequential experiments to achieve zero-downtime during TAB's periodic connection drops

**Decisions Made:**
- **Proxy routing for Worker B — abandoned**: WebShare proxy gave Worker B a different source IP (Brazil), but TAB still dropped both connections in the same second. Proved TAB's disconnects are NOT IP-correlated.
- **VIC jurisdiction for Worker B — abandoned**: NSW (Worker A) vs VIC (Worker B) still dropped same-second. Proved TAB jurisdictions share a single global rotation timer and return identical prop sets.
- **Dual-WS zero-downtime architecture — dead end confirmed** after 3 disproven hypotheses: (1) staggered connection ages, (2) different source IPs, (3) different jurisdictions. TAB runs a global server-side periodic sweep that kills ALL live WS connections simultaneously regardless of client-side variation.

**Lessons Learned:**
- TAB's push infrastructure (`push.beta.tab.com.au`) uses a **global wall-clock-based connection rotation** — not per-IP, not per-session-age, not per-jurisdiction. All connections die in the same second.
- TAB jurisdiction parameter (NSW/VIC) doesn't affect data — same 1100 propositions, same update counts. It's cosmetic routing, same backend.
- WebShare proxies ARE compatible with TAB's WS endpoint (valid handshake, full data flow) — useful knowledge if proxy routing needed for other purposes.
- Pre-deploy probe pattern (test WS handshake through proxy before deploying) caught issues early and saved debug time.
- Current reconnect gap after TAB drop is ~6-15s (down from 60-90s before warm-cache fix). The warm-cache approach is the real win.

**Action Items:**
- Only remaining viable path for TAB zero-downtime: **minimize reconnect time** (pre-staged sessions, persistent subscription state → target 1-2s gap)
- **Investigate VPS restart-looping**: `superwin.service` was being stopped externally every 2-6 minutes (not crashing — clean `systemctl stop` calls). Unknown watchdog or second user session causing this. Must identify and fix before further experiments.
- Revert dual-jurisdiction code (VIC not needed, adds complexity for no benefit)
- Consider reverting proxy code path too (or keep as dead code if useful later)

### Session (17:45) [superwin]

**Context:** Monitoring and validating the no-sleep reconnect optimization for the TAB websocket adapter in the v3 value-betting scanner (VPS deployment).

**Key Exchanges:**
- Observed TAB worker reconnect timing after removing the 5s mandatory sleep: disconnect→resubscribe gap measured at **3-4 seconds** (down from 6-15s previously)
- Validated no thrashing — workers reached stable state between drops, with 7+ min continuous uptime observed post-reconnect
- Confirmed catalogue still populated (118 TAB open races) and no new error patterns introduced
- After 68 min of monitoring, system remained stable across all 9 bookies

**Decisions Made:**
- **Accept 3-4s reconnect as good enough** — target was sub-2s but remaining time is TLS handshake + Engine.IO + namespace ack + 1053 subscribe frames. Further optimization would require meaningful refactor for marginal gain.
- **Alert count (climbed to 100) deemed non-actionable** — all "Regression" warnings from betr/boostbet/tab tier rotation making per-cycle counts dip below 70% baseline. Noisy threshold, not a real problem.

**Lessons Learned:**
- The 3s reconnect floor is dominated by TLS handshake + Socket.IO namespace negotiation + sending 1053 subscribe frames — not something easily optimized without parallelizing handshake stages
- TAB's server-side rotation cadence appears variable (not fixed 3 min) — one PID showed 11 min uptime with only one drop vs prior 3-5 min cadence. Fast reconnect may coincidentally land before TAB's rotation timer windows.
- Sample size caveat: only 1 disconnect event captured in this monitoring session. Mechanically validated but needs more observation cycles for statistical confidence.

**Action Items:**
- Consider another observation cycle to confirm 3-4s reconnect holds across multiple rotation events (sample size was only 1)
- Optionally investigate alert threshold tuning to reduce noisy "Regression" warnings from tier rotation dips

