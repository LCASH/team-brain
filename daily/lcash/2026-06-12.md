# Daily Log: 2026-06-12 (lcash)

## Sessions

## Memory Maintenance

### Session (09:47) [Knowted admin]

**Context:** Knowted competitive pricing analysis — reviewing live-verified competitor tiers, updating the master comparison doc, and locating a dormant LinkedIn inbox reader project.

**Key Exchanges:**
- Full competitor pricing recap (audit live-verified 2026-06-11 for Fathom/Avoma/Sybill/Gong; third-party for Granola/Otter/Fireflies/Loom/tl;dv): Fathom Business $34/$25; Avoma Startup $19 + CI add-on $29 = $48 coaching parity; Sybill Pro $30; Gong quote-only (~$100+/seat per G2/Vendr); Otter Pro minutes cut 6,000→1,200; Granola capped free tier after $125M Series C at $1.5B; tl;dv Business $59.
- Knowted positioning vs. locked tiers (Free $0 / Team $19 / Enterprise $69): 24–80% cheaper than coaching-parity competitors; Granola Business ($14) is the only cheaper option but lacks coaching layer.
- User wanted the comparison uploaded to a Google Doc — Drive MCP not authenticated, so workflow is `pbcopy` the markdown and paste manually.
- Found the LinkedIn inbox reader: **"Claude Bot"** at `~/Documents/Claude Bot/` (last touched 11 Feb 2026) — `inbox_server.py` + `inbox_ui.html` serve a local inbox UI at localhost:8888 via headed Chrome with persistent profile in `linkedin_user_data/`; also includes `automation_server.py`, Apollo integration, optional Supabase backend, and a stubbed AI-draft endpoint.

**Decisions Made:**
- Updated `workstreams/competitive-analysis/master-comparison-2026-06.md` to live-verified pricing: added coaching-parity row, corrected Avoma to $19, Fathom Business to $34/$25, marked Gong as quote-only with G2/Vendr footnote.
- Removed Gong's "5–15%/yr renewal increases" and "50–100% early-termination penalty" claims from the doc — Perplexity verification showed they're anecdotal review claims, not in primary sources.

**Lessons Learned:**
- Gong pricing claims must always cite G2/Vendr reviews, never gong.io (publishes nothing).
- Fireflies.AI has two pending federal BIPA class actions (Fricker N.D. Ill., Cruz C.D. Ill.) over meeting-bot voiceprint collection — relevant for competitive/compliance messaging.
- Claude Bot has no git repo — version it before modifying; LinkedIn DOM selectors likely stale after 4 months.
- Google Drive MCP is available but unauthenticated — run `/mcp` to enable direct doc uploads.

**Action Items:**
- Re-verify Fathom + Avoma pricing quarterly (closest to Knowted's $19 price point).
- Re-verify Granola free-tier cap and Otter Pro minute cut before publishing externally.
- Paste updated master comparison into the Google Doc (or authenticate Drive MCP for direct upload).
- Pending offers on Claude Bot: smoke-test against current LinkedIn DOM, wire AI-draft endpoint to Anthropic SDK, and/or move it into the Knowted workstreams structure.

### Session (10:02) [Knowted admin]

**Context:** Built out Knowted social ad creative system — HTML/CSS→PNG renderer, logo library, comparison concepts, preview UI, and attempted Gemini image-gen integration.

**Key Exchanges:**
- User asked what AI image model generates the ads — answer: none. Generator is headless Chrome (Puppeteer) rendering HTML+CSS to PNG. Chosen deliberately: pixel-perfect text and exact brand colors, which diffusion models (Midjourney/DALL-E/Flux) can't deliver for text-heavy ad cards.
- User requested more creative comparison visuals incl. Claude + Zoom/Teams logos, and a preview website matching the competitor-research dashboard pattern. Delivered: 4 new logo-based templates (integrationRow, logoGrid, vsLogo, stackPile), ~10 new concepts (vs-Fathom/Granola/Otter/Gong, "captures everywhere", Claude MCP angle), Vite+React+Tailwind UI at localhost:5180.
- User provided a Google AI API key for image generation; built `gemini.js` wrapper + `test-gemini.js`, but all image models are blocked on the key's free tier.

**Decisions Made:**
- Hybrid image architecture: diffusion (Gemini/Imagen) for backgrounds/scenes only, HTML+CSS owns all text and brand elements — prompts explicitly say "no text, no logos".
- Logo system uses placeholder chips (brand color + first letter) until real SVGs are dropped in `social-images/logos/` — templates auto-pick them up, no code change.
- Legal posture: competitor cards use publicly known brand colors as identifiers (nominative use), all claims sourced from the factual audit; Fireflies class-action referenced without naming them ("Pending federal class actions in this category").
- API key stored in gitignored `.env` (with `.gitignore` covering output PNGs, generated assets, node_modules).

**Lessons Learned:**
- Google AI free tier sets image-generation quota to **zero** across all models: gemini-3-pro-image, gemini-3.1-flash-image, gemini-2.5-flash-image all 429 with `limit: 0`; imagen-4.0 returns 400 "paid plans only". Any free Google key hits this wall.
- Imagen uses the `predict` endpoint; Gemini-N image models use `generateContent` — wrapper handles both with fallback. Gemini-N doesn't take aspect ratio as a parameter; must inject into the prompt.
- Metric template wrapped awkwardly with long suffixes — fixed by stacking suffix below the number.
- Adding a new output size is a one-liner in the `SIZES` array in concepts.js.

**Action Items:**
- User to decide: (A) enable billing on Google AI project (~$0.02–0.04/image, recommended — wrapper works immediately), (B) Replicate/Flux adapter (~$0.003/image, needs 15-line adapter), or (C) stay text-only.
- User to drop real brand SVGs into `social-images/logos/` (20 placeholders registered), then `node generate.js` to re-render.
- Open offers: more templates (dark-mode statements, announcement card, carousel sequencer, testimonial card once Danny's quote locks), HTML index grid of all PNGs.
- README contains the 4-week × 3 posts/week posting plan mapped to the locked content cadence.

### Session (12:21) [Knowted admin]

**Context:** Working on a LinkedIn inbox bot — got it scanning unread threads by switching from Playwright's vanilla Chromium to AdsPower over CDP.

**Key Exchanges:**
- Confirmed the AdsPower + CDP wiring worked: unread threads pulled into the local UI after Scan Unread. User reported "okay it worked they are loaded in."

**Decisions Made:**
- Switched from Playwright-launched Chromium to AdsPower profile `k19yb91n` over CDP because LinkedIn's bot detection nuked the vanilla Playwright fingerprint (banner "Chrome is being controlled by automated test software" + CF challenge).
- Patched `stop()` to disconnect from AdsPower rather than close the browser (closing would kill the AdsPower window).
- Server runs at `http://localhost:8888`; AdsPower local API at `http://local.adspower.net:50325/api/v1/browser/start?user_id=k19yb91n`.

**Lessons Learned:**
- Playwright's default Chromium fingerprint is trivially detectable by LinkedIn (CF + bot detection); AdsPower's real fingerprinting passes as a regular user.
- `clear_cookies()` before re-injecting wipes AdsPower's existing LinkedIn session each restart — forces LinkedIn to re-validate, and repeated restarts can itself trigger detection over time. Cleaner pattern: only inject cookies if `linkedin_cookies.json` is newer than profile's last activity OR no `li_at` already present.
- Pasted cookie dump was missing `li_a` (Sales Nav enterprise token) — regular `/messaging/` works via `li_at` + `li_ep_auth_context`, but Sales Nav-specific URLs would fail.
- If AdsPower profile already has a logged-in LinkedIn session, skipping the cookie clear lets the existing session stand (avoids "session conflict" page).

**Action Items:**
- Stress-test the reply path: type + send a reply via local UI to confirm write side works, not just read.
- Wire AI Draft endpoint: `POST /api/ai-draft` that pulls conversation, calls Anthropic with founder-voice system prompt, returns draft to reply box. Anthropic key already in env.
- Pipeline integration (afternoon build): tag inbox conversations against outreach CSV contacts for a unified "replies from people in my pipeline" view — queued for possible Thursday shift.
- Apply hygiene patch: conditional cookie injection based on file freshness / existing `li_at`.
- Resolve project move destination — `calendar.app.google` link wasn't valid; recommended `Knowted admin/workstreams/outreach-gtm/inbox-bot/`, then git-init.

### Session (12:31) [Knowted admin]

**Context:** Uploading and indexing the "Business Co-Founder's Operating Guide" into Knowted's admin docs and Claude memory system.

**Key Exchanges:**
- User provided a comprehensive co-founder operating guide document defining the full scope of Luke's (business) and Jeffrey's (technical) responsibilities across 8 and 6 areas respectively
- Document was saved to Knowted admin docs as `cofounder-operating-guide.md` and indexed in memory as `project_knowted_operating_model.md`
- Cross-linked from STATUS.md workstream board and folder map for discoverability

**Decisions Made:**
- Created a standalone file rather than merging into existing `project_knowted_team.md` or `project_knowted_action_items.md` — the operating model is broader than both
- Added a "How this maps to active workstreams" section to bridge the static guide to live execution
- Indexed in MEMORY.md with links to related team-equity and action-items memories

**Lessons Learned:**
- The guide's own prioritization logic exposes a gap: **#1 and #2 P0s are investor pipeline + financial model/data room**, but actual recent work has been weighted toward marketing/content/competitive analysis
- Financial model, data room, and cap table have **no active workstream** — these are the most raise-critical parked items with the July raise looming
- The guide is designed to surface exactly this kind of gap: "here's the full surface, what's P0, what's parked, and what's actually moving"

**Action Items:**
- Surface the financial model + data room gap at next monthly review (4 Jul) or sooner given July raise timeline
- Jeffrey's 2 investor intros are 3 days overdue (due 9 Jun) — chase these as highest-leverage warm entry points
- Consider standing up workstreams for: finance/bookkeeping, recording-consent law, data room, financial model, cap table

### Session (13:10) [Knowted admin]

**Context:** Wiring LinkedIn inbox bot to use AdsPower fingerprinted browser + adding AI-powered message drafting + anti-detection hardening

**Key Exchanges:**
- Successfully connected Playwright to AdsPower's CDP endpoint (profile `k19yb91n`) instead of spawning detectable headless Chromium — cookies injected, unread threads loaded
- Built `/api/ai-draft` endpoint with motion-aware drafting (sales leader, entrepreneur, investor, 1:1 warm, auto-pick) powered by Claude API, with voice context from `voice_context.py`
- Account getting flagged/logged out by LinkedIn bot detection — diagnosed root causes and applied comprehensive anti-detection layer

**Decisions Made:**
- Use AdsPower's existing fingerprinted browser via CDP rather than Playwright's own Chromium — avoids headless detection entirely
- Single-file voice config (`voice_context.py`) as source of truth for all drafting — voice rules, product facts, motions, example messages all in one place
- Cookie injection strategy: only re-inject if `linkedin_cookies.json` is newer than last activity OR no `li_at` present (avoids unnecessary session revalidation on restarts)
- Conservative drafter behavior: under-promise, never invent features, return `NO_REPLY_NEEDED` for closed threads

**Lessons Learned:**
- Even with AdsPower fingerprinting, Playwright's default instant-click/instant-type/fixed-sleep patterns are trivially detectable — must humanize every interaction (bezier mouse curves, per-char typing with typo simulation, gaussian jitter on sleeps)
- `navigator.webdriver`, missing `chrome.runtime`, empty `navigator.plugins`, and `permissions.query` hang are all bot-detection signals that need patching at the Playwright context level even when AdsPower handles fingerprinting
- LinkedIn detection is volume + frequency driven more than fingerprint: ≤4 scans/day, ≤25 unreads/scan, ≤10 replies/session, ≤50 daily messages, restrict to local working hours
- Code-level fixes can't undo existing account flags — need 24-72 hours of clean manual behavior to cool off

**Action Items:**
- Set `ANTHROPIC_API_KEY` in shell env so AI Draft endpoint works (currently 500s without it)
- Add rate-limiting constants (`MAX_DAILY_SCANS=4`, `MAX_UNREADS_PER_SCAN=25`) with 429 responses
- Let account rest 24-72 hours if logout persists post-patch (account-level flag, not code issue)
- Move project to `Knowted admin/workstreams/outreach-gtm/inbox-bot/` and git-init (still pending)
- `li_a` cookie (Sales Nav enterprise token) missing from latest cookie paste — will break Sales Nav URLs if needed later

### Session (13:45) [Knowted admin]

**Context:** GTM launch planning for screen-recording product — unblocking marketing via decision register, then designing an agentic "operating system" to run departments autonomously.

**Key Exchanges:**
- Diagnosed marketing block as a decision problem, not a content problem: claims (unlimited, 95% accuracy, no-bot, capture-everywhere) can't ship until legality/truth/pricing are locked. Structured into 3 gates: (1) legal & truth, (2) pricing completeness, (3) launch sequencing.
- Built an 8-decision register (launch-decisions.md) with owners, defaults, and what each unblocks; wired into STATUS.md. Fast path: 30 min with Jeffrey (product-truth matrix + Team-at-launch), 10 min pricing edge cases, one lawyer hour ($200) for recording-consent + comparison ads.
- User uploaded Jeffrey's counsel brief on recording consent — filed at expert/6-legal-compliance/counsel-brief-recording-consent.md.

**Decisions Made:**
- **D5 (Team at launch): YES** — Team works from launch. Privacy boundary: no team/free = fully local, videos never upload to cloud (hard guarantee, no account needed); Team mode requires login, which is the gate that turns on cloud sync/sharing.
- **D6 (Website): two-phase** — waitlist site ships now with existing design; separate product-site revamp for app go-live end of June.
- **D1 (Legal posture): materially stronger than assumed** — local screen recording, no bot, user-is-a-participant is the safest architecture in the category (vs Otter/Fireflies bot litigation where a third party intercepts). Ship-now marketing line: "Records your screen, on your device, as you — no bot. Nothing leaves your laptop unless you share it." Lawyer engagement still needed for all-party-consent edge + ToS before paid comparison ads / July diligence.
- Launched 10-agent background workflow (8 departments + accountability layer + implementation architect) to design the self-evolving agentic OS: expert/ files + STATUS.md as shared memory, scheduled agents write back to learning logs, Monday chief-of-staff briefing + Friday review surface only what needs the user.

**Lessons Learned:**
- The legal/product/marketing alignment is strongest when they share one line: login = cloud on, no login = local-only. Same boundary serves all three.
- Ambiguity flagged: user said Team users "need to be linkedin" — interpreted as logged-in/authenticated, but could mean LinkedIn SSO. Needs confirmation.

**Action Items:**
- Confirm "linkedin" meaning (authenticated vs LinkedIn SSO) with user.
- Synthesize 10-agent workflow output into OS design doc, per-department playbooks, run-script when it completes; start with Monday chief-of-staff briefing.
- User to decide which accounts/API keys to wire in for the agentic system.
- Still open: D2 (product-truth matrix with Jeffrey), D3 (Enterprise market-or-hold), D4 (pricing edge cases), D7 (comparison-ad aggressiveness + lawyer review).
- Offered drafts: Jeffrey product-truth checklist; one-page consent-law brief for the lawyer hour.

### Session (14:22) [Knowted admin]

**Context:** Built a self-sufficiency "asks ledger" mechanic for the Knowted chief-of-staff system and scheduled it as a weekly cloud routine via Claude Code remote agents.

**Key Exchanges:**
- Scheduled cloud agents run in Anthropic's cloud and require a git repo — local doc-graph had to be pushed to GitHub before any routine could run.
- User confirmed pushing confidential strategy docs (investor plan, legal brief, financials, pricing) to a **private** GitHub repo was acceptable after being asked explicitly.
- Test run failed with `github_repo_access_denied` — user must do a one-time GitHub authorization for Claude Code cloud at https://claude.ai/code/routines before the routine can clone the private repo.

**Decisions Made:**
- Created private repo `LCASH/knowted-os` (74 markdown doc files, 492K) — gitignored heavy/sensitive bulk: recorder-app, scraper, node_modules, large CSVs, nested git repos. Cloud agent only needs the markdown doc-graph.
- Built `expert/asks-ledger.md` mechanic: every chief-of-staff run completes work AND surfaces one autonomy-raising ask (`🙋 One ask` line). Resolved/declined asks are never re-asked; provided asks become permanent capabilities. Rule baked into `.claude/skills/chief-of-staff/SKILL.md`.
- Created cloud routine `trig_011bAmUD9vATJhhZKufMycMb` — "Knowted Chief of Staff — Monday Briefing", Mondays 06:30 Brisbane (Sun 20:30 UTC), clones repo, writes `weekly-briefing.md` + daily ask, commits/pushes back. Environment: `env_01KpozgE7RnQtZHpa3sJzm43`.
- Ask #1 (hosting) marked resolved in ledger — capability gained: chief-of-staff runs autonomously without laptop on.

**Lessons Learned:**
- Cloud routines have a 1-hour minimum cron interval; cron expressions are UTC (Brisbane = UTC+10, so Monday 06:30 local = Sunday 20:30 UTC).
- Private repos need explicit GitHub authorization in Claude Code cloud settings — repo creation + push alone is insufficient; fire a test run to catch this rather than waiting for the first scheduled run.
- Routine management URL pattern: `https://claude.ai/code/routines/{ROUTINE_ID}`.

**Action Items:**
- User: authorize GitHub for Claude Code cloud so it can access `LCASH/knowted-os`, then re-run the routine to validate the full loop (otherwise it self-validates Monday 15 Jun).
- Proposed next build: step 4, Investor Relations heartbeat routine (drafts investor messages into the Monday briefing) — pending user decision to queue now or after Monday's run proves the spine.
- P0 status flagged: 3 of 4 raise-critical P0s stalled — headline of the first briefing.

### Session (15:03) [Knowted admin]

**Context:** Building the "knowted-os" autonomous business OS — fixing cloud routine GitHub access, deciding execution substrate, and pivoting to local manual skill-based runs.

**Key Exchanges:**
- GitHub access for the Anthropic cloud routine was fixed by setting the Claude GitHub app to **All repositories** — the Monday chief-of-staff routine (trig_011bAmUD9vATJhhZKufMycMb) then ran end-to-end: cloned `LCASH/knowted-os`, read all 8 departments, regenerated `weekly-briefing.md`, and pushed to a `claude/*` branch with no local machine involved.
- The cloud agent self-evolved during its run: improved the briefing copy ("the gap is sends, not work") and appended ask #5 to the asks-ledger on its own (Investor Relations heartbeat activation).
- User asked how to visually watch progress — three views provided: live routine run UI, GitHub commit history as build timeline, and `weekly-briefing.md` as the daily surface. Offered (not yet built) an "OS Control Room" local dashboard.
- User dropped a Google Calendar link tagged "os" — purpose still unclarified.

**Decisions Made:**
- User initially wanted a **VPS as the agent's own computer** (persistent browsers, logged-in sessions, evolving toolset) — a 6-agent parallel design workflow was run for this.
- User then pivoted: **run local and manual for now**, executing agents as skills on demand; VPS design parked as future roadmap at `expert/agent-computer-vps.md` (Hetzner box, headful-Xvfb LinkedIn sessions, 6-phase rollout DRAFT-only → authenticated reads → supervised sends, permission firewall).
- Built one reusable **`dept-heartbeat`** skill engine instead of 8 separate skills (department loops are ~95% identical); accepts `sales`, `marketing`, `customer-success`, `investor-relations`, `finance`, `legal`, `people`, `strategy`, `1`–`8`, or `all`.
- Cloud routine left running as a free weekly backup alongside manual mode.
- Local `main` was fast-forwarded manually to the cloud's branch; future runs still push to new branches by default (commit-to-main reconfiguration offered but not yet done).

**Lessons Learned:**
- Anthropic cloud routines are ephemeral/sandboxed — can't hold persistent browser sessions or accumulate tools; local runs actually have *more* capability (AdsPower browser, Perplexity, Drive available).
- Cloud agent defaults to `claude/*` branches + PRs, creating a weekly merge step for doc-only updates; branch landed with a name suffix (`claude/cool-edison`).
- Dashboard-you-must-remember-to-open is a known trap per the OS design — briefing remains the daily surface.

**Action Items:**
- Decide: reconfigure routine to commit directly to `main` (no weekly PR) — offered, pending user confirmation.
- Clarify intent for the Google Calendar link (likely Calendar MCP connector for the chief-of-staff briefing).
- Pending user "go" on ask #5 (Investor Relations heartbeat / P0 #1 loop).
- 3 pending asks in asks-ledger: Drive auth + CRM sheet, cash-on-hand + infra costs, data-room folder.
- Optional: build the OS Control Room dashboard (dept health lights, briefing, asks queue, git timeline).
- Decide whether to disable the cloud routine for purely manual mode.

### Session (16:01) [Knowted admin]

**Context:** Building out the "knowted-os" chief-of-staff system — weekly briefing generation, an asks-UI dashboard, Google Drive MCP vetting, and two new skills (adspower-browser, skill-smith).

**Key Exchanges:**
- User confused by the dashboard's "one ask right now" — concept explained (OS asks for one capability per run; answered asks become permanent capabilities shown as green "gained" cards) and UI rebuilt with a self-explanatory header
- Security review of `@piotr-agier/google-drive-mcp` v2.2.0: read actual code, not README — only official Google/MCP-SDK deps, no phone-home (all calls to Google or localhost), no shell-exec/eval, install hook is just esbuild, tokens stored chmod-600. Verdict: safe; full writeup saved to expert/integrations-google.md
- Google access strategy research (via Perplexity): claude.ai Google Drive connector is the zero-setup path for local-manual mode now; a Google service account (JSON key, share only the specific sheet/folder) is the durable least-privilege path for future unattended/always-on use

**Decisions Made:**
- Pin google-drive-mcp to 2.2.0 and review diffs before upgrading (single-maintainer supply-chain risk); scope to `drive.file` + `spreadsheets`, not full `drive`
- Prefer service account over OAuth-as-user for the unattended agent — smaller blast radius
- skill-smith wired as the closing step of every dept-heartbeat run with hard guardrails: only touches `.claude/skills/**`, additive/reversible (git), one change per run, never weakens safety/voice rules, never grants new act-power (send/spend/delete stay human-gated); all changes logged to expert/evolution-log.md
- Briefing anti-patterns codified: one line per department, de-dupe cross-department items, never a red flag without a recovery step, escalate on days-since-movement not zero outcomes (pre-launch), read source-of-truth fresh (STATUS.md / tier-structure.md)
- Two evolution engines now formalized: asks-ledger grows *access*, skill-smith grows *skills*

**Lessons Learned:**
- claude.ai connectors (e.g. Google Drive MCP) can't be authorized programmatically — user must run `/mcp` → authorize themselves
- "One ask per run" pattern works but abstract asks (e.g. voice feedback) confuse users vs concrete access asks — UIs surfacing them need explainer text
- AdsPower pattern captured in skill: start profile → connect Puppeteer over CDP → scrape → disconnect (never close); includes profile IDs and LinkedIn rate-discipline rules
- skill-smith only evolves from real runs, not idle time — flywheel starts on first dept-heartbeat
- Briefing headline: 3 of 4 P0s stalled, but the gap is *sends*, not work — drafts already exist
- Dashboard regenerates via `node os-dashboard/build.js`, zero dependencies, reads truthfully from asks-ledger.md + weekly-briefing.md

**Action Items:**
- Luke: authorize claude.ai Google Drive connector via `/mcp` (unlocks Sales + Marketing loops reading live state)
- Luke: run `dept-heartbeat investor-relations` — drafts investor touches and triggers skill-smith's first real improvement
- Ask #5 pending: paste back any draft lines that don't sound like Luke (resolves voice-feedback ask, sharpens future drafts)
- Future: when going always-on, switch Google access to scoped service account; future dept-heartbeat scheduled agents will write their own pulses so chief-of-staff only aggregates

### Session (16:33) [Knowted admin]

**Context:** Running Knowted business-OS department heartbeats (Investor Relations, then Finance) — drafting raise artifacts and confronting the company's true financial position.

**Key Exchanges:**
- User disclosed **runway is currently 0** — assistant reframed: burn ~$460 AUD/mo vs ~$400 AUD MRR (~$280 USD at FX 1.427) = roughly -$60 AUD/mo, gap covered by Braedon. Not "burning toward a wall," but zero shock buffer makes the raise survival-critical, not a milestone.
- Strategic reframe: the local-first/free-tier product thesis is exactly right for a zero-cash founder — product is the distribution, growth needs no ad spend. Game collapses to two $0 levers: users (ship desktop app + free tier end-June via Jeffrey, deploy waitlist site, record Video #1, LinkedIn outreach) and investors (chase Jeffrey banker intros, send June update).

**Decisions Made:**
- IR heartbeat drafted 4 artifacts in `workstreams/outreach-gtm/drafts/`: jeffrey-chase-banker-intros (P0 #1, overdue 3 days), banker-intro-followup Touch-1, investor-1pager (doubles as first data-room artifact, P0 #2), monthly-update-june-2026 (P0 #4) — 3 of 4 raise P0s advanced.
- Built the Finance runway tracker (`expert/5-finance-operations/runway.md`) with the honest zero-cash numbers; corrected Finance expert state and appended pulse.
- skill-smith improvement: codified the IR run's standard 4-artifact set + warm-intro-vs-cold distinction into dept-heartbeat's "Learned patterns" section (logged in evolution-log.md). Pending Luke's approval: applying a "Warm intro path" to investor-sequence.md.
- No raise figure quoted in any draft — locks 4 Jul.
- User pivoted to deploying the waitlist site live with quick adjustments (next active task).

**Lessons Learned:**
- "0 runway" with near-break-even burn + a covering backer ≠ typical runway crisis; escalate on buffer/shock risk and traction, not the zero itself.
- skill-smith only touches `.claude/skills/**` — workstream doc changes (like investor-sequence.md edits) must be proposed for human application.
- With zero buffer, daily filter is "did this get me closer to a user or an investor?" — building infrastructure waits; shipping and conversations don't.

**Action Items:**
- Luke: send jeffrey-chase-banker-intros today (highest leverage this week); send June update to banker + Braedon group.
- Deploy waitlist site live with quick adjustments (in progress).
- Decide on applying warm-intro-path proposal to investor-sequence.md.
- Remaining from briefing: lawyer hour; run `dept-heartbeat finance` follow-ups; protect the end-June desktop app/free tier ship date.

### Session (16:46) [Knowted admin]

**Context:** Pushed the knowted.io waitlist site rewrite live (commit `f5120cb` on main, Vercel auto-deploy) including final audit fixes.

**Key Exchanges:**
- Verified the `/sales` comparison page was already audit-corrected (accurate Avoma $19+$29 CI add-on, Fathom pricing footnote, "compared against Fathom/Avoma/Gong only" disclaimer, correction email, precise Fireflies citations) before its first public deploy.
- Identified that the waitlist form had no endpoint configured — it fell back to `localStorage`, so signups would silently never reach the owner.

**Decisions Made:**
- Chose Google Apps Script → Google Sheet over Formspree for email capture: $0, unlimited (Formspree free tier caps at 50/month), and the Sheet doubles as the CRM seed. Setup files committed: `WAITLIST-SETUP.md` + `waitlist-google-apps-script.gs`.
- Reconciled the "100+" vs "over 12 languages" contradiction to a defensible "12+" pending verification of the transcription engine.
- Removed unverified social proof ("Trusted by many teams" — only one paying customer); fake G2/SOC2/HIPAA badges remain feature-flagged off.
- Ignored CTABanner fixes — it's dead code, not rendered.
- Footer repositioned from "Meeting intelligence for managers" to local-first, no-bot positioning; leftover "Try Free" relabeled "Join the waitlist."

**Lessons Learned:**
- A waitlist site can "look live" while capturing nothing — always verify the form endpoint env var (`VITE_WAITLIST_ENDPOINT`) is set before launch.
- Audit-flagged strings can linger in unrendered components; check which components are actually mounted before fixing copy.

**Action Items:**
- User must complete the 3-min Google Apps Script setup (Sheet → Apps Script → deploy Web app → set `VITE_WAITLIST_ENDPOINT` in Vercel → redeploy) **before sharing the link anywhere**.
- Confirm Vercel deploy landed on knowted.io.
- If transcription is Whisper-based (~99 languages), bump the languages claim back to "100+" — one-edit change pending user confirmation.

