# Daily Log: 2026-04-13 (lcash)

## Sessions

## Memory Maintenance

### Session (07:13) [value-betting-scanner]

**Context:** Morning health check on value betting system after previous day's restart and API key issues.

**Key Exchanges:**
- NBA soft books recovered overnight from 3/8 → 5/8 (Bet365 2.0, Sportsbet, Neds, Ladbrokes AU, PointsBet AU) — browser scrapers needed warmup time after restart
- Trail writes stopped at ~9pm last night when NBA games ended; no morning games yet, so idle tracker is expected behavior
- VPS tracker running 13+ hours with 1,364 inserts (all from yesterday's game window), now idling

**Lessons Learned:**
- Browser-mediated scrapers (Bet365 2.0, PointsBet AU) can take hours to warm up after a restart — don't panic if soft book count is low immediately post-restart; check again next morning
- Trail system correctly idles between game windows — 0 new trail entries between last game end and next game start is normal, not a failure

**Action Items:**
- Investigate Betstamp and BlackStream workers — both subprocesses alive but producing 0 odds persistently (not a key issue, likely a scraper-level problem). Two of the 3 missing soft books depend on these.
- PointsBet AU came online overnight; the remaining gap to 8/8 is Bet365-via-Betstamp (365), Surge/BlackStream (960), and BetIT (961)

### Session (07:45) [value-betting-scanner]

### Session (ongoing) [value-betting-scanner]

**Context:** Optimizing push worker cycle time from 55s to under 5s, and fixing fixture name deduplication in the odds aggregator.

**Key Exchanges:**
- Push worker cycle was 55s → first gzip+parallel fix dropped to 27s → still too slow
- Moved `gzip.compress()` to `asyncio.to_thread()` but discovered the real bottleneck was server-side: NBA `/api/v1/odds` took 7.15s (2.6MB), MLB took **20.26s** (8.6MB) — local endpoint calls shouldn't take 20s
- Implemented **snapshot cache** on sport servers: background task rebuilds serialized odds snapshot every 2s, endpoint serves pre-built bytes instead of serializing on-demand
- Results: NBA 7.15s→1.63s, MLB 20.26s→1.25s (16x faster), push cycle **55s→1.9s**
- Trails flowing again: 35 new picks, 867 updates in 5 cycles post-fix
- Discovered **fixture name splitting** — same game appearing 3 times in dashboard because sources use different formats ("Miami Heat vs Atlanta Hawks" vs "ATL Hawks @ MIA Heat" vs "Atlanta at Miami"). Root cause: `state.py:104` sets fixture_name only on market creation, never updates. Fixed to prefer canonical "vs" format.

**Decisions Made:**
- Server-side snapshot cache (rebuild every 2s in background) instead of client-side optimization — the bottleneck was serialization, not network or compression
- Canonical fixture name preference ("vs" format) applied during merge, not just on creation

**Lessons Learned:**
- `gzip.compress()` in an async event loop blocks everything — `asyncio.gather` doesn't help if CPU-bound work runs synchronously. But this was a red herring; the real bottleneck was the serialization inside the HTTP endpoint itself
- When a local HTTP endpoint takes 20s, suspect serialization cost on large payloads — pre-computing and caching the response body is the right fix
- Fixture name normalization must happen at merge time, not just creation time, when multiple data sources feed the same market store

**Action Items:**
- Investigate why many markets have **zero sharps** — user flagged this as a data quality issue, deferred while fixing cycle time
- Verify bet365 2.0 game scraper picks up upcoming games after restart (7 NBA games in the 10:00 AM ET window)
- Confirm fixture name deduplication working after deploy (no more split games in dashboard)

### Memory Flush (08:17) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (08:50) [value-betting-scanner]



**Context:** Continuing value betting scanner stabilization — data integrity cleanup, trail reliability assessment, and theory configuration review.

**Key Exchanges:**
- Confirmed Mini PC and VPS are in sync with 1,731 bet365 markets; dashboard "128" was browser cache from before restart
- Detailed breakdown of why Under picks are sparse: Overs outnumber Unders ~1.6x, 62% of markets have zero sharps, layered filters compound down to ~4 picks
- Honest assessment of system reliability for backtesting — identified what's solid (push cycle, snapshot cache, canonicalization, resolver) vs. caveats (sharp sparsity, dead soft book workers, trail granularity)
- Historical trail data declared unreliable for time-series backtesting due to 55s push cycle vs 30s sharp freshness cutoff creating intermittent capture with selection bias and gaps
- Confirmed backtesting should use `trail_entries` joined to `nba_tracked_picks` (not picks alone) for time-series analysis
- Confirmed new theories require NO backend code changes — theories are rows in `nba_optimization_runs` Supabase table, cached every 5 min, with configurable sharp weights, devig method, EV threshold, prop filters, and soft book filters

**Decisions Made:**
- Deleted 393 picks with `triggered_ev` 50-100% (clearly bad math from line mismatches)
- Cleaning orphaned trail_entries where pick_id no longer exists in nba_tracked_picks (from both today's 393 deletion and earlier 1,218 deletion)
- Historical trails before ~2026-04-13 07:30 UTC should be treated as corrupted by push latency — only use for aggregate ROI (scalar fields), not tick-level analysis
- `sharp_count = 1` picks left in DB — filter at query time, don't delete

**Lessons Learned:**
- 55s push cycle >> 30s sharp freshness cutoff meant trails were captured intermittently with selection bias — you can't reconstruct continuous odds movement from pre-fix data
- `captured_at` timestamps in trails reflected when the slow push arrived at VPS, not when the scraper actually observed the odds — a subtle but meaningful distinction
- Trail contamination is sport/side-agnostic (not Under-specific), but different sharp coverage patterns across line sizes could create differential effects
- Theory system is fully data-driven via Supabase — only new devig algorithms or new filter types require code changes

**Action Items:**
- Verify orphan trail_entries cleanup completed successfully (was running in background)
- Consider adding a marker/note that trails before 2026-04-13 ~07:30 UTC are corrupted by push latency
- Fix Betstamp (book 365) and BlackStream/Surge (book 960) stale workers — both alive but returning `odds=0`
- Investigate zero-sharps issue flagged earlier (deferred from previous session)
- Recommended backtest query filter: `created_at >= '2026-04-13'`, `sharp_count >= 2`, `triggered_ev BETWEEN 1 AND 30`

### Session (09:32) [value-betting-scanner]

**Context:** Value betting scanner stabilization — data integrity cleanup, trail reliability assessment, and theory configuration review.

**Key Exchanges:**
- Confirmed Mini PC and VPS are in sync with 1,731 bet365 markets; dashboard "128" was browser cache from before restart
- Detailed breakdown of why Under picks are sparse: Overs outnumber Unders ~1.6x, 62% of markets have zero sharps, layered filters compound down to ~4 picks
- Honest assessment of system reliability for backtesting — identified what's solid (push cycle, snapshot cache, canonicalization, resolver) vs. caveats (sharp sparsity, dead soft book workers, trail granularity)
- Historical trail data declared unreliable for time-series backtesting due to 55s push cycle vs 30s sharp freshness cutoff creating intermittent capture with selection bias and gaps
- Confirmed backtesting should use `trail_entries` joined to `nba_tracked_picks` (not picks alone) for time-series analysis
- Confirmed new theories require NO backend code changes — theories are rows in `nba_optimization_runs` Supabase table, cached every 5 min, with configurable sharp weights, devig method, EV threshold, prop filters, and soft book filters
- Corrected framing on trail data: pre-fix trails are **real values at wider sample intervals**, not corrupt/garbage — each captured point is valid, just sparser. Most backtests (CLV, win rate, ROI) are unaffected; only sub-minute tick-level analysis is degraded.
- Reviewed docs landscape — `ARCHITECTURE.md` and `SYSTEM_GUIDE.md` both stale (last touched Apr 8), `brain/` wiki most actively maintained
- Jrue Holiday Assists Under 5.5 @ 1.909 on Bet365 2.0 — system tracks this prop historically but current market only has 1 matching sharp at line 5.5 (Novig), likely won't trigger enough theory passes

**Decisions Made:**
- Deleted 393 picks with `triggered_ev` 50-100% (clearly bad math from line mismatches)
- Cleaned 30,507 orphaned trail_entries where pick_id no longer exists in nba_tracked_picks (from both today's 393 deletion and earlier 1,218 deletion)
- `sharp_count = 1` picks left in DB — filter at query time, don't delete
- Pre-fix trails before ~2026-04-13 07:30 UTC: treat as lower temporal resolution, not corrupted — still usable for aggregate analysis
- Updated `SYSTEM_GUIDE.md` with "Recent Changes (April 2026)" section covering push/snapshot/canonicalization fixes, resolver fixes, and architectural shifts
- Created `brain/findings/2026-04-13-push-latency-and-trail-sparsity.md` with corrected framing (real data at wider intervals, not garbage)
- Pushed 9-file commit to origin/main, clean rebase on top of teammate's max-odds-filter and odds-range-calibration findings

**Lessons Learned:**
- 55s push cycle >> 30s sharp freshness cutoff meant trails were captured at wider intervals — you can't reconstruct sub-minute continuous odds movement from pre-fix data, but aggregate analysis is fine
- `captured_at` timestamps in trails reflected when the slow push arrived at VPS, not when the scraper actually observed the odds
- Trail sparsity is sport/side-agnostic (not Under-specific), but different sharp coverage patterns across line sizes could create differential effects
- Theory system is fully data-driven via Supabase — only new devig algorithms or new filter types require code changes
- Important framing distinction: sparse ≠ corrupt. Each trail data point captured under the old regime was a real observation — just fewer of them per unit time

**Action Items:**
- Verify orphan trail_entries deletion completed (30,507 entries in batches of 200, was running in background)
- Fix Betstamp (book 365) and BlackStream/Surge (book 960) stale workers — both alive but returning `odds=0`
- Investigate zero-sharps issue flagged earlier (deferred from previous session)
- Teammate added 3 new brain/findings worth reviewing: `nba-bet365-deep-dive`, `calibration-by-odds-range`, `profitable-segments-under` — the Under segments one is directly relevant to the under-pick sparsity concern
- Recommended backtest query filter: `created_at >= '2026-04-13'`, `sharp_count >= 2`, `triggered_ev BETWEEN 1 AND 30`

