# Daily Log: 2026-05-08 (lcash)

## Sessions

## Memory Maintenance

### Memory Flush (00:06) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (00:36) [value-betting-scanner]

**Context:** Empirically proving bet365 WebSocket subscription mechanism works for real-time odds repricing detection on MLB player props (TEX@NYY fixture).

**Key Exchanges:**
- User challenged whether WS subscriptions were legitimate since no odds were being returned — led to definitive proof test
- Subscribed to 10 PAs from 5 actively-pushing live fixtures; 2/10 returned real `L_U_DELTA` pushes within 30 seconds, confirming mechanism is valid
- User correctly identified that repricing detection is the whole point — confirmed this is a success, not a failure

**Decisions Made:**
- Initial 288 PA subscribe was incomplete; expanded to full 1354-PA universe across 4 FIs (`194210784` main + 3 alt-FI variants) — always enumerate all FIs/PAs, don't assume one endpoint has everything
- TEX@NYY silence accepted as genuine data-state (pre-game equilibrium), not a protocol bug — 29,788 PA-minutes of subscription time with zero events while live fixtures pushed normally
- v4 architecture confirmed as final: HTTP for initial state → WS PM-subscribe all PAs → listen for `L_U_DELTA` → update live odds in-place → feed devig/EV pipeline

**Lessons Learned:**
- bet365 WS subscribe/response format is asymmetric: client sends `PM{FI}-{PA_ID}`, server responds on `L{FI}-{PA_ID}_30_0` topic
- Delta payload format: `\x15L{FI}-{PA_ID}_30_0\x01U|OD=...;HA=...;SU=...;|`
- Parser regex for incoming frames: `\x15L(\d+)-(\d+)_30_0\x01U\|([^|]+)`
- Pre-game MLB player props don't reprice at 6h out — sharps aren't active yet; repricing starts closer to first pitch
- Even on busiest live fixtures, individual PAs only push 5-11 times/min — 20-50% hit rate in any 30s window is normal
- EMPTY acks from server confirm subscription format is accepted (60→180→235→300 growing across refresh waves)
- SPA's default broadcast topics (`OVInPlay_30_0`, `OV_POPULAR_30_0`) never carry pre-game MLB fixture data — passive monitoring alone is useless for pre-game

**Action Items:**
- Update mini PC v4 parser regex to `PM`+`L_U` shape and ship real-time scanning
- Run near-tipoff monitoring session to confirm repricing transition window empirically
- Document the full subscribe→listen→parse pipeline as reference for `ws_nba.py` / `ws_mlb.py` implementations

### Memory Flush (07:14) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (07:48) [value-betting-scanner]

**Context:** Investigating whether bet365 pushes pre-game prop odds changes via WebSocket (WS) or only via HTTP polling — running empirical probes on live MLB fixtures.

**Key Exchanges:**
- Ran a "super observer" probe on fixture E25665124 that subscribed to 1464 PAs across 3 FIs, captured WS frames + HTTP snapshots over 12 min. Result: 0 WS price deltas, 0 HTTP odds changes, but 84 PAs removed (market suspensions/closures).
- Discovered two new WS frame prefixes (0x31 and 0x23) — likely control-plane/session routing, not data delivery.
- Pivoted to a more naturalistic test: open the actual bet365 page, expand all dropdowns, passively observe WS traffic while running parallel HTTP T0/T1 diff for ground truth comparison.

**Decisions Made:**
- Run dual-probe approach (passive WS observation + HTTP diff) simultaneously to get definitive coverage rate comparison — if HTTP shows N changes and WS shows 0, that's conclusive proof pre-game props are HTTP-only.
- Dropped active PM/BS subscribing in favor of letting the SPA do its own subscribing naturally (more realistic test).

**Lessons Learned:**
- Audit code had a hardcoded FI from a previous game (`PM194210784-*` instead of `PM194210790-*`) — caused a false INVALID verdict. Always parameterize fixture-specific checks.
- The `expand_all` selector only found 1 collapsible element — bet365's MLB prop page may start mostly expanded, or the CSS selectors don't match bet365's actual class names. Selector fragility is a recurring issue.
- Across all tests (30+ min of observation across multiple fixtures): consistent pattern of heavy global WS traffic but **zero price deltas for pre-game props**. Strong emerging conclusion: bet365 pre-game prop odds are HTTP-poll only.

**Action Items:**
- Await final results from both probes (~08:01 finish) for definitive HARD MISS confirmation
- If confirmed, document "bet365 pre-game props = HTTP-only" as an architectural fact for the value betting scanner
- Consider implications for scanner architecture: must use HTTP polling for pre-game odds, WS only useful for in-play

### Session (09:04) [value-betting-scanner]

**Context:** Shipped bet365 v4 WebSocket parser/orchestrator fixes (NBA + MLB), verified end-to-end, then deployed to mini PC after discovering degraded discovery state.

**Key Exchanges:**
- Fixed sticky-context bug in `_extract_pa_to_fi()` — PA-inline FI was polluting `current_fi` for sibling PAs
- Live verification proved WS pricing movement works: 450 deltas decoded in 60s from passive L-format broadcasts (OD 80%, HA 22%, SU 11% extraction rates)
- TEX@NYY showed 0 PM deltas during 30s window — confirmed as pre-game equilibrium, not a code bug
- Stage 9 empirical test: 11-min HTTP-vs-WS comparison found **4 real prop repricings but zero WS pushes** despite 1354 active PM subscriptions — bet365 pre-game MLB streaming doesn't push deltas even with correct format

**Decisions Made:**
- Topic format changed to `PM{FI}-{PA_ID}` (no `_30_0` suffix) — uses per-PA FI from `_extract_pa_to_fi` instead of global `fixture_id`
- Inject now targets `premws-pt1` specifically instead of first-open WS; auth token (`A_*`) captured via `WebSocket.prototype.send` hook
- Discovery PD-path over-collection (Issue 1) confirmed already solved — `valid_d_vals` self-adapts from Pass 1 real games; the 1-fixture stall was session-age staleness, not a filter bug
- Deploy all 4 files (`ws_nba.py`, `ws_mlb.py`, `ws_nba_v4.py`, `ws_mlb_v4.py`) in one shot with clean restart

**Lessons Learned:**
- Always verify with live test before claiming "done" — synthetic regex tests passed but real wire verification was still needed
- Mini PC discovery degradation pattern: 18 fixtures → 1 fixture over ~2 hours, then stuck for 7+ hours; root cause is SPA session aging, fix is clean restart
- Pre-game bet365 WS does NOT stream price deltas even with correct PM subscription format — this is a market architecture limitation, not a code bug. HTTP polling remains necessary for pre-game odds.
- `__lastSent` buffer (200 outgoing frames) is sufficient to capture auth tokens — 17 frames were available at check time

**Action Items:**
- Verify post-restart discovery recovers and finds today's full slate (expect ≥95% FI mapping in `K/K have FI for PM-subscribe` log line)
- Near-tipoff test still needed: confirm PM/BS deltas decode correctly when lines actually move (the "last mile" verification)
- Evaluate hybrid approach: HTTP polling for pre-game + WS for in-play, given Stage 9 findings

### Memory Flush (09:38) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (10:09) [value-betting-scanner]

**Context:** Debugging and fixing the v3 value-betting scanner's MLB discovery pipeline on the Dell mini PC, specifically trying to get bet365 MLB market discovery working for a single-game day (Padres @ Cardinals).

**Key Exchanges:**
- Pipeline health script (`scripts/pipeline_health.sh`) was built and confirmed working — first time having a one-page pipeline status view
- Fix A (INJECT_JS strict premws-pt1 only) and Fix B (discovery event_id length ≥7 filter) both verified working in production logs
- NBA pipeline fully healthy: 1171 markets, 917 matched (78%), 271 EV picks, top at 124% EV
- MLB discovery was genuinely BLOCKED — `locator.count()` hanging indefinitely with no timeout wrapper

**Decisions Made:**
- Added timeout wrapper to discovery click loop so it falls through to URL fallbacks instead of hanging (fixes the 13-min silent hang)
- Added `'Baseball'` as fallback sidebar label for MLB (some bet365 layouts use parent sport name)
- Added Path 4 bare-league URL fallback (`/AC/B16/C20525425/` without `/AS/` prefix)
- Decided against further blind URL iteration — recommended manual click as fastest unblock for today's single-game edge case

**Lessons Learned:**
- bet365 sidebar text changes contextually (e.g., "NBA Playoffs" vs "NBA") — discovery click targets need to account for this
- bet365's `marketscontentapi` does NOT return games when a league has only 1 fixture for the day — this is a corner case that breaks all automated URL-based discovery paths
- The game IS in `gethomepagepods` (homepage carousel) but uses a different segment shape than standard `marketscontentapi` PA segments — not parseable by current Pass-1 logic
- `locator.count()` in Playwright has no implicit timeout (unlike `bbox()` which had 3s) — always wrap CDP/Playwright calls with explicit timeouts
- Tailscale direct connection to mini PC works for API calls; SSH-curl had buffering issues

**Action Items:**
- Consider building "manual fixture override" env var (`MLB_FIXTURE_OVERRIDE_URL`) for edge cases where auto-discovery fails (~20 lines, option B)
- Consider parsing `gethomepagepods` response shape to extract single-game fixtures as an additional discovery path
- Re-verify MLB discovery on a normal multi-game day to confirm timeout fix + Baseball fallback work end-to-end
- User needs to manually click into Padres@Cardinals game on AdsPower to unblock today's MLB pipeline

### Session (10:44) [value-betting-scanner]

**Context:** Debugging and fixing v3/v4 scanner MLB discovery + assessing props streaming health on Dell VPS

**Key Exchanges:**
- bet365's `marketscontentapi` for league `C20525425` returns 0 fixtures on single-game MLB days — all 4 URL fallback paths (sidebar click, "Baseball" label, `/AS/AC/B16/...`, bare `/AC/B16/...`) yielded nothing
- User pointed out the actual MLB competition root URL is `D48/E1096/F10` format — discovery never tried this because it's where sidebar click *should* land, but the click was failing silently
- Root cause found: Playwright's `page.get_by_text("MLB", exact=True).first.count()` hangs indefinitely on bet365's heavy SPA even with 3s timeout wrapper — the span IS in the DOM at (50, 438) but Playwright's locator API gets stuck
- Fix deployed: replaced Playwright text-locator with direct `page.evaluate(JS)` DOM scan that walks `<span>/<a>/<div>` for exact text matches and returns center coords — universal for all sports
- Verified live: `CDP-clicked sidebar 'MLB' at (64, 446)` → `captured markets (len=25015)` → `found 17 pre-game fixtures`

**Decisions Made:**
- JS DOM scan over Playwright locators for all sidebar clicks — Playwright's locator API is unreliable on bet365's SPA (rationale: DOM element exists but locator resolution hangs)
- Path 0 homepage-pod nav added as pre-emptive step but correctly self-skips when target is futures-only (`PD ... is futures-only`)

**Lessons Learned:**
- Playwright `get_by_text()` can hang on heavy SPAs even when the element is visible and clickable — `page.evaluate()` with raw JS DOM traversal is more reliable for bet365
- bet365 doesn't surface single-game MLB days through standard league-level marketscontentapi — the game only appeared when sidebar click actually navigated to competition root
- **Handoff doc described Stream B active recovery as "shipped" but it wasn't actually deployed** — only the passive `logger.error()` and threshold constant were in the code, not the close+reopen recovery logic. Verify claimed implementations against actual deployed code
- `_add_lock` (asyncio.Lock per orchestrator) completely serializes per-game walks, defeating the outer `SETUP_CONCURRENCY=3` semaphore — MLB with 17 fixtures × 26 GIDs would take ~68 min sequential
- MLB orchestrator page stuck at body=13663 (vs normal 50-130KB) — login/captcha splash state, and no auto-recovery fires

**Action Items:**
- Build and deploy actual Stream B recovery (close page → reopen → re-measure → raise on persistent <40KB) into `ws_mlb_v4.py` + `ws_nba_v4.py`
- Remove or rework per-orchestrator `_add_lock` so `SETUP_CONCURRENCY=3` delivers real parallelism (needs separate Playwright pages per concurrent walk)
- Manually kick MLB AdsPower Chrome page to clear degraded body state as immediate unblock
- NBA health: 77% match rate, 846 PAs on 30s refresh, 2/4 games captured (CLE@DET and SA@MIN failed — no wizard body)
- MLB health: 17 fixtures discovered but 0 PAs delivered — stuck on first add_game for 13+ min due to small-body page

### Session (11:17) [value-betting-scanner]

**Context:** Refactoring v3 value betting scanner for parallel game processing and fixing bet365 boot/discovery failures

**Key Exchanges:**
- User asked why NBA was only looking at one game — answer was 2 of 4 games already in-progress (correctly marked invalid by fail-fast), plus refresh loop was still sequential even though add_game had been parallelized
- User asked "are you sure you're clicking MLB?" / "you haven't hardcoded have you?" — honest answer: yes, partially. `default_league_id` values hardcoded in `sport_config.py`. URL fallbacks using those IDs are now dead (bet365 deprecated `#/AC/B16/C20525425/` pattern)
- MLB discovery failing because page booted to bare `bet365.com.au/` without `#/HO/` hash, so SPA sidebar never rendered → DOM scan timeout → fell through to dead URL fallbacks → 0 fixtures

**Decisions Made:**
- Lower boot health threshold from 40K to 20K body length — 40K was too strict, bet365 homepage legitimately renders 28-43KB; real validation happens at wizard level (250KB+)
- Recovery on degraded boot: warn + continue instead of fatal RuntimeError — orchestrators no longer crash on slow homepage renders
- Force-navigate to `#/HO/` after boot if URL is bare — ensures SPA renders sidebar for dynamic click-based navigation
- Drop dead URL fallback paths rather than updating hardcoded competition IDs — return empty instead of hitting deprecated endpoints
- Parallel refresh loops via `asyncio.gather()` + per-game tabs for both NBA and MLB — sequential refresh was taking longer than the 30s interval on bigger slates

**Lessons Learned:**
- `_add_lock` (asyncio.Lock) was the bottleneck serializing all game setup — removing it and using per-game browser tabs enables true parallelism
- bet365 cached cookies auto-login works reliably; the 60s wasted trying to click a non-existent "Log In" button was pure waste — poll `_is_logged_in()` first for up to 10s before attempting click flow
- bet365 SPA requires hash-based routing (`#/HO/`) to render navigation elements; bare URL loads a minimal shell without sidebar
- Hardcoded competition IDs (`C20525425`, `C20604387`) are fragile — the IDs themselves haven't changed but the URL patterns around them have been deprecated by bet365
- For fail-fast: games that return no wizard body or 0 PAs should be permanently skipped (`_failed_fixtures` set) — previously retried every 5 min, wasting ~17s/game/cycle
- MLB's 26 G-ID walk should bail after 4 consecutive empty walks — saves ~3 min per stale/completed game

**Action Items:**
- Monitor next discovery cycle to confirm `#/HO/` fix resolves MLB sidebar click failure
- Watch for `refresh loop: N PAs updated across M games in X.Xs (parallel)` log line confirming parallel refresh is live
- Consider making `default_league_id` values dynamically discovered rather than hardcoded
- Tomorrow's bigger NBA slate (6-10 games) will be the real test of parallel refresh — sequential would exceed 30s interval

### Session (12:11) [value-betting-scanner]

**Context:** Debugging and fixing bet365 scanner v4 — NBA single-game issue, MLB sidebar click failure, parallel refresh deployment

**Key Exchanges:**
- NBA showing only 1 active game: 2 of 4 games already in-progress (correctly marked invalid), but SA@MIN's inject failed due to `premws_not_ready` — WS briefly closed during MLB's parallel recovery and 5×1.5s retry window too short
- Sequential refresh loop had shared-state race: one `self._page` + one `self._current_eid[0]` meant response bodies got dropped or mis-attributed when page navigated between games before responses arrived
- 3+ bet365 homepages opening at startup: login + orchestrator boot + discovery worker each open their own tab — 3 navs × 2 sports = up to 6 navigations
- MLB DOM scan hanging: `page.evaluate()` walking live `querySelectorAll('span,a,div')` collection never returned — bet365 SPA mutates DOM during `getBoundingClientRect`, hanging the live NodeList iteration

**Decisions Made:**
- **Parallel refresh via asyncio.gather()** over per-game tabs (both NBA and MLB) — eliminates shared-state race, wall-clock = slowest single game (~15s) instead of sum-of-all
- **Removed Path 3 + Path 4 dead URL fallbacks** — deprecated bet365 URLs returning "page no longer available", only wasting time. Kept Path 0 (sidebar click) and Path 2 (dynamic PD nav from gethomepagepods)
- **Inject retries bumped 5×1.5s → 12×2s** — gives WS reconnect window 24s instead of 7.5s
- **Crash-proof DOM scan**: `Array.from()` materializes live collection before walking, per-element try/catch, 8000-element cap + early break on 5 matches

**Lessons Learned:**
- Live NodeList iteration (`querySelectorAll`) can hang in heavy SPAs if the DOM mutates during element access (e.g., `getBoundingClientRect` triggers layout recalc which triggers SPA observer which modifies DOM) — always materialize with `Array.from()` first
- Sequential refresh loops with shared page state create attribution races — per-resource isolated tabs eliminate entire class of bugs
- When retry windows are shorter than recovery windows (WS reconnect), inject silently fails — retry budget must exceed expected recovery time

**Action Items:**
- Future: consolidate startup tab usage — have orchestrator + discovery reuse login's already-loaded tab (saves ~2 navs × 2 sports = ~30s on cold boot)
- Monitor MLB sidebar click with new crash-proof scan — confirm `scanned: N` diagnostic prints and click succeeds
- User requested full server shutdown (kill v3 python, end V3_Scanner schtask, stop both AdsPower profiles) — not yet executed, deferred for push

### Session (12:48) [scraper]

**Context:** Building a Facebook group scraper to find closer-hiring posts in `highticketclosersusa` group

**Key Exchanges:**
- Debugged browser automation issues: wrong tab grabbed (Facebook profile instead of group), CDP connection breaks, AdsPower port changes
- Discovered that `role="article"` captures both posts AND comments — needed to filter comments out
- Found that `[data-ad-rendering-role="story_message"]` is the correct selector for real posts (not `role="article"`)
- Went from 0 → 37 (mixed junk) → 89 real posts after selector fix

**Decisions Made:**
- Use `data-ad-rendering-role="story_message"` as the post selector instead of `role="article"` — the latter grabs comments and empty placeholders
- Use `evaluate()` instead of `inner_text("body")` locator to avoid timeout issues on FB pages
- Built a multi-category classifier (HIRING_CLOSER, HIRING_BOTH, HIRING_SETTER, SEEKER, OTHER_CLOSER, OTHER) instead of binary — distinguishes closer vs setter hiring for better ICP targeting
- Sort CSV output with closer-hiring posts prioritized at top

**Lessons Learned:**
- Facebook DOM: comments also have `role="article"` — discriminate with `data-ad-rendering-role="story_message"` for actual posts
- Facebook stores canonical numeric group IDs (e.g., `524440572541344`) even when groups have vanity slug URLs (`highticketclosersusa`)
- AdsPower browser automation: CDP port changes on restart — always re-check the port
- Posts load lazily as you scroll; initial DOM may only have empty placeholder articles
- `networkidle` wait strategy helps ensure posts render before extraction

**Action Items:**
- Tighten classifier: fix false positives where seekers ("I'm looking to break into...") and training sellers are miscategorized as HIRING_CLOSER
- Consider filtering by post recency/date for fresher leads

### Memory Flush (13:13) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (13:19) [value-betting-scanner]

**Context:** Reviewing VPS-to-scanner data architecture for transitioning from mini PC to Eve server

**Key Exchanges:**
- User asked how VPS queried structured data from the mini — initial answer incorrectly stated push model (ev_push_worker.py). User corrected this.
- Actual architecture: **VPS pulls from mini over Tailscale** via `v3/vps/server.py` — FastAPI proxy on VPS port 8803 that forwards browser requests to mini's v3 odds-server (port 8900)

**Decisions Made:**
- The v3 VPS architecture is a **pull/proxy model**, not push. VPS proxies all API calls (`/v1/odds`, `/v1/stream`, `/v1/picks`, `/v1/games`, `/v1/ping`) from browser → VPS → mini over Tailscale (`MINI_PC_URL = http://100.67.233.95:8900`)
- For Eve transition: same proxy model works — just need Tailscale on Eve and update `MINI_PC_URL` in VPS env to Eve's Tailscale IP

**Lessons Learned:**
- There were **two** data-delivery patterns across versions: the older `ev_push_worker.py` (push model from v2-era sport servers) AND the v3 pull/proxy model in `v3/vps/server.py`. The v3 model is the current/correct one
- `v3/vps/server.py` also has a background `_ping_loop()` hitting `/v1/ping` every 20s for status caching — this is what powers the dashboard connectivity indicator

**Action Items:**
- Install Tailscale on Eve (blocker for VPS→Eve connectivity)
- Update `MINI_PC_URL` in VPS env to Eve's Tailscale IP once Tailscale is set up
- Restart `v3.vps.server` after config change

### Session (13:56) [value-betting-scanner]

**Context:** Testing MLB/NBA discovery + orchestrator pipeline locally against AdsPower, then implementing parallel G-ID walk optimization for MLB add_game bottleneck.

**Key Exchanges:**
- Local discovery test: MLB (15-18 matchups) and NBA (4 matchups) both pass. Path 0 futures-only safety check works correctly — falls through to Path 1 sidebar click.
- Boot health gate (Stream B) verified: both orchestrators boot with body >40KB threshold.
- Status registration (Stream C) verified: `/v1/status` returns correct per-sport keys.
- MLB `add_game` sequential bottleneck identified: 26 G-ID walk takes ~172s per game, making 18-game slates take 51+ minutes.
- Parallel G-ID walk implemented (`GID_PARALLELISM=4` tabs per game) — achieved **5.9× speedup** (172s → 29s per game).
- **Rate-limiting discovered post-implementation**: 4 parallel tabs caused bet365 to soft-block — only 10/26 G-IDs returned bodies (72 PAs instead of expected 250-400). User reported odds disappearing in UI during burst.

**Decisions Made:**
- `GID_PARALLELISM=4` is too aggressive — causes rate-limiting. Need to drop to 1 or add inter-nav sleeps (or both). Awaiting user confirmation.
- Tri-state game classifier added: `live_or_completed` (start_time < now → never retry) vs `pregame_unverified` (start_time > now → retry next cycle). Not yet exercised in production.
- `SETUP_CONCURRENCY=5` (existing per-game parallelism in DiscoveryWorker) is fine — the bottleneck was intra-game, not inter-game.

**Lessons Learned:**
- bet365 rate-limits aggressively on concurrent contentapi navigations from the same session — even 4 parallel tabs triggers soft-blocking (empty bodies).
- The 26 G-ID sequential walk was the real MLB bottleneck, not the per-game `add_game` dispatch (which already had concurrency=5).
- 16/26 G-IDs returning empty is the rate-limit signal — missing prop types include Hits, Strikeouts, RBIs, Runs, Total Bases, Singles/Doubles/Triples.
- Local testing can't exercise the tri-state classifier (needs 0-body games), tab corruption recovery (needs broken pages), or boot health degraded path (needs unhealthy AdsPower).

**Action Items:**
- Awaiting user confirmation: can they see Hits/Strikeouts/RBIs in bet365 UI for HOU@CIN? (confirms rate-limit vs legitimately missing markets)
- Dial back `GID_PARALLELISM` from 4 → likely 1-2, and/or add 2-3s human-like sleeps between G-ID navigations
- Deploy to mini PC once parallelism tuning is settled
- Monitor tri-state classifier + tab corruption recovery in production

### Session (14:29) [value-betting-scanner]

**Context:** Fixing bet365 scraper bot detection and sidebar click bugs, deploying to Eve (mini PC)

**Key Exchanges:**
- Identified `ctx.add_init_script(INTERCEPTOR_JS)` as smoking gun for bot detection — applies WS wrapper + `window.__wsObjs`/`__lastSent` globals to ALL tabs including walk tabs, making them trivially fingerprintable
- Benchmarked 5 parallelism/injection configs; **2-tab + page-level inject + no decoy = 76 PAs in 71.8s** (best balance)
- Sidebar "MLB" click had `matched=2` — was hitting wrong element. Fixed with candidate iteration + post-click URL verification (`C20525425` in URL)
- Deployed discovery.py to Eve, fix verified working — but Eve's bet365 SPA boots at 55KB vs 130KB normal (degraded/stale session), so 0 fixtures found despite correct navigation

**Decisions Made:**
- `GID_PARALLELISM = 2`, `DECOY_WAIT_SECS = 0.0` locked as final config — IP-level rate limit ceiling, decoy unnecessary with clean walk tabs
- Page-level WS hook (`self._page.add_init_script`) instead of context-level — walk tabs spawn without bot fingerprints
- Incremental deploy strategy: discovery.py first → Stream B (boot health gate) → full deploy — validate each layer before stacking

**Lessons Learned:**
- `add_init_script` at context level pollutes every page in the browser context — always scope to specific pages that need it
- `window.WebSocket.toString()` no longer returns `[native code]` after reassignment — trivial anti-bot fingerprint
- When multiple DOM elements match a text selector, iterate candidates and verify post-click URL rather than taking first match
- 55KB boot body (vs 130KB normal) = degraded session state — no code fix will help until AdsPower session/login is refreshed
- Local test results are depressed when sharing AdsPower session with manual browsing (same IP rate-limit bucket)

**Action Items:**
- Check AdsPower login state on Eve — likely needs manual login refresh or CAPTCHA resolution
- Deploy Stream B (`ws_nba_v4.py` + `ws_mlb_v4.py`) with boot health gate for auto-recovery of degraded sessions
- Deploy remaining Stream A/C changes after confirming healthy ~130KB body + >0 fixtures on Eve
- Eve connection info: `192.168.0.92`, user `the-fellowship`, pwd `Ring1357!`, repo at `~/value-betting-scanner`, v3 runs in tmux session `v3`

### Session (15:07) [value-betting-scanner]

(Apologies — looks like the conversation context was already the summary! Let me provide the daily log entry.)

**Context:** Refactored MLB v4 scraper from 26-G-ID walk to single BB wizard fetch, yielding 11× more props in 26× fewer requests.

**Key Exchanges:**
- Investigated why Eve (Ubuntu mini server) was returning empty bodies — underlying AdsPower session/login problem, not a code issue
- User asked about remote desktop options for Eve — provided xrdp, AnyDesk, Chrome Remote Desktop options with install commands
- User redirected focus to local testing and asked about historical v1 approach (`bet365_mlb_game_v1.py` / `bet365_mlb_game_worker.py`)
- Analysis of v1 revealed it used a single BB wizard endpoint (`/I99/`) returning all props in one fetch — the 26-G-ID walk was an architectural regression
- Ran proof-of-concept: wizard returned 838 PAs (9 prop types, 20 players) vs 76 PAs from the old 26-G-ID walk
- Full e2e test passed: 865 PAs, 9.7s add_game time, 100% FI mapping, WS topics injected successfully

**Decisions Made:**
- **Switch MLB v4 to wizard-first pattern** (matching NBA's existing approach) — rationale: 11× more props, 26× fewer requests, eliminates rate-limit cascade entirely
- **Keep old walk functions in code for instant rollback** — one-block switch in `add_game` to revert if wizard path breaks in prod
- **Use AdsPower profile `k1c9tptw`** for local testing

**Lessons Learned:**
- The `/I99/` BB wizard endpoint returns ALL prop types (~268KB) in a single fetch — same endpoint NBA already uses successfully
- The 26-G-ID walk was added for "completeness" (CO/combo variants) but those markets have no sharp pair and produce zero +EV picks — we were paying 26× the request budget for nothing
- v1's architecture was actually superior for this specific use case — newer isn't always better
- Alt lines (full threshold ladders) come free in the wizard response

**Action Items:**
- Deploy wizard refactor to Eve (rsync + restart) — deployment commands documented in V3_SYSTEM_GUIDE.md
- Verify Eve's AdsPower session is healthy before deploying (check for login/CAPTCHA screen)
- Deploy Stream B (boot health gate: `ws_nba_v4.py` + `ws_mlb_v4.py`) for auto-recovery on degraded boots
- Monitor post-deploy: search logs for `wizard fetch` + `PAs mapped from wizard` to confirm success
- After MLB wizard is stable, deploy parallel G-ID walk only if needed for non-wizard markets

### Session (15:38) [value-betting-scanner]

**Context:** Refactored MLB bet365 scraper from 26-G-ID walk to single BB wizard fetch per game, tested E2E, and validated centralized odds JSON output.

**Key Exchanges:**
- MLB wizard refactor of `add_game()` in `ws_mlb_v4.py` — 7.4× faster (71.8s → 9.7s), 11.4× more PAs (76 → 865), 26× fewer HTTP fetches
- Full production-style test (`python -m v3.startup`) ran successfully: 5313 markets, 488 pulse, 25 +EV picks flowing
- User asked about data refresh cadence — NBA 30s, MLB 60s, WS deltas <1s in-play; discovery every 30min (5min near tipoff)
- Confirmed `/v1/odds?sport=mlb` endpoint produces centralized JSON with all books side-by-side per market

**Decisions Made:**
- Old walk functions (`_walk_game_parallel` etc.) kept in same file for instant one-block rollback if wizard breaks in prod
- `about:blank` parking between cycles is intentional (avoids SPA rate-limit competition) — optional change to `bet365.com.au/#/HO/` offered but not acted on

**Lessons Learned:**
- **Critical bug caught**: `_refresh_loop` was still using old 26-G-ID walk after `add_game` was refactored to wizard — caused 364 requests/min per cycle instead of 14. Always audit ALL callers when refactoring a data-fetch pattern, not just the initial load path.
- Extracted `_fetch_wizard_for_game()` as shared helper (single source of truth) used by both `add_game` and `_refresh_loop`
- Wizard endpoint returns ALL prop types in one body — new props appear automatically on next refresh cycle, no separate "prop discovery" step needed

**Action Items:**
- Deploy to Eve (rsync command + log-tail commands documented in V3_SYSTEM_GUIDE.md)
- Monitor first production refresh cycle for `(wizard)` log signature confirming fix is active
- Expected prod impact: MLB initial population ~10min → ~25s, market count ~1,400 → ~12,000+, rate-limit cascade eliminated

### Memory Flush (16:29) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (17:30) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (17:54) [value-betting-scanner]

**Context:** Debugging memory leaks and UI performance in the value betting scanner's scraper + dashboard on Eve VPS

**Key Exchanges:**
- Identified a Playwright page leak in `ws_nba_v4.py::_fetch_wizard_in_tab` and `ws_mlb_v4.py::_fetch_wizard_body` — missing `try/finally` around page lifecycle meant `CancelledError` during asyncio sleep would leak pages. Third site (`_walk_gids_in_one_tab`) already had try/finally.
- Deployed the fix but RSS still grew at ~3 GB/min — page leak was NOT the root cause of the memory blowup. A clean restart (possibly after clearing stale `__pycache__`) showed healthy steady-state at ~22 MB/min growth, suggesting the earlier blowup was a one-off (stale bytecode from rsync).
- Built and ran a per-book freshness probe: bet365 pristine (sub-3s), sharp books showing ~197s "stale" is misleading — just means no line moves since last OpticOdds REST seed, SSE deltas are still flowing. Freshness ≠ data quality for sharps; should track delta events/min instead.
- UI performance audit via Playwright on AdsPower: render() median 33-93ms, 10-14 calls/25s — not arithmetically slow. FPS=1 on NBA was a red herring (backgrounded tab throttles rAF). Added a debounce to render() but it barely activates. Asked user to specify what interaction feels laggy.

**Decisions Made:**
- Wrap all Playwright page lifecycles in `try/finally` for `page.close()` — defense against CancelledError leaks
- Add `__pycache__` clearing to the restart/deploy script to prevent stale bytecode issues after rsync
- Freshness probe (`/tmp/freshness_probe.py`) and `pipeline_health.sh` are the two freshness monitoring tools; freshness metric only meaningful for bet365, sharps need delta-rate tracking

**Lessons Learned:**
- `try/finally` around async page lifecycle is critical — `CancelledError` in `await asyncio.sleep()` silently skips cleanup code
- A 3 GB/min RSS blowup that disappears on clean restart points to stale `__pycache__` / bytecode from rsync deploys, not necessarily a code leak
- Playwright rAF-based FPS measurement is useless on backgrounded/unfocused tabs (throttled to 1 Hz) — need to ensure tab is focused or use different metrics
- SSE-driven dashboard that rebuilds full DOM on every delta *looks* like the problem but measured render cost was modest; need user-reported interaction details to find real bottleneck

**Action Items:**
- Add `__pycache__` clearing to deploy/restart script
- Add RSS watchdog that kills v3 if RSS > 4 GB
- Commit `freshness_probe.py` to repo at `scripts/freshness_probe.py`
- Patch `pipeline_health.sh` default `MINIPC_IP` to Eve's TS IP
- Get user feedback on which specific UI interaction is laggy before further optimization
- Consider tracking sharp book delta events/min as a true freshness metric

### Session (18:36) [value-betting-scanner]

**Context:** Debugging and fixing a critical memory leak in v3 value betting pipeline that caused RSS to balloon from ~300MB to 35GB+

**Key Exchanges:**
- Initial assumption was page-leak (stale Chromium tabs from AdsPower) — disproved: only 3 tabs each, clean
- Second hypothesis was unbounded websocket queue in `_raw_ws_listener` (Network.enable flooding) — applied `max_queue=128` + cheap substring filter before `json.loads` — didn't help
- Third hypothesis was fresh Chromium needed per v3 start — bounced AdsPower profiles — same growth pattern, disproved
- Disabled WS listener entirely via env var gate (`V3_WS_LISTENER=0`) — leak persisted, ruling out WS layer entirely
- Used **py-spy** on live process to get real stack traces — pinpointed `_stream_events` at `v3/pipeline/server.py:304` (SSE producer) as the hot loop
- **Root cause:** `store.change_event.wait()` never cleared the event after waking — once set, every subsequent `wait_for` returned instantly, spinning the asyncio loop at millions of iterations/sec, leaking Task + TimerHandle objects at ~75 MB/sec

**Decisions Made:**
- Applied `change_event.clear()` before wait in SSE producer — same pattern already fixed in tracker but never applied here
- Kept all the defensive WS fixes (max_queue, substring filter, try/finally page lifecycle) even though they weren't the root cause — good hygiene
- Re-enabled WS listener (`V3_WS_LISTENER=1`) after confirming real fix worked

**Lessons Learned:**
- **Don't guess at memory leaks — profile first.** Three hypotheses were wrong before py-spy instantly found the real culprit
- **asyncio Event.wait() without Event.clear() is a spin-loop bug** — if the event stays set, wait_for returns instantly forever. This is a recurring pattern (happened in tracker too, then again in SSE producer)
- **py-spy is the right tool for live Python memory/CPU issues** — `py-spy dump --pid` gives immediate stack traces without attaching a debugger
- **RSS growth rate can mislead about the source** — the ~75MB/s rate happened to match httpx SSL context creation estimates, leading to a red herring

**Action Items:**
- Audit any other `change_event.wait()` patterns in the codebase for the same missing `.clear()` bug
- Full deployment completed: server.py fix, WS hardening, startup.py bind fix, dashboard debounce, scripts promoted, system guide updated, firewall locked down

### Session (19:11) [value-betting-scanner]

**Context:** Debugging and fixing a critical memory leak in the v3 value betting pipeline, then committing/deploying all hardening work.

**Key Exchanges:**
- RSS was climbing ~22 MB/min (70 MB → 16 GB in 3 min), CPU pinned at 87%, logs freezing
- Systematically ruled out: AdsPower/Chromium state (fresh profiles didn't help), WS listener (env-gated off, leak persisted), httpx SSL contexts
- py-spy stack dump revealed the real culprit: `_stream_events` SSE producer at `v3/pipeline/server.py:304`
- `change_event.wait()` never cleared → `wait_for` returned instantly → infinite spin allocating Task + TimerHandle objects at ~75 MB/s

**Decisions Made:**
- One comprehensive commit (`afbf499`) for all session work rather than granular commits — tied to same shipping cycle, coherent story for collaborator
- `V3_WS_LISTENER` env gate kept at `1` (re-enabled) after confirming WS wasn't the leak source
- VPS ufw port 8900 restricted to Tailscale IP only (security hardening)

**Lessons Learned:**
- **Same bug, different location:** The `change_event.clear()` fix was already applied to the tracker but missed in the SSE producer — when fixing an event-loop spin bug, grep the entire codebase for the same pattern
- **py-spy is the right first tool** for memory leaks in async Python — stack dumps immediately showed the hot loop instead of guessing from log timing or httpx patterns
- **Post-deploy verification matters:** VPS was serving 404 on `/nba` and `/mlb` because the deploy didn't actually take — the running process was still the old version. Always hit the routes after restart.
- After fix: RSS stable at 305 MB @ 19 min (~0.8 MB/min), CPU 22%, both sports flowing

**Action Items:**
- Row virtualisation for MLB dashboard — 539-row initial mount causes a 2-second long-task and scroll jank (27 "awful" frames in 25s)
- Watch for the `change_event.clear()` pattern anywhere else in the codebase (could exist in other producers/consumers)

