# Daily Log: 2026-04-14 (lcash)

## Sessions

## Memory Maintenance

### Session (07:56) [superwin]

**Context:** bet365 racing adapter — Tuesday morning stress test attempt, discovery-timing gap found.

**Key Exchanges:**
- Fired up adapter to test full Tuesday morning racing card vs. previous Sunday evening run
- Splash HTTP endpoint (`nexttojumpcontentapi/splash?...T1#`) returned **0 meetings** at 07:52 AEST — bet365 hasn't populated the Today splash that early
- Confirmed race data IS flowing via WebSocket OV topics (Manawatu, Townsville visible in WS frames) — different data path than what adapter uses
- Previous Sunday 16:30 run: splash returned 16 meetings and 45 active runners; Tuesday 07:52: pipeline aborts at discovery with 0 results

**Decisions Made:**
- **Identified architectural gap**: adapter relies solely on splash HTTP endpoint for discovery, but bet365 populates WS topics hours earlier — this is a timing dependency, not a code regression
- **Recommended WS-based discovery (#2)** as the right long-term fix — parse race info directly from WS OV topics to eliminate splash-timing dependency entirely
- **Deferred implementation** pending decision: build WS discovery now vs. wait for splash to populate and stress-test scaling issues first

**Lessons Learned:**
- **bet365 splash endpoint has a population delay** — the "Today" splash returns empty until ~09:00 AEST, but WS topics already carry live race data by 07:50. The SPA's "Next to Jump" view uses WS, not the splash API
- **Two distinct data paths exist**: HTTP splash (batch, delayed) vs. WS OV topics (real-time, always-on) — relying on only one creates a time-of-day blind spot
- **Sunday evening testing masked this** because splash was already fully populated by afternoon

**Action Items:**
- Build WS-based discovery path to parse race info from OV topics (eliminates splash-timing dependency)
- Re-run full adapter test once splash populates (~09:00+ AEST) to stress-test scaling on a real Tuesday card
- Outstanding from previous session: fix runner map degradation after ~5 meetings, investigate subscription chunk-dropping (only ~45 runners respond regardless of chunk count)

### Memory Flush (08:31) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (09:21) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (10:00) [superwin]

**Context:** Debugging bet365 racing adapter WebSocket streaming — low coverage despite successful discovery and subscription.

**Key Exchanges:**
- WS discovery initially failed (0 frames captured); fixed `main()` to return `False` on discovery failure so supervisor retries instead of treating clean exit as success
- Successful run: 8 AU/NZ meetings discovered, 556 participants across 33 fixtures, 12 subscription chunks sent
- Coverage stuck at 4/556 (1%) — only 4 fixtures returning prices, each showing `1/1` runner mapped despite ~17 avg per fixture
- Root cause identified: stream was hijacking `pshudws.z1.365lpodds.com` (different WS cluster) instead of `premws-pt1.365lpodds.com`; the 3 premws connections were CLOSED (state=3), leaving only the pshudws endpoint which carries different/limited data

**Decisions Made:**
- Updated WS hijack logic to prefer `premws` cluster over `pshudws` — premws is where full fixture odds flow
- Updated rehijack logic to also prefer premws
- Made supervisor retry on discovery failure (return False instead of implicit success)

**Lessons Learned:**
- **bet365 uses multiple WS clusters** (`premws`, `pshudws`) carrying different data — hijacking the wrong one gives partial/empty odds
- **WS connection state matters**: premws connections can go CLOSED (state=3) mid-session, leaving only non-primary clusters active — need to detect and recover
- **1% coverage with valid subscriptions** is a strong signal of wrong-endpoint hijack, not a subscription or parsing issue

**Action Items:**
- Verify premws-preferring logic works on next supervised run
- Add WS endpoint validation/logging so wrong-cluster hijack is immediately visible in health output
- Still outstanding: runner map degradation after ~5 meetings, subscription chunk-dropping (~45 runner response ceiling)

### Session (10:44) [value-betting-scanner]

**Context:** Investigating and attempting to fix the bet365 MLB game scraper — discovered API migration, tested v1 rollback, identified missing click/scroll triggers, built v3 hybrid fix.

**Key Exchanges:**
- WebSocket frame dumps revealed bet365's shared WS channel carries cross-sport noise (esoccer, tennis), not MLB prop data — the 10s wait captured nothing useful
- Bet365 moved MLB player props from `betbuilderpregamecontentapi/wizard` (v1) to a lazy-subscribe `matchbettingcontentapi` model (v2) — neither endpoint returns prop data without UI interaction
- Git archaeology confirmed v1 (Apr 6, commit `9d3eee4`) used the same BB wizard endpoint NBA still uses successfully today
- User recalled the old scraper clicking into games — confirmed v1 had `_click_sgm_tab()` and scroll triggers that the Apr 8 v2 migration dropped
- Built v3 merging v1's click/scroll/largest-response tricks with v2's parser, deployed for testing

**Decisions Made:**
- Rolled back WS listener experiment (was just collecting noise) — reverted to HTTP-only parse
- Tested v1 separately as `bet365_mlb_game_v1.py` before modifying production code — safe parallel test pattern
- v3 approach: always click tab + scroll + keep largest response, rather than hash-nav alone — re-enables the UI-triggered subscription flow that v2 lost

**Lessons Learned:**
- **bet365's MLB API change is sport-specific**: NBA player props still served from BB wizard, MLB props stripped out and moved behind lazy-subscribe WS model
- **v2 migration (Apr 8) silently broke 3 critical behaviors**: tab clicking, scroll-triggered lazy loading, and largest-response retention — the endpoint change masked the real regression
- **Hash navigation alone doesn't fire bet365's UI JS routing** — real tab clicks are needed to trigger WS subscription messages for prop markets
- **Intersection observers on bet365** require scroll events to fire lazy-load subscriptions for off-viewport markets
- **Git archaeology is invaluable** for understanding regressions — diffing v1 vs v2 revealed exactly which behaviors were lost

**Action Items:**
- Verify v3 captures MLB prop data (check for `Batter Props: N bytes -> M odds` with M > 0)
- If v3 fails, investigate whether bet365 requires the WS subscription protocol to be reverse-engineered (lazy-subscribe may not be triggered even with clicks)
- Monitor whether bet365 applies the same lazy-subscribe migration to NBA (would break that scraper too)
- v1 archived at `ev_scanner/bet365_mlb_game_v1.py` for future reference

### Memory Flush (11:15) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (11:24) [superwin]

**Context:** Testing bet365 racing adapter deployment approaches on VPS — validating teammate's deployment doc against reality, exploring WebSocket vs REST architecture.

**Key Exchanges:**
- Teammate's OCEAN_DEPLOY doc detailed full bet365 API reverse-engineering: endpoints, pipe-delimited format, auth via `x-net-sync-term` header, anti-bot stack assessment
- Doc claimed "Cloudflare Bot Management ❌ Not present" — tested from residential AU IP. VPS reality: every request from DigitalOcean Sydney ASN (`170.64.213.223`) gets `__cf_bm` cookie + 403 challenge page, including raw curl with Chrome 146 UA
- Upgraded `curl_cffi` to 0.15 on VPS; tested chrome146/131/124/120 impersonation — all 403'd. Confirmed block is IP-reputation-based at Cloudflare edge, not TLS fingerprint
- Tested WebSocket endpoint (`wss://premws-pt1.365lpodds.com/zap/`) separately — also behind Cloudflare (CNAME → `cdn.cloudflare.net`), same 403 block. WS endpoint is NOT a shortcut around the IP block
- User asked about WebSocket for live odds — confirmed WS is push-based sub-second vs REST's 5s polling gap, and the `x-net-sync-term` token embeds directly in WS subscription frame as `A_{token}`

**Decisions Made:**
- **Raw Python WebSocket is the ideal primary path** once IP block is solved — `websockets` library + token, no Playwright on VPS, no 500MB RAM overhead
- REST polling via `curl_cffi` becomes fallback only (for when WS drops)
- Token is IP-independent once issued and lives for hours — can be captured on Mac and shipped to VPS if needed (one capture per day suffices)
- Waiting on user for clean egress IP workaround before proceeding with Phase 1

**Lessons Learned:**
- **Cloudflare IP reputation blocking is ASN-level**: DigitalOcean datacenter IPs are on Cloudflare's bad-reputation list regardless of TLS fingerprint, headers, or user agent
- **Teammate's anti-bot assessment was environment-dependent**: "no bot management" was true from residential AU IP, false from datacenter IP — always validate from the actual deployment environment
- **bet365's WS subscription protocol embeds auth in-band**: `\x16{PM topics},A_{x-net-sync-term}` — no separate HTTP auth header needed once WS is open
- **Different domains, same wall**: `www.bet365.com.au` and `premws-pt1.365lpodds.com` both route through Cloudflare CDN with identical blocking behavior

**Action Items:**
- Waiting on user's clean egress IP workaround (residential proxy, Mac-based relay, or network-level solution)
- Once unblocked: pivot Phase 1 to raw-WS architecture (no Playwright on VPS)
- Need proxy to support HTTPS CONNECT + WebSocket upgrades if going proxy route
- Token capture fallback plan: prototype on Mac captures token, ships to VPS daily

### Session (11:46) [value-betting-scanner]

### Session (11:24) [superwin]

**Context:** Full system health check followed by deep-dive into AFL betting performance and trail data integrity.

**Key Exchanges:**
- System status: NBA 3,633 markets, MLB 663, AFL 45 (Bet365 only), NRL 0 (off-season). VPS relay healthy at 4,359 markets, tracker running 188 cycles.
- AFL results schema mismatch discovered: AFL stores `'win'/'loss'/'void'` lowercase while NBA uses `W/L/P` — any analytics query filtering on `W/L` silently drops all AFL results
- AFL actual record: 270-694 (28% WR), **-34.2% flat-bet ROI** over 964 settled picks despite +1.16% avg CLV — CLV/ROI paradox
- Root cause identified: **no genuine sharp AFL books in the system**. "Sharps" are Bet Right, PointsBet AU, PickleBet (AU soft books), FanDuel/DK/BetRivers (US books that copy AFL prices from AU wholesalers). Devig is circular — true-prob estimate derived from the same soft prices we're betting against
- 83% of AFL picks use `one_sided_consensus` devig method, never calibrated against outcomes
- Confidence scorer never ran on AFL (0/1000 picks have confidence values)
- Trail system confirmed working — 687 entries written in last hour, all MLB (only live sport). Real trail data lives in `books` JSONB column; scalar `odds`/`line` columns on `trail_entries` are dead schema
- AFL backtest data inventory: 201/1000 have `sharp_snapshot` (full re-devig possible), 742 have legacy `sharp_trail` JSONB
- NBA 25h pick gap confirmed as schedule (no games until tomorrow), not a pipeline issue

**Decisions Made:**
- **AFL picks should be killed or paused at current theory weights** — losing 330 units in two weeks, no profitable segment exists under current sharp-book config
- **Need a real AFL price-maker before recalibrating** — Betfair Exchange or Sportsbet (book 900, currently listed as soft) are the candidates
- **Schema normalization needed** — `win/loss/void` → `W/L/P` or teach analytics to handle both

**Lessons Learned:**
- **CLV means nothing when the closing line isn't sharp** — "beating the close" from phantom sharps produces misleading +EV signals while actual P/L is deeply negative
- **`one_sided_consensus` devig is dangerous without calibration** — drives 83% of AFL volume on untested assumptions
- **US sportsbooks are not independent price-makers for AFL** — they wholesale from AU markets, so using them as "sharps" creates circular devig
- **Silent schema mismatches cause invisible data loss** — the `win` vs `W` issue meant AFL results appeared as zero resolved for weeks

**Action Items:**
- Fix AFL result schema (`win/loss/void` → `W/L/P`) or make analytics code handle both formats
- Identify real AFL sharp source: check if Sportsbet (book 900) or Betfair Exchange is available via OpticOdds
- Offline re-devig of 201 `sharp_snapshot` picks across method × weight combos to test if any config produces positive ROI
- Enable confidence scorer for AFL
- Clean up dead `odds`/`line` scalar columns on `trail_entries` table
- Fix stale workers: Bet365 book 365 (odds=0) and BlackStream/Surge book 960 still outstanding from prior sessions

### Memory Flush (12:53) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (13:19) [value-betting-scanner]



**Context:** Deep-diving AFL betting analysis — reviewing raw OpticOdds historical data quality, backtest methodology flaws, and whether "true lines" are discoverable from available data.

**Key Exchanges:**
- User challenged prior analysis ("I don't like what you have built upon") and asked to see raw AFL odds/results history from OpticOdds
- Generated a raw CSV (14,567 rows, 23 fixtures, Mar 6-28) joining OpticOdds odds to actual player results with zero analysis applied
- Raw data independently confirmed the Over bias: Disposals Overs hit 41.5% vs Unders 51.1%; Goals Overs only 32.6%
- Exposed that Bet Right (34.0% WR) and Picklebet (34.5% WR) — both weighted 1.0 as "sharps" — have the worst Disposals Over calibration, while Sportsbet hits 50.7% (near-fair)
- Identified 9 specific flaws in the backtest approach before running it

**Decisions Made:**
- **Backtest with current theories is necessary but insufficient** — it will reproduce the same circular bias that caused production losses because the 5 "sharp" books are highly correlated retail shops, not market makers
- **Empirical edge-finding without devig is the right next step** — slice historical data by (market, side, book, odds bucket) and compare hit rate to implied probability directly, bypassing the circular devig trap entirely
- **"True lines" don't exist in the AFL OpticOdds data** — no market maker (Pinnacle/Betfair) exists for AFL player props, so all we can find is "retail consensus errors," not true probabilities

**Lessons Learned:**
- **Retail consensus ≠ true line** — averaging 5-10 correlated retail books gives you retail consensus, not a sharp price. AFL player props have no market maker on OpticOdds
- **Self-referential devig is a trap** — when the "sharp" books used for devig are themselves biased (Bet Right/Picklebet pricing Overs 16 points too long), every +EV signal is actually chasing the bias
- **Empirical edge-finding bypasses the circular trap** — directly measuring hit rate vs implied probability needs no devig, no weights, no theories
- **Cache has real limitations** — no time-series (only OLV/CLV snapshots), Bet365 barely present (3/23 fixtures), only Goals+Disposals markets, and opening line ≠ production trigger moment

**Action Items:**
- Run `backtest.py` with both active AFL theories (Goals Consensus + Disposals 5-Book) for completeness
- Build and run empirical edge scan: slice 14,567-row CSV by (market, side, book, line decile, odds decile), report slices where hit rate > implied probability
- Verify cache completeness: check if 23 fixtures = all completed AFL matches in Mar 6-28 window (survivorship bias check)
- Confirm `backtest.py` applies the same `_get_max_line_gap()` filter as production

### Session (13:49) [value-betting-scanner]



**Context:** Planning AFL betting variant testing — designing forward-looking A/B tests for AFL disposals theories via the value betting tracker.

**Key Exchanges:**
- User proposed "cast the net" — start tracking multiple AFL theories forward so they can backtest on real trails instead of thin retro data
- Confirmed VPS tracker is healthy: 988 AFL markets, 11 books streaming, 2,315 cycles, 0 errors, ~8s cadence
- Discovered architectural gotcha: picks are deduped by deterministic `pick_id`, and `triggered_by` only records the first (alphabetical) theory that passes — so overlapping variants won't produce independent pick rows

**Decisions Made:**
- **Option B (offline replay) over Option A (persist theory_evs)** — recommended deploying one liberal "net cast" theory (min_ev=1, broad book set) that captures the full universe of interesting picks, then replaying 5 variant hypotheses offline against trail data. No schema changes, no deploys, no rollback risk
- **Deactivate AFL Goals Consensus theory** — 6,180-combo analysis proved structural break, not worth tracking forward
- **Replace Picklebet (912) with Sportsbet (900) in sharp weights** — Picklebet is a clone of Bet Right; Sportsbet is independent and had 50.7% calibration on Disposals Overs (near-fair)
- **Keep existing `AFL Disposals 5-Book` as control** alongside the new liberal net-cast theory

**Lessons Learned:**
- **Pick dedup architecture limits multi-variant A/B testing** — same (player, prop, side, line, game_date, soft_book) always hashes to same UUID, so only one theory gets attribution per pick row
- **Offline replay is equivalent to live multi-variant** — if trails capture full sharp+soft odds timelines, any devig method / weight combo can be applied retroactively without production changes
- **Liberal capture + offline filtering > multiple production theories** — deploy one theory that over-captures, then slice offline, rather than deploying 5 overlapping theories that fight over pick dedup

**Action Items:**
- Execute SQL: deactivate AFL Goals Consensus theory
- Execute SQL: insert `AFL Disposals Net Cast` theory (min_ev=1, books 900/901/903/908/911, min_sharp_count=2)
- Verify AFL trail writing works on VPS before Apr 16 Carlton v Collingwood fixture
- Write `scripts/afl_replay_variants.py` — offline replay of 5 variant hypotheses (baseline, under-only, power devig, sportsbet-in, no-entain) against trail data
- Monitor Apr 16 fixture: check trail coverage %, peak EV vs trigger EV timing, per-variant flat-bet ROI
- Pending user decision: Option B only vs B+A (also persist `theory_evs` JSONB column on picks)

### Session (14:31) [value-betting-scanner]

**Context:** AFL theory audit and cleanup — discovered 6 active theories (not 2), found structural Over-bias bug, planned deactivation + new net-cast theory deployment.

**Key Exchanges:**
- Confirmed belt-and-suspenders approach: Option B (offline replay) + Option A (persist `theory_evs` JSONB column)
- Discovered **6 active AFL theories** in `nba_optimization_runs`, not the 2 previously assumed — 3 Goals + 3 Disposals
- Found root cause of 951:13 Over/Under imbalance: `AFL Disposals` theory uses `one_sided_consensus` method which hard-skips Unders at line 548 (`if not key.endswith("_over"): continue`), and it fires first alphabetically, hijacking `triggered_by` on shared pick_ids
- `AFL Disposals OLV` identified as legacy near-duplicate of `AFL Disposals 5-Book` (differ by one book: OLV has BetRivers 802, 5-Book has TAB 908)

**Decisions Made:**
- **Deactivate all 5 non-essential theories** — 3 Goals (calibration proved −28.9% best case), `AFL Disposals` (wrong devig method), `AFL Disposals OLV` (legacy duplicate)
- **Keep `AFL Disposals 5-Book` as baseline control** alongside new `AFL Disposals Net Cast`
- **Net Cast config:** min_ev=1, min_sharp_count=2, books 900/901/903/908/911, multiplicative+poisson — deliberately liberal to over-capture for offline replay
- **Replace Picklebet (912) with Sportsbet (900)** — Picklebet is a Bet Right clone; Sportsbet is independent with 50.7% Disposals Over calibration

**Lessons Learned:**
- **`one_sided_consensus` on two-sided markets is structurally broken** — silently skips all Under picks before EV computation, creating invisible systematic bias
- **Alphabetical theory ordering + pick dedup = hidden attribution hijack** — broken theory sorted first steals `triggered_by` from correct theories on the same pick
- **Theory proliferation without audit creates compound bias** — 4 of 6 theories were Over-only capable, making the aggregate signal appear intentional when it was accidental
- **Always audit the full theory set before deploying new variants** — assumed 2 active theories, found 6

**Action Items:**
- Execute 5 Supabase UPDATEs to deactivate broken/redundant theories (backup saved to `brain/raw/data-exports/afl_theories_backup_2026-04-14.json`)
- Insert `AFL Disposals Net Cast` theory row
- Patch `server/tracker.py` ~line 889 to persist `theory_evs` as JSONB on `nba_tracked_picks`
- Run `ALTER TABLE nba_tracked_picks ADD COLUMN theory_evs JSONB` migration
- Deploy code change to VPS + mini PC, verify via `/api/v1/health`
- Verify AFL trail writing works before Apr 16 Carlton v Collingwood
- Write `scripts/afl_replay_variants.py` for post-round offline variant comparison
- Post-Apr 16 checklist: trail coverage %, peak EV timing, per-variant flat-bet ROI

