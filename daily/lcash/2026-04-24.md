# Daily Log: 2026-04-24 (lcash)

## Sessions

## Memory Maintenance

### Memory Flush (08:04) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (09:10) [value-betting-scanner]

**Context:** Morning health check, fixing overnight failures, and consolidating bet365 Chrome instances from two to one shared instance.

**Key Exchanges:**
- Overnight: push worker died, both Chrome sessions expired, OpticOdds erroring — three separate failures
- bet365 blocks simultaneous logins from same account on two browsers — NBA Chrome login kept failing because MLB Chrome was already logged in
- User corrected assumption: "check the nba chrome says logged in also never assume the system is good" — don't report system healthy without verifying every component

**Decisions Made:**
- **Shared Chrome on port 9223** instead of two separate Chrome instances (9223 + 9224). MLB scraper updated to attach to 9223. Eliminates session conflict entirely.
- Port 9224 no longer used. MLB Chrome killed. Login script only targets 9223.
- `_kill_stale_chrome` in MLB scraper won't kill shared Chrome — attach logic sets `self._chrome_proc = None`

**Lessons Learned:**
- bet365 enforces single-session-per-account — two Chrome profiles on same account = second one gets rejected silently
- Push worker PID check via WMI was using `'%ev_push%'` which missed matches due to spaces in actual command line. Process was running fine, monitoring was wrong.
- Never report "system operational" without checking every individual component. Assumptions compound — if you assume NBA is fine because MLB worked, you miss the session conflict.

**Action Items:**
- Add push worker watchdog (it keeps dying overnight; schtask only helps on boot, not mid-session crashes)
- Consider adding push worker check to existing NBA watchdog on port 8800

### Session (09:42) [value-betting-scanner]

**Context:** Debugging why value betting picks were flashing out / disappearing from the dashboard mid-session.

**Key Exchanges:**
- Traced pick disappearance to OpticOdds REST poller on mini PC erroring → sharp data aging past `MAX_ODDS_AGE_S=180` → zero true probability → zero picks
- Investigated whether rate limiting was the cause: 4,782 REST requests/min across 57 sportsbooks — initially looked like a smoking gun
- User correctly pushed back: OpticOdds would return HTTP 429 if rate-limited, not silently drop connections
- Confirmed errors are `httpx.RemoteProtocolError` ("Server disconnected without sending a response") — TCP-level drops, not HTTP error responses

**Decisions Made:**
- **No changes needed to request volume** — the 57-sportsbook REST polling is intentional (sharp books for devig, AU soft books for bet targets, consensus books for theories)
- **Current error handling is correct** — `_poll_opticodds()` catches exceptions, retries in 5s, VPS SSE streams provide backup sharp data
- **Picks flashing out is correct behavior** — showing stale sharp data would be worse than temporarily showing zero picks

**Lessons Learned:**
- `Server disconnected without sending a response` = upstream infrastructure issue, not rate limiting. Rate limits return HTTP 429 with a response body; TCP drops mean server-side load/crash
- VPS SSE streams are more resilient than mini PC REST polling — persistent connections with auto-reconnect vs. independent HTTP requests that each can fail
- The `MAX_ODDS_AGE_S=180` filter is working as designed — it correctly removes stale sharp data rather than computing EV on outdated odds

**Action Items:**
- Monitor OpticOdds REST stability over time — if drops become frequent, consider reducing mini PC polling to sharp+target books only or routing sharp data from VPS back to mini PC

### Session (09:48) [value-betting-scanner]

**Context:** Debugging why value betting picks were flashing out of the dashboard during OpticOdds API outages

**Key Exchanges:**
- Picks disappeared because OpticOdds REST poller on mini PC got `Server disconnected without sending a response` (httpx.RemoteProtocolError), causing sharp data to age past `MAX_ODDS_AGE_S=180` → zero true probability → zero picks
- Initially suspected rate limiting (4,782 REST req/min across 57 sportsbooks), but confirmed it's OpticOdds infrastructure dropping TCP connections — rate limiting would return HTTP 429, not raw TCP disconnects
- VPS SSE streams stayed alive during outages because persistent connections with auto-reconnect are more resilient than REST polling making fresh HTTP requests every 5s

**Decisions Made:**
- **Keep all 57 sportsbooks in REST poller** — not just the 5 sharp books. We need 5 sharp (FanDuel, DK, PropBuilder, Novig, Pinnacle) for devigging, 6 AU soft books for bet targets, and ~46 consensus books for one-sided consensus devig theories
- **Increased `MAX_ODDS_AGE_S` from 180 to 600** (10 min) to prevent flash-outs during typical 1-5 min OpticOdds outages. Sharp lines don't move much in 10 min so stale data is acceptable vs showing zero picks

**Lessons Learned:**
- `httpx.RemoteProtocolError: Server disconnected without sending a response` = upstream TCP issue (load balancer drop, server crash), NOT rate limiting. Rate limits produce HTTP 429 with a response body
- SSE streams are fundamentally more resilient than REST polling for real-time odds data — persistent connection vs new HTTP request per cycle
- The existing error handling in `_poll_opticodds()` (catch, log, retry in 5s) is correct; the problem was only that `MAX_ODDS_AGE_S` was too tight to survive the retry period
- Error was intermittent and self-healing — affected random sportsbook batches, came and went, confirming flaky upstream not a ban

### Memory Flush (09:50) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (10:14) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (10:56) [value-betting-scanner]

**Context:** Building a TAB.com.au scraper for the value betting scanner to pull NBA player props

**Key Exchanges:**
- Mapped the full TAB API hierarchy: `/sports/{Sport}/competitions/{Comp}/matches/{Match}` — public REST, no auth, just browser-like headers + Akamai cookies (handled by `curl_cffi`)
- Single competition-level request (`/sports/Basketball/competitions/NBA?jurisdiction=NSW`) returns ALL matches with ALL markets inline — 1.6MB, no pagination, no individual match fetches needed
- TAB has two player prop formats: **Over/Under lines** (betOptionSpectrumId 837/838/839/1789/1791/1787/1788) with 2 propositions per player per market, and **Threshold markets** (3515, 3520, etc.) with all qualifying players in one market
- Confirmed 7 prop stat types: points, rebounds, assists, PRA, threes, blocks, steals

**Decisions Made:**
- Use competition endpoint (not individual match endpoints) — one request gets everything for all NBA games
- Poll discovery every 5 min, full market pull every 60s
- TAB only posts player props close to tipoff — other games show 47-86 markets vs 326 for the nearest game
- Proposed using threshold market full names ("Anthony Edwards (MIN)") to build name mapping for O/U abbreviated names ("A Edwards") — self-contained within same API response, no external lookup needed

**Lessons Learned:**
- TAB abbreviates player names inconsistently in O/U props: "A Edwards", "Jad McDanls", "D DiVincenz", "T Hardawy" — existing fuzzy matcher won't handle first-initial-only format
- Competition endpoint returns market data inline for all matches — no need for the individual match endpoint at all
- Response is ~1.6MB for NBA — should cache, not poll aggressively
- `betOptionSpectrumId` values are stable across matches (837 = Player Points O/U everywhere)
- All 103 Over/Under pairs came back complete (both over and under for every line)

**Action Items:**
- Implement name expansion step using threshold market full names → abbreviated name mapping before emitting `ScrapedOdds`
- Capture MLB prop `betOptionSpectrumId` values (Baseball sport id=14, same endpoint pattern)
- Capture competition IDs for MLB, NFL, NHL (currently only NBA=1001 is confirmed)
- Integrate TAB scraper with market matcher engine and verify end-to-end pairing with sharp book data

### Session (11:29) [value-betting-scanner]

Since this is a conversation review request (not an action request), I should analyze the conversation context and produce a structured daily log entry.

**Context:** Building and deploying a TAB (Australian bookmaker) scraper for the NBA value betting scanner

**Key Exchanges:**
- TAB's competition endpoint returns all match data in a single 1.6MB request — no need for individual match fetches
- TAB uses abbreviated player names (e.g., "A Edwards" instead of "Anthony Edwards") which would break fuzzy matching with other books
- User chose Option 1: expand names inside the TAB scraper using threshold market names (which contain full names) rather than improving the global fuzzy matcher
- TAB requires `curl_cffi` because plain `httpx` gets blocked by Akamai bot protection
- Since `curl_cffi` is synchronous and the worker loop is async, TAB runs in a thread via the direct_scraper_worker

**Decisions Made:**
- TAB assigned book_id 908, treated as a soft book alongside Bet365 2.0 (same EV theories apply)
- Name resolution handled at scraper level using TAB's own threshold market data to build abbreviated→full name mapping (15/15 exact matches)
- TAB polls every 60 seconds via the direct_scraper_worker pattern (same as other scrapers)
- Props only available close to tipoff — the scraper already discovers all matches but props appear progressively

**Lessons Learned:**
- TAB posts player props closer to game time — only 1 of 8 NBA matches had full props (the one nearest tipoff had 326 markets vs 47-86 for others)
- TAB's own response contains both abbreviated and full names in different market types — threshold markets have full names, O/U markets have abbreviated — so the scraper can self-resolve without external lookups
- `curl_cffi` needed for Akamai-protected sites (TAB) — plain HTTP clients get blocked

**Action Items:**
- Monitor TAB (908) picks appearing in Supabase to confirm end-to-end EV tracking works
- Watch for edge cases where threshold markets don't provide a full name mapping for all O/U players
- 206 TAB odds flowing on mini PC — 12 +EV opportunities found in initial local test (avg +2.2% EV, best was Jokic Blocks Under 0.5 @ 2.45 at +8.3% EV)

### Session (12:12) [value-betting-scanner]

**Context:** Deploying TAB (book ID 908) as a new soft book into the value betting pipeline end-to-end.

**Key Exchanges:**
- Ran local EV scan against TAB for MIN vs DEN — found 12 +EV opportunities, best was Jokic Blocks U0.5 @ 2.45 (+8.3% EV)
- 112/206 TAB odds matched to VPS sharp data locally
- Deployed TAB scraper to mini PC: installed `curl_cffi`, pushed files, confirmed 206 markets flowing
- Dashboard didn't show TAB in soft books dropdown — `SOFT_IDS` array in `dashboard.html` was missing 908 (even though `BOOK_NAMES` had it). Fixed and pushed.
- TAB returned 0 odds on mini PC because games had started/finished and props were pulled. Tomorrow's games don't have props posted yet.

**Decisions Made:**
- TAB (908) added to `SOFT_BOOK_IDS` in `models.py` so the tracker evaluates it as a soft book
- TAB integrated into `direct_scraper_worker.py` with 60s polling interval, running in a thread (same pattern as other scrapers)
- Updated `/vb` skill and `CLAUDE.md` with TAB details (book ID, module map, troubleshooting)

**Lessons Learned:**
- TAB posts player props ~2-3 hours before tipoff and removes them once games go in-play — unlike some books that keep pre-match lines longer
- Dashboard `SOFT_IDS` array is separate from `models.py` `SOFT_BOOK_IDS` — both need updating when adding a new soft book
- `BOOK_NAMES` having an entry doesn't mean the book appears in the dropdown; the `SOFT_IDS` filter array gates visibility
- Tracker is event-driven (waits for market data changes), so 0 cycles is expected when there's no data flowing

**Action Items:**
- Monitor TAB picks tomorrow when props go live (~2-3 hours before tipoff) to confirm end-to-end pipeline works in production
- Verify tracker cycles start and TAB +EV picks appear in Supabase and dashboard

### Session (12:49) [value-betting-scanner]

**Context:** Debugging and extending the TAB sportsbook scraper to extract threshold markets and deploying TAB integration end-to-end across mini PC → VPS pipeline.

**Key Exchanges:**
- TAB scraper was finding 0 odds despite 8 NBA matches existing — root cause: scraper only extracted O/U props (type 837/838), but TAB posts threshold markets (20+ Points, 10+ Rebounds) hours before O/U lines appear
- User asked whether threshold markets with different lines than sharp books could still be evaluated for +EV (e.g., TAB 9.5+ Points @ 2.0 vs sharp 11+ Points @ 1.90) — answer: yes, via interpolation
- Initial EV analysis showed 300%+ fake EV — logit interpolation breaks on discrete count props with large line gaps
- Switched to `interpolate_for_prop()` which routes count props (Threes/Blocks/Steals/Rebounds/Assists) through Poisson and Points/combos through calibrated logit — realistic results: 32 picks at +8.3% avg EV

**Decisions Made:**
- Added threshold market extraction as fallback when O/U props aren't available — converts "20+ Points" → Over 19.5 format so it flows into existing pipeline
- Steals/Blocks/Threes are highest-value threshold props because line gaps from sharp books are small (1 point) and Poisson handles cleanly

**Lessons Learned:**
- TAB posts threshold markets hours before O/U lines — valuable early coverage window
- Logit interpolation over-extrapolates on discrete/count props — must use Poisson for Threes, Blocks, Steals, Rebounds, Assists
- Mini PC has `DISABLE_TRACKER=1` and `DISABLE_RESOLVER=1` by design — it only scrapes and pushes to VPS. Tracker/resolver run on VPS only
- VPS needed `908` added to `SOFT_BOOK_IDS` separately — deploying scraper changes to mini PC doesn't automatically update VPS models

**Action Items:**
- Monitor VPS tracker to confirm TAB threshold picks start flowing through after deployment
- Verify Poisson interpolation accuracy on real results once bets settle
- Consider whether `max_line_gap` in NBA theories needs tuning for threshold-to-sharp comparisons

### Session (13:22) [value-betting-scanner]

**Context:** Debugging why TAB (soft book 908) wasn't generating value betting picks after being added to the system.

**Key Exchanges:**
- Mini PC has `DISABLE_TRACKER=1` and `DISABLE_RESOLVER=1` by design — it only scrapes/pushes odds; VPS runs tracker and resolver
- VPS had old code missing 908 in `SOFT_BOOK_IDS` — deployed manually (deploy script requires committed code)
- TAB threshold markets (Over 19.5, Over 24.5) use coarse lines that don't match sharp book lines (26.5, etc.)
- None of the 7 NBA theories had `max_line_gap` set, so only exact line matches were eligible
- Added `max_line_gap=4` to all 7 theories — affects all soft books, not just TAB
- Theory cache refreshes every 5 minutes; required VPS restart to force pickup
- Tracker EV cap at 50% (line 737) handles crazy threshold EVs

**Decisions Made:**
- Applied `max_line_gap=4` globally to all theories rather than creating TAB-specific theory
- Accepted that TAB may naturally produce fewer picks — it's a sharpish Australian book with odds closer to true than Sportsbet/Neds

**Lessons Learned:**
- Architecture reminder: mini PC = scraper/pusher only, VPS = tracker/resolver. Code changes to models (like `SOFT_BOOK_IDS`) need VPS deploy too
- `max_line_gap` is critical for threshold-style books that use coarse lines — without it, interpolation doesn't happen
- Theory cache (5-min TTL) can silently prevent config changes from taking effect; restart service to force refresh
- TAB's threshold odds having both sides scraped (274 fresh markets, 213 with sharp overlap) doesn't guarantee +EV — the book itself may be too sharp

**Action Items:**
- Monitor TAB picks over next few days to see if closer-to-tipoff lines produce more +EV opportunities
- Consider whether `max_line_gap=4` is too aggressive for non-threshold soft books (affects all books globally)

### Session (14:09) [value-betting-scanner]

**Context:** Debugging why TAB threshold market picks weren't appearing in the value betting tracker, then testing an OpticOdds fixture API call.

**Key Exchanges:**
- TAB markets were present in VPS state (274 fresh markets) but zero TAB picks were being tracked
- Systematic debugging through logging levels, theory soft_books config, and time filters to isolate the root cause
- After fix deployed, 11 TAB picks immediately tracked with EV ranging +2.6% to +68.7%
- OpticOdds fixture `439A1AC1DBDB` returned empty — fixture doesn't exist on their platform

**Decisions Made:**
- **Separated pick creation from trail recording**: picks stored at 24h before game (captures early +EV from TAB threshold props), trails start at 3h before game (saves I/O, records odds movement that matters for backtesting). Previously `max_hours_before_start=3` gated everything.
- Set `max_hours_before_start=24` on standard theories so they evaluate TAB threshold markets which are posted 10+ hours before tipoff

**Lessons Learned:**
- **TAB threshold markets are Over-only and posted far in advance** (~10+ hours before game). The default `max_hours_before_start=3` was silently filtering them all out before the soft book evaluation loop ever ran.
- **Debugging order for "0 picks" issues**: check (1) data presence in state, (2) theory `soft_books` config, (3) time window filters (`max_hours_before_start`), (4) `max_line_gap` — the time filter was the non-obvious culprit here.
- Two NBA theories (Pinnacle, Crypto Edge) have `soft_books` restricted to prediction markets only (950, 970, 971) — they will never evaluate TAB (908) by design.
- `__pycache__` timestamp confirms code deployment, but code path may still not execute due to upstream filters.

**Action Items:**
- Verify TAB trails begin recording once games enter 3h window
- Investigate source of OpticOdds fixture ID `439A1AC1DBDB` — not valid on their platform
- Updated `/vb` skill with TAB scraper section, pick/trail timing split, and new troubleshooting rows

### Session (14:40) [value-betting-scanner]

**Context:** Auditing OpticOdds API key coverage and diagnosing why SSE streams weren't producing data after VPS restart

**Key Exchanges:**
- Discovered OpticOdds API key only has **NBA basketball** access — all other sports (MLB, soccer, tennis, hockey, NRL, AFL) return empty/403
- SSE streams appeared stuck but were actually running `sse_startup` theory creation for 432 auto-discovered leagues — all of which the key can't access
- Basketball SSE connects fine; all 22 non-basketball SSE streams fail with 400 "not enabled for your API key"
- Confirmed the key doesn't even cover other basketball leagues (NBL, EuroLeague, CBA) — NBA only

**Decisions Made:**
- Need to disable non-basketball SSE streams to stop wasted compute on sports that always fail (pending user confirmation)
- MLB cannot do EV calculations — Bet365 is the only source, no sharp book data to devig against

**Lessons Learned:**
- OpticOdds API key scope: REST fixtures only returns NBA; SSE only connects for basketball sport type; everything else is blocked
- MLB's 275 markets all come from Bet365 game scraper on mini PC (port 8803) — zero OpticOdds involvement despite `opticodds_mlb_gl` task existing
- NRL/AFL mini PC servers are dead — not running at all
- `sse_startup` auto-discovery creates theories for hundreds of leagues without first checking if the API key has access — wastes 5+ min on boot
- Full infra status: NBA=working (sharps+softs), MLB=Bet365-only (no sharps), NRL/AFL=dead, everything else=no API access

**Action Items:**
- Disable non-basketball SSE streams and auto-discovery for inaccessible sports
- Decide whether to upgrade OpticOdds key for multi-sport access or focus on NBA-only
- Fix `sse_startup` to check API key permissions before creating theories for 432 leagues
- Decide what to do about MLB — currently no EV possible without sharp data
- Restart or investigate NRL/AFL mini PC servers if those sports are still wanted

### Session (15:47) [value-betting-scanner]

**Context:** Debugging VPS SSE startup, discovering API key limitations across sports, and adding sport filtering

**Key Exchanges:**
- SSE startup was alive but stuck in auto-discovery/theory creation phase (432 leagues, sequential Supabase inserts taking 5+ min)
- Tested OpticOdds API key across all sports — **only NBA basketball returns data**; every other sport (MLB, soccer, tennis, hockey, esports, etc.) returns empty or 400 errors
- MLB data comes exclusively from Bet365 game scraper on mini PC (port 8803) — zero sharp book comparison, no EV calculation possible
- NRL and AFL servers not running on mini PC at all

**Decisions Made:**
- Added `SSE_SPORTS` env var to `server/main.py` to filter which SSE streams to activate — controlled via VPS env, no code redeploy needed to change
- Set `SSE_SPORTS` to all sports (not just basketball) so non-basketball streams will auto-activate when key is upgraded — they'll get 400 errors and silently retry in the meantime, no harm done
- Auto-discovery still runs for all leagues but only filtered sports get streams started

**Lessons Learned:**
- `sse_startup` takes 5+ min on first boot with 432 leagues — sequential theory existence checks then sequential Supabase inserts; this is normal
- OpticOdds API key tiers are sport-specific — current key only covers NBA basketball (not even other basketball leagues like NBL/EuroLeague)
- The entire multi-sport SSE infrastructure (22 streams, 432 leagues) was spinning up against a key that could only see NBA — massive wasted compute

**Action Items:**
- Upgrade OpticOdds API key to cover more sports (MLB, soccer, tennis, hockey, esports) — then just update `SSE_SPORTS` env var and restart
- MLB needs sharp book data source for EV calculation — currently Bet365 soft book only
- NRL/AFL servers on mini PC need to be restarted if those sports are wanted

### Session (16:35) [value-betting-scanner]

**Context:** Debugging VPS value betting scanner — SSE stream startup, OpticOdds API key coverage, and multi-sport data availability

**Key Exchanges:**
- SSE startup appeared stuck but was actually running 432 sequential theory creation inserts (auto-discovered leagues) — normal first-boot behavior taking 5+ minutes
- Tested OpticOdds API key against every sport: **only NBA basketball returns data** — MLB, soccer, tennis, hockey, esports all return empty or 400 errors
- MLB has 275 markets but all from Bet365 game scraper only — zero sharp book data, so no EV calculation possible
- NRL and AFL servers aren't even running on the mini PC
- Basketball SSE confirmed working; all other sport SSE streams fail with "not enabled for your API key"

**Decisions Made:**
- Added `SSE_SPORTS` env var to `server/main.py` to filter which sports get SSE streams — allows runtime control without code redeploy
- Set `SSE_SPORTS` to all sports (not just basketball) so when key is upgraded, streams auto-activate — failed connections just silently retry with no harm
- Auto-discovery still runs for all leagues but only enabled sports get streams started

**Lessons Learned:**
- OpticOdds API key scoping: this key covers basketball REST (NBA only) and basketball SSE — nothing else. No other basketball leagues (NBL, EuroLeague) either
- `sse_startup` sequential Supabase theory inserts are the bottleneck on first boot (432 leagues = 5+ min) — consider batching
- Current data coverage: NBA = full (sharps + softs), MLB = Bet365 soft only (no sharps), NRL/AFL = dead, everything else = no access

**Action Items:**
- Upgrade OpticOdds API key to cover more sports (MLB, soccer, tennis, hockey, esports)
- MLB needs sharp book data to calculate EV — currently useless for betting signals
- Restart NRL/AFL servers on mini PC if those sports are needed
- Consider batching theory creation to speed up `sse_startup`

### Session (16:37) [value-betting-scanner]

**Context:** Debugging MLB Crypto Edge pick resolution failures and VPS stability after multiple restarts

**Key Exchanges:**
- MLB Crypto Edge picks all had `trail_count: 0` and `result: None` — traced to OpticOdds API lacking baseball fixture access
- Wired up existing `fetch_mlb_stats()` from podcast pipeline as MLB resolver fallback
- Added sport parameter to resolve endpoint (was hardcoded to `SPORT_KEY`)
- VPS deployment hit `No module named 'ev_scanner.podcast'` — podcast module wasn't deployed to VPS

**Decisions Made:**
- Use MLB Stats API (`ev_scanner/podcast.py:fetch_mlb_stats`) as resolver fallback instead of OpticOdds for baseball — OpticOdds key doesn't cover baseball fixtures
- Trail stats are only computed at resolution time (`_compute_trail_stats` runs when pick is graded) — this is by design, not a bug
- Resolve endpoint now accepts a `sport` parameter instead of hardcoded sport key

**Lessons Learned:**
- OpticOdds API access is sport-specific — NBA works but MLB fixtures return 0 results
- Supabase URL length limit hit when passing 658 pick IDs in one `in.()` clause — chunking needed
- VPS restarts clear all market data (rebuilds from push quickly) but SSE streams take 5-10 min to come online
- SSE startup and auto-resolver running simultaneously can flood OpticOdds API, causing SSE to get stuck — need to stagger them (auto-resolver has 5-min delay)
- VPS restarts break dashboard browser connections — users need hard refresh (Cmd+Shift+R)

**Action Items:**
- Verify MLB pick resolution actually completes end-to-end on VPS (was blocked by missing podcast module)
- Monitor SSE stream startup reliability — OpticOdds dropping connections was an issue
- Consider reducing auto-resolver's API load during SSE startup window

### Memory Flush (17:44) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (17:51) [value-betting-scanner]

**Context:** Monitoring NBA injury news for value betting implications during playoffs.

**Key Exchanges:**
- Shams Charania report: Shai Gilgeous-Alexander (ankle) ruled out for Game 2 vs Phoenix Suns on 2026-04-24
- Classified as high-impact negative event for OKC Thunder

**Lessons Learned:**
- SGA ankle injury is a game-changing line mover for OKC/PHX series — monitor for overreaction in spreads and totals

**Action Items:**
- Check for value on Phoenix spread/ML before lines adjust fully
- Monitor OKC total unders — offense drops significantly without SGA
- Watch for SGA status updates ahead of Game 3

