# Daily Log: 2026-05-21 (lcash)

## Sessions

## Memory Maintenance

### Session (07:57) [value-betting-scanner]

**Context:** Fixing resolver grading gaps and wiring CLV display end-to-end for manual picks in the topdown journal.

**Key Exchanges:**
- User flagged only 46/64 manual picks had `closing_sharps` — assistant initially misdiagnosed 16 as "tonight's games" due to UTC confusion. User pushed back ("they have all played") and was correct.
- Root-caused two real bugs: (1) resolver loop only scanned `yesterday UTC`, never today, so late-evening ET games with `game_date = today UTC` sat unresolved ~24h; (2) OO fixture lookup `start_date_after` is boundary-exclusive, missing games whose `start_date` exactly equalled the query bound.
- User then asked why resolved picks still showed `—` in CLV column — answer was the `opening_fallback` guard hid CLV even when `closing_sharps` data existed. Built a new `sharp_consensus` fallback: median CLV across all books in `closing_sharps` JSONB at the same line.

**Decisions Made:**
- Resolver loop now iterates `{today, yesterday} ∪ get_unresolved_dates()` per cycle (14-day staleness gate still applies) — ensures same-day-UTC picks get graded within hours, not next day.
- Backfill script widens OO fixture lower bound by 12 hours to avoid boundary-exclusive misses.
- CLV column priority chain: `sharp_clv_pct_true → optic_clv_pct → synthetic_clv_pct → sharp_consensus (NEW) → clv_pct (non-fallback only) → null`.
- `sharp_consensus` uses median across matched books at exact line from `closing_sharps` JSONB — displays as e.g. `+8.42% consensus n4`.

**Lessons Learned:**
- **Don't trust system state over user domain knowledge.** User knew games were complete; system showed `result IS NULL`. The bug was in the resolver scan window, not the game schedule. Always investigate the pipeline before blaming the data.
- **UTC date boundaries bite hard.** A game at `2026-05-20T00:13Z` has `game_date = 2026-05-20` but in ET that's still "May 19 evening." Resolver scanning only yesterday (`2026-05-19`) misses it entirely.
- **OO `start_date_after` is exclusive at exact boundary.** `start_date_after=2026-05-20T00:00:00Z` does NOT return a fixture with `start_date=2026-05-20T00:00:00Z`. Widen by ≥1s (we chose 12h for safety).
- **`closing_sharps` JSONB is rich enough to derive consensus CLV** without needing Pinnacle-specific data. Median across available books at exact-line gives a reasonable sharp-consensus estimate for manual picks that lack `sharp_trail`.

**Action Items:**
- Wire daily scheduled backfill cron on VPS (e.g. 4am UTC) so `closing_sharps` populates automatically for manual picks without manual script runs.
- Consider pushing the `{today, yesterday, backlog}` default directly into `resolve_picks()` itself rather than only in the `startup.py` loop, so ad-hoc CLI calls also benefit.

### Session (08:36) [value-betting-scanner]

**Context:** Debugging and fixing the VB (value betting) resolver pipeline — picks not resolving, CLV not displaying, and documenting the full resolver system.

**Key Exchanges:**
- User reported 16 picks stuck as "pending" despite games having finished — root cause was the resolver loop only scanning `yesterday UTC`, never `today UTC`
- User asked why CLV wasn't showing for all resolved picks in the topdown journal — the display layer was hiding `opening_fallback` CLV even when `closing_sharps` data existed; added a `sharp_consensus` fallback using median of per-book closings
- User asked for a plain-English explanation of the resolver — provided a comprehensive breakdown of W/L grading, CLV computation, trail stats, and what manual vs theory picks get

**Decisions Made:**
- Resolver loop now iterates `{today, yesterday} ∪ get_unresolved_dates()` per cycle instead of only yesterday (prevents 24h grading delay for late-night ET games that land on "today" in UTC)
- OO fixture lookup `start_date_after` boundary widened by 12h to handle exact-boundary games (Knicks-Cavs bug)
- CLV display priority chain: `sharp_clv_pct_true → optic_clv_pct → synthetic_clv_pct → sharp_consensus (NEW) → clv_pct (non-fallback only) → null`
- `sharp_consensus` computed at display time from `closing_sharps` JSONB — median of `(b365_open / book_close - 1) × 100` across matched books

**Lessons Learned:**
- UTC date boundaries are a recurring footgun: games at e.g. 00:13 UTC are "today" in UTC but "yesterday evening" in ET — any resolver/query that assumes `yesterday` will miss them
- OpticOdds `start_date_after` filter is **exclusive** at exact boundaries — need to pad the lower bound
- Manual journal picks structurally cannot have peak EV, avg EV, or devig CLV because there's no trail data — only consensus CLV from closing_sharps is available
- `closing_source` tag on each pick is critical for knowing whether CLV is a real measurement or a structural zero

**Action Items:**
- Consider daily scheduled backfill cron for closing_sharps
- Optionally push the default-target fix (`today + yesterday`) into `resolve_picks()` itself rather than just the loop caller
- 3 picks still missing closing_sharps (niche lines no sharp quoted) — structurally unrecoverable

### Session (09:10) [superwin]

**Context:** Value betting scanner — THE MULT settlement bug fix, NZ harness venue analysis, and phone replay research doc for a friend.

**Key Exchanges:**
- THE MULT (place-market edge) had 31 picks tracked with full EV trails, but settlement was using win-only logic instead of place (top-3 finish). After fix: ROI swung from **-62.6% to +48.45%**, win rate from 25% to 51.7%.
- NZ harness venue breakdown: Alexandra Park (+35% ROI, 29 picks) and Addington (+27% ROI, 22 picks) are profitable; **Cambridge is a dead zone** (-60% ROI, 22 picks, -13.32u).
- AU harness massively outperforms NZ harness: +39% ROI on 919 picks vs +0.5% ROI on 81 picks. NZ has higher win rate (33% vs 21%) but shorter prices — markets are sharper.

**Decisions Made:**
- Shipped place-market settlement fix for THE MULT (commit `57a3600`) — resolver now checks `position in [1,2,3]` for place edges instead of `status == "WINNER"`
- Disabled CLV for place picks (BSP/LTP are win-market only, would give misleading values)
- Top-3 heuristic used for now; storing `place_num_winners` from Betfair deferred to later for fields <8 runners (pays only 2 places)

**Lessons Learned:**
- Settlement logic must be market-type-aware — a single `status == "WINNER"` check silently destroyed place-market ROI reporting, making a +48% edge look like -63%
- NZ harness win rate > AU but ROI far worse — higher WR doesn't mean more profitable when prices are shorter (sharper markets)
- Cambridge harness is a consistent -EV trap across all edges; similar to the Greyhound $12-20 odds dead zone pattern — high-data-low-edge venues where prices are already sharp

**Action Items:**
- Consider adding Cambridge harness exclusion rule (similar to greyhound odds guard)
- Monitor THE MULT for NZ harness picks as sample grows
- Store Betfair `place_num_winners` at detection time for more accurate small-field settlement
- `docs/PHONE_REPLAY_RESEARCH.md` created — friend needs to answer the 5 open questions before starting the mobile reverse-engineering work
- Phone replay key unknowns: Play Integrity enforcement, Android Keystore-bound secrets, and custom RASP will determine if full remote replay is possible or if a live device is needed in the refresh loop

### Session (09:43) [superwin]

**Context:** Betr sportsbook scraper optimization — testing proxy strategies vs residential IP, then building sticky-proxy-session architecture, followed by edge tracking validation.

**Key Exchanges:**
- Tested single WebShare proxy at 10 req/sec → Cloudflare killed it after ~90 requests. Residential IP handled 6000/6000 at 10 req/sec. VPS datacenter IP locked instantly.
- User proposed "1 unique proxy per race with persistent session" — this was the breakthrough: 1500/1500 at 5 req/sec sustained with cookie continuity (`__cf_bm` warmup)
- Sticky-proxy-session mode chosen over Dell relay (option A) — simpler architecture, 10× more throughput, better resilience

**Decisions Made:**
- **Sticky-proxy-session on VPS over Dell relay** — 460 req/sec headroom vs 10 req/sec residential ceiling, no extra infra, spread across 93 IPs vs single point of failure. Dell plan shelved.
- **Betr ingest endpoint + passive adapter were built then immediately deprecated** — sticky proxy made VPS-side scraping viable again, so Dell relay became unnecessary
- Cookie session continuity (`__cf_bm`) is the key to Cloudflare tolerance — makes same IP ~50× more lenient (5 req/sec sustained vs 90 total before ban)

**Lessons Learned:**
- **Cloudflare bot detection is session-aware, not just rate-aware.** Stateless round-robin at 10/sec from one IP → banned after 90 reqs. Same IP with persistent cookie jar at 5/sec → 1500/1500 clean. The `__cf_bm` cookie warmup makes Cloudflare treat proxy traffic like a real user.
- **Residential IP isn't unlimited either** — Dell's residential IP got 1015'd after cumulative load from Mac tests + smoke test. The "residential = free pass" assumption was wrong.
- **Sticky proxy architecture:** deterministic hash of eventId → fixed proxy, each proxy has own aiohttp session + cookie jar, auto-refresh sessions every 25 min (before `__cf_bm` 30-min TTL), 429 → quarantine 2 min + auto-reassign to next proxy.

**Action Items:**
- Investigate 11 races missing from `race_results` (83 stuck picks) — likely Betfair stream subscription edge case where market-close MCM was missed
- Add stuck-pick health alert (picks >60 min past jump and unresolved — currently silent)
- Document `racing-mult` edge in edge-pick-theories.md — TAB place market with 1.1× cash multiplier, 41.8% WR on 55 settled picks, added 2026-05-19
- 65 settled picks with no CLV are expected (small AU venues with no Betfair trading) — not a bug, just data sparsity

### Session (09:55) [value-betting-scanner]

**Context:** Diagnosed and fixed a critical MLB resolver bug that graded picks using stats from wrong games (wrong-date contamination via merged-dict fallback)

**Key Exchanges:**
- Traced exact code path showing how resolver fell through per-date lookup → merged flat dict, returning yesterday's stats for today's unplayed game (e.g., Kiner-Falefa TB=3 from May 19 applied to May 20 pick)
- Explained the 4-layer reliability model: (1) fixture completion gate, (2) kill merged fallback, (3) historical audit, (4) game_pk matching
- User chose to ship Layer 2 immediately, defer Layer 3 audit to after context compression

**Decisions Made:**
- `skip_data_not_ready` as the new counter name — distinguishes from `skip_no_player` in logs
- Apply fix to all sports, not just MLB — same merged-fallback pattern exists for NBA
- Layer 3 (theory pick audit) deferred — will scope after session context compression
- Layer 4 (game_pk matching) out of scope for now

**Lessons Learned:**
- Merged-dict fallback was intended as leniency ("if date lookup misses, try by name") but converts "no data yet" misses into "wrong data" hits — leniency in resolvers is dangerous
- The date-specific lookups were already correct (ET conversion working fine); the flaw was the fallback path that bypassed date semantics entirely
- Fixture completion gate alone (Layer 1) isn't sufficient — need to also prevent wrong-date stats from entering the grading path (Layer 2)

**Action Items:**
- Layer 3: Historical audit of theory picks for silent wrong-grades from merged-fallback contamination (deferred, needs separate session)
- Layer 4: Match stats by `game_pk` (MLB Stats API per-game id) instead of player name alone — strongest future-proofing (not yet scoped)
- Plan doc saved at `brain/findings/2026-05-21-resolver-layer-2-kill-merged-fallback.md` (~360 lines)
- Commits: `9ae67a6` (Layer 1 fixture gate), `d8da8a9` (Layer 2 kill merged fallback) — both on origin/main, deployed to Eve

### Memory Flush (10:17) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (10:49) [superwin]

**Context:** Reverse-engineering bet365's racing WebSocket protocol and SPA anti-bot protections to get live odds streaming working.

**Key Exchanges:**
- Proved bet365's WS topic format is `PM{fixture_id}-{participant_id}` — server echoes back acknowledgments but returns `EMPTY` for markets not activated by the SPA
- Discovery feed (`DR2488S12`) works and returns AU/NZ meetings + fixture IDs correctly
- Tested every programmatic navigation variant (`window.location.hash`, `history.pushState`, `page.goto`, fake anchor clicks, hashchange dispatch) — all silently fail or 403
- Only real Playwright `locator.click()` triggers the racecoupon API and activates WS market fan-out

**Decisions Made:**
- **"All meetings" view doesn't exist** — bet365 architecturally prevents seeing a full day card; Next-To-Jump is the only usable racing list
- **Click-loop scanner is the viable path** — click each visible NTJ race via Playwright, capture racecoupon, piggyback WS for odds. Covers ~2 hours ahead (which aligns with the 0.5–180 min scanner gate anyway)
- **Park bet365 as a follow-up project** — not worth building today given 4 other bookies are healthy and producing +10% ROI

**Lessons Learned:**
- bet365's WS is a **click-gated state machine**, not a subscribe-and-receive bus — server-side activation only happens when SPA authoritatively navigates via real click
- Cloudflare blocks all direct HTTP to racecoupon API outside the SPA context
- WS passive traffic (L1, L18, Media_L30_Z0, DR) flows without activation, but PM (price/odds) data does not
- The `sync_term` (1192 chars) rotates every ~45s — needs fresh capture for any subscription attempt

**Action Items:**
- Build click-loop scanner for NTJ races (~2 hour project, next session)
- Commit VPS-push patch + AdsPower auto-page-open as clean baseline
- Consider whether bet365's marginal value (5-10% more EV picks) justifies ongoing maintenance vs the existing 4-bookie coverage

### Session (11:33) [superwin]

**Context:** Integrating bet365 as 5th sportsbook in the v3 value-betting racing scanner — replacing broken HTML scrape with click-loop approach.

**Key Exchanges:**
- WS wildcard subscription experiment: all prefix variants (`FI`, `FX`, `PM`, `PMA`, `F`) returned EMPTY — server acknowledges topic but won't fan out data without SPA activation
- Confirmed bet365 WS topic format is `PM{fixture_id}-{participant_id}` (proven by `TO=M...-...` echo in EMPTY responses)
- Confirmed no "all meetings" view exists in bet365 — NTJ (Next-To-Jump) is the only racing list without navigating into a specific meeting first
- End-to-end flow working: click-scan → racecoupon capture → WS odds streaming → VPS push → catalogue merge. 14 bet365 races in VPS catalogue, 8 matched with 5-bookie odds.

**Decisions Made:**
- **Click-loop scanner over HTML scrape** — bet365's WS is a fanout model where server-side activation is a click-gated state machine, not a subscribe-and-receive bus. Only real Playwright clicks activate markets.
- **Accept NTJ-only coverage** (~24 races, ~2h rolling window) — sufficient because edge picks fire in the 0.5–180 min pre-jump band anyway.
- **Ship now, periodic re-scan later** — `clicked_scan_au_races(already_activated=...)` is wired for dedup but no periodic caller yet.

**Lessons Learned:**
- bet365 WS won't serve odds unless the SPA itself has authoritatively activated the market via a real click path — `window.location.hash` navigation is silently ignored
- First click in the loop often misses (cold-start issue) — needs a warm-up click or retry
- `locator.click()` works on Eve in isolation but can fail on stale page state in tmux — always kill and relaunch clean
- VPS ingest expects `{"races": [...]}` wrapper, not raw list (got HTTP 422 before fixing)
- 6/20 bet365 races pushed but unmatched on VPS — NZ races or timing/type mismatches with canonical catalogue

**Action Items:**
- Wire periodic re-scan (every 5 min) into `stream_odds_via_browser` loop — ~10 lines, `already_activated` dedup is ready
- Fix first-click cold-start miss (warm-up click or retry logic)
- Investigate 6 unmatched bet365 races (NZ coverage gap vs canonical catalogue mismatch)

### Session (11:40) [superwin]

**Context:** Reverse-engineering bet365's WebSocket protocol and shipping a click-loop racing scanner to Eve

**Key Exchanges:**
- Probed bet365 WS with fixture-wildcard subscriptions — all returned EMPTY, proving markets require SPA click-activation before odds fan out
- Confirmed no "all meetings" view exists — bet365 architecturally prevents it (hash nav ignored, only real clicks activate markets)
- Built click-loop scanner that clicks NTJ races via Playwright, captures racecoupons, piggybacks WS for live odds
- Local test: 7/8 races activated, 73 runners with names + fractional win odds (92% coverage)
- Eve deployment: 20/20 click-scan succeeded, VPS push 20 sent / 8 matched (6 unmatched are NZ-only or timing mismatches)

**Decisions Made:**
- Replaced broken HTML-scrape `_scan_venue` approach with Playwright locator.click loop — HTML navigation silently ignored by bet365's anti-bot
- VPS push body must be `{"races": [...]}` not raw list (422 error caught and fixed)
- `max_clicks=20` default for NTJ scan — covers ~2h window of upcoming races
- Parked "find all meetings view" — confirmed architecturally impossible on bet365
- bet365 value is incremental (+5-10% more EV picks) over existing 4-bookie set — worth having but not critical path

**Lessons Learned:**
- bet365 WS is a fanout subscription model gated by SPA activation — you can subscribe to any `PM{fid}-{pid}` topic but get EMPTY until the SPA has clicked into that market
- First click in a loop often misses (cold-start timing) — warm-up click or retry needed
- Eve's AdsPower Chrome may have stale page state between runs — always kill tmux session and re-launch clean
- Locator.click works on Eve (standalone test proved it) even when a previous tmux session appeared stuck — stale state was the issue, not the click mechanism
- bet365 NTJ coverage rolls forward naturally — races enter the window as earlier ones jump

**Action Items:**
- P1: Wire periodic re-scan (5-min cadence) into `stream_odds_via_browser` — `already_activated` param already wired, just needs the caller loop (~10 lines)
- P2: Fix first-click cold-start miss (warm-up click or retry logic)
- P3: Improve NZ race matching (6 unmatched bet365-only races in catalogue)
- Commit pushed: `e9c6fab` — `feat(bet365): replace HTML scrape with click-loop scanner` on `LCASH/superwin`
- Ops doc created: `docs/BET365_RACING_OPERATIONS.md` with full runbook + prioritised plan (P1–P7)

### Memory Flush (12:14) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (12:40) [TAKEOVER]

**Context:** Security hardening of Supabase app (portal lockdown fix) and diagnosing/fixing broken Twilio inbound SMS

**Key Exchanges:**
- Applied a migration to production that created `account_owners_safe` view, `admin_update_bowler` RPC, and updated triggers — all verified live
- Committed and pushed fix (`65ae93b`) that swapped 13 read sites to safe view and 9 write sites to RPC wrapper
- Verified Vercel deployment by checking live bundle hashes and code-split chunks all contained new references
- Diagnosed Twilio inbound SMS outage: messages stopped arriving May 18 after signature verification was added in commit `43c8802`

**Decisions Made:**
- **Backfill before fix:** Prioritized recovering 47 missed inbound messages from Twilio API before fixing the forward path, so users could see/respond to missed conversations immediately
- **Multi-URL signature verification:** Rather than hardcoding a single URL, the fix tries canonical `SUPABASE_URL + /functions/v1/receive-sms` first, then `req.url`, then reconstructed from forwarded headers — resilient to Supabase Edge Runtime URL mismatches
- **Backfill metadata tagging:** All 47 backfilled messages carry `metadata.backfilled = true` and preserve original `MessageSid` as `external_id` for dedup

**Lessons Learned:**
- **Supabase Edge Runtime `req.url` is NOT the public URL** — it's an internal runtime URL. Twilio signs against the public webhook URL, so signature verification using `req.url` will always fail. This is a known footgun when adding webhook signature verification to Supabase Edge Functions.
- **Volume cliff analysis is the fastest way to diagnose webhook failures** — querying `messages` by date immediately pinpointed May 18 as the break date, which matched the security commit timestamp
- **`database.types.ts` was JSON-wrapped since commit `89cc108`** — pre-existing issue that broke `tsc -b` but didn't block Vite builds since `build:web` is just `vite build`

**Action Items:**
- Click-test the 9 flows on takeover-khaki.vercel.app (Bowlers list, BowlerDetail, Inbox thread, kanban drag, PortalLeads mark, note edit, Finance pages, BookieHub, EdgeLibrary)
- Regenerate `database.types.ts` properly (currently JSON-wrapped, breaks TypeScript checking)
- Monitor inbound SMS volume over next 24h to confirm the fix holds

### Session (12:57) [superwin]

**Context:** Fixing bet365 race data quality issues in the value-betting scanner — duplicate runners, vacant boxes, and result-prefix leaking into horse names.

**Key Exchanges:**
- Confirmed bet365 odds are flowing and updating on VPS (44% churn rate across race types R/G/H)
- Pushed `feat(bet365): periodic rescan + authoritative per-fixture race_type` (55fa62d)
- User flagged poor data quality on dashboard screenshot — only 1/9 runners had bet365 odds, duplicate "Katie's Quick" rows, and messy display

**Decisions Made:**
- Three-tier race_type resolution: CL from racecoupon body → DR2488S12 discovery map → "R" fallback
- 5-min periodic rescan inside `stream_odds_via_browser` to catch new races
- Three data-quality fixes deployed simultaneously:
  1. **Name dedup** in click-scan PA loop (bet365_stream.py) — deduplicate runners by normalized name, keep first occurrence
  2. **Vacant box filter** — strip `*** VACANT BOX ***` entries from both click-scan and racecoupon parser
  3. **Result prefix strip** — remove "1. " / "8. " position prefixes leaking into runner names from finished races (both bet365_stream.py and bet365_local.py)
  4. **Catalogue-level dedup** in `race_catalogue.py` as a safety net at ingest

**Lessons Learned:**
- Bet365 emits two participant_ids for the same horse (Win vs Place market) — click-scan must dedup by name
- Finished races leak result-position prefixes into runner names (e.g., "1. Lacy Sarah")
- `*** VACANT BOX ***` entries in greyhound races need filtering before they reach the catalogue
- Always check a non-scratched runner when debugging odds flow — scratched runners have empty odds objects

**Action Items:**
- Verify all three fixes against live API after Eve rediscovery + catalogue rebuild (~2.5 min wait in progress)
- Stale `Shepparton|R|5` etc. records will age out naturally — no action needed

### Session (13:30) [superwin]

**Context:** Fixing three data-quality bugs in the superwin VPS race catalogue (duplicates, vacant boxes, saddle-prefix bleed)

**Key Exchanges:**
- Verified and fixed three distinct data-quality issues: duplicate runners, vacant/placeholder boxes, and saddle-number prefix bleed (e.g., "1. Lacy Sarah" instead of "Lacy Sarah")
- Initial fixes resolved bet365 dupes/vacants but missed Betfair scaffold path and TAB "...." placeholder names
- Extended sanitizer twice to cover all ingest paths before reaching 0/0/0 across 285 races

**Decisions Made:**
- Created a shared `_sanitize_runner_name()` helper used in both scaffold creation and canonical-add paths, rather than patching each path individually — prevents future regressions when new ingest paths are added
- Filtered vacants at scaffold time (not just display time) to prevent them propagating downstream
- `_normalize_name("....")` returning empty string was explicitly handled — empty-after-normalize runners are now dropped

**Lessons Learned:**
- `race_catalogue.py` has multiple ingest paths (scaffold, `_merge_race`, canonical-add, result loader in `main.py`) — sanitization must happen at every entry point or via a shared helper
- Stale scaffolds can survive restarts if there's a race window between SCP deploy and tmux kill — always kill first, then deploy
- TAB/TabTouch publish literal "...." as runner names for scratched/unnamed entries; normalizing these to empty and then skipping prevents phantom duplicates

**Action Items:**
- Commits pushed: `04da9b0` (initial 3-fix) and `d2fe2a6` (extended sanitizer helper)
- Files changed: Eve (`bet365_stream.py`, `bet365_local.py`), VPS (`race_catalogue.py`, `main.py`)
- Monitor for any new ingest paths that bypass `_sanitize_runner_name()` in future adapter work

### Session (14:00) [superwin]

**Context:** Wiring bet365 place odds into the superwin racing catalogue (greyhounds/horses)

**Key Exchanges:**
- All three prior data-quality fixes confirmed clean across 285 races (0 duplicates, 0 vacant boxes, 0 saddle-prefix bleed) — committed as d2fe2a6
- Confirmed live odds streaming + clean horse names across 5-bookie merged races
- Investigated adding bet365 place odds — initially found `FP` field in PM stream chunks and assumed it was "Fractional Place"

**Decisions Made:**
- **Snapshot place from racecoupon body, not stream** — PM stream `FP` field is actually "Fractional Price" (duplicate of win fractional), not place. bet365 reuses the same field name with different semantics in racecoupon body vs WS stream. Racecoupon body has genuinely distinct FW (win) and FP (place) values.
- **Heuristic guard: only accept place if strictly < win** — racecoupon FP is inconsistent across race types (Ipswich R4 had place=win for all 9 runners). A genuine place market is always shorter than win, so anything else is noise/missing data.

**Lessons Learned:**
- bet365 reuses field names across contexts with different meanings: `FP` = Fractional Place in racecoupon body, but Fractional Price (win duplicate) in PM stream chunks. Always cross-validate field semantics between static and streaming payloads.
- Greyhound/horse place odds don't move fast enough to need streaming — 5-min racecoupon refresh cycle is sufficient for value detection.
- When a newly-parsed field produces values identical to an existing field, treat it as a red flag (likely parsing the same underlying data, not a new data source).

**Action Items:**
- Verify heuristic fix produces strictly place < win for all runners with place values
- Future: reverse-engineer bet365 WS Place topic (likely `PP` prefix) if live place streaming is ever needed

### Session (14:47) [value-betting-scanner]

**Context:** Debugging and fixing multi-layer resolver bugs that caused cross-game stat leakage when same-matchup games occurred on consecutive days (MLB SD@LAD, LAA@OAK)

**Key Exchanges:**
- Traced OKC vs Spurs test case to show how `game_date` batching (Step 1) and ET-date stats lookup (Step 3) worked correctly, but Layer 1's fixture gate dropped date info from its completed-set — allowing name collisions
- Discovered **two-bug stack**: Layer 1 fixture gate (`54d925f`) was correct but unreachable because the pick SELECT clause didn't include `fixture_name` — so `pick.get("fixture_name")` always returned None and the gate was silently bypassed
- User's key insight: don't gate on external "is game completed?" — instead trust **absence of stats data** as the signal. If MLB Stats API has nothing for player X on date Y, skip and retry next cycle

**Decisions Made:**
- **Layer 1 fix** (`f7ae928`): Added `fixture_name` to the SELECT clause so the fixture-completion gate actually runs
- **Layer 2.5 fix** (`15ae6bf`): Killed the OpticOdds fallback for MLB — absence of MLB Stats API data now means "not published yet, retry" instead of falling back to OO's UTC-bucketed dict (which contained yesterday's same-team stats)
- Belt-and-suspenders: Layer 1 (fixture gate) + Layer 2 (per-date stats only) + Layer 2.5 (no OO fallback for MLB) — any single layer would have caught today's bug

**Lessons Learned:**
- A SELECT clause silently omitting a column makes downstream `.get()` return None — the gate logic looked perfect but was unreachable. Always verify the data actually reaches your guard code
- UTC-bucketed dicts (`opt_stats_by_date`) can contain stats from the prior ET day's late games (e.g., 2026-05-20T01:40Z UTC start lands in May 20 UTC bucket but is a May 19 ET game) — this is the root cause of cross-game leakage
- NBA avoided the bug only because 48h gap between same-matchup games kept UTC buckets separate; back-to-back games (common in MLB and NBA B2Bs) would eventually trigger it
- May 20 cycle showed `skip_fixture_not_complete=45/91` — ~half of picks have stored fixture names that don't match OO's display name variants. Not urgent but worth a future name-normalization pass

**Action Items:**
- Monitor LAA@OAK picks (59 remaining null) to confirm correct grading once game completes
- Investigate the 45/91 fixture-name mismatch on May 20 picks (stored name vs OO display name divergence)
- Layer 3 historical audit: quantify how many prior picks were silently mis-graded before the SELECT fix, since the gate was unreachable for all prior resolver cycles
- Consider applying the "no OO fallback" pattern to NBA as well (currently only MLB), since NBA B2Bs could trigger the same UTC-bucket collision

