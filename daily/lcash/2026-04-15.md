# Daily Log: 2026-04-15 (lcash)

## Sessions

## Memory Maintenance

### Session (07:14) [value-betting-scanner]

VPS is a DigitalOcean droplet, btw. Hard reboot via DigitalOcean dashboard would be the fastest path.

**Update**: The mini PC trackers *do* write to Supabase directly — the VPS being down only affects:
- VPS dashboard visibility
- VPS relay tracker (backup writes)
- Push notifications (if the push worker routes through VPS)

Picks ARE still being tracked by the mini PC. The only gap is the VPS relay's supplementary writes.

---

Let me know once you've power-cycled it, or if you want me to keep retrying SSH in the background.

### Memory Flush (08:06) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (08:21) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (11:04) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (13:06) [value-betting-scanner]



**Context:** Diagnosing and fixing stale odds display on the VPS dashboard — bet365 odds shown on picks weren't matching what bet365 actually had, due to no visibility into how old the scraped odds were.

**Key Exchanges:**
- User reported NBA picks appearing stale (already tipped-off games still showing) — explained this is picks for live games awaiting resolver grading, not a data freshness issue. Suggested adding `game_start > now()` filter.
- User reported bet365 odds on dashboard not matching actual bet365 odds when going to place bet — could be scraper lag, ingest lag, or UI not updating.
- Built a diagnostic plan and implemented "Layer A" — end-to-end `source_captured_at` timestamp plumbing to show odds age on the dashboard.

**Decisions Made:**
- Plumb `source_captured_at` from scraper parse-time all the way through worker → server ingest → state → SSE → dashboard, rather than just stamping `now()` at ingest (which hides true scraper-to-display latency).
- Use ratchet-only logic for `source_captured_at` (never moves backwards) to avoid stale timestamps overwriting fresher ones.
- Color-code odds age in dashboard: green ≤30s, yellow ≤90s, orange ≤180s, red >180s.
- Only show age for bet365 rows (OpticOdds rows stay blank since they don't have `source_captured_at` yet).

**Lessons Learned:**
- `datetime.utcnow()` produces naive datetime whose `.timestamp()` assumes local time — latent tz bug fixed by switching to `datetime.now(timezone.utc)`.
- The existing `captured_at` field was being re-stamped with `now()` at each layer (worker, server ingest, VPS relay), making it impossible to measure true end-to-end latency from scraper to dashboard.
- Bet365 continues serving pre-game-style lines on some markets after tipoff (bet-builder props, stale cache), so the scanner keeps ingesting them as if they're valid.

**Action Items:**
- Deploy Layer A changes (mini PC + VPS) — user hasn't confirmed deploy yet
- Consider adding `game_start > now()` filter to dashboard picks to hide tipped-off games
- Layer A is diagnostic only — once odds age is visible, further layers (B+) needed to fix actual staleness causes if identified

### Session (13:38) [value-betting-scanner]

**Context:** Diagnosing and fixing stale odds display on the VPS dashboard — bet365 odds shown on picks weren't matching what bet365 actually had, due to no visibility into how old the scraped odds were.

**Key Exchanges:**
- User reported NBA picks appearing stale (already tipped-off games still showing) — explained this is picks for live games awaiting resolver grading, not a data freshness issue. Suggested adding `game_start > now()` filter.
- User reported bet365 odds on dashboard not matching actual bet365 odds when going to place bet — could be scraper lag, ingest lag, or UI not updating.
- Built a diagnostic plan and implemented "Layer A" — end-to-end `source_captured_at` timestamp plumbing to show odds age on the dashboard.
- Identified 6 distinct causes of odds drift: snapshot lag (5–65s pipeline), scraper missed updates, different bet365 surfaces (BB wizard vs Match tab), book 365 vs 366 disagreement, account-specific pricing, and SSE ghost markets never being deleted.

**Decisions Made:**
- Plumb `source_captured_at` from scraper parse-time all the way through worker → server ingest → state → SSE → dashboard, rather than just stamping `now()` at ingest (which hides true scraper-to-display latency).
- Use ratchet-only logic for `source_captured_at` (never moves backwards) to avoid stale timestamps overwriting fresher ones.
- Color-code odds age in dashboard: green ≤30s, yellow ≤90s, orange ≤180s, red >180s.
- Only show age for bet365 rows (OpticOdds rows stay blank since they don't have `source_captured_at` yet).

**Lessons Learned:**
- `datetime.utcnow()` produces naive datetime whose `.timestamp()` assumes local time — latent tz bug fixed by switching to `datetime.now(timezone.utc)`.
- The existing `captured_at` field was being re-stamped with `now()` at each layer (worker, server ingest, VPS relay), making it impossible to measure true end-to-end latency from scraper to dashboard.
- Bet365 continues serving pre-game-style lines on some markets after tipoff (bet-builder props, stale cache), so the scanner keeps ingesting them as if they're valid.
- The 30s hash-skip blackout in server/main.py can add up to 30s of unnecessary staleness — single biggest quick-win to remove.
- SSE deltas merge new markets but never delete old ones (polling fallback does); creates ghost markets on long-lived dashboard tabs.

**Action Items:**
- Deploy Layer A changes (mini PC + VPS) — user hasn't confirmed deploy yet
- Consider adding `game_start > now()` filter to dashboard picks to hide tipped-off games
- Layer A is diagnostic only — once odds age is visible, further layers (B+) needed to fix actual staleness causes if identified
- Quick wins prioritized: kill 30s hash-skip, reduce 15s→8s refresh interval, SSE delete-missing parity, prefer-freshest soft book collapse, confirm-on-click verification

### Session (14:17) [value-betting-scanner]



**Context:** Strategic decision to abandon HTTP-polling diagnostic approach (Layer A / `source_captured_at` plumbing) and commit fully to WebSocket-based architecture for bet365 odds streaming.

**Key Exchanges:**
- User declared "ws is the only way" — firm commitment to WS as the sole architecture, not an experiment
- Discussed three options for uncommitted Layer A changes (stash on branch / revert / ship); user chose **revert**
- All Layer A `source_captured_at` changes across 4+ files were reverted, working tree restored to session-start state

**Decisions Made:**
- **WS-only commitment**: HTTP polling is dead; WebSocket streaming is the path forward for bet365 odds. Layer A diagnostic plumbing (making HTTP staleness visible) is pointless if there's no HTTP polling.
- **Revert Layer A, don't stash**: User chose clean revert over keeping scaffolding on a branch — no desire to carry diagnostic code forward.
- **Three-phase build plan agreed**: Phase 1 (standalone WS probe to answer subscription model unknowns) → Phase 2 (NBA WS adapter port mirroring racing adapter architecture) → Phase 3 (cutover after 24h side-by-side comparison).

**Lessons Learned:**
- Two critical unknowns must be resolved before the WS port: (1) does BB wizard auto-subscribe to all prop markets or require explicit `PM{...}` subscription messages, (2) what's the bet365 subscription ID format for player props (racing uses `PM{fid}-{pid}`, props likely different — `EV{ev_id}` or `MA{ma_id}`).
- The racing adapter already proves the CDP-based WS pattern works; the NBA port should copy that architecture (HTTP discovery for ID mapping + persistent WS frame listener via raw CDP + in-memory market state updated on `U|` frames).

**Action Items:**
- Update plan file to retire diagnostic-first framing, replace with WS-only commitment + 3-phase plan
- Build Phase 1 probe (`scripts/probe_bet365_ws.py`) — standalone, no production touch, attaches to existing Chrome via CDP, logs raw WS frames for 5 min to confirm subscription model and ID format
- Phase 2: new `ev_scanner/bet365_game_ws.py` + `server/bet365_game_ws_worker.py`, run side-by-side with HTTP scraper
- Phase 3: 24h comparison, then disable HTTP path and delete all staleness mitigation code (hash-skip, refresh intervals, `captured_at` overwrites)
- Consider MLB WS feasibility after NBA probe results

