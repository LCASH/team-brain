# Daily Log: 2026-04-15 (lcash)

## Sessions

## Memory Maintenance

### Session (07:14) [value-betting-scanner]

VPS is a DigitalOcean droplet, btw. Hard reboot via DigitalOcean dashboard would be the fastest path.

**Update**: The mini PC trackers *do* write to Supabase directly — the VPS being down only affects:
- VPS dashboard visibility
- VPS relay tracker (backup writes)
- Push notifications (if the push worker routes through VPS)

Picks ARE still being tracked by the mini PC. The only gap is the VPS relay's supplementary writes.

---

Let me know once you've power-cycled it, or if you want me to keep retrying SSH in the background.

### Memory Flush (08:06) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (08:21) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (11:04) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (13:06) [value-betting-scanner]



**Context:** Diagnosing and fixing stale odds display on the VPS dashboard — bet365 odds shown on picks weren't matching what bet365 actually had, due to no visibility into how old the scraped odds were.

**Key Exchanges:**
- User reported NBA picks appearing stale (already tipped-off games still showing) — explained this is picks for live games awaiting resolver grading, not a data freshness issue. Suggested adding `game_start > now()` filter.
- User reported bet365 odds on dashboard not matching actual bet365 odds when going to place bet — could be scraper lag, ingest lag, or UI not updating.
- Built a diagnostic plan and implemented "Layer A" — end-to-end `source_captured_at` timestamp plumbing to show odds age on the dashboard.

**Decisions Made:**
- Plumb `source_captured_at` from scraper parse-time all the way through worker → server ingest → state → SSE → dashboard, rather than just stamping `now()` at ingest (which hides true scraper-to-display latency).
- Use ratchet-only logic for `source_captured_at` (never moves backwards) to avoid stale timestamps overwriting fresher ones.
- Color-code odds age in dashboard: green ≤30s, yellow ≤90s, orange ≤180s, red >180s.
- Only show age for bet365 rows (OpticOdds rows stay blank since they don't have `source_captured_at` yet).

**Lessons Learned:**
- `datetime.utcnow()` produces naive datetime whose `.timestamp()` assumes local time — latent tz bug fixed by switching to `datetime.now(timezone.utc)`.
- The existing `captured_at` field was being re-stamped with `now()` at each layer (worker, server ingest, VPS relay), making it impossible to measure true end-to-end latency from scraper to dashboard.
- Bet365 continues serving pre-game-style lines on some markets after tipoff (bet-builder props, stale cache), so the scanner keeps ingesting them as if they're valid.

**Action Items:**
- Deploy Layer A changes (mini PC + VPS) — user hasn't confirmed deploy yet
- Consider adding `game_start > now()` filter to dashboard picks to hide tipped-off games
- Layer A is diagnostic only — once odds age is visible, further layers (B+) needed to fix actual staleness causes if identified

