# Daily Log: 2026-04-27 (lcash)

## Sessions

## Memory Maintenance

### Session (07:38) [value-betting-scanner]

This appears to be a session focused on building and deploying a TabTouch/Kambi scraper for the value betting pipeline. Here's the daily log:

**Context:** Building and deploying TabTouch (Kambi) scraper for NBA + MLB player props as a second AU soft book alongside TAB.

**Key Exchanges:**
- Reverse-engineered the full Kambi API structure from captured network traffic — REST endpoints, WebSocket push protocol, market taxonomy (106 unique criterion labels for NBA)
- Kambi API is fully public (no auth, no cookies), uses integer encoding (odds/1000, line/1000), provides full player names and stable `participantId` across markets
- MLB on Kambi has proper Over/Under lines (not just thresholds like TAB) — 182 betOffers per game, 11 player prop types

**Decisions Made:**
- Assigned book ID 909 to TabTouch (Kambi) — already existed in `SOFT_BOOK_IDS` and `BOOK_NAMES`
- Used REST polling at 60s intervals (not WebSocket) for initial implementation — faster to ship, WebSocket upgrade deferred
- Scraper integrated directly into `direct_scraper_worker.py` alongside TAB polling

**Lessons Learned:**
- Kambi powers multiple AU bookies with the same API structure, just different operator codes (`rwwawa` = TabTouch/RWWA)
- Key difference from TAB: Kambi returns 1 outcome per betOffer (Over OR Under separately), while TAB returns 2 outcomes per market
- TabTouch provides `participantId` (stable player ID across markets) — useful for player matching
- MLB Kambi props: Home Runs, Hits, Runs, Bases, RBIs, Doubles, Strikeouts, Stolen Bases, Outs

**Action Items:**
- Build Kambi WebSocket consumer (Socket.IO v4) for real-time odds instead of 60s REST polling — would improve trail accuracy and catch fleeting edges
- Monitor TabTouch (909) data flowing through to picks/trails — needs validation that player matching works with full names vs abbreviated TAB names
- Check if other Kambi operators (different codes) are worth adding as additional soft books

### Session (08:10) [value-betting-scanner]

**Context:** Debugging value betting dashboard trail charts that were showing incorrect EV visualization

**Key Exchanges:**
- User noticed graph showed negative EV but table showed positive EV for the same pick
- Deep dive revealed the trail chart's `_drawTrailChart` was NOT applying line interpolation when sharp and soft books had different lines (e.g., sharp at 13.5, soft at 14.5) — the table's `computeTrueProb()` uses `interpolateForProp()` but the chart skipped it entirely
- After fixing interpolation, Under picks were still inverted — trail stores `odds` = this side, `u` = opposite side, but `devigMultiplicative()` always expects (Over, Under) order. Under picks had the args swapped
- Final deep dive on "prove they aren't wrong" revealed some picks are genuinely near-zero or slightly negative EV *now* even though they were +EV at detection — FanDuel dragging weighted average up in Aggressive theory

**Decisions Made:**
- Applied `interpolateForProp()` in the chart's true-odds computation to match the table's logic
- Added Over/Under swap detection so Under picks pass correct arg order to `devigMultiplicative()`
- Chart is now considered "telling the truth" — picks can drift from +EV to -EV as sharp odds change post-detection

**Lessons Learned:**
- Trail chart and table were using fundamentally different true-odds calculations — chart was raw devig, table used interpolation across line gaps. Always ensure visualization matches the same math as the triggering logic
- Over/Under argument ordering is a subtle but critical bug — trail data stores odds relative to the pick's side, not in canonical Over/Under order
- `max_line_gap: 4` in Aggressive theory is very generous and allows picks that may be borderline +EV at detection, then quickly drift negative
- Browser caching can mask deployed fixes — user may see old JS even after VPS push

**Action Items:**
- Consider whether dashboard should visually flag picks that have drifted from +EV to -EV since detection
- Review `max_line_gap` setting on Aggressive theory — 4 points may be too generous, creating marginal picks
- Potentially add cache-busting to JS assets so chart fixes take effect immediately

### Session (08:45) [value-betting-scanner]

**Context:** Deep debugging of value betting dashboard — chart display bugs, EV calculation discrepancies, and architectural redesign to show per-theory true lines.

**Key Exchanges:**
- Charts showed true (purple) line ABOVE soft (green) line on +EV Under picks — user caught the inversion
- Table showed true odds = 1.83 (+3.8% EV) but manual server-side computation gave true odds = 1.96 (-0.4% EV) for the same pick
- User questioned whether bet365 soft odds matched reality — revealed they were looking at Rebounds solo (Under 5.5 @ 1.74) vs the dashboard's Rebounds+Assists combo (Under 8.5 @ 1.95) — different markets entirely

**Decisions Made:**
- **Under picks devig swap fix**: Trail stores `odds` = this side, `u` = opposite. For Under markets, must swap before passing to `devigMultiplicative(over, under)` which always expects Over first
- **Per-theory true lines in chart**: Draw separate colored lines per qualifying theory instead of one blended purple line, so user can see which theories agree vs disagree
- **Per-theory true odds in table**: Each theory EV column now displays that theory's specific true odds above the EV%, computed independently using that theory's sharp books, weights, and devig method

**Lessons Learned:**
- **Data freshness causes EV flip-flops**: FanDuel goes stale periodically (captured_at drifts past 600s). When only DraftKings is fresh → true=1.83 (+EV). When both are fresh → FanDuel drags average to 1.96 (break-even). The pick's EV depends on which sharps happen to be fresh at render time
- **Sharp disagreement = low confidence**: When FanDuel says -EV and DraftKings says +EV, blending them into one number hides the conflict. Per-theory display surfaces this disagreement
- **Single true line is misleading**: Different theories (Aggressive, AltLine-V1, Pinnacle Only) use different sharps with different weights — the "true" line IS different per theory. Showing one blended value obscured whether a pick was genuinely +EV
- **Combo vs solo prop confusion**: "Rebounds Assists" combo at line 8.5 is a completely different market from "Rebounds" solo at line 5.5 — matching is correct but the UI labeling can confuse users checking odds manually on the book's site

**Action Items:**
- Consider adding a confidence indicator or sharp count badge so picks supported by only 1 sharp book are visually flagged as lower confidence
- Monitor whether FanDuel staleness pattern is systematic — may need shorter staleness threshold or per-book freshness indicators

### Session (09:23) [value-betting-scanner]

**Context:** Dashboard improvements for value betting scanner — theory display, league pills, bet365 book ID fix, and chart visualization fixes.

**Key Exchanges:**
- Per-theory true odds now displayed inline in EV columns (true odds, EV%, CLV per theory)
- Bet365 2.0 worker monitored: crashed ~08:44, auto-restarted, rebuilt game pages within minutes — auto-restart mechanism confirmed working
- Dashboard only had NBA/MLB/Pinnacle/Crypto pills — user wanted to see all basketball and baseball leagues in one view

**Decisions Made:**
- Added **"All BBall"** and **"All Base"** virtual pills using a `sportKeySet` filter approach (set of sport_keys per sport category) rather than trying to match by OpticOdds sport category, since market data only carries our `sport_key`
- Basketball set: nba, euroleague, germany_bbl, japan_b1, korea_kbl, argentina_lnb, spain_acb, etc.
- Baseball set: mlb, kbo, npb, cpbl, college_baseball, milb_aaa

**Lessons Learned:**
- **MLB bet365 game scraper was using `BET365_2_BOOK_ID = 365` (old betstamp ID) instead of 366 (Bet365 2.0)**. All MLB game scraper data was invisible on the dashboard because the dashboard filters for book 366. Fixed by changing MLB worker to write as 366.
- **Soft trail chart gap**: When soft odds don't change overnight, the chart shows sharp theory lines with no soft reference. Fix: draw a **dashed green horizontal line** extending the last known soft value across the full time range.
- **Under pick chart interpretation is confusing**: For Unders, soft ABOVE true = +EV (you're getting paid more than the true probability says you should). This is the opposite visual intuition from Overs. The chart is mathematically correct but easy to misread. May need a visual indicator or annotation clarifying EV direction for Under picks.

**Action Items:**
- Consider adding a visual EV direction indicator on the chart for Under picks (e.g., shaded region or arrow showing which direction is +EV) to prevent misreading
- Verify bet365 2.0 MLB data is now flowing correctly as book 366 after the fix

### Session (09:58) [superwin]

**Context:** Deploying 7 bug fixes to the SuperWin edge scanner on the VPS, then verifying health and updating the `/diagnose` skill.

**Key Exchanges:**
- Deployed 7 scanner fixes: liquidity fallback (`selection_matched=0`), freshness timestamps on all BookieOdds, stale odds filter (30s), warmup guard (120s bookie check), max EV cap (>100% = garbage), dropped pick logging, same-race correlation flag
- Discovered legacy `server/models.py` was missing new fields (`selection_matched`, `updated_at`) while `server/app/core/models.py` had them — legacy bookies import from the legacy path, so both needed updating
- Updated `/diagnose` skill with new checks: EV trail verification, pick journal stats, edge scanner health, cron restart validation

**Decisions Made:**
- EV > 100% capped as data garbage (eliminates false signals like "Giggle Hat 330%")
- Stale odds threshold set at 30 seconds; warmup guard at 120 seconds
- Same-race count stored per pick for future correlation analysis (requires Supabase `ALTER TABLE` to add column)

**Lessons Learned:**
- Legacy vs new model paths: `server/models.py` and `server/app/core/models.py` can drift — must update both when adding fields
- `selection_matched=0` is valid (low liquidity market) — don't treat it as "no data"
- Need Supabase schema migration (`same_race_count` column) alongside code deploys

**Action Items:**
- Run Supabase SQL: `ALTER TABLE public.superwin_edge_picks ADD COLUMN IF NOT EXISTS same_race_count int DEFAULT 1;`
- Monitor disk usage (85% of 8.7GB) — auto-clean triggers at 90%
- 5 stuck NZ picks are permanent (no Betfair results for NZ racing) — consider auto-voiding NZ picks

### Session (10:32) [superwin]

**Context:** Debugging and improving SuperWin racing scanner's Betfair venue matching coverage

**Key Exchanges:**
- TAB showing 0/29 races mid-session was just a REST poll timing issue (16-second fetch window) — not a real problem
- Orange dots in TAKEOVER mean "no Betfair match" — venues have TAB+TabTouch but no Betfair market to compare against
- Identified that many orange venues actually DID have Betfair data but under different names or race types

**Decisions Made:**
- Updated venue `race_types` arrays to include missing types (e.g., Sandown needed G, Q Straight needed G, Launceston needed H, Pinjarra needed H, etc.) — the venues table only had partial race type coverage
- Added "Sandown Park" as alias for Sandown greyhounds, Q2 Parklands alias for Q Straight
- Restarted service to trigger full rediscovery cycle with updated venue config

**Lessons Learned:**
- Betfair venue matching happens at catalogue ingest time during discovery — updating the venues table race_types alone doesn't retroactively fix matches, a restart/rediscovery cycle is needed
- TAB REST polling has a 16-second window where race counts look low mid-fetch — not a bug
- Q Straight, Q1 Lakeside, Q2 Parklands are all different tracks at the Queensland (Albion Park) facility — confusing naming
- Betfair genuinely doesn't market every AU regional meeting (Ararat harness, Globe Derby Park harness, Northam greyhounds, Pinjarra harness) — those will always be orange
- Coverage improved from ~67% to 85% Betfair match rate after fixes

**Action Items:**
- Monitor that the venue race_type and alias fixes persist across restarts
- 5 stuck NZ picks are permanent (no Betfair results for NZ racing) — consider auto-voiding them
- Disk at 85% — will auto-clean at 90% but worth monitoring
- Updated the `/vb` diagnose skill with edge scanner, pick journal, and cron restart checks

### Memory Flush (10:36) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (10:37) [value-betting-scanner]

**Context:** Processing real-time NBA injury news for value betting impact

**Key Exchanges:**
- Shams Charania report: Kevin Durant ruled OUT for Game 4 tonight (Rockets vs Lakers) — left ankle sprain
- Classified as high-urgency injury event expected to materially move spreads/totals

**Action Items:**
- Monitor Rockets-Lakers Game 4 line movement post-KD ruling (game ~01:30 AM ET 4/28)
- Watch for any follow-up on KD ankle sprain severity / Game 5 availability

### Memory Flush (10:46) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (10:47) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (11:03) [value-betting-scanner]

**Context:** Testing and deploying the news-driven betting pipeline (news_agent) end-to-end, from Twitter monitoring through pick generation to Supabase storage.

**Key Exchanges:**
- Full pipeline test with "KD ruled out" tweet: classifier → dedup → odds fetch → Stage 2 research agent → guardrails → Supabase — all working, produced 4 picks
- Pipeline cost breakdown: Stage 1 ~$0.01 (Haiku), Stage 2 ~$1.03 (Sonnet), total ~$1.04 per event
- Stage 2 is the expensive part (~$1, 2-5 min) — the research agent analyzing odds and historical CLV data
- Deployed to mini PC with 87 Twitter accounts rotating, monitoring @UnderdogNBA (NBA) + @FantasyLabsMLB (MLB)

**Decisions Made:**
- Only monitor best person per sport: @UnderdogNBA for NBA, @FantasyLabsMLB for MLB (signal-to-noise optimization)
- Cleared all previous news picks and events for a clean slate before live deployment
- News picks tagged with `triggered_by=news_v1_*` to track performance separately from theory-gated picks

**Lessons Learned:**
- **Dedup timezone bug**: Games at e.g. 01:30 AM UTC April 28 = "tonight" April 27 local time. Classifier assigned different dates on each run, causing dedup to miss duplicates. Fix: search ±1 day window instead of exact date match.
- Classifier can hallucinate team associations from stale training data (thought KD was still on Suns) — team assignment from classifier isn't fully reliable
- SSH deployment: `start` command doesn't work over SSH without a GUI session — needed alternative approach (task-based)

**Action Items:**
- Monitor live news agent performance — compare news-triggered picks (`triggered_by LIKE 'news_%'`) vs regular theory-gated picks
- Watch for classifier team-assignment errors in production (KD/Suns confusion pattern)
- Consider hardening classifier with current roster data or post-classification team validation

### Session (11:34) [value-betting-scanner]

**Context:** Aligning the VB frontend dashboard's true-odds / EV computation with the backend tracker (`server/tracker.py`)

**Key Exchanges:**
- Audited six computation discrepancies between `dashboard/index.html` and `server/tracker.py` by tracing `findPair`, devig methods, Poisson interpolation, line-gap penalty, EV cap, odds-freshness, cross-validation, and `is_over` handling

**Decisions Made:**
- **Poisson interpolation**: removed the fallback-to-logit for gaps >1.0 — backend always uses Poisson for count props, so frontend should too
- **Line gap penalty**: replaced "prob-shrink toward 0.5" (which inflates low-probability events) with backend-style weight decay inside `computeTrueProb`
- **EV cap**: removed the hard 50 % cap — backend has none
- **Soft odds freshness**: tightened from 600 s → 300 s to match backend
- **Cross-validation check**: added sharp-vs-soft devig >5 pp filter, matching backend's filter
- **`is_over` case sensitivity**: changed `m.side === 'Over'` to case-insensitive `.toLowerCase()` comparison, matching backend

**Lessons Learned:**
- When the same devig/EV math exists in two codebases (frontend display + backend tracker), even small implementation differences (fallback interpolation method, penalty model, cap, freshness window) compound into noticeably different outputs
- Prob-shrink toward 0.5 is *not* equivalent to weight decay — it disproportionately inflates EV for low-probability legs
- Case sensitivity bugs in `is_over`/side fields are easy to miss because they only surface for specific market formats

**Action Items:**
- Consider extracting shared devig/EV math into a single source of truth (shared module or generated constants) to prevent future drift between frontend and backend

### Session (12:15) [value-betting-scanner]

**Context:** Debugging value betting infrastructure — dashboard showing 0 picks across all pills (Pinnacle, Crypto), and configuring news agent monitoring accounts.

**Key Exchanges:**
- Dashboard pills (Crypto, Pinnacle) showing nothing → traced to NBA tracker at 0 cycles, not a display/filtering issue
- Root cause: earlier `taskkill /im python.exe` killed all Python processes; scheduled tasks didn't cleanly recover — watchdog restarts servers without `SPORT` env var, so tracker never initializes
- VPS health endpoint showed 0 background tasks / 0 tracker cycles even though syslog showed active theory refreshes — health check counts tasks differently than what's actually running
- News agent: confirmed @ShamsCharania was missing from enabled NBA monitoring accounts; activated alongside @UnderdogNBA and @FantasyLabsMLB

**Decisions Made:**
- Added @ShamsCharania as enabled NBA news source (was already configured but not active)
- Kill stale processes and do clean restart via schtasks (not direct python launch) to preserve env vars

**Lessons Learned:**
- `taskkill /im python.exe` is a sledgehammer — kills ALL sport servers, OpticOdds, push workers. Recovery requires restarting each via its scheduled task (which carries the SPORT env var). Watchdog auto-restarts don't set env vars correctly.
- NBA tracker showing 0 cycles is the canonical symptom of "server started without SPORT env var"
- VPS SSE startup takes ~5 min on cold boot (493 leagues, sequential theory checks). Don't panic if health shows 0 immediately after restart.
- Dashboard pills read from `/api/v1/ev` (live stream of +EV picks), NOT from resolved results. Empty pills = engine not finding +EV, not a display bug.

**Action Items:**
- Verify NBA and MLB trackers reach >0 cycles after clean restart
- Confirm VPS SSE streams come online and start generating picks for Pinnacle/Crypto leagues
- Consider hardening the watchdog restart to include SPORT env var (recurring failure mode)

### Session (13:06) [superwin]

**Context:** Debugging and fixing Betfair venue matching issues in the value betting scanner (TAKEOVER), improving Betfair race coverage from 67% to 85%.

**Key Exchanges:**
- Betfair uses different venue names than TAB for the same tracks (e.g., "Sandown Park" vs "Sandown", "Globe Derby" vs "Globe Derby Park", "Q2 Parklands" vs "Q Straight")
- Venues had missing `race_types` — e.g., Ararat only had "R" but today ran harness "H", Pinjarra only had "R" but ran "H"
- Evening greyhound/harness markets (Northam, Pinjarra) are orange because Betfair doesn't publish markets until 1-3 hours before first race — not a matching bug

**Decisions Made:**
- Added venue aliases (Globe Derby → Globe Derby Park) and updated race_types on multiple venues (Ararat +H, Pinjarra +H, Northam +G, etc.)
- Service restart triggered fresh discovery cycle that resolved most mismatches
- Decided NOT to delete any picks — all 20 at time of check were legitimate detections with correct data, warmup guard prevented false picks during restarts

**Lessons Learned:**
- Venue matching happens at catalogue ingest time — just updating the venues table race_types isn't enough without a restart/rediscovery cycle
- Betfair venue naming is inconsistent across race types at the same physical track (especially QLD greyhound tracks: Q Straight, Q2 Parklands, Q1 Lakeside are all at Albion Park)
- Betfair publishes regional greyhound/harness markets only 1-3 hours before first race — early orange status on evening cards is normal, not a bug
- The warmup guard and freshness filters successfully prevented false picks during service restarts

**Action Items:**
- Monitor Northam (G) and Pinjarra (H) later in the day to confirm they turn green when Betfair publishes markets
- Consider adding systematic venue alias discovery — compare Betfair market names against TAB names automatically to catch future mismatches
- Note: early-morning picks at low liquidity (<$500) show inflated EV (30-44%) — backtest data + liquidity filter handles this but could tighten detection window

### Session (13:36) [superwin]

**Context:** Debugging venue matching issues between Betfair and TAB APIs in the value betting scanner, then deep analysis of pick profitability data to find optimal betting filters.

**Key Exchanges:**
- Betfair/TAB venue name mismatches identified: "Globe Derby" vs "Globe Derby Park", race type misconfigurations (venues missing H/G types), and timing issues where Betfair hasn't published evening markets yet
- Betfair coverage went from 67% → 85% after fixing venue race_types and adding aliases
- Confirmed all 20 picks during the restart period were clean — warmup guard and freshness filters prevented false picks
- Comprehensive 16-dimension profitability analysis across 487 picks revealing clear signals

**Decisions Made:**
- Added "Globe Derby" as alias for "Globe Derby Park" to fix Betfair matching
- Added missing race types (H for Ararat, G for Northam, H for Pinjarra) to venue configs
- Created `edge-pick-theories.md` to track 10 theories for weekly validation as sample grows

**Lessons Learned:**
- **Harness racing = 83% of all profit** from only 21% of picks. 27% win rate, +152% ROI. Harness markets are structurally less efficient than thoroughbred/greyhound
- **Market efficiency cliff at ~$1,500 matched**: below = +62% ROI, above = -9% ROI. The sweet spot is $500-$1,000 liquidity
- **$750-$1K liquidity is the goldmine**: 26% win rate, +170% ROI, +28% CLV
- **EV sweet spot is 12-15%**: below 12% is noise, above 15% is unreliable (break even)
- **4-5 detection scans = sustained edge** (+108% ROI); single scans are noise
- **Cash Multiplier mode (TAB, 1.1x boost)** has highest win rate (24%) and best ROI (+70.3%) with lowest avg liquidity ($1,523) — finds edge in thin markets
- **SuperPicks** generates most picks but lowest CLV (+4%) — edge partly manufactured by boost, not purely market mispricing
- **Normal odds** has best CLV (+14.2%) — when it fires, the bookie is genuinely mispriced
- **Positive CLV = real edge**: +164u/+54% ROI vs +23u/+13% when CLV<=0
- **Growing liquidity = stronger signal**: money flowing in confirms the Betfair price
- Betfair publishes greyhound/harness markets 1-3 hours before first race — evening cards appear later in the day
- **Optimal filter identified**: Cash Multiplier + Harness + liq≥$500 + scans≥2 = 339% ROI (17 picks, 47% win rate — small sample, needs validation)

**Action Items:**
- Run weekly review against the 10 theories in `edge-pick-theories.md` as sample grows
- Monitor whether the $1,500 efficiency cliff and harness dominance hold with more data
- Validate the Cash Multiplier + Harness optimal filter as sample size increases beyond 17

### Memory Flush (14:20) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (14:51) [value-betting-scanner]

**Context:** Debugging VPS/mini PC scanner infrastructure, adding sharp snapshots to news picks, deploying news dashboard

**Key Exchanges:**
- VPS and mini PC scanners showed 0 picks — root cause was stale processes restarted by watchdog without `SPORT` env var, so tracker never started cycling
- Twitter news workflow confirmed working — classifier correctly blocking noise (postgame recaps, historical commentary), waiting for actionable breaking news
- User asked if sharp book odds are captured at pick time for CLV backtesting — regular picks yes, news picks no

**Decisions Made:**
- Added sharp snapshot fields to `news_agent/pick_writer.py` (lines ~229-253) so news picks now capture: `sharp_snapshot`, `sharp_books_used`, `sharp_count`, `opening_odds`, `triggered_ev` — same fields regular picks already had
- Deployed updated pick_writer to mini PC via Tailscale

**Lessons Learned:**
- Watchdog restart issue: when Windows watchdog restarts the scanner server, it doesn't pass the `SPORT` env var → tracker starts but never cycles. Must restart via scheduled task which has env vars configured
- `taskkill /im python.exe` kills ALL Python processes including the ones that were working — need more targeted restarts
- SSE startup on VPS takes ~5 min (auto-discovers 493 leagues, creates theories) — don't panic during boot

**Action Items:**
- News dashboard deployed: `http://170.64.213.223:8802/news.html` (standalone) + "News Edge" tab on main dashboard
- Tailscale to mini PC: `ssh Dell@100.67.233.95`
- Monitor that sharp snapshots actually populate on next news pick generation
- Consider fixing the watchdog restart to include env vars so `SPORT` is always passed

### Session (15:23) [value-betting-scanner]

**Context:** Fixing VPS/mini PC infrastructure and expanding value betting coverage to all basketball and baseball leagues

**Key Exchanges:**
- Deep dive into EV opportunities revealed 360 clean +EV picks across NBA (284) and MLB (76), but zero from other leagues despite having 182 active theories covering 50+ basketball/baseball leagues worldwide
- Identified that OpticOdds subscription covers all basketball and baseball leagues — massive untapped opportunity

**Decisions Made:**
- Expand SSE streams to feed all basketball/baseball leagues with full player prop data, not just moneylines
- Include ALL soft books (AU + prediction markets) in every theory, not just prediction markets for non-NBA/MLB leagues
- Use `schtasks /Run` for watchdog restarts instead of `subprocess.Popen` (preserves env vars from batch files)

**Lessons Learned:**
- **SSE market_map default is moneyline-only** — Line 781 in server/main.py defaults to `{"moneyline": "Moneyline"}` if no market_map passed. The SSE consumer at line 1224-1230 was created without one, silently dropping ALL player props for every league
- **SSE sportsbooks were too narrow** — Only 7 books (Pinnacle + prediction markets). Expanded to 13 by adding US sharps + AU soft books
- **Auto-created Pinnacle theories restricted soft_books to prediction markets only** (950, 970, 971, 980, 981, 982) — AU soft books (900-903) were excluded, meaning even with SSE data flowing, theories wouldn't evaluate AU book mispricing
- **Watchdog was broken since March 25** — disabled schtask, wrong Python path, Popen launches without env vars. Root cause of recurring server deaths

**Action Items:**
- Monitor SSE streams when European/Asian games kick off (Euroleague, KBO, NPB, Spain ACB) to confirm player props and +EV picks appear
- Verify AU soft book odds are actually flowing through SSE for non-NBA/MLB leagues (data is event-driven, need active fixtures)
- 6 frontend/backend true odds parity fixes deployed — monitor dashboard for consistency with backend calculations
- All 169 theories patched with expanded soft_books — verify Supabase updates persisted correctly

### Session (15:55) [superwin]

**Context:** Working on value betting scanner — added market depth fields to EV trails and discovered a tracking blindspot.

**Key Exchanges:**
- Walked through what `liquidity` actually means: it's `selection_matched` (total $ already traded on a runner), NOT available depth. `lay_size` is the real "can I bet this?" field.
- Trail data only exists from Apr 27 onward (deployed yesterday). 3% EV-change threshold keeps it lean — avg 3.1 entries per pick, not hundreds.
- Today's performance: 147 picks, 15 winners, **-36.8u** day. All-time still +144u (was +181u).
- Trail storage is ~43KB/day — negligible. No need to compress or delete on settlement. Keep forever for backtesting.

**Decisions Made:**
- Added `lay_sz`, `bk_sz`, `sel_m` to every trail entry for full market depth picture
- Keep trail data on pick rows permanently — 22MB/year worst case, not worth optimizing
- 3% EV-change threshold is the right balance (filters noise, captures meaningful moves)

**Lessons Learned:**
- **Blindspot discovered:** Scanner only persists picks when EV >= 10% (`min_ev` threshold). Picks that appear at 6% on TAKEOVER and grow to 14% — the journal misses the lead-up below 10%. The early-formation phase (6-9% EV) may be where edge starts forming.
- `sel_m` shows null for stream-based updates (needs `trd` field not all markets emit) — known gap
- 502 picks / 5 days is nowhere near enough to validate any theory. Need ~2,000 for overall confidence, ~200 per sub-filter for combo validation.
- Single-threaded async loop handles parallelism fine — one scan cycle captures all races, writes as one batch, no race conditions.

**Action Items:**
- Build "Option 2": track EV trail in memory from first detection at ANY EV level, but only persist to journal when pick crosses 10% — writes full history including sub-10% lead-up
- Monitor `sel_m` null issue on stream-based updates
- Continue collecting data — need 3-4 more weeks before any statistical claims hold

### Memory Flush (16:29) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (17:03) [value-betting-scanner]

**Context:** Debugging and hardening the bet365 MLB player props scraper — fixing MG expansion clicks, pitcher props tab, worker stability, and checking VPS SSE stream health.

**Key Exchanges:**
- Confirmed bet365's exact tab naming: `"Batter Props"` (capital P) vs `"Pitcher props"` (lowercase p) from live DOM inspection
- Player props on bet365 are HTTP-only (not WebSocket) — confirmed from April 15 WS probe
- Identified why MG expansion clicks were failing: `get_by_text().click()` doesn't trigger bet365's SPA event handlers on collapsed market group containers

**Decisions Made:**
- **Single worker (N_WORKERS=1)** instead of 3 — multiple workers opening pages in the shared NBA Chrome (CDP port 9223) crashes the browser context. One worker cycles all 8 games sequentially (~5 min full rotation).
- **3-tier click strategy** for MG expansion: (1) Playwright native click, (2) JS locate + Playwright mouse.click at coordinates, (3) JS MouseEvent dispatch as last resort
- **Tab-filtered MG names** — only attempt batter MGs on Batter Props tab, pitcher MGs on Pitcher props tab

**Lessons Learned:**
- bet365's SPA requires real mouse events (Playwright native or coordinate-based), not JS `.click()` on DOM elements — JS click doesn't trigger their event handlers
- Sharing a Chrome instance via CDP works fine with 1 extra page, but 3+ pages cause context crashes
- Track HTTP response count before/after each MG click to verify the click actually triggered an API call
- The `_auto_create_theories` function on VPS makes 266 sequential Supabase calls and silently kills the SSE startup pipeline — this has been the pattern every restart

**Action Items:**
- Fix VPS `_auto_create_theories` — needs batched Supabase calls, error handling, or skip if theories already exist (blocking KBO/NPB/Euroleague SSE data)
- Monitor MLB scraper output after battle-tested deploy: expect 19-20/26 batter MGs + 7-8 pitcher MGs expanded per game, 500-800 odds per game
- Verify single worker stability over multiple cycles (no more Chrome context crashes)

