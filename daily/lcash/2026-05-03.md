# Daily Log: 2026-05-03 (lcash)

## Sessions

## Memory Maintenance

### Session (08:10) [value-betting-scanner]

**Context:** Diagnosed and fixed silent data flow failure on VPS mini PC deployment; services had crashed days ago with no alerting configured.

**Key Exchanges:**
- Discovered bet365 scraper subprocess dead since April 27, push worker dead since April 30 — no one was notified because Discord webhook wasn't configured in `.env`
- After restarting services and verifying, data flow restored: 11K+ markets, all soft books (Bet365, Sportsbet, TAB, PointsBet, etc.) flowing to VPS
- User asked how to ensure mini PC files stay current and clean going forward

**Decisions Made:**
- Added Discord webhook URL to `.env` and wired login failure detection to alerts in both bet365_nba_v3.py and bet365_mlb_v3.py
- Created three-tier verification strategy: `verify_mini_pc_sync.sh` (check only), `verify_mini_pc_sync.sh --fix` (auto-cleanup), and `sync_and_verify.sh` (full deployment with verification)
- Performed full sync and deployment to mini PC, killing all stale processes, clearing `__pycache__`, and verifying data flow

**Lessons Learned:**
- Silent failures are critical gap: services can crash without any notification if alerting infrastructure (Discord webhook) isn't wired up
- Need explicit process cleanup during redeployment — stale Python processes holding old code versions can persist invisibly
- Deployment verification must be automated; manual checks miss edge cases

**Action Items:**
- Daily: Monitor health endpoint (`curl http://100.67.233.95:8800/api/v1/health`)
- Weekly: Run `./scripts/verify_mini_pc_sync.sh --fix` for cleanup
- Before any deploy: Run `./scripts/sync_and_verify.sh` for guaranteed clean state

### Session (08:29) [value-betting-scanner]

**Context:** Investigating why 0 picks were showing on NBA/MLB dashboards despite active theories and running tracker.

**Key Exchanges:**
- Found tracker is running (5,270 cycles, 100 total picks inserted historically) but 0 picks in latest cycle
- Health checks showed data flowing: 4,082 Bet365 markets, 8,272 line matches, push worker active
- NBA system appeared fully operational based on health endpoint data
- MLB server had crashed (exit code 1, OpticOdds connection error), restarted it
- **User corrected investigation**: Bet365 scrapers are actually struggling to connect/match/push odds

**Lessons Learned:**
- Health endpoint checks may have been incomplete or misleading about actual scraper connectivity
- "0 picks" root cause is likely odds not flowing from Bet365, not theories/tracking as initially assumed
- Need to check actual Bet365 scraper error logs rather than relying on aggregate health metrics

**Action Items:**
- Investigate actual Bet365 scraper connectivity issues (connection failures, matching problems, push failures)
- Check Bet365 scraper error logs directly
- User request was interrupted mid-message

### Session (08:40) [value-betting-scanner]

**Context:** Debugged MLB tracker producing 0 picks despite 3,684 markets and operational systems

**Key Exchanges:**
- NBA was healthy with 5 picks; MLB had markets but last_picks=0 despite 6 tracker cycles
- Diagnosed root cause as `max_line_gap=NULL` on `Pinnacle MLB` theory — defaults to exact-match-only, silently rejecting all markets due to bet365/sharp line mismatches (e.g., 0.5 vs 1.5)

**Decisions Made:**
- Patched `Pinnacle MLB` theory via Supabase: `max_line_gap: 4.0`
- Followed the established fix pattern from 2026-04-30 NBA incident (same root cause, different sport)

**Lessons Learned:**
- This is a recurring bug: whenever a new theory is created or sport is added, `max_line_gap` must be explicitly set (default NULL causes silent rejection of all markets)
- Canonical symptom: `tracker.last_picks=0` with `cycles>0` and `markets>0` with no errors — check theory `max_line_gap` immediately
- The validation script in the codebase correctly identifies this pattern

**Action Items:**
- Monitor MLB picks resumption after theory cache TTL (~5 min)
- When adding new theories or sports in future: always set `max_line_gap` to 4.0 unless calibration data specifies otherwise

### Session (11:32) [value-betting-scanner]

**Context:** User troubleshot Bet365 NBA scraper authentication and discovered a fundamental architectural mismatch between current system and actual requirements.

**Key Exchanges:**
- Verified Bet365 scraper is running and producing 1136 odds/cycle from 4 active sources
- Confirmed data flows from mini PC → push worker → VPS (4,166 markets with Bet365)
- Identified rediscovery schedule: every 30 minutes, or every 5 minutes if game within 60 min of tipoff
- Found that Celtics vs 76ers game (May 2, 23:30 UTC) was missed due to session expiry at 23:22 UTC (8 min before tipoff)

**Decisions Made:**
- Restarted NBA server to trigger fresh discovery after auth restoration

**Lessons Learned:**
- Auth expiry on mini PC causes games to be skipped entirely—no graceful recovery
- v3 CDP scraper approach is brittle: prone to connection loss, tab leaks, session management
- Current periodic refresh architecture (10-30s) doesn't meet "live and in sync" requirements
- Fundamental disconnect: built system is fragmented relay with stale data; user needs merged Bet365+OpticOdds live player props feeding automated pick generation

**Action Items:**
- Clarify definition of "live and in sync" (sub-second? 5s? 30s tolerance?)
- Investigate Betstamp as alternative Bet365 data source (credentials in .env suggest viability)
- Redesign data pipeline: single merge point for Bet365+OpticOdds enrichment instead of competing scrapers
- Evaluate whether v3 CDP approach is sustainable vs. API-based integration

### Session (11:56) [value-betting-scanner]

**Context:** Deployed and debugged a unified odds aggregator on mini PC that collects from multiple sport servers and pushes to VPS.

**Key Exchanges:**
- Initially aggregator showed `market_count: 0` despite sport servers running—culprit was uvicorn background task not executing as expected
- Health endpoint `pulse` counter was diagnostic breakthrough: showed polling WAS happening (pulse incremented) but no markets were being fetched
- Bet365 odds missing until NBA server restarted due to auth expiration in scraper

**Decisions Made:**
- Rewrote aggregator with explicit error logging and PowerShell for remote execution instead of troubleshooting SSH path issues
- Kept VPS `/push/odds` handler unchanged—it already supported multi-sport payloads via `mdata.get("sport") or sport` fallback pattern

**Lessons Learned:**
- Uvicorn background tasks (`@app.on_event("startup")`) can fail silently; health/monitoring endpoints are essential for debugging
- Response format compatibility matters when merging data across servers—aggregator returns list from sport servers that must match VPS expectations
- Mini PC as single aggregation point is cleaner than separate pushes for each sport

**Action Items:**
- Monitor aggregator stability (verify it stays at 1677+ markets over time)
- Confirm Bet365 scraper auth renewal process to prevent recurrence

### Session (12:12) [value-betting-scanner]

**Context:** Diagnosed and fixed MLB picks not appearing on dashboard despite active pick generation in Supabase.

**Key Exchanges:**
- User reported "MLB unfortunately showing nothing" on dashboard despite earlier diagnosis confirming 6,249 MLB markets and recent picks in Supabase
- Root cause identified: `dashboard/data.json` was 25 days stale (last updated April 8); VPS had recently restarted and background tasks weren't running

**Decisions Made:**
- Added `_dashboard_update_loop()` background task to VPS that queries Supabase picks every 30s and writes to `dashboard/data.json`
- Simplified approach by having VPS write dashboard data directly (rather than mini PC) since VPS already has the picks in memory
- Restarted VPS service to deploy the fix

**Lessons Learned:**
- VPS restarts don't automatically restart background tasks — they need explicit startup logic
- Dashboard staleness was a pipeline architecture gap: no task was continuously syncing live picks to the UI data file
- Aggregator architecture is sound (mini PC → 4 sport servers → VPS → Supabase → dashboard), but requires explicit background task to surface picks to UI

**Action Items:**
- Monitor dashboard update task to verify picks populate in UI within 30s cycles
- Verify `_dashboard_update_loop()` is successfully running post-deployment

### Session (16:47) [value-betting-scanner]

**Context:** Debugging dashboard display issue where MLB pill shows no picks despite 877+ picks in Supabase with valid EV data (1-3%).

**Key Exchanges:**
- Dashboard updater was failing silently due to missing `theory_name` column in Supabase query
- Fixed query and reformatted output to use `ev_picks` key that dashboard expects
- Confirmed picks ARE in Supabase with realistic EV values, but dashboard not rendering them when filtered by bet365
- Dashboard has two loading paths: stored picks from Supabase OR computed picks from matched markets

**Decisions Made:**
- Changed dashboard writer to output `ev_picks` key format instead of raw picks structure
- Restarted VPS service after fixing the query column issue
- Narrowed problem from data generation to dashboard display/filtering layer

**Lessons Learned:**
- The EV values appearing as 990%+ in data.json were display artifacts; actual Supabase picks have correct 1-3% EV
- Sport filtering is broken (NBA picks appearing in MLB query suggests mixed data in picks table)
- Dashboard needs either matched market data (soft+sharp book pairs) OR direct Supabase querying; current architecture missing one of these
- Pushworker (bet365 scraper) status needs verification to confirm bet365 odds are flowing to mini PC

**Action Items:**
- Determine which data delivery approach to use: debug dashboard JS loading, push additional market data from VPS, or have browser query Supabase directly
- Verify bet365 scraper is running and odds are captured in tracked picks
- Investigate why sport filtering allows cross-sport picks in results

### Memory Flush (17:33) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

