# Daily Log: 2026-05-28 (lcash)

## Sessions

## Memory Maintenance

### Session (00:03) [superwin]

**Context:** Shipped pick-level lineage instrumentation to the SuperWin edge scanner to track system health metadata per pick.

**Key Exchanges:**
- Added 5 lineage columns to `superwin_edge_picks`: `scanner_uptime_secs`, `bookie_fresh_secs`, `betfair_fresh_secs`, `bookie_status_at_detect`, `service_restarts_30m_at_detect`
- User correctly called out that the initial ROI comparison query was flawed — NULL historical picks were mis-classified as "unstable" due to a bad CASE WHEN. 18,620 historical picks have NULL lineage and can't be backfilled.
- User also flagged that lineage is forward-only and doesn't help with existing data analysis — accepted this limitation.

**Decisions Made:**
- Lineage captures per-bookie state (not just system-wide) — `bookie_status_at_detect` and `bookie_fresh_secs` are specific to the bookie on each pick
- Schema columns are nullable so historical picks coexist without issues
- `sys_ctx` capture wrapped in try/except so lineage failures never break pick persistence
- Dashboard doesn't surface lineage fields yet — they're query-only in Supabase for now

**Lessons Learned:**
- First 30s after deploy, `bookie_status_at_detect` was NULL because `_state.get_bookie_status()` hadn't populated yet — harmless but visible in data
- One pick showed `betfair_fresh_secs=50s` while `tab` was in `fetching` state — possible discrepancy between the freshness gate (per-runner `BookieOdds.updated_at`) and lineage measurement (adapter-wide `last_fetch_at`). Worth investigating.
- Forward-only instrumentation is useful but doesn't answer "was past performance affected by instability" — historical signals like `ev_trail`, `detection_count`, `peak_ev_pct`, `minutes_to_jump` are the tools for retrospective analysis

**Action Items:**
- Once picks settle (tomorrow), run stable vs unstable ROI comparison query
- Investigate the `betfair_fresh_secs=50s` outlier — gate vs lineage measurement discrepancy
- Consider adding lineage fields to dashboard Picks tab (grey footer line per pick card)
- System hit 4h10m uptime (best run of the day), NRestarts=0 across all services

### Memory Flush (00:58) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (00:59) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (01:30) [superwin]

**Context:** Overnight monitoring of SuperWin VPS via SSH crash watcher

**Key Exchanges:**
- SuperWin crash watcher experienced repeated SSH drops (~every 16 min) overnight but auto-reconnected each time
- NRestarts stayed at 0 throughout — no actual service crashes, only SSH connection drops

**Lessons Learned:**
- SSH drops occurring at ~16 min intervals suggest a firewall/idle timeout on the network path to VPS
- The resilient watcher's auto-reconnect design proved its value — handled all drops without intervention
- Discord watcher (running server-side) is immune to local SSH issues and remains the authoritative crash monitor

**Action Items:**
- Consider investigating the ~16 min SSH timeout (likely `ServerAliveInterval` / `ClientAliveInterval` or network firewall idle timeout) to reduce unnecessary reconnect churn

### Session (02:02) [superwin]

**Context:** Overnight SuperWin crash watcher monitoring via SSH

**Lessons Learned:**
- SSH connection to VPS drops consistently every ~16 minutes overnight — likely a firewall/idle-timeout issue on the network path. The resilient auto-reconnect wrapper handles it gracefully, but the root cause (idle timeout) should be investigated (e.g., `ServerAliveInterval` in SSH config).

**Key Exchanges:**
- ~10+ SSH drops observed between 00:19–02:02, all auto-reconnected successfully. NRestarts stayed at 0 throughout — SuperWin services remained stable; only the monitoring SSH tunnel was affected.
- Discord watcher (running server-side) is the authoritative crash monitor, unaffected by local SSH instability.

**Action Items:**
- Consider adding `ServerAliveInterval 60` / `ServerAliveCountMax 3` to SSH config for the VPS to prevent idle-timeout drops

### Session (02:34) [superwin]

**Context:** SuperWin crash watcher running overnight with resilient SSH reconnection

**Lessons Learned:**
- SuperWin crash watcher SSH connection drops every ~16 minutes consistently, always reconnects successfully with NRestarts=0 — the drops are network-level (likely SSH keepalive timeout), not application crashes
- The resilient reconnect loop works as designed but generates excessive noise in the conversation with zero actionable signal
- Discord watcher on the server is the authoritative crash-detection source, making the chat-based SSH monitor redundant for overnight monitoring

**Action Items:**
- Consider stopping the chat Monitor for SuperWin crash watcher — it's noise with no signal value beyond confirming the reconnect loop works
- Investigate the ~16-min SSH drop pattern (likely a NAT/keepalive timeout that could be fixed with `ServerAliveInterval` tuning)

### Memory Flush (03:07) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (03:38) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (04:10) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (04:49) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (05:21) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (05:53) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (06:24) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (07:06) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (07:51) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (08:31) [value-betting-scanner]

**Context:** Diagnosing and documenting bet365 NBA alt-line `is_main` flag accumulation bug in the v3 value-betting scanner.

**Key Exchanges:**
- Deep root-cause analysis of why bet365 NBA Points markets show wrong main lines — traced through three interacting code paths: `_tag_main_lines()` median tagging, state.py's "main can't be demoted" guard (line 393), and `_pick_main()` tiebreaker by `captured_at`
- User requested bet365 raw data format documented as JSON examples at three layers: raw pipe-delimited wire format → parsed `ScrapedOdds` → merged state store output
- Live wizard fetch on OKC@SA confirmed bet365 publishes exactly 3 lines per (player, side) — alt-low / main / alt-high triplet — and the median IS the correct centre line within a single refresh

**Decisions Made:**
- Will wait for an active NBA game to implement the fix (user's preference)
- Confirmed the fix needs TWO changes, not one: (A) sticky-main for per-refresh tagging stabilisation, AND (B) state.py must allow `is_main=True → False` demotion when current refresh disagrees — without (B), historical mis-flagged alts persist indefinitely

**Lessons Learned:**
- bet365's `is_main` bug is caused by three independently-correct-looking pieces of code interacting badly: per-refresh median tagging + no-demotion guard + stale line accumulation with no per-line eviction
- `_pick_main()` resolves multiple `is_main=True` entries via `max(captured_at)` — effectively random, which is why the displayed "main" jumps between alt-low and alt-high
- VPS proxy strips `alts` on serialization (display gap only, engine runs on Eve and sees alts)
- bet365 publishes 26 distinct EV.NA groups for NBA; 3 player prop groups are silently dropped (Quarter Props, Quarter Milestones, Player First) — no conflation risk
- No NBA prop_type duplication exists; `EV_NAME_MAP` has single entry for Points

**Action Items:**
- Fix `_tag_main_lines()` with sticky-main logic (~20 LOC)
- Fix state.py line 393 guard to allow main→alt demotion when current refresh disagrees
- Consider adding per-line eviction in state.py (currently only market-level `cleanup_stale_markets` exists)
- Revisit VPS proxy alt-stripping (cosmetic — dashboard shows only the broken picked main)

### Session (09:02) [value-betting-scanner]

**Context:** Extending Polymarket Gamma scraper with MLB game-level markets (Run Line + Total Runs) and auditing full scraper fleet health.

**Key Exchanges:**
- Added MLB Run Line and Total Runs parsers to Polymarket Gamma scraper, tripling MLB odds from 308→902
- Discovered and fixed multi-game collapse bug (same matchup across different dates pooling into one market_key) — solved with 48h future-cap on events
- Added 30¢ max-spread filter to skip illiquid Polymarket markets where devig would be noisy
- Investigated a Gamma=5.556 vs other=1.61 deviation — turned out to be stale store data from prior deploy, not a scraper bug
- Confirmed NBA player props (Points/Rebounds/Assists, ~60 markets) already flowing via Gamma for Finals
- Combo props (PRA, P+R, etc.) are wired in scraper but Polymarket doesn't list them — zero combo markets available
- Full scraper audit: 7 direct scrapers + ~30 OpticOdds-relayed books, all healthy

**Decisions Made:**
- 48h future-cap chosen over other approaches to prevent multi-game collapse — simple, effective, no false negatives for daily betting
- 30¢ max-spread threshold (p75 of spread distribution) to filter illiquid markets
- Total Runs uses clean `"Team A vs Team B"` player_name convention despite existing buggy `"Over N.N"` entries in store
- MLB game-level parsers gated to `self.sport == "mlb"` only — NBA orchestrator untouched
- Decided against combo-prop logging alert (user said not needed)

**Lessons Learned:**
- Polymarket `startDate` is market creation date, not game date — must use `startTime`/`endDate` for game-time filtering
- Multi-game collapse is a real risk when the same team matchup occurs across multiple dates (e.g., series games) — future-cap prevents it
- Store staleness after deploy can create phantom deviations — always check `updated_at` timestamps before investigating "bad" odds
- Polymarket combo prop coverage is zero even during NBA Finals — prediction markets stick to single-stat props

**Action Items:**
- 5 commits ahead of origin/main, not yet pushed (user hasn't confirmed push)
- Bet365 MLB diff-key collision warning worth follow-up (duplicate market_keys with different odds in same batch)
- MLB WS periodic re-subscribe occasionally fails after 12 retries — monitor for persistence
- NRL/AFL scrapers exist but aren't enabled on Eve (`ENABLED_SPORTS=nba,mlb`) — flip when ready
- Total Runs join rate is 0% due to existing encoding bug in store — will self-resolve once upstream encoding is fixed

### Memory Flush (09:39) [server]

FLUSH_OK - Nothing worth saving from this session

### Session (10:12) [value-betting-scanner]

**Context:** Multi-phase rollout of bet365 alt-line support in the value-betting scanner (Phases 1–5)

**Key Exchanges:**
- Phases 1–3 shipped with 133 tests passing, no regressions. Phase 4 confirmed pipeline is already alt-aware.
- Phase 5 (tracker persistence) revealed two parallel pick-emitting paths: `v3.core.engine.DevigEngine` (dashboard, read-only) and `server/tracker.py:compute_ev_picks` (Supabase persistence). Only the latter matters for shipping alt picks to production.
- User correctly flagged that assistant was jumping to conclusions about Phase 5 options (A/B/C) without understanding the full system architecture.

**Decisions Made:**
- `is_alt` dropped from `ScrapedOdds` writes — `is_main=False` denotes alt, matching the field's docstring. `EVPick.is_alt` semantics untouched. Documented in `test_alts_tagged_not_main`.
- Phase 5 approach: in-place refactor of `compute_ev_picks` to iterate `[soft_centre] + alts[]`, not a v3 engine swap. Line dimension added to `composite_key` for dedup, market_key kept line-less to preserve pair-mirror contract.

**Lessons Learned:**
- **`DataStore` extends `AppState`** (`v3/pipeline/store.py:29`) — Phase 2's state guard IS live in production via inheritance. Earlier assumption that `server/state.py` might not affect prod was wrong.
- **Production runs `python -m v3.startup`** on Eve (Linux VPS), not `server/main.py`. Single process: scrapers → DataStore → Tracker → `compute_ev_picks` → Supabase.
- **v3 engine ≠ production persistence path.** The `/v1/picks` endpoint uses DevigEngine but is read-only (dashboard). `compute_ev_picks` is the sole write path. "Unify codepaths" (option C) was misframed — v3 engine lacks ~15 production features (prop_filter, exclude_sharp_books, cross-validation rejection, one-sided fallback, etc.). True unification is a 2-3 session refactor.
- **Trace data flow before recommending architecture changes.** Pattern-matching on file names led to a false dichotomy (engine swap vs in-place) that wasted a round-trip.

**Action Items:**
- Execute Phase 5: make `compute_ev_picks` alt-aware in-place (iterate `[soft_centre] + alts[]`, pair-side lookup at same line, line-aware composite_key)
- Future: consider full v3 engine / compute_ev_picks unification as a separate multi-session project

### Session (10:47) [value-betting-scanner]

**Context:** Mass re-resolution of ~47K MLB player prop picks that were incorrectly graded due to an adjacent-day stats bug active before the 5-25 deploy fix.

**Key Exchanges:**
- Initial M8 audit flagged 120 disagreements → traced to picks resolved during a deploy gap where Eve had the old (buggy) resolver code
- Blast radius expanded from 5,914 picks (7-day window) → 47,269 picks (everything resolved before the 5-25 16:01 deploy)
- Dry-run → execute → audit loop ran multiple times as scope and bugs were discovered

**Decisions Made:**
- Widened re-resolve window from 5-21 onward to "all picks resolved before 5-25 16:01 deploy" after finding M8 disagreements outside the narrow window (e.g., Feduccia resolved 5-20)
- Adopted backup-first approach: full pre-fix snapshot saved to `/tmp/affected_picks.json` before any writes
- After the v1 bug, switched to "dry-run + explicit approval" before executing any further corrections

**Lessons Learned:**
- **Critical: always include `game_start` when querying picks for re-resolution.** The `et_from()` function falls back to `game_date` (UTC) when `game_start` is missing, which produces wrong ET dates for late-night games — this caused v1 to write WRONG values for the night-game cohort (~385 picks made worse)
- **SQL without `ORDER BY` + `LIMIT` is sampling, not scoping.** The initial 5000-row wider audit showed 0 disagreements because the unordered sample missed the bad rows entirely
- **Idempotent scripts save you.** The re-resolve script could be safely re-run because it compares stored vs computed and only patches differences
- **Don't rush multi-thousand-row production patches.** V1 executed too fast, broke night-game picks, required a v2 correction pass

**Action Items:**
- Execute the v2 corrected dry-run (2,925 changes) — pending user approval
- Optionally verify the void→graded cohort (177 picks, e.g., Soroka/Castillo/Weathers on 3-30) via MLB Stats API before executing
- Re-run M8 audit after v2 execution to confirm disagreement rate drops to near-zero
- Consider adding `game_start` as a required field in any future bulk pick queries to prevent the ET fallback bug

### Session (11:47) [value-betting-scanner]

**Context:** Continuing bet365 alt-lines pipeline (Phase 5+), discovered sharp-side alt-line data is being ignored in the tracker's devig calculations.

**Key Exchanges:**
- User asked for a JSON walkthrough of how Over/Under lines link across bet365 alts — assistant traced the full compute_ev_picks pipeline with worked examples showing 6 picks (3 lines × 2 sides) with correct per-line pairing
- User caught a critical design gap: sharps are locked to centre-only reads while bet365 emits alts at multiple lines. User's intent was always closest-line matching (e.g., soft@25 pairs to sharp@24, soft@30 pairs to sharp@30 exactly)
- Assistant confirmed the gap by tracing four sharp-read sites in `server/tracker.py` that all use `_pick_main` → centre only, ignoring sharp alts already stored in state

**Decisions Made:**
- **Sharp alt-line matching needed:** Port `v3/core/engine.py:_find_best_entry` semantics (exact → main → closest) into `server/tracker.py`'s four sharp-read call-sites: `_compute_true_prob:620`, `_build_sharp_snapshot:343`, `_compute_one_sided_true_prob:741`, and cross-validation block at `~1124`
- **Keep BET365_ALTS_ENABLED=false** until Phase 7 (theory line_tier_filter) ships — without it, theories fire on all 3 tiers producing noisy one_sided_fallback picks at ~-20% ROI

**Lessons Learned:**
- BetRivers (#1 Brier 0.2426) and Hard Rock (#2 Brier 0.2429) are alt-line specialists — 50-67% trail coverage but only 4-9% pick-time contribution because tracker reads centres only. Fixing sharp alt reads would unlock their full signal
- Brain finding `2026-05-04-sharp-clv-bias-alt-lines.md` already documented negative sharp CLV on alt-line picks caused by interpolation noise — root cause is forcing centre→target when the sharp has a closer alt available
- v3 engine (`_find_best_entry`) already implements the correct pattern; it just needs to be ported to the persistence-path tracker

**Action Items:**
- Build `_find_best_sharp_pair` helper and swap into 4 call-sites in `server/tracker.py`
- Ensure byte-equivalent output when sharps have no alts (graceful degradation)
- Phase 7 still blocking: schema migration adding `line_tier_filter` on `nba_optimization_runs` + tagging existing theories with `["centre"]`

### Memory Flush (12:27) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (12:31) [superwin]

**Context:** Multi-bookie value-betting scanner reliability and freshness improvements on VPS

**Key Exchanges:**
- Built TabTouch FOB Phase 2: wired `FobPriceChanged` MQTT events → runner odds via proposition map, achieving sub-2s FRESH (was 0-60s)
- Investigated all bookies' FRESH cadences to identify which could be improved to sub-2s
- Attempted neds WebSocket build using previously documented Socket.io protocol — connected successfully but received zero frames despite active race subscriptions

**Decisions Made:**
- Kept neds WS module (`neds_stream.py`) as reference but did NOT wire it into production — protocol notes from prior recon did not work as documented
- Decided betr/boostbet are REST-ceiling bookies (no WS endpoints discovered) — further improvement requires new recon
- Stopped neds WS work rather than shipping non-working code; next step requires live browser DevTools recon to capture actual WS frames

**Lessons Learned:**
- **Neds WS protocol docs were inaccurate**: POC connected + subscribed with documented events but got 0 frames over 30s. Prior "verified empirically" claims didn't hold — may need auth, different event names, or different handshake sequence
- TabTouch FOB proposition map pattern (REST builds `_proposition_map`, stream routes `FobPriceChanged` via callback) is a reusable pattern for any bookie with separate REST + streaming odds
- After FOB Phase 2, TabTouch FRESH went from 0-60s bouncing → 0-1s sustained within ~60s of warmup

**Action Items:**
- Neds WS recon needed via attached browser DevTools (openbrowser or live Chrome) to capture actual frame protocol — estimated 30-60 min focused session
- bet365 Eve push still down — needs Eve-side investigation (outside VPS scope)
- Tab still has intermittent 13s spikes during REST discovery cycle — potential future optimization

### Session (12:59) [value-betting-scanner]

**Context:** Mass re-resolution of MLB player prop picks to fix Eve scraper's adjacent-day stat bug across April-May 2026

**Key Exchanges:**
- Initial dry-run on narrow window (5/21+) found 393 corrections needed; executed successfully
- M8 audit still showed 20% disagreement → realized window needed to go back to ALL picks resolved before the 5/25 16:01 deploy (when Eve's bug was fixed)
- Wider scope (47,269 picks) found 2,680 corrections; executed successfully
- Post-execution audit still showed 10% disagreement → discovered a **bug in the fix script itself**: the SELECT didn't include `game_start`, so `et_from()` fell back to `game_date` (UTC), causing night-game picks to look up wrong day's stats
- Had to re-fetch all 47,269 picks with `game_start` from Supabase and re-run

**Decisions Made:**
- Widened re-resolve window from "5/21 onwards" to "everything resolved before 5/25 16:01 deploy" — the Eve bug existed since forever, not just recent days
- Added manual verification checkpoints after the v1 mistake — sampled void→graded and W↔L flips against MLB Stats API before executing v2
- Kept backups at each stage in `/tmp/` for rollback capability

**Lessons Learned:**
- **Always include `game_start` in pick queries when ET date matters.** UTC `game_date` diverges from ET date for night games (7pm+ ET). This is the same class of bug Eve had — adjacent-day lookups
- **Dry-run → sample verify → execute pattern is critical for bulk DB mutations.** The v1 rush broke ~half of night-game picks by writing new wrong values
- **Idempotent scripts save you** — v2 could overwrite v1's bad writes because the script compares against MLB truth, not against "what was there before"
- The `et_from()` fallback behavior (silently using `game_date` when `game_start` is missing) is a footgun — should probably raise/warn instead of silently degrading

**Action Items:**
- Execute the verified v2 correction (2,925 picks) and re-run M8 audit to confirm disagreement drops near zero
- Consider adding a guard/warning in `et_from()` when it falls back to `game_date`
- Final audit numbers need to be recorded once v2 completes

### Session (13:36) [value-betting-scanner]

Below is a summary of the conversation — which consists of a single deep healthcheck sweep with Gamma (Polymarket) focus. I'll evaluate whether anything is worth logging.

The conversation is essentially one healthcheck report with no back-and-forth discussion, no decisions being debated, no code changes, and no action items explicitly called out. However, there are a few noteworthy observations:

---

**Context:** Ran a full v3 value-betting pipeline healthcheck with deep focus on the Polymarket Gamma scraper integration.

**Key Exchanges:**
- Full pipeline healthcheck returned **GREEN** overall with two YELLOW observability flags
- Gamma scraper confirmed alive end-to-end: 236 markets, NBA 60 / MLB 176, env vars active, refresh logs current

**Lessons Learned:**
- Gamma per-market freshness p90 can look alarming (4+ hours) but is expected behavior — Polymarket uses diff-only push, and thinly-traded player-prop alt-lines simply don't move often. Not a scraper death signal.
- The 3c engine throttle patch (deployed earlier today) is working: 8 pair-vig warnings in 4h, properly throttled at 60s/book, now enriched with market_key + line for diagnosis
- Alt-line Run Line markets (`-3.0, -2.5, -4.5, -1.5`) are still triggering pair-vig warnings due to multi-game-collapse — a pre-existing issue now made visible by the new diagnostic log format

**Action Items:**
- 🟡 Consider addressing the alt-line multi-game collapse root cause — Run Line markets pairing across collapsed game-dates. Not urgent but now visible thanks to enriched logging.

### Session (14:11) [value-betting-scanner]

**Context:** Deploying and auditing the sharp-alt fix for the v3 value-betting pipeline, discovering and fixing three critical data-poisoning bugs introduced by the Phase 5 sharp-alt code.

**Key Exchanges:**
- User corrected a misconception about matching logic — soft-to-sharp matching IS fully dynamic (high-to-high, low-to-low), not centre-bound. Proved with running code across 3 test scenarios.
- User requested full data-flow audit after deploy, which uncovered 3 critical bugs all introduced by the sharp-alt fix that shipped earlier the same day.

**Decisions Made:**
- Fix all three bugs before flipping `BET365_ALTS_ENABLED` — C1 was actively poisoning one-sided/MLB-fallback picks for ~6 hours.
- `pair_line_tolerance` tightened from 2.0 → 0.001 (C3 fix). The old 2-point tolerance was originally defensive against write races but is now unnecessary with Phase 2's per-line storage.
- C2 test adjusted: falling back to centre pair when alts aren't available on both sides is correct (matches main devig path behaviour). Initial "no pair at target" assertion was wrong.

**Lessons Learned:**
- **C1 — consensus_line poisoning:** `_compute_one_sided_true_prob` read the centre line via `books.get()` even when `_find_best_single_sharp_entry` had selected an alt. Produced ~25 percentage points of spurious EV per affected pick.
- **C3 — cross-line synthetic vig:** `pair_line_tolerance=2.0` allowed pairing Over@24.5 with Under@25.5 from the same sharp — a synthetic pair that no book actually quotes. Only surfaced once sharp alts were live.
- **C2 — extra_sharp_data alt-unaware:** The JSONB column recorded centre-only sharp data while main devig used alts — would silently corrupt Phase 7 calibration data.
- **Sharp-alt fix was NOT flag-gated (audit item N):** The sharp-alt math changes shipped to all sports/theories immediately, while only the bet365 alt *parsing* was behind the flag. This meant the three bugs were live from the moment of deploy.

**Action Items:**
- Deploy fixed `tracker.py` to Eve (fixes are local, not yet on VPS)
- Run `/VB-V3-Healthcheck` post-deploy to confirm no regressions
- Decide whether to flag picks from the ~6-hour dirty window (C1 poisoned one-sided path) for exclusion in future analytics
- Deferred audit items still open: H2 (WS frame mutation skips `_classify_lines`), H3 (Phase B drops alt picks if alts disappear), E (pair-side staleness), N (sharp-alt not flag-gated)
- After clean deploy: flip `BET365_ALTS_ENABLED=true` to activate alt parsing

### Session (14:50) [server]

**Context:** Shipping WebSocket streaming for Neds bookie and stress-testing Betr/BoostBet polling limits to achieve 1-second odds freshness

**Key Exchanges:**
- Neds WS streaming went live after a two-fix breakthrough: event name is `"subscribe"` (not `"subscription"`) and price frames are double-nested `args[0].data.data`
- Real-browser CDP recon confirmed Betr and BoostBet have **zero** WebSocket connections — REST polling is their only path
- Betr rate ceiling discovered: **~5 req/s per IP** before 429s burst (tested with production proxy). At 6 req/s, 10×429 within 1.6s
- Betr's speed comes from sticky proxy binding (`event_id % pool_size`) spreading load + Semaphore(3) concurrency cap + persistent `__cf_bm` cookies (25min reuse)
- BoostBet's speed comes from batch endpoint (`POST /event-races-by-ids`, 20 ids per request) — 200 races = 10 POSTs

**Decisions Made:**
- Shipped `HOT_WINDOW` tightened to 15min (from 60min) and BoostBet `poll_interval` dropped from 3s → 1s
- Accepted 1-3s freshness floor as the practical limit without adding reliability risk
- Deferred Betr Semaphore(3→5) and BoostBet split-batch optimizations — only revisit if specific missed picks demonstrate the lag cost

**Lessons Learned:**
- Betr's 5 req/s per-IP ceiling means Semaphore(3) is the sweet spot — Semaphore(5) would eat into headroom
- BoostBet's bottleneck is POST latency (0.7-2.5s), not our polling — can't parallelize a single POST
- Single 429 in 5-min soak indicates we're at healthy operating point; pushing harder hits diminishing returns
- Neds runs a sharper book than competitors — streaming delivers fresh odds but +EV picks remain rare (market conditions, not tech)

**Action Items:**
- Let current config soak overnight, monitor 429 counts
- Revisit Betr Semaphore bump (3→5) and BoostBet split-batch only if missed picks are traced to 1-2s lag

### Session (15:38) [value-betting-scanner]

**Context:** Data poisoning audit + fix rollout for the v3 value-betting pipeline, followed by scraper coverage gap analysis across AU soft books.

**Key Exchanges:**
- Executed "Option C" for the data poisoning backfill — surgically re-stamped 7 of 8 affected picks using corrected devig, rather than blind-restamping all 221 picks in the audit window
- Wembanyama pick was the cleanest evidence the C3 fix matters: flipped from +2.98% → -11.83% EV (should never have been tracked)
- Caruso pick flagged as `audit_no_valid_sharp_pair` — fixed code returned None on same trail data, meaning ALL its sharps were cross-line synthetic pre-fix
- Harris/Acuna/Duran "anytime → 3+ hits" picks confirmed as extrapolation noise both ways (phantom-EV pattern)
- Full scraper survey: 7/8 direct scrapers green, BlackStream dead since at least 2026-04-13
- MLB has far thinner direct AU coverage than NBA (effectively bet365 + TAB + Polymarket Gamma only)
- Bet365 covers 14 prop types / 2,062 MLB markets that no other direct scraper captures

**Decisions Made:**
- Consolidated all findings into `brain/findings/2026-05-28-data-poisoning-audit-fixes.md` as canonical reference with "Current state (read this first)" header
- Added three new canaries to `/VB-V3-Healthcheck`: alt-rollout deployment check (4d), C3 sharp-alt fix canary (4e), one-sided >100% EV phantom-EV visibility check
- Caruso pick left at original EV but flagged for exclusion — too risky to restamp when fixed code can't reproduce a valid sharp pair

**Lessons Learned:**
- `pick_filter_reason LIKE 'audit_%'` is the canonical query pattern for excluding all audit-window restamped/flagged picks
- Bet365 has 9 MLB monopoly prop types (no AU competition) including 7 "CO" milestone variants — devig accuracy on these has no cross-book validation
- Sportsbet and PointsBet are the two biggest direct-scraper blind spots — both have MLB props but we only get them via OO relay with lag
- Tier 1 opportunity (props other AU books offer but we lack direct scrapers) vs Tier 2 (bet365 monopoly props needing devig accuracy focus) is a useful prioritisation frame

**Action Items:**
- Verify devig accuracy on the 9 bet365-monopoly MLB props (no cross-AU validation exists)
- Build Sportsbet direct scraper (highest-volume single addition, ~200 extra MLB markets/cycle)
- Build PointsBet direct scraper (second priority, ~400 markets, note Fanatics acquisition may need recon refresh)
- Optionally extend Neds/Betr/BetIT to MLB if their APIs expose it
- BlackStream/Surge (960) remains dead — decide whether to decommission or rewire into v3

### Session (16:09) [value-betting-scanner]

**Context:** Continued bet365 alt-line rollout; pivoted to reverse-engineering AU soft-book scrapers via AdsPower browser recon.

**Key Exchanges:**
- Completed full bet365 alt-line implementation (Phases 0-8): parser, state, engine, tracker, dashboard changes across ~15 files with 94 tests
- Data poisoning audit uncovered 3 critical bugs (C1: consensus_line reading wrong source, C2: extra_sharp_data cross-line contamination, C3: pair-line tolerance 2.0 allowing synthetic cross-line pairs)
- All 3 bugs fixed, deployed to Eve, 7 production picks restamped via `scripts/restamp_audit_2026_05_28.py`
- Surveyed all soft-book scrapers outside OpticOdds — identified bet365-monopoly MLB props (9) and OO-only AU props (5)
- Produced per-scraper extension map with priority order: Betr MLB → TAB MLB → TabTouch MLB → Neds MLB → Sportsbet greenfield → PointsBet greenfield
- Started AdsPower browser session (profile `k19yb91n`) with all 7 AU bookies loaded for network capture recon

**Decisions Made:**
- Tightened pair-line tolerance from 2.0 to 0.001 (C3 fix) — prevents cross-line synthetic sharp pairs
- Chose restamp over delete for dirty-window picks — preserves bet history while correcting EV/edge values
- Alt-line deployment is env-flag gated (`BET365_ALTS_ENABLED`) — no theory opt-in needed
- User wants to personally reverse-engineer AU scrapers to expose more markets (particularly MLB props)

**Lessons Learned:**
- "Seems like you are jumping to conclusions when you should take time to understand the larger system" — trace production entry points (startup.py → DataStore → tracker) before editing
- ScrapedOdds has no `is_alt` field; only EVPick does — use `is_main=False` for parser-side alt marking
- Supabase stores UTC; convert AEST before querying dirty windows
- Scripts must be inside repo (not /tmp) to be visible in transcript
- Sharp alt-line specialists (HR/BR) had 67%/52% trail coverage but only 8.5%/3.7% pick contribution — root cause was centre-only matching

**Action Items:**
- Reverse-engineer AU bookie APIs via CDP network capture (Betr G-codes, TabTouch sport paths, Neds category IDs, BetIT marketGroups, TAB missing props)
- Sportsbet and PointsBet greenfield scrapers still needed
- Monitor alt-line rollout in production for calibration data
- `extra_sharp_data` JSONB column seeding for forward calibration analysis

### Session (16:56) [superwin]

**Context:** Full SuperWin value-betting scanner health check and venue linking on production VPS

**Key Exchanges:**
- Ran comprehensive diagnosis of all 9 bookies — bet365 still dead (Eve zombie process), TAB browser flapping with 122s REST timeouts, betr warm-cycle still hitting 30s spikes
- 680 picks generated across edges today; 3 silent edges explained (bet365 down, neds too sharp for mug formula)
- 467 stuck picks across 41 races — settlement scanner missing those markets (pre-existing issue)
- Identified and resolved 4 unmatched "Moe" venue variants across betfair/tab/tabtouch/sportsbet

**Decisions Made:**
- Created canonical "Moe" venue (Latrobe Valley, VIC, race_types=[R]) in Supabase rather than trying to alias to another venue — it's a legitimate standalone racecourse
- Step-2 duplicate venue scan produces false positives when grouping by (race_type, race_number, date) alone — different venues sharing the same race number on the same day get flagged incorrectly

**Lessons Learned:**
- The `/link-venues` Step-2 duplicate scan logic is too aggressive — it flags any two venues sharing `(race_type, race_number, date)` which catches legitimate different races at different venues. Needs a smarter heuristic (e.g., check if runners overlap, not just race metadata)
- Venues that aren't in the canonical table silently create standalone races per bookie instead of merging — no error is raised, they just miss Betfair lay prices and EV calculation

**Action Items:**
- Restart bet365 Eve tmux session — process running 6 days, only 0:18 CPU, ingest endpoint timing out
- Clean up 21 stale `/tmp/playwright_*` dirs on VPS
- Investigate sports worker `snapshot POST error:` with empty error body (occurred twice, may self-resolve)
- Monitor TAB browser memory growth — REST timeouts every ~3 min compensated by stream layer
- Track the 467 stuck picks / settlement scanner gap (pre-existing)

### Session (17:45) [value-betting-scanner]

**Context:** Building AU sportsbook scraper adapters for v3 value-betting scanner — endpoint recon, raw payload capture, per-bookie normalizers, and canonical matching format.

**Key Exchanges:**
- Confirmed 5 AU bookies (PointsBet, TAB, Neds, TabTouch, BetIT) collectively match or exceed bet365's NBA prop coverage — only 1Q Assists and 1Q Rebounds missing
- PointsBet's real endpoint is `/api/mes/v3/events/{event_id}` (2.9 MB, 978 outcomes) not the league feed (80 KB)
- BetIT silently migrated from 123bet to bmapollo platform — current live scraper is broken/stale (42 markets vs 523 possible)
- Built 5 standalone adapter modules (pure parsers: raw JSON → ScrapedOdds) with fixtures and 7 passing tests
- Live health audit: 4 scrapers running on Eve with healthy discovery, but new adapters are local-only

**Decisions Made:**
- Each bookie gets isolated `adapter.py` (pure parser) + `scraper.py` (orchestrator) architecture — easier to swap when platforms change (validated by BetIT migration)
- Player name reconciliation needed — BetIT uses "D. Vassell" abbreviated format vs full names elsewhere
- TAB scraper needed 19 new combo threshold spec IDs added (Points Rebounds, Points Assists, Rebounds Assists ladders, 45+ Points)

**Lessons Learned:**
- BetIT platform migration (123bet → bmapollo) happened silently — scraper kept running but returning minimal data. Need alerting on market-count drops
- PointsBet direct scraper doesn't exist yet — entirely reliant on OpticOdds relay, single point of failure
- 381 unique player+prop+side keys across union, 135 covered by ≥3 bookies (strong devig signal threshold)
- DD/TD props have different outcome shapes — eventClass carries the market type, outcome name is just the player

**Action Items:**
- Write 3 missing orchestrators: BetIT (rewrite to bmapollo, ~80 lines curl_cffi), PointsBet (new, ~150 lines httpx), TAB (wire-compatible, just deploy)
- Deploy updated scrapers to Eve VPS
- Wire each scraper's orchestrator to call its adapter (replace inline parse logic)
- Consider alerting on market-count anomalies to catch silent platform migrations

### Session (20:42) [superwin]

**Context:** Value betting scanner — linking unmatched "Moe" venues, reviewing tracking performance, and fixing settlement scanner backfill bug

**Key Exchanges:**
- Linked 4 unmatched Moe/MOE venue variants across betfair/tab/tabtouch/sportsbet to a new canonical "Moe" venue (Victorian thoroughbred)
- Tracking review: +221.4u / +37.5% ROI on 591 settled picks — healthy day
- Investigated 72 stuck picks → root cause: `_market_start_times` is in-memory only, doesn't survive service restart, so old markets fall out of scanner visibility

**Decisions Made:**
- Added startup backfill that seeds `_market_start_times` + `_persistent_races` from unsettled picks on restart — so yesterday's races are visible to settlement scanner
- Added `listMarketCatalogue` re-fetch with fallback name matching (strip "N. " prefix, sortPriority) to rebuild `_persistent_sel_map`
- Decided NOT to fix the historical gap (2+ day old picks) — orphan voider handles those at 72h, acceptable tradeoff
- No secondary results source needed for now

**Lessons Learned:**
- Betfair `listMarketBook` with `SP_TRADED` projection returns no position data for OLD CLOSED markets — runner status but not winners
- `listMarketCatalogue` returns empty for most markets >1 day old — sel_map rebuild only succeeded 1/3 attempts
- The "duplicate venues" scan grouping by `(race_type, race_number, date)` produces false positives — different venues with same race number on same day aren't duplicates
- Settlement scanner's in-memory state (`_market_start_times`, `_persistent_sel_map`, `_persistent_races`) is the fragile surface — catalogue snapshot persists but scanner dicts don't

**Action Items:**
- Memory saved at `.claude/memory/settlement-scanner-backfill.md` documenting the backfill behavior and limits
- Future improvement: persist `_persistent_sel_map` + `_persistent_races` to disk alongside catalogue snapshot (bigger project, not urgent)
- Monitor that fresh settlements show full data (bsp=True, ltp=True, positions>0) going forward

