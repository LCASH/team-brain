# Daily Log: 2026-05-25 (lcash)

## Sessions

## Memory Maintenance

### Memory Flush (07:51) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (07:57) [value-betting-scanner]

**Context:** Completing the CLV Under-pick backfill (Bug #2 side-encoding fix) across ~15,788 historical picks

**Key Exchanges:**
- Built `scripts/recompute_clv_under_picks.py` — idempotent, dry-run-by-default, with `--sport`, `--limit`, `--pick-id`, `--apply` flags
- Ran progressive validation: 20 picks → 500 picks → full 15,788
- Austin Riley manual verification confirmed Over-price was being stored as Under-price; post-fix shows correct positive American odds (+156 to +192)
- Supabase pagination needed: default 1000-row cap required adding internal auto-pagination loop (200-pick pages)

**Decisions Made:**
- Progressive rollout strategy (20 → 500 → full) to catch issues early before touching all 15k picks
- `now_empty` picks (310 total) — accepted as honest nulls rather than filling with inflated wrong values
- 4,273 picks reclassified to `entry_snapshot`/`opening_fallback` closing source — correct behavior when normalization reveals sparse trail data

**Lessons Learned:**
- Supabase `.order().limit()` defaults to 1000 rows max; need explicit pagination for bulk operations
- When re-running after partial completion, must filter out already-patched picks (852/1000 in first paginated batch were duplicates)
- CLV Δ distribution: median +0.82, mean -1.08 — most picks weren't dramatically wrong (Over/Under decimals often similar magnitude). Extreme outliers (-453, +95) were longshot-side picks where sign-flip math produced wildly inflated bogus CLV

**Action Items:**
- Commit backfill script to git (currently uncommitted)
- Dashboard preference reorder: put `optic_clv_pct` first in `getProductionCLV` (~3 LOC)
- Verify tonight's MLB resolutions (first post-fix Under picks resolved live)
- Alt-line race (state stores one line per book; alt-lines overwrite) — structural fix, lower priority
- Moneyline / Total same-line flips (~1k+ in 15 min) — separate investigation

### Session (08:26) [server]

**Context:** Enriching the SuperWin value-betting scanner's SSE stream with ~40 new fields across 4 phases, so bots don't rely solely on confidence score.

**Key Exchanges:**
- User asked to populate the SSE stream with "pretty much every value we track" beyond confidence score
- Laid out a 4-phase plan: (1) promote 12 hidden `_foo` attrs to public fields, (2) add 17 BookieOdds/Race/Runner fields, (3) state-tracked fields (peak_ev, first_detected_at, same_race_count), (4) historical ROI cache by (mode, race_type, ev_band, mtj_band)
- User confirmed "all four phases without breaking everything"
- Confirmed all underscore reads/writes are internal to scanner.py — no external dependents, safe to rename

**Decisions Made:**
- All new fields are `Optional[...]` defaulting to `None`; `to_dict()` strips `None` → backward compatible with existing frontend/bots
- `confidence_parts` kept as a dict (not flattened) for cleanliness
- Band helpers added for EV/MTJ bucketing: `_ev_band()` and `_mtj_band()` for historical ROI keying
- Historical ROI loaded from Supabase at startup + refreshed periodically, keyed by `(mode_slug, race_type, ev_band, mtj_band)`
- Phase 3 state tracking uses the existing `_live_opportunities` dict to track `peak_ev` and `first_detected_at` across scan cycles

**Lessons Learned:**
- TAB red-pill flicker is pre-existing: `BookieStatus` resets to `race_count=0` during REST rediscovery (~27s window every 60s) — not caused by today's patches
- TAB stream disconnects every 30-60s with "Cannot write to closing transport" errors — reconnects fast but could be improved
- The proposition_id patch deployed earlier is working correctly — TAB producing opps with valid prop IDs

**Action Items:**
- Deploy was interrupted mid-implementation — changes to `models.py` and `scanner.py` were in progress but not fully deployed
- Need to complete syntax verification and deploy the enriched stream
- TAB status flicker fix still pending (preserve race_count during REST rediscovery in `app/adapters/tab.py` ~line 225)
- TAB stream transport disconnect frequency worth investigating separately

### Session (08:29) [value-betting-scanner]

**Context:** Backfilling CLV (closing line value) corrections for ~15,788 Under-side picks in the value-betting scanner after fixing a side-encoding bug (Bug A).

**Key Exchanges:**
- Walked through the full backfill process: 500-pick test batch → full paginated run across ~2,884 pages → all 15,788 Under picks patched with 0 failures
- Verification of 5 sample picks against OpticOdds: 4/5 correct, 1 outlier (Otto Lopez) caused by Bug B (alt-line race), not Bug A
- Detailed walkthrough of Bug B — the alt-line race condition in `server/state.py` where the market_key doesn't include `line`, so alt-line odds overwrite main-line odds in the same slot

**Decisions Made:**
- Committed backfill script (`aabb975`) + dashboard fallback reorder (`8976d63`) + earlier resolver fix (`d968539`) — all three layers of the CLV fix are now deployed
- Dashboard now prefers `optic_clv_pct` (OO-direct) over `sharp_clv_pct_true` in the topdown fallback chain
- Bug B fix: Option 3 (de-noise at trail layer, ~10 LOC in tracker.py) identified as pragmatic fix; Option 2 (per-line slot refactor, ~150 LOC) identified as correct long-term fix

**Lessons Learned:**
- The CLV Δ distribution was centered near zero (median +0.82, mean -1.08) — most picks weren't dramatically wrong because Over/Under decimals are similar magnitudes. Extreme outliers (-453, +95) were longshot picks where sign-flip math produced wildly inflated bogus CLV
- `now_empty=310` picks had only alt-line trail rows; line filter correctly rejected all books, producing honest nulls instead of bogus values
- OpticOdds `is_main` flag is inconsistent across books — some mark ALL lines as `is_main=True`, defeating the state store's alt-clobber guard
- The alt-line race corrupts CLV in two ways: (1) missing books when trail captures wrong line, (2) noisy odds when trail captures correct line but during price thrashing

**Action Items:**
- Bug B fix — implement Option 3 (trail-layer de-noise) as pragmatic near-term fix
- Bug C — moneyline/Total same-line flips (1k+ in 15 min), separate investigation needed
- Bug D — automate `backfill_optic_clv.py` via cron to keep `optic_clv_pct` coverage high
- Option 2 long-term refactor — per-line slot in state store (market_key includes line)

### Session (08:59) [value-betting-scanner]

I'll check the NBA scraper logs for the new tri-state classifier behavior.

**Context:** Diagnosing and fixing bet365 NBA scraper going stale for 19+ hours due to a permanent fixture blacklist bug, while MLB's tri-state classifier kept it healthy.

**Key Exchanges:**
- Traced NBA bet365 staleness (22.7h old markets) to `_failed_fixtures` set in `ws_nba_v4.py` that permanently blacklists any fixture whose wizard fetch fails — no retry, no time-based expiry
- MLB scraper (`ws_mlb_v4.py`) has a tri-state classifier (`pregame_active`, `live_or_completed`, `pregame_unverified`) that only permanently skips post-start-time failures and retries pre-game ones
- NBA playoffs uniquely expose this because the same fixture_id stays in the lobby across days (unlike MLB's daily fresh slate)
- Separately identified a latent OO `start_date_after` boundary-exclusive bug in the resolver — fixtures starting at exact midnight UTC get missed by Layer 1 gate, dropping ~406 picks/cycle

**Decisions Made:**
- Immediate fix: restart v3 to clear `_failed_fixtures` (done, NBA recovered in 4 min — same event_id that failed at 13:06 captured 320k/440 PAs at 08:49)
- Permanent fix: port MLB's tri-state classifier to `ws_nba_v4.py` AND add 6h TTL auto-expiry on blacklisted fixtures (coded and deployed)
- Kept legacy `_failed_fixtures` set intact for back-compat (matches MLB pattern)

**Lessons Learned:**
- A flat "never retry" blacklist is fine for sports with daily fixture turnover (MLB) but catastrophic for playoff series with recurring fixture_ids (NBA)
- The wizard failure at 13:06 was transient/state-dependent — bet365 returned a 26k post-game body, but the same event_id returned 320k bytes after restart. The blacklist was the real problem, not the wizard endpoint
- Discovery logs showing `+2 new, -0 removed (slate=0)` every 30 min was the smoking gun — `add_game()` was being called but silently returning at the blacklist check (logged at DEBUG, invisible in production logs)
- `start_date_after` in OO API is exclusive (not inclusive) — same boundary bug already fixed in `backfill_closing_sharps.py` on 2026-05-20, but still unfixed in the resolver's `_fetch_completed_fixture_names`

**Action Items:**
- Verify tri-state classifier + 6h TTL is working correctly on Eve post-deploy (in progress — PID 2505872)
- Fix resolver's `_fetch_completed_fixture_names` OO boundary-exclusive bug (subtract 1 second from `start_date_after` for midnight-UTC fixtures) — no code written yet
- Consider promoting the `add_game` blacklist skip from `logger.debug` to `logger.info` so future silent skips are visible in production logs

### Session (10:16) [superwin]

**Context:** Surfacing hidden SSE fields on the value-betting opportunity stream, then running a comprehensive hyperparameter sweep to discover new edge-pick theories.

**Key Exchanges:**
- Promoted 11 hidden `_foo` stashes + 12 extras from bookie_odds/bf/race/runner to public fields on the SSE stream (23 new fields total)
- Ran full hyperparameter sweep across 10K+ settled picks: race_type × edge × bookie × mtj_band × ev_band × liq_band × CLV × overlay × same_race_count × detection_count × bf_drift × trail_len
- Discovered 6 new candidate theories (A-F) that override/extend existing theory doc

**Decisions Made:**
- Phase 1+2 only for SSE fields (keep it simple, no payload bloat)
- `confidence_parts` kept as dict (not flattened) — implicit from "just show the values"
- Two deliverable docs created: validated baseline (`edge-pick-theories.md`) + playbook with candidates + backtest methodology (`edge-pick-playbook-2026-05-25.md`) → both in ~/Downloads

**Lessons Learned:**
- **CLV ≥15% = +37.9% ROI (2595 picks)** — cleanest signal but post-hoc only (not available at detection)
- **Bookie-vs-BSP overlay is real-time usable**: overlay < -5% → -15.6% ROI (2081 picks). Hard filter candidate at place-time
- **same_race_count ≤ 2**: sole picks +13.8%, 4-6 runners in same race → ~0%. Correlated-bets curse
- **detection_count sweet spot = 4-6**: +18.2% ROI. One-shots are noise, long-burns are stale. Wait ~10s before firing
- **Odds $5-8 is the strongest single dimension** — not in prior theory doc. Drives best 2D/3D combos
- **Greyhound IS profitable at odds $5-8** (+30.9% on 1137 picks) — contradicts prior "G is weak" line
- **Liquidity U-shape**: <$500 and $5000+ both beat mid-bands ($1.5-5K is the trough)
- **CANNON config**: racing-superpicks + tabtouch + odds $5-8 → +24.9% on 1599, lower CI +11.6%
- **bf_lay drift down + Harness = +46.7%** but bf_drift down + Greyhound = -7.8% (opposite signal)
- **Shorter trails are better**: quick-fire (1 entry) outperforms slow-burn revisits
- Restarting scanner during off-hours means racing bookies (TAB, TabTouch, Betfair) don't register until cron fires — must manually run `racing_schedule.sh start`

**Action Items:**
- Backtest all 6 candidate theories through the 10-box checklist before deploying
- Wire `same_race_count ≤ 2` + `min detection_count = 4` into live scanner (cheap wins)
- Surface `same_race_count`, `detection_count`, `trail_len`, `overlay`, `bf_drift` on live stream so consumers can filter
- Update confidence scorer to bake in overlay, bf_drift, trail_len signals
- Investigate BSP-at-place-time feasibility (place ≤30s before jump for overlay filter)

### Session (10:47) [value-betting-scanner]

**Context:** Closing out the CLV pipeline fix cascade (May 21-25) — final backfills, doc updates, and system status review.

**Key Exchanges:**
- Full system state review requested — produced comprehensive inventory of all open threads, infra status, and prioritized next steps
- Identified that re-running backfill on historical Bug B picks (#2 from original plan) would NOT help — trails were captured before the per-line fix, so same data produces same result
- Optic CLV backfill recovered 358 picks (MLB 108 + NBA 250) but total coverage remains low (~600 of 98k) due to OO purging completed-game odds

**Decisions Made:**
- Bug C (moneyline/total same-line flips) remains deferred — smaller cohort, no user pain
- Layer 3 historical audit still deferred — "how much did we mis-grade in the past" is a separate appetite question
- Prioritized resolver doc refresh over new feature work — reliability of reference docs > new code

**Lessons Learned:**
- OO purges completed-game data aggressively, limiting retroactive optic CLV backfill to ~2-day recovery window with ~11% hit rate
- Forward fixes don't retroactively heal trail data — Bug B per-line fix only benefits captures going forward, not historical trails
- Doc staleness compounds fast — 4 days of rapid fixes left `resolver.md` significantly behind reality

**Action Items:**
- Verify tonight's first post-Bug-B MLB resolutions (scheduled ~1h from session end) — confirms forward grading is clean
- Set up `backfill_optic_clv.py` as cron (every 2h on Eve) to keep optic coverage high
- Layer 3 historical audit — quantify how many old W/L grades were wrong from now-fixed bugs
- BetOnline (813) forward audit — needs ~1 week of data
- MLB Pinnacle Additive walk-forward — needs clean 14-day window with n≥100

