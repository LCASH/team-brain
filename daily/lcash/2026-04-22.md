# Daily Log: 2026-04-22 (lcash)

## Sessions

## Memory Maintenance

### Session (08:48) [value-betting-scanner]

**Context:** Value betting scanner — NBA tracker fix and MLB scraper parallel worker architecture overhaul

**Key Exchanges:**
- NBA picks were disappearing ~15 min before actual tip-off because OpticOdds reports scheduled start time, not actual tip-off. Added 15-min buffer to `_is_game_live()`.
- MLB bet365 scraper refactored from sequential single-page to **3 parallel `_WorkerPage` instances** — round-robin game distribution, 30-min rediscovery loop redistributes games across workers.
- Worker count scales dynamically: `min(N_WORKERS, len(game_list))`, static cap at `N_WORKERS = 3`.
- Overnight results confirmed architecture works: **6,669 odds, 12 prop types, 13-14/15 games**, workers producing 500-850+ odds/game (Dodgers@SF hit 851 odds, 16 markets expanded, 168KB).

**Decisions Made:**
- Scrapped old MLB parallelization plan — parser/navigation already fixed, only speed remained.
- Capped at 3 workers to balance speed vs Chrome memory/detection risk on Dell.
- Deployed immediately rather than waiting for tomorrow's test window.

**Lessons Learned:**
- OpticOdds scheduled times ≠ actual tip-off — always add a ~15 min buffer for NBA.
- Market expansion still underperforms on mini PC (0-7 expanded vs 15-17 local) — Chrome profile/SPA cache build-up may help over time.
- Dell handles 3 Chrome tabs + all servers fine (7.2 GB RAM free with everything running).
- MLB scraper overnight stability confirmed — no crashes, no memory issues, workers cycle correctly.

**Action Items:**
- Monitor MLB market expansion numbers on Dell — gap vs local still exists
- NBA tracker may need server restart for `_is_game_live` fix to take effect (cycles=0, inserts=0 in morning check)

### Session (08:59) [value-betting-scanner]

**Context:** Building a "newsbets" transcript-to-picks extraction system for MLB betting podcasts, plus enhancing the Pinnacle dashboard with new breakdown sections.

**Key Exchanges:**
- Added win/loss avg odds columns, timing windows (0-30min through 8h+), by-line, by-odds-range, and over/under breakdowns to pinnacle.html — all dynamic with existing filters
- Designed a 4-table data model for newsbets: `sources`, `episodes`, `picks_extracted`, `pick_mentions` — with `pick_hash` (subject+prop+side+line+game_date) for deduplication across transcripts
- Successfully grabbed YouTube captions from WagerTalk TV "Total Bases 4/20" using `yt-dlp` — 10,040 lines, 47K chars of cleaned transcript, no API keys needed
- First pass keyword extraction was too broad (21 vague segments); rewrote to strict pattern matching requiring **subject + prop + side + line** all present in same context

**Decisions Made:**
- No API keys for transcript scraping — `yt-dlp` for YouTube captions, browser scraping for podcast sites
- Strict pick extraction rules: only fire on explicit patterns like "[team] team total over 3.5" or "[player] over 6.5 strikeouts" — vague mentions filtered out
- Pick hash format intentionally matches scanner's market key pattern so OpticOdds cross-referencing is straightforward
- Confidence scoring from language signals: "best bet" (90), "lock" (85), "love this" (80), down to "maybe" (25)

**Lessons Learned:**
- YouTube dynamic rendering blocks Playwright selectors for video lists — `yt-dlp` is the reliable path for captions
- Raw keyword matching on betting transcripts produces way too much noise; need strict co-occurrence of subject+prop+side+line within the same sentence/breath
- Hosts often reference each other's picks ("my pick is exactly what Brian said") — consensus signals across hosts in same episode are a real pattern to track

**Action Items:**
- Set up Supabase tables for newsbets data model (sources, episodes, picks_extracted, pick_mentions)
- Build the scheduled pipeline: new episode detection → yt-dlp captions → extract → store → display
- Wire extracted picks to OpticOdds for odds verification, EV computation, and W/L resolution
- Expand beyond WagerTalk to other sources: Baseball Betting Show (Greg Peterson), MLB Gambling Podcast (SGPN), Beating The Book (Gill Alexander), On Deck
- Test extraction quality on multiple transcripts before trusting confidence scores

### Session (09:21) [value-betting-scanner]

**Context:** Debugging why NBA live odds tracking and picks stopped working; VPS crash recovery

**Key Exchanges:**
- VPS server crashed at 23:08 yesterday — SSE 400 errors cascaded across all sports and killed the process silently
- Tracker had 0 picks today despite 560 yesterday and 689 the day before — crash was the root cause
- Investigated data.json staleness (14 days old on VPS) — turned out to be irrelevant since dashboard now reads from SSE `/api/v1/stream` with `/api/v1/odds` fallback, not data.json
- After restart: 11,615 markets loaded, tracker began cycling, 802 trail updates catching up, first new pick inserted within ~4 cycles
- User confused by dashboard showing 2.45/2.00 vs bet365 showing 1.95/1.83 — explained these are devigged true odds vs soft book odds (separate columns), the gap IS the edge

**Decisions Made:**
- VPS restarted manually — no automated recovery was in place for the SSE cascade failure

**Lessons Learned:**
- VPS can silently die from SSE 400 error cascades and stay dead for 10+ hours with no alerting
- Cold start after VPS crash: theories have 5-min cache TTL that's empty on restart, so first cycle finds nothing — not a parallelism issue
- The `/status` endpoint showing "0 theories" is a display bug — theories ARE loaded internally
- Mini PC NBA server had been running 21 days without restart (since April 1) — the tracker "waiting for data" message was misleading
- Architecture reminder: EV evaluation runs on mini PC engine, VPS is a relay that receives push data and tracks/resolves picks

**Action Items:**
- Need automated VPS crash recovery or health-check alerting — 10 hours of silent downtime is unacceptable
- Fix `/status` endpoint to correctly display loaded theory count
- Investigate why SSE 400 errors cascade and kill the process instead of being handled gracefully
- Consider removing or updating stale data.json references to avoid future confusion

### Session (09:42) [value-betting-scanner]

**Context:** Debugging VPS value betting dashboard showing stale/incorrect odds after overnight crash restart

**Key Exchanges:**
- Dashboard showed Harrison Barnes Threes Under at 2.45 (stale) when bet365 live was 1.833 — user flagged the discrepancy
- Traced data flow: mini PC pushes live odds → VPS API (correct) → dashboard render (stale for stored picks)
- User reported correct live picks briefly appearing, then stale stored picks "flashing" over them

**Decisions Made:**
- Enriched stored Supabase picks with live SSE odds before rendering (replaced frozen `book_odds` with current soft book values)
- Normalized market keys in dedup filter to handle `"harrison barnes_threes_under"` vs `"harrison_barnes_threes_under"` mismatch — spaces vs underscores in player names
- Fixed dedup so stored picks are filtered out entirely when live picks exist for same player+prop+side

**Lessons Learned:**
- **Market key format inconsistency**: stored picks generate keys with underscores in player names, live API uses spaces — this caused dedup failures and stale duplicates leaking through
- **Stored picks carry frozen odds**: `book_odds` from Supabase stays at insertion-time value; must refresh from live data before rendering or EV% calculation
- **VPS cold start after crash**: first tracker cycle needs to re-cache theories (5-min TTL), then evaluate — not a parallelism issue, just cold start latency
- **data.json is legacy**: dashboard reads from SSE `/api/v1/stream` with `/api/v1/odds` fallback, not data.json — stale data.json was a red herring

**Action Items:**
- Monitor for other market key format mismatches beyond player name spaces/underscores
- Consider standardizing key generation to one canonical format across Supabase writes and live computation

### Memory Flush (09:44) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (09:45) [value-betting-scanner]

**Context:** Extracting betting picks from a "Total Bases" podcast transcript (2026-04-20 episode)

**Key Exchanges:**
- Transcribed 5 picks from the Total Bases podcast: Skubal K's O5.5, Tigers ML, Soto TB O1.5, Yankees U8.5, Braves -1.5 RL
- Fixtures weren't available for the target date, but picks were still extracted using player/team name lookups
- Confidence levels assigned based on language cues ("best bet" → 90, "leaning toward" → 40)

**Lessons Learned:**
- Podcast pick extraction works even without fixture data for the date — player/team lookups are sufficient fallback
- Language intensity mapping for confidence: "best bet" ~90, "I'm going/taking" ~65, "I like" ~55, "leaning toward" ~40

### Session (09:47) [value-betting-scanner]

**Context:** Extracting MLB betting picks from the "Total Bases" podcast episode for daily tracking

**Key Exchanges:**
- Parsed 6 explicit betting picks from podcast transcript with player attributions, lines, and confidence levels
- Identified a combined 3-team closing parlay (Braves ML / Rockies F5 O2.5 / Orioles ML)
- Had to re-record Reds pick after initially including a line parameter that wasn't explicitly mentioned in the episode

**Lessons Learned:**
- When recording picks from podcasts, distinguish between firm picks vs. conditional/lean language (e.g., "if I played this" = lower confidence, "been pretty much gold" = higher confidence)
- Line movements matter for context — Rockies F5 team total moved from 1.5 to 2.5, which changes the pick's value profile
- Attribute picks to specific hosts (Brian Leonard vs. Tokyo Brandon) for tracking accuracy over time

**Action Items:**
- Track parlay season record (currently -8.3 units on parlays) — hosts noted needing a few hits to recover

### Session (09:55) [value-betting-scanner]

**Context:** Building a podcast picks extraction pipeline for the value betting scanner — YouTube transcript → Claude Agent SDK extraction → Supabase storage → dashboard

**Key Exchanges:**
- Tested yt-dlp transcript fetching; discovered `@Wagertalk` is the correct YouTube handle (not `@WagerTalkTV`)
- MLB Gambling Podcast channel is dead (no posts since Oct 2025) — removed from active channels
- Claude Agent SDK `query()` API works for extraction; `ClaudeSDKClient` class does not — use `query()` directly
- Successfully extracted 6 picks from Total Bases 4/20 episode (46K char transcript) with proper confidence scoring

**Decisions Made:**
- Used Claude Agent SDK with tool-based extraction (record_pick tool) rather than regex/prompt-only parsing — gives structured output with confidence scores
- WagerTalk has multiple MLB shows under one channel handle — configured as: Total Bases, Drew's Daily Diamond, MLB Double Picks, Under the Radar, Prop Hitters
- No direct Supabase DB URL available — migration must be run manually via SQL Editor
- VTT parser strips timestamps and deduplicates overlapping subtitle segments

**Lessons Learned:**
- yt-dlp handle format matters: `@Wagertalk` works, `@WagerTalkTV` does not — always verify channel handles before hardcoding
- Claude Agent SDK: use `query()` function, not the `ClaudeSDKClient` class
- Date derivation from YouTube publish time + episode title is reliable for MLB daily shows
- 46K char transcript → 6 picks is a reasonable extraction rate for game-preview podcasts

**Action Items:**
- Run `019_podcast_picks.sql` migration in Supabase SQL Editor
- Full end-to-end test: fetch → extract → write → resolve → report
- Verify Supabase tables exist after migration before running pipeline
- Consider adding more active MLB podcast channels beyond WagerTalk

### Session (10:12) [value-betting-scanner]

**Context:** Extracting betting picks from Total Bases podcast transcript for April 20, 2026 games

**Key Exchanges:**
- Extracted 10 betting picks from podcast audio/transcript including moneylines, run lines, team totals, game totals, and a 3-leg parlay
- Recorded each pick with confidence ratings, line values, and attribution to specific podcast hosts (Tokyo Brandon, Brian Leonard, Host)

**Decisions Made:**
- Assigned confidence ratings to each pick based on host conviction/language (e.g., Nationals F5 O1.5 at confidence 80 as highest, Tigers ML at 40 as "reluctant")
- Categorized picks into structured bet types (ML, RL, team totals, game totals, parlay)

**Lessons Learned:**
- Podcast pick extraction works well with structured categorization by bet type, confidence level, and attribution
- Historical success patterns mentioned by hosts (like Nationals F5 totals) can inform confidence ratings

**Action Items:**
- Track results of these April 20 picks to evaluate podcast accuracy over time
- Consider building a systematic tracker for podcast pick performance by host and bet type

### Session (10:14) [value-betting-scanner]

**Context:** Extracting betting picks from an MLB podcast transcript (Total Bases episode, April 20, 2026)

**Key Exchanges:**
- Systematically extracted 7 distinct betting picks from podcast audio/transcript, including game totals, team totals, moneylines, and a parlay
- Assigned confidence levels (55-80) based on host language cues ("lean" = lower, "best bet of the day" = higher, repeated success = highest)

**Decisions Made:**
- Confidence scoring methodology: mapped host conviction language to numeric confidence (e.g., "maybe a decent play" → 55, "been pretty much gold" with 8-win track record → 80)
- Excluded individual player props that lacked actionable direction from the hosts
- Captured line movement context (Rockies 1st 5 originally Over 1.5, moved to 2.5 at +130)

**Lessons Learned:**
- Podcast pick extraction works well with structured table output — hosts often bury picks in conversational flow, so systematic extraction catches picks that casual listening might miss
- Parlay identification requires cross-referencing multiple segments — the three parlay legs (Braves ML / Rockies 1st 5 O2.5 / Orioles ML) were mentioned across different parts of the episode

**Action Items:**
- Track outcomes of the 7 picks to validate confidence scoring methodology over time
- Consider building a more formal podcast-to-picks pipeline if this becomes a recurring workflow

### Session (10:31) [value-betting-scanner]

**Context:** Debugging and fixing pick flashing/flickering issues on the value betting dashboard after removing stored Supabase pick merging

**Key Exchanges:**
- User reported picks still flashing in/out after the stored pick merge removal was deployed
- Traced through multiple layers: stored picks → live computation → SSE reconnection → VPS reconciliation

**Decisions Made:**
- Removed stored pick merge entirely for live SSE sports (NBA/MLB) — only show `computeEVPicks()` output; kept stored picks for virtual sports (NRL/AFL/Pinnacle) since they have no SSE
- Added 15-min buffer to dashboard's `isGameLive()` to match the server-side fix — prevents picks from vanishing at scheduled `game_start` before actual tip-off
- Stopped `loadStoredEVPicks()` from triggering `render()` every 30s for non-virtual sports — was causing unnecessary DOM rebuilds
- Changed SSE snapshot handler from `markets = {}` (clear-and-replace) to merge-based approach — stale markets filtered naturally by existing 5-min `MAX_SOFT_ODDS_AGE_S` check

**Lessons Learned:**
- SSE reconnection + aggressive server-side `reconcile_sport()` was the real root cause of flashing — each reconnect wiped client markets and replaced with a potentially smaller VPS snapshot
- Multiple independent bugs can stack to produce the same symptom (flashing): stale stored picks, missing game-start buffer, unnecessary re-renders, and snapshot clearing all contributed
- VPS `reconcile_sport()` removes markets not in the current push — if push cycles have varying market sets, this causes instability downstream

**Action Items:**
- Monitor whether VPS-side `reconcile_sport()` itself needs softening (it aggressively removes markets every 5s, which reduces snapshot completeness for reconnecting clients)
- Consider whether SSE reconnection should request a "full" snapshot vs relying on merge behavior long-term

