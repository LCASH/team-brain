# Daily Log: 2026-07-22 (lcash)

## Sessions

## Memory Maintenance

### Session (00:39) [introrender]

**Context:** Building a polished intro animation for the Knowted app demo video — logo extraction, CSS animation, and video rendering.

**Key Exchanges:**
- Extracted the Knowted brain-check mark from the full logo using connected-component labeling (union-find), dropping the wordmark/K letter cleanly
- Embedded the mark as base64 data-URI PNG in both `intro-preview.html` and `IntroLogoClick.tsx` for portability
- Fixed cursor glide stutter — root cause was fade-in during motion + mid-path waypoint causing velocity stutter; fixed by fading in at rest then one continuous `cubic-bezier(.65,.05,.36,1)` ease
- Rendered animation to video using Puppeteer (headless Chrome) stepping the animation clock frame-by-frame + ffmpeg encode → `knowted-intro.mp4` (1080p60, 4.2s, H.264)

**Decisions Made:**
- **Drop the "knowted" wordmark text** from the intro rather than fixing the font — system font looked generic next to the custom logo; mark alone as centered app-icon is cleaner
- **Darker green background** preferred over original or lighter variants — deep, moody brand green with white mark contrast

**Lessons Learned:**
- CSS animation-to-video pipeline: Puppeteer deterministic frame stepping (no dropped frames) + ffmpeg encode is reliable for perfectly smooth renders
- Connected-component labeling (hand-rolled union-find, no scipy) works well for separating interleaving raster elements when simple x-axis cropping fails
- Base64 data-URI embedding keeps HTML previews fully standalone — no external file dependencies

**Action Items:**
- Assets saved: `src/assets/knowted-mark-white.png`, `~/Downloads/knowted-mark-white.png`, `~/Downloads/knowted-intro.mp4`
- Video variants offered but not yet requested: ProRes, 4K, transparent overlay, logo-hold (no wipe)
- Final green shade may need further tuning based on user feedback

### Session (00:39) [Knowted admin]

Let me try one more approach — querying the files directly by the folder ID:The Drive MCP only has an `authenticate` tool available — the actual file-reading tools aren't loaded yet because it's connected to b8steel@gmail.com, not your Knowted account. Even re-authing won't help since the issue is account mismatch, not permissions.

**Here's the fastest path:** Just tell me which 10 images you picked. Either:

- **Paste the filenames** (e.g. `pr-01.png, nb-03.png, fu-05.png...`)
- **Or paste the 10 headlines** from the images

These are all images we just generated together, so I have every single one locally. Once I know which 10 you selected, I'll write all 10 captions immediately — same voice as the samples I showed you earlier (hook → pain → fix → headline as the button → soft CTA).

