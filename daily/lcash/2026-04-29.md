# Daily Log: 2026-04-29 (lcash)

## Sessions

## Memory Maintenance

### Session (08:19) [value-betting-scanner]

**Context:** Debugging bet365 Chrome scraper setup on mini PC — login detection, Chrome rendering issues, and architecture walkthrough for the value betting system.

**Key Exchanges:**
- User logged into bet365 on one Chrome but login detection was incorrectly reporting status — checking for "login" in URLs doesn't work because bet365 keeps the same URL regardless of auth state
- The "Log In" button visibility check was also unreliable — button reported "not visible" but Chrome was rendering as black screen
- User asked for clear architecture diagram of the scraping system — provided full flow from Chrome tabs → workers → servers → VPS → dashboard
- Confirmed bet365 allows multiple simultaneous browser sessions (separate Chrome profiles = separate devices)

**Decisions Made:**
- Use persistent Chrome profile dirs (`C:\Users\Dell\bet365_nba_profile`, `C:\Users\Dell\bet365_mlb_profile`) instead of temp dirs — survive reboots and don't get cleaned up
- Can't launch GUI Chrome apps via SSH on Windows — must be done directly on mini PC desktop/Command Prompt
- Need to fix the login detection mechanism — current URL-based and button-visibility checks are both unreliable for bet365

**Lessons Learned:**
- bet365 login detection can't rely on URL checks (URL stays the same logged in or out) or DOM button visibility checks (page may not render properly)
- Windows blocks GUI app launches from SSH sessions — a critical constraint for remote management of the mini PC
- Chrome can bind to a debug port (9223/9224) and respond to CDP commands while rendering a black screen visually — process running ≠ window visible
- Persistent Chrome profiles preserve login cookies across restarts, but still need manual CAPTCHA solve on first login

**Action Items:**
- Fix bet365 login detection to use a more reliable method (e.g., check for account-specific DOM elements, cookie presence, or API response)
- Launch both Chromes (9223 NBA, 9224 MLB) directly on mini PC desktop and log in manually
- Restart NBA server (8800) and MLB server (8803) once Chromes are up and logged in
- Consider documenting the "can't SSH launch Chrome on Windows" limitation in the mini PC setup docs
- Close the 53+ duplicate tabs issue — scraper opens game pages before login is confirmed, creating duplicates

### Memory Flush (08:47) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:50) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (08:51) [value-betting-scanner]

**Context:** Deploying NBA and MLB value betting scrapers on Windows mini PC via Tailscale, dealing with Chrome rendering/login issues

**Key Exchanges:**
- Chrome launched from background processes (SSH/scheduled tasks) renders as black screen on Windows — only Chrome launched manually from the desktop gets GPU rendering
- Scraper's `start()` method was killing user's manually-launched Chrome and spawning its own (invisible) one — needed fix to attach to existing Chrome first
- Both bet365 accounts use credentials: Janklu565 / Arilue2!
- MLB scraper already had attach-first logic; NBA scraper needed it added
- CAPTCHA blocks automated login — must be solved manually on the mini PC

**Decisions Made:**
- **Stop NRL/AFL servers, focus on NBA/MLB** — user's request to switch active sports
- **Fix scrapers to attach-first, launch-only-if-needed** — prevents scrapers from killing manually-launched (visible, logged-in) Chrome instances
- **User must manually launch Chrome from desktop** — Windows limitation: background processes can't render GUI apps with GPU

**Lessons Learned:**
- Windows blocks GUI rendering for processes spawned via SSH or background services — Chrome opens but shows black screen
- The `--disable-blink-features=AutomationControlled` flag triggers a harmless warning banner in Chrome — can be dismissed
- Scraper architecture must handle "Chrome already running" case by attaching to existing instance rather than kill-and-relaunch
- bet365 login flow includes CAPTCHA that can't be automated — requires manual intervention on first login

**Action Items:**
- User needs to relaunch both Chrome windows manually on mini PC (NBA on 9223, MLB on 9224) and log in
- Start NBA and MLB servers once Chrome instances confirmed logged in
- Verify the attach-first fix works correctly when servers start (scrapers should connect to existing Chrome without killing it)

### Memory Flush (09:14) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (09:16) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (09:22) [value-betting-scanner]

**Context:** Debugging bet365 game scraper tab leaks and recovering crashed VPS pipeline

**Key Exchanges:**
- Identified 3 bugs in `bet365_game.py` causing Chrome tab leaks: (1) `_setup_task` not cancelled in `close()`, (2) discovery page leaked on mid-exception, (3) `_cleanup_stale_tabs` only matches `/B18/` URLs, missing homepage/failed-nav tabs
- Full architecture walkthrough of `Bet365GameScraper`: `start()` → discover games → spawn 3 tasks (`_setup_task` opens per-game BB tabs with response listeners, `_refresh_task` reloads stale tabs every 5s, `_rediscover_task` finds new games every 30min) → `scrape_cycle()` just collects cached odds passively

**Decisions Made:**
- Fixes proposed but not yet applied: add `_setup_task` to cancel list in `close()`, wrap discovery page in try/finally

**Lessons Learned:**
- The `_setup_task` leak is the **main culprit** for tab accumulation — each Chrome-death restart cycle leaks a full set of game tabs because the old setup task keeps running after `close()`
- Worker crash root cause was Node.js EPIPE; server subsequently died too, but Chrome stayed alive on port 9223 allowing clean reattach
- VPS recovery sequence: kill stale worker → restart NBA_Server + Push_Worker scheduled tasks → worker auto-attaches to existing logged-in Chrome → games discovered and odds flowing within ~60s

**Action Items:**
- Fix `close()` to cancel `_setup_task` (primary tab leak fix)
- Add try/finally around discovery page creation in `_discover_games()`
- Consider expanding `_cleanup_stale_tabs` to handle non-`/B18/` URLs (homepage, failed navigations)

### Session (09:57) [value-betting-scanner]

**Context:** Debugging and fixing the news-to-picks pipeline — classifier bugs, missed events, and expanding MLB monitoring

**Key Exchanges:**
- News agent missed KD OUT, Franz Wagner calf strain, Reaves return upgrade because the mini PC lost power and the agent wasn't running
- KD tweet processed but yielded 0 picks — classifier mapped KD to Phoenix Suns (old team) instead of Houston Rockets despite tweet explicitly saying "Rockets"
- After fix, KD tweet successfully generated 4 quality picks (LAL spread, Sheppard pts over, Reaves pts under, Thompson pts over)
- MLB monitoring was essentially dead — only 1 account (FantasyLabsMLB, inactive 26 days) was enabled

**Decisions Made:**
- Don't add extra monitoring/restart processes — watchdog already handles it, mini PC just lost power (user pushback on "obnoxious" additions)
- Bet365 scraper for MLB is nice-to-have, not essential — OpticOdds provides 7,018 markets with full AU soft book + sharp book coverage
- Focus energy on MLB news monitoring pipeline, not bet365 scraper debugging

**Lessons Learned:**
- LLM classifier will override explicit tweet context with stale training data (KD→Suns). Fix: inject live player rosters from odds API into classifier prompt
- Injured players won't appear in odds roster — classifier must NOT block tweets about players missing from roster (they're missing BECAUSE they're injured)
- Classifier fix was two-part: (1) "trust team name in tweet over your knowledge" prompt rule, (2) live roster injection from VPS odds data
- Commit `f57f557` deployed with the roster injection fix

**Action Items:**
- MLB news pipeline now monitoring: Passan, Rosenthal, Heyman, Feinsand, Buster_ESPN, MLBTradeRumors, BNightengale (all newly enabled)
- Odds snapshot before/after feature still to be built (measuring book movement during analysis window)
- Validate MLB picks quality when first real MLB news event flows through the fixed pipeline

### Memory Flush (10:11) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (10:29) [value-betting-scanner]

**Context:** Optimizing MLB scraper (bet365) — migrating from slow SPA navigation to direct URL game page loading

**Key Exchanges:**
- Confirmed intermediate architecture: `goto(games_list_url)` → `click(team_name)` works for MLB (~15s/game vs old 30s)
- User asked how MLB differs from NBA — NBA uses direct Bet Builder URL, MLB was thought to require SPA module loading first
- **User challenged assumption** by sharing a direct game URL with `/I0/` suffix and asking to test it — it worked perfectly (22 MGs, 106 PAs loaded directly)
- User caught that 0-odds (live) game pages were staying open as wasted Chrome tabs

**Decisions Made:**
- **Adopted direct I0 URL navigation** for MLB game pages — eliminates need for games-list-boot + team-click flow entirely. Single `page.goto(url_with_I0)` per game
- **Added in-play detection**: check for `/IP/` in URL after navigation, close page immediately if live — prevents wasted tabs/memory
- Pages that get 0 odds after setup are also closed rather than kept as persistent pages

**Lessons Learned:**
- The `/I0/` suffix in bet365 URLs specifies the active tab — without it, SPA doesn't load game content; with it, direct navigation works like NBA's Bet Builder approach
- Always test direct navigation with full URL parameters before assuming SPA requires multi-step boot sequences
- User domain knowledge ("I'm sure it would work") was correct — don't over-trust earlier negative test results if the URL pattern wasn't exact

**Action Items:**
- Monitor MLB scraper in production to confirm I0 direct nav consistently works for pre-game games
- Verify in-play cleanup logic actually closes tabs (no orphan pages accumulating)

### Session (10:59) [value-betting-scanner]

**Context:** Debugging and optimizing the MLB scraper on a mini PC — closing wasted tabs for live/in-play games that return 0 odds.

**Key Exchanges:**
- Games with 0 odds (live/in-play) were keeping Chrome tabs open, wasting memory and slots
- I0 direct URLs don't redirect to `/IP/` like clicking from the games list does, so the in-play URL check didn't work for direct navigation
- Event_id length filter (>10 digits) doesn't work for MLB because MLB event_ids are all 8 digits (25587xxx), unlike NBA
- The real detection method: check page content for live indicators immediately after load, before running the full capture timeout

**Decisions Made:**
- Added fast in-play content detection on page load (~8s) instead of waiting for full capture timeout (~30s) to detect duds
- Removed `click_target` references since `_MLBGamePage` no longer uses them
- Added close-on-0-odds logic to `_rediscover_loop` as well
- Two Chrome profiles were conflicting on port 9224 — killed the old `bet365_mlb_game_profile` instance

**Lessons Learned:**
- `taskkill` on the server kills Chrome too — need to manually relaunch Chrome on the mini PC after killing processes
- MLB event_ids are shorter than NBA (~8 digits vs 10+), so NBA's event_id length heuristic doesn't transfer
- bet365 WS data can list games as pre-game (`ES=="2"`) even when they're actually live — page content is the reliable signal
- Two Chrome instances can't bind to the same `--remote-debugging-port`; the second one silently fails to bind

**Action Items:**
- Verify MLB scraper only opens 3 tabs (CHI Cubs, KC Royals, MIA Marlins) after clean restart
- Confirm Chrome on port 9224 is properly reattached after relaunch

### Session (11:32) [value-betting-scanner]

**Context:** Fixing EPIPE crashes in the value betting scanner's browser automation — replacing Playwright-managed game pages with raw CDP WebSocket connections.

**Key Exchanges:**
- Debugged why `Network.responseReceived` CDP events weren't firing — needed `Page.navigate` instead of `Page.reload`, and `Page.enable` first
- Identified root cause of EPIPE: Playwright's internal event handling conflicts when new tabs are created via CDP HTTP while Playwright is still connected to Chrome
- Discovered that finished games (PHI) don't serve BB wizard data, so testing needed live/upcoming games

**Decisions Made:**
- **Disconnect Playwright after discovery, reconnect only for rediscovery** — Playwright is needed for DOM interaction during `_discover_games()` and `_rediscover_loop()`, but CDP game pages don't use it. Disconnecting Playwright before game setup eliminates EPIPE crashes.
- **Raw CDP WebSocket (`websockets` library) for all game page management** — Python talks directly to Chrome over TCP WebSocket, bypassing Node.js subprocess pipes entirely.

**Lessons Learned:**
- Playwright and raw CDP tab creation conflict — Playwright's internal event handling on its WebSocket connection triggers EPIPE when new tabs appear via CDP HTTP endpoints
- `Page.navigate` fires `Network.responseReceived` events; `Page.reload` alone does not (needs explicit `Page.enable` or similar)
- Tab count matching (`tabs=5/4` → `tabs=6/6`) is a reliable health indicator — previous Playwright approach leaked to `tabs=21/4`
- The pattern of "use high-level tool for complex DOM work, drop to low-level for simple page management" avoids framework conflicts

**Action Items:**
- Monitor tomorrow's live games to confirm stability under full load
- Watch for rediscovery reconnect/disconnect cycle working correctly over longer runs
- Consider whether Playwright can be fully replaced (even for discovery) to eliminate the dependency entirely

### Session (12:02) [superwin]

**Context:** Deep analysis of value betting scanner (SuperWin) performance data — building multi-dimensional filter matrices and investigating trail/CLV signal quality.

**Key Exchanges:**
- Built full 5D matrix (Mode × Race Type × Odds × Liquidity × MTJ) revealing each mode has different optimal timing: Cash Multi prints late (mtj<2min), SuperPicks mid-window (5-10min), Normal needs 5-10min for genuine mispricing
- Expanded to 8D matrix adding trail-derived dimensions (EV trajectory, volatility, lay depth, speed to peak) — 16 total tracked dimensions
- Investigated why 60% of picks lack trail data: 45% pre-deploy (Apr 23-26), 15% single-scan, only 19 picks blocked by 3% threshold — threshold is NOT the bottleneck
- Deep CLV analysis revealing it doesn't predict individual winners but is the best portfolio-level filter

**Decisions Made:**
- Keep 3% EV movement threshold — lowering it would only capture 15-36 more picks, not worth I/O risk
- Trail data will fix itself naturally as pre-deploy data ages out (~80%+ coverage expected within a week)
- Single-scan picks should be filtered or flagged — 149 picks at -29% ROI, -43u losses

**Lessons Learned:**
- **Harness + $12-20 + liq$500-1.5K + mtj 2-10min + sc2+** is the most robust signal across all dimension cuts (485% ROI on 10 picks, 383% on 15)
- **Single-scan picks (detection_count=1) are noise, not signal** — worst performing segment at -29% ROI, validates multi-scan requirement from a new angle
- **$6-8 odds is a confirmed dead zone** across every cut; Greyhound $20+ is 0/40
- **CLV is a portfolio filter, not a pick predictor** — 76% of all profit comes from CLV≥15% picks; CLV<15% is breakeven
- **CLV × Liquidity is the real edge**: liq $500-1.5K + CLV 0-10% = +128% ROI; liq $500-1.5K + CLV 20%+ = +98% ROI
- **CLV is the ON/OFF switch for harness**: positive CLV = +89-181% ROI, negative CLV = -51% ROI
- **ev_flat + vol_med** (stable mispricing, moderate movement) consistently profitable across modes
- Each race type has its own optimal timing: Greyhounds 2-5min, Harness 5-10min, Thoroughbreds split by odds

**Action Items:**
- Add CLV findings to theories file (theories 19+)
- Consider implementing `detection_count >= 2` filter to exclude single-scan picks from journal/backtest
- Update the 8D matrix in theories file with CLV cross-dimensional findings
- Monitor trail data coverage as pre-deploy data ages out

### Session (12:38) [value-betting-scanner]

**Context:** Debugging and enhancing the news-to-picks pipeline — Discord monitoring, encoding fixes, and a critical sport detection bug.

**Key Exchanges:**
- Embiid mid-game injury analysis crashed due to Windows cp1252 encoding failing on Czech player name (`č` character). Fixed with `PYTHONIOENCODING=utf-8` in batch file.
- Added Discord webhook notifications at every pipeline decision point: blocked tweets, passed tweets, picks generated, zero picks.
- Discussed whether `odds_movement` data should be persisted in Supabase — decided no, it's ephemeral session data. Cleaned out the Supabase persistence attempt.

**Decisions Made:**
- **Odds movement stays ephemeral** — logged + Discord only, no Supabase column. It's just a before/after snapshot comparison within a single pipeline run.
- **Discord webhook as primary monitoring** — every tweet decision (block/pass/picks/zero) gets a notification so we don't have to check logs.

**Lessons Learned:**
- **`--sport` default="nba" silently broke MLB processing.** Because `sport="nba"` is truthy, the `or` short-circuit in `sport or _detect_sport_from_source()` meant auto-detection never ran. Every MLB tweet (MLBTradeRumors, Passan, Rosenthal) was processed as NBA, fetching NBA markets for MLB players → 0 picks. Fix: default to `None`.
- **Stale `.pyc` files caused silent deployment failures** — agent kept running old code even after editing source. Must clear `__pycache__` on deploy.
- **Windows cp1252 encoding wall extends beyond our code** — previously hit on `→` arrows in log messages, now hit on player names in external data. `PYTHONIOENCODING=utf-8` is the systemic fix.

**Action Items:**
- SSE startup crash (theory creation blocks streams) — still open
- MLB bet365 scraper stability (Chrome keeps dying) — still open
- News agent instructions refinement for better pick quality — still open
- Monitor Discord for first correctly-classified MLB tweet to confirm sport detection fix works

### Session (13:13) [value-betting-scanner]

**Context:** Debugging and stabilizing bet365 MLB/NBA scrapers — Playwright EPIPE crashes and process management

**Key Exchanges:**
- Root cause identified: `ValueError: I/O operation on closed pipe` fires in asyncio internals when Playwright's Node.js subprocess dies during disconnect — can't be caught with normal try/except
- Fix approach: custom asyncio exception handler on the event loop to suppress the pipe error (deployed to both MLB and NBA workers)
- Raw CDP architecture validated: tabs open/close correctly, in-play detection works via CDP, tab count stays low (~4 tabs)
- Worker crash interval improved from ~1 min to ~4 min with the suppression, but Playwright discovery phase still causes eventual crashes

**Decisions Made:**
- Added custom event loop exception handler rather than refactoring discovery to raw CDP (pragmatic short-term fix vs bigger rewrite)
- Created `docs/BET365_SCRAPER_ARCHITECTURE.md` documenting the full scraper flow for both sports

**Lessons Learned:**
- Playwright EPIPE errors propagate through asyncio event loop background callbacks — `try/except` and `warnings.filterwarnings` don't catch them; only a custom `loop.set_exception_handler()` works
- Watchdog processes may survive even after their scheduled task ends — need to kill both the schtask AND the process explicitly
- NBA's `_disconnect_playwright()` pattern was a reference but insufficient alone since the error fires after disconnect completes

**Action Items:**
- Move discovery phase to raw CDP to eliminate Playwright entirely (root cause fix for remaining crashes)
- Alternative: isolate Playwright in a subprocess so its crash doesn't kill the main worker
- Investigate why KC Royals/CHI Cubs had WebSocket timeout — possibly Chrome overwhelmed from rapid tab open/close cycles

### Memory Flush (13:29) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (13:29) [value-betting-scanner]

**Context:** Debugging value betting dashboard — frontend vs backend EV calculation discrepancies and trail recording issues

**Key Exchanges:**
- Investigated why chart showed stale soft odds (2.41) while table showed current (2.50) — root cause: trail entries stop recording when pick falls outside 3-hour gate or tracker can't match pick_id
- Discovered pick_id mismatch between mini PC and VPS trackers — initially suspected hash algorithm difference (MD5 vs SHA-256) but confirmed both use same code; actual issue was input data differences
- Found VPS market data was stale (bet365 showing 2.4) while dashboard showed 2.50 — dashboard was displaying stale client-side cached data
- Discovered some dashboard picks (e.g., Sengun Threes Under 0.5) never created in Supabase — dashboard computes EV client-side from live data but tracker evaluates server-side from potentially stale data
- **CRITICAL BUG FOUND:** Frontend `computeTrueProb()` produces wildly different numbers than manual calculation — Pinnacle true odds showed 1.28 (47.7% EV) in table vs correct 1.749 (8.2% EV). Aggressive showed +7.6% EV in table vs correct -0.9% EV

**Decisions Made:**
- Added live soft odds as final chart point (band-aid fix while investigating deeper trail issues)
- Identified need for proper audit of frontend vs backend calculation parity

**Lessons Learned:**
- Dashboard client-side EV computation and server-side tracker computation can diverge due to: different sharp book availability, different staleness windows, different devig methods
- Pick_id generation is sensitive to exact input normalization — different tracker instances must produce identical IDs
- Trail depth issues stem from tracker timing/synchronization, not chart rendering
- **The frontend `computeTrueProb()` has a significant bug** producing fundamentally wrong true odds and inflated EV numbers — this undermines trust in all displayed EV values

**Action Items:**
- **P0:** Fix frontend `computeTrueProb()` bug — true odds and EV calculations are materially wrong
- **P0:** Ensure frontend and backend use identical devig methods, sharp book selection, and staleness windows
- Investigate whether Over/Under swap logic is incorrect in the table's code path
- Address client-side vs server-side pick evaluation gap (picks shown as +EV on dashboard but never created by tracker)
- Improve trail recording reliability — ensure VPS tracker matches all active picks

### Session (13:30) [value-betting-scanner]

**Context:** VB resolver audit wrap-up, Bet365 2.0 Points investigation, and dashboard UI fix for theory column overflow.

**Key Exchanges:**
- Cross-verified combo props (PRA, PR, PA, RA) by summing individual stats — zero mismatches across all checks, confirming OpticOdds individual stats are trustworthy
- 111 DD/TD picks with stale encoded values couldn't be re-resolved due to name matching failures — nulled them so production resolver (with fuzzy matcher) will retry after deploy
- Bet365 2.0 (book 366) Points picks appeared low (user saw 21) but actual total is 233 — still lowest prop but not broken; bet365 prices Points more efficiently so less EV found
- 119 Points Under markets confirmed live from 366 right now — data pipeline is healthy

**Decisions Made:**
- Null out 111 unresolvable DD/TD picks rather than force-match — let production resolver's better fuzzy matcher handle them post-deploy
- Collapse theory columns into single "Best EV" column when >3 theories exist on dashboard — eliminates horizontal scroll on Pinnacle pill while preserving detail in expandable sharp rows

**Lessons Learned:**
- **Never trust pre-computed derived stats from APIs** — compute from primitives (DD/TD was encoded stat-line concatenation, not binary flag)
- **Extreme win rates (>90% or <10% on 20+ picks) should trigger automatic sanity alerts** — DD Over at 100% across 350+ picks was a broken grader, not edge
- **Pick deduplication needed for analytics** — same player-game outcome tracked as 3+ picks across soft books inflates W/L counts ~2.6x; affects ALL prop types, not just DD
- Low pick count for a prop doesn't necessarily mean broken scraper — can mean the book prices it efficiently so scanner finds less mispricing

**Action Items:**
- Deploy DD/TD resolver fix to mini PC + VPS (111 nulled picks waiting for re-resolution)
- Consider adding automatic win-rate sanity alert to resolver summary log
- Build deduplicated analytics view for true performance measurement across all prop types

### Session (13:30) [value-betting-scanner]

**Context:** Debugging X/Twitter GraphQL API rate limits for MLB speed test scraper

**Key Exchanges:**
- Browser DevTools monitoring revealed X uses **different GraphQL query IDs** than the script (`naBcZ4al-iTCFBYGOAMzBQ` vs `V7H0Ap3_Hh2FyS75OCDO3Q` for UserTweets), but testing confirmed QID doesn't matter — X tracks rate limits by **endpoint type + auth_token**
- Raw curl test confirmed the 429 persisted for 1+ hours, indicating an **extended rate limit ban** (not the normal 15-min window), likely triggered by repeated 429 loops during earlier testing
- Each 429 response itself may count as a flag, meaning checking if the limit cleared actually pushes the window forward

**Decisions Made:**
- Refused to use suspicious third-party credentials (credential dump format with throwaway email domains like `@genflow.cfd`, `@printgate.icu`)
- Decided to wait for overnight rate limit reset on the `@hgdluke` account rather than pursue workarounds
- Confirmed MLB speed analysis is already complete via web research (beat writers break MLB injuries, not national insiders)

**Lessons Learned:**
- X/Twitter extended rate limit bans can last hours+ when triggered by repeated 429 loops — the normal 15-min window doesn't apply after abuse flagging
- GraphQL query IDs rotate but share the same rate limit pool per auth_token — swapping QIDs doesn't bypass limits
- Checking rate limit status with API calls can reset/extend the ban window
- Hashed token strings from credential dumps aren't usable as `auth_token` cookies — X API needs the actual session token from a logged-in browser

**Action Items:**
- Wait overnight for `@hgdluke` rate limit to clear, then re-run MLB speed test
- Consider creating a dedicated second X account for API scraping to avoid future rate limit issues on the primary account

### Session (13:30) [value-betting-scanner]

**Context:** Migrated NBA bet365 scraper from Playwright to raw CDP WebSocket for game page management, fixing chronic EPIPE crashes.

**Key Exchanges:**
- Root cause of EPIPE: Playwright's Node.js subprocess pipe overflows when new CDP tabs are created while Playwright is still connected to Chrome. Fix = disconnect Playwright after discovery, before game pages open.
- Before/after results: went from 5-10 restart cycles with `tabs=21/4` (massive leaks) to single-cycle startup with `tabs=5/4→6/6` (perfect match), 2060 odds streaming, zero crashes.
- Deep architecture analysis of MLB scraper: still on Playwright, even more vulnerable — 3 tabs/game (I0, I2, I5), ~15 DOM clicks per setup, up to 45 Playwright pages on a full slate.
- Server died silently ~42 min after successful startup — no error in logs, but game setup via raw CDP was confirmed stable (crash happened post-setup, not during).

**Decisions Made:**
- Hybrid architecture: Playwright for discovery (DOM interaction), raw CDP WebSocket for game pages (no Node.js pipe involved)
- Let NBA raw CDP changes bake overnight before migrating MLB
- MLB migration should consolidate 3 Playwright pages per game into 1 CDP WebSocket using hash navigation (`window.location.hash`)

**Lessons Learned:**
- Playwright + CDP HTTP tab creation = guaranteed EPIPE once you exceed ~3 pages (Node.js pipe traffic overflow)
- Raw CDP WebSocket (`websockets` library, Python → Chrome over TCP) completely eliminates the Node.js subprocess pipe — the core crash vector
- `_disconnect_playwright` / `_reconnect_playwright` pattern needed for rediscovery loops (Playwright reconnects briefly, then disconnects again)
- MLB's `matchbettingcontentapi` uses MG→MA→PA structure (vs NBA's `betbuilderpregamecontentapi` with EV→MA→PA) but interception pattern is identical

**Action Items:**
- Monitor NBA overnight to confirm raw CDP stability across full game lifecycle
- Migrate MLB to `_CDPPage` pattern — adapt for `matchbettingcontentapi` URL filter and I2/I5 hash navigation within single tab
- Investigate silent server death (no error logged at 11:28) — possibly lingering Playwright process or OS watchdog
- Wrote architecture doc to `brain/findings/2026-04-29-nba-cdp-architecture.md` covering full flow

### Session (13:30) [value-betting-scanner]

**Context:** Completing Action Network podcast backfill, comparing NBA/MLB pick performance vs JuiceBox, and improving the podcasts dashboard UX.

**Key Exchanges:**
- Action Network extraction yielded **1,078 picks from 106 episodes** (807 NBA, 248 MLB), bringing total dataset to **3,899 picks, 2,639 resolved**
- Action Network NBA: +58.6% ROI on 325 picks, but inflated by moneyline underdog wins (+203.5% ROI on 73 ML picks, only 39.7% WR). Player props are genuinely sharp: **points +17.4%, PRA +15.6%, rebounds +48% ROI**
- Head-to-head: Action Network NBA player props (~+17% ROI on 200+ picks) outperform JuiceBox NBA (~+3% ROI on 121 picks)
- Action Network MLB is mediocre at -7.5% ROI

**Decisions Made:**
- Dashboard date range should auto-reload on change (no manual "Load Results" click needed)
- Replaced dropdown date range with **calendar date pickers** (From/To) plus quick-range presets for flexible timeframe analysis
- Default to "All time" on page load so full dataset is visible immediately

**Lessons Learned:**
- ROI headline numbers can be misleading — Action Network's +58% NBA ROI is real but driven by a few big underdog ML hits, not consistent edge. Always decompose by prop type
- `yt-dlp --dump-json --skip-download` is the reliable way to get upload dates from YouTube streams (slower at ~2-3s per video but accurate)
- Dashboard UX: date filters that don't auto-reload confuse users — always re-fetch on filter change

**Action Items:**
- Trend analysis across time windows (user mentioned wanting to find trends in the data)
- Mini PC access reminder: `ssh Dell@100.67.233.95` (Tailscale IP), more reliable than local network IP

### Session (13:30) [value-betting-scanner]

**Context:** Stabilizing bet365 MLB/NBA scrapers — raw CDP architecture, Playwright crash-looping, and Windows watchdog management

**Key Exchanges:**
- Raw CDP tab architecture is working: tabs open/close correctly, in-play detection works, Chrome stays clean (~4 tabs)
- Workers crash-loop due to Playwright EPIPE → ValueError in asyncio internals that can't be caught with try/except
- Added custom asyncio exception handler to suppress pipe errors — extended crash interval from ~1 min to ~4 min
- Created `docs/BET365_SCRAPER_ARCHITECTURE.md` documenting the full scraper flow

**Decisions Made:**
- Raw CDP for game tabs is stable; Playwright discovery phase is the remaining crash source
- Next step: either move discovery to raw CDP too, or isolate Playwright in a subprocess (bigger change, deferred)
- Disabled the `EVScannerWatchdog` scheduled task to stop auto-restarts while debugging

**Lessons Learned:**
- `schtasks /End` only kills the *current run* of a Windows Scheduled Task — the schedule stays active and Windows re-fires it on the next trigger. Must use `schtasks /Change /TN <name> /DISABLE` to actually prevent re-triggering
- `wmic process delete` doesn't reliably kill processes; `taskkill /PID <pid> /F` is more reliable
- Playwright disconnect fires ValueError in a background thread/callback outside user code — `warnings.filterwarnings` and try/except won't catch it; asyncio loop exception handler is the only hook point
- Raw CDP approach avoids the EPIPE entirely since there's no Playwright intermediary for game tabs

**Action Items:**
- Move Playwright-based game discovery to raw CDP (eliminates last crash source)
- Re-enable watchdog (`schtasks /Change /TN EVScannerWatchdog /ENABLE`) once scrapers are stable
- News agent must stay running — exclude from bulk kill operations

### Session (13:30) [value-betting-scanner]

**Context:** Monitoring and improving the Twitter news agent pipeline (scraping → classification → pick generation) and deploying the news picks dashboard.

**Key Exchanges:**
- Twitter workflow is live and classifying tweets correctly — blocking postgame noise (LeBron stats, historical commentary, ejection opinions) while waiting for actionable pre-game news
- News picks were missing sharp book snapshots — regular picks had them but news picks only stored soft book odds

**Decisions Made:**
- Added `sharp_snapshot`, `sharp_books_used`, `sharp_count`, `opening_odds`, and `triggered_ev` fields to news pick inserts in `pick_writer.py` — enables CLV backtesting (compare sharp odds at pick time vs closing)
- Deployed news dashboard to VPS at `170.64.213.223:8802/news.html` + added "News Edge" yellow tab link on main dashboard
- Had to deploy updated `index.html` to both VPS **and** mini PC — push worker on mini PC was overwriting VPS with old version every 5 seconds

**Lessons Learned:**
- Push worker sync gotcha: deploying only to VPS gets overwritten if the mini PC push worker still has the old file — must deploy to both
- Tweet dedup at scraper level would save cost — same tweet processed twice from different poll cycles (classifier blocks it cheaply but unnecessary)
- Windows `cp1252` encoding can't handle special characters (Czech/Slovak names) — caused a non-fatal skip. Consider forcing UTF-8 in tweet processing
- 87 rotating accounts = zero 429s over 4+ hours of continuous scraping

**Action Items:**
- Add tweet ID dedup at the scraper level to avoid reprocessing the same tweet across poll cycles
- Fix the `charmap` encoding error — force UTF-8 for tweet text processing
- No picks generated yet (all postgame noise) — monitor for pre-game injury/lineup news when tonight's games approach

### Memory Flush (13:30) [Knowted admin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (13:31) [Knowted admin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (14:17) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (14:18) [value-betting-scanner]

**Context:** Debugging value betting dashboard — frontend EV calculations vs backend tracker discrepancies, stale trail data, and pick ID mismatches.

**Key Exchanges:**
- Chart showed soft odds at 2.41 but table showed 2.50 — traced to trail entries not updating when live odds changed
- Pick ID mismatch discovered: Supabase had `92f914be...` but local generation produced `99f5218a...` — turned out to be MD5 vs SHA-256 difference in test, not actual tracker mismatch
- VPS market data showed bet365 at 2.4 (fresh, 118s old) while dashboard showed 2.50 — dashboard was displaying stale client-side cached data
- Some picks (e.g., Sengun Threes Under 0.5) exist as +EV on dashboard but were never created in Supabase — frontend evaluates EV client-side from live data, tracker evaluates server-side from possibly stale data
- **CRITICAL BUG FOUND:** Frontend `computeTrueProb()` produces wildly different numbers than manual calculation — Pinnacle true odds showed 1.28 (47.7% EV) in table vs manually computed 1.749 (8.2% EV). Aggressive showed +7.6% in table vs manually computed -0.9%.

**Decisions Made:**
- Added `liveOdds` parameter to chart rendering so current soft odds appear as final data point (band-aid fix)
- Determined the trail data from VPS is actually correct — the dashboard table was showing stale values
- Agreed that frontend and backend MUST produce identical true odds calculations

**Lessons Learned:**
- Trail entries only record within 3-hour gate before game time
- Dashboard computes EV client-side from live SSE data while tracker computes server-side — different market staleness windows cause disagreements on which picks are +EV
- The `computeTrueProb()` JavaScript function has a significant bug producing incorrect true odds — this undermines all EV calculations shown to the user
- Pick creation depends on server-side evaluation passing, so picks visible on dashboard may have no Supabase record and thus empty charts

**Action Items:**
- **P0:** Fix `computeTrueProb()` frontend bug — the devig math is producing fundamentally wrong true odds (Pinnacle showing 1.28 instead of 1.749)
- **P0:** Audit frontend devig method per theory vs backend `compute_true_prob()` to ensure identical calculations
- Investigate whether Over/Under swap logic is wrong in the table's code path
- Ensure frontend and backend use the same sharp book set, staleness windows (`MAX_ODDS_AGE_S`), and devig methods
- Address the gap where dashboard shows +EV picks that tracker never created in Supabase (empty charts)

### Session (14:21) [value-betting-scanner]

**Context:** Debugging the news agent pipeline — a Twitter→Discord alert incorrectly classified an MLB roster move (Cubs outrighting Scott Kingery) as a "scratched" event, then silently dropped it with no follow-up.

**Key Exchanges:**
- Traced the full pipeline flow: classifier → PASSED Discord message → silent early-exit with no picks and no explanation
- Identified the tweet ("Cubs Outright Scott Kingery") was a 40-man roster transaction, not a game-day lineup scratch

**Decisions Made:**
- Two bugs confirmed that need fixing:
  1. **Classifier vocabulary gap** — the Haiku classifier prompt at `classifier.py:42` lacks BLOCK rules for waiver/DFA/outright/option roster moves (transactions ≠ game-day status changes)
  2. **Silent early-exit on no-odds** — `runner.py:108-116` returns silently after posting PASSED to Discord, should instead fire a `:warning: No odds for {team}` Discord message so the pipeline doesn't look like it died

**Lessons Learned:**
- "Outrighted" vs "scratched" is a common NLP confusion for baseball — the classifier needs explicit negative examples for roster transactions
- Three silent early-exit paths exist after PASSED (dedup, no-odds, analysis-0-picks) — only the last one posts to Discord, making debugging hard
- The no-odds path is the most likely culprit when PASSED fires but nothing follows (team not in current odds cache)

**Action Items:**
- Patch classifier SYSTEM_PROMPT BLOCK section to explicitly block waiver/DFA/outright/option-to-minors roster moves
- Add Discord notification on the no-odds early-exit path in `runner.py`
- Consider auditing the other silent early-exit (dedup) for similar Discord notification gaps

### Session (15:01) [Knowted admin]

**Context:** ICP validation research — scraping Facebook group "Sales People Closers" contributors and validating against LinkedIn via browser automation (AdsPower)

**Key Exchanges:**
- Scraped 68 unique contributors from FB group with engagement points; got FB profile URLs for top 10 only (FB leaderboard DOM limitation for positions 11+)
- Ran ICP validation pass 1 on top 10: **2 strong ICP matches** (Elias Saracco — sales/AI agency CEO; Calvin Gilbert — VP Sales & Operations), 3 unclear/name-mismatch, 2 definite non-ICP, 3 need refined queries
- Started pass 2 covering positions 11-30 + retries on unclear names

**Decisions Made:**
- Search Google with `"linkedin.com: (name)"` pattern instead of navigating LinkedIn directly or using direct Google search URLs — avoids bot detection while still pulling LinkedIn snippets
- Switch from parallel tab searches to sequential one-at-a-time with human-pace delays — parallel burst of 15 triggered Google captcha immediately

**Lessons Learned:**
- **AdsPower browser automation + Google rate limiting:** Direct search URLs (`google.com/search?q=...`) get instantly flagged as bot. Must navigate to google.com → type in search box → press Enter to mimic real user
- **Parallel browser tabs = captcha magnet:** Even with real cookies in AdsPower, 15+ simultaneous Google searches triggers captcha. One-at-a-time with delays is the only reliable approach
- **FB leaderboard scraping ceiling:** Facebook only renders profile links for top ~10 in group leaderboard; positions 11+ show as text-only (would need individual FB searches to get URLs)

**Action Items:**
- Pass 2 validation still running (16 targets at human pace) — check results
- Refined searches needed for: Whinnie Ramirez (`+ "Surge Institute"`), Gabriel Kibbey (`+ sales`)
- Begin outreach to confirmed ICP: Elias Saracco, Calvin Gilbert
- Consider validating positions 31-68 if pass 2 yields good hit rate

### Session (15:32) [value-betting-scanner]

**Context:** Debugging and hardening the news_agent pipeline in the value-betting-scanner project — a Twitter→classifier→odds→analysis→Discord pipeline for sports betting picks.

**Key Exchanges:**
- User noticed a tweet about "Cubs Outright Scott Kingery" was classified as `[scratched]` and passed through to Discord, but no picks followed and no explanation was posted
- Traced the root cause: Haiku classifier lacked vocabulary for roster transactions (outright/DFA/optioned), and the no-odds early-exit path was silent after posting PASSED to Discord
- Second batch of misclassifications: Travis Bazzana debut labeled `[return]`, Don Mattingly firing labeled `[signing]` — both burned $0.20-0.65 in Stage 2 compute for structurally un-pickable news
- User reported Discord going quiet — diagnosed as genuinely quiet news window, not a failure

**Decisions Made:**
- Added explicit BLOCK rules in classifier.py for: roster transactions, coaching/manager changes, MLB debuts/first call-ups, fringe call-ups
- Added `:warning: NO ODDS` Discord message on the no-odds early-exit path (runner.py:117) so silent exits are visible
- Veteran IL activations and recalls of established players still PASS — they have prop markets

**Lessons Learned:**
- The classifier's event_type schema lacked categories for non-game-day-status news (roster moves, coaching changes, debuts), causing Haiku to force-fit into existing types like `scratched`, `return`, `signing`
- Silent early-exit paths after a PASSED message create confusion — always close the loop in Discord
- Every task restart creates a small polling blind spot (~4 min) because `_last_seen` seeds from the current latest tweet on startup
- `twitter_scraper.py:165-166` swallows all exceptions silently — if Twitter rotates GraphQL `qid`, the agent dies with zero indication. Quiet failure looks identical to quiet news.

**Action Items:**
- Add heartbeat logging ("polled N accounts, M new") every ~5 min to distinguish quiet-news from silent-failure
- Add fetch-failure warning when all accounts return empty for extended period
- Consider fixing the deploy-window blind spot (seed `_last_seen` from before the bounce, not after)
- VPS health check probe uses wrong address (`127.0.0.1:8800` vs real `170.64.213.223:8802`) — cosmetic but misleading

### Session (16:07) [value-betting-scanner]

**Context:** Architecting bet365 MLB prop scraper — went through three architecture pivots based on empirical probing of bet365's API surfaces.

**Key Exchanges:**
- User corrected assumption about game/prop availability — only 12 games, fewer with active props. Data verified: 4 games had Hits props (not 1), TB Rays had fewest (25 vs 68-70).
- Debated three approaches (A: hybrid, B: pure per-game, C: scroll probe). Chose C → led to Plan v2 (sub-tab architecture).
- Plan v2 tested: 7 sub-tabs = ~7 HTTP calls vs original ~225. But discovered only 4 of 25 per-game market groups exist on games-list sub-tabs.
- Pivoted to Plan v3: per-game tabs unavoidable for full prop coverage (25 MGs/game). Sub-tabs still useful for Alt Game Total + Alt Run Line (cover all 15 games in one shot).

**Decisions Made:**
- **Direct `fetch()` via `Runtime.evaluate` doesn't work** — bet365 returns 200-but-empty. Must use `page.goto()` + `on_response` capture. (Anti-scraping measure.)
- **Same-URL re-goto doesn't refire API** — refresh loop must cycle through different URLs (bounce strategy).
- **WS streaming definitively ruled out** on all three surfaces tested (NBA per-game, MLB per-game, MLB games-list Hits sub-tab). Zero prop pid matches across 700+ frames total. HTTP-only architecture confirmed.
- **Plan v2 (sub-tab-only) abandoned** — games-list covers only 4 market types (Hits, Pitcher K's, HR, Total Bases) + 2 alt lines. Per-game tabs have 25 MGs including RBIs, Runs, Stolen Bases, Pitcher Earned Runs, all batter alt props.

**Lessons Learned:**
- bet365's games-list prop sub-tabs are an incomplete surface — useful for supplementary bulk fetches (Alt Game Total/Run Line cover all 15 games) but not a replacement for per-game depth.
- bet365 CDN caching is very sticky — identical 32,241-byte responses 30 minutes apart.
- Two formats per stat exist (CO milestones vs O/U) — games-list only has O/U; many edges live in CO milestone alt lines on per-game tabs.
- Architecture probing before committing to a plan saved major rework — each 10-min probe reshaped the entire approach.

**Action Items:**
- Probe per-game coupon URL (`/F19/I0/` via `matchbettingcontentapi/coupon`) to check if all 25 MGs come back in one response without click-expand (Option B proposed, not yet executed)
- Rewrite plan to v3 if coupon probe fails — per-game with direct-URL nav + games-list supplementary for alt lines
- Revised estimate: ~375 partial calls/cycle (25 partials × 15 games), refresh interval likely 30s not 15s

### Session (17:29) [Knowted admin]

**Context:** ICP research and outreach campaign for Knowted — scraping a Facebook group's top contributors, validating via LinkedIn, and drafting personalized cold DMs.

**Key Exchanges:**
- Iterated on anti-bot strategy for AdsPower browser: direct Google URLs triggered captchas; switching to google.com homepage → search box → `linkedin.com: "name"` format with 4-8s delays and human-paced typing achieved 30/30 searches with zero captchas
- Validated top 30 group contributors against ICP — found only 6 of 30 are actual buyers (agency founders/VPs hiring closers), rest are individual closers looking for jobs
- Crafted 6 personalized outreach messages with specific constraints: no em dashes, research framing ("not a pitch"), pre-handle Fathom/Gong objection, hint at video, play on desired outcome

**Decisions Made:**
- Sequential single searches over parallel tabs to avoid bot detection
- "Research, not a pitch" framing as primary cold DM strategy — asks for their expertise instead of selling
- Staggered send schedule: Tier 1 (actively hiring) day 1, Tier 2 day 2-3
- CSV over markdown for outreach tracking (columns: send order, status, replied, reply notes, message)

**Lessons Learned:**
- AdsPower browser detected easily with parallel tabs or direct search URLs; sequential + homepage search box is the safe pattern
- `linkedin.com: "name"` Google query reliably surfaces the right LinkedIn profile
- In sales-focused Facebook groups, most active contributors are job-seeking reps, NOT buyers — the real ICP (people hiring those reps) are lurkers, not top posters
- The hiring signal (posting "🚨 HIRING SETTERS") is the strongest outreach trigger — they have an active pain point right now

**Action Items:**
- Send outreach messages in priority order starting with Aljaž Heine and Jonathan Kalvik Seterøy
- Draft follow-up messages for non-responders after 5-7 days (discussed but not yet created)
- Outputs saved to `icp-research/outreach-tier1.csv`, `icp-research/icp-validation-final.md`, `icp-research/icp-top30.csv`

### Session (18:02) [value-betting-scanner]

**Context:** Rewriting bet365 MLB per-game prop scraper — replacing DOM clicks with deterministic URL hash-nav pattern

**Key Exchanges:**
- Discovered that hash-nav to `/AC/B16/{league}/D19/E{event}/{tab}/G{gid}/S^1/` triggers SPA partials with real data — no clicks needed (~1.3s/fetch)
- Confirmed bet365 does NOT stream pre-game player props via WebSocket — WS is only for in-play/live markets
- Confirmed bet365 UI polls HTTP autonomously every ~6-7 seconds when idle on a game tab (most return tiny "no-change" 57b responses)
- Brute-forced discovery of all 25 MG G-ids; one (Pitcher Strikeouts CO milestone G163122) requires `tab_root_coupon` strategy instead of `partial_hashnav`

**Decisions Made:**
- Architecture is HTTP polling (not WS streaming) — bet365 simply doesn't expose pre-game props on WS
- 24/25 MGs use `partial_hashnav`, 1 uses `tab_root_coupon` fetch strategy
- Leaning toward Option B for Cap 7: port URL-nav fetcher into production's existing orchestrator (~50 line change) rather than rebuilding orchestrator from scratch

**Lessons Learned:**
- Bet365 renamed "Runs Batted In" → "RBIs" — production has been **silently dropping ~109 RBI records per game**. Map patch applied.
- The `S^1` URL segment is critical for triggering SPA partials
- G163122 (Pitcher Strikeouts CO) times out on hash-nav because it's the default I5 view — SPA serves it inline in the tab root coupon
- Pre-game prop odds change via HTTP poll cycle, not push — bet365 UI does the same thing we do

**Action Items:**
- Decide A vs B for Capability 7 (orchestrator rebuild vs port into production)
- Deploy RBIs map fix to production (immediate value — recovering lost data)
- Consider faster polling interval (10-15s) now that we know bet365 UI polls at ~6s
- "Pitcher to Record First Strikeout" parser support (named-option format) — low priority, only 2 records

### Session (18:40) [value-betting-scanner]

**Context:** Building and testing bet365 MLB scraper rewrite — orchestrator (Cap 7) through output writer (Cap 10), with concurrency tuning.

**Key Exchanges:**
- Confirmed bet365 UI polls HTTP every ~6-7s per game tab (not WS for pre-game props). Tiny "no-change" responses (~50 bytes), full ~5KB body only when trader moves a line.
- User rejected suggestion to port wins back into production's existing orchestrator — "production was not reliable. not one bit of it… keep moving closer test by test." Preference saved.

**Decisions Made:**
- **Full rewrite over production patching:** User explicitly wants capability-by-capability validated rewrite, not a 50-line patch to production's broken orchestrator.
- **Raw CDP per game + Playwright only for discovery:** Mirrors production's working pattern for multi-tab stability.
- **Bounded concurrency (sem=3) for refresh loops:** Full-parallel gather of 15 scrapers triggered bet365 anti-scraping — odds count dropped 12401 → 6145. Capping at 3 concurrent refreshes restored full data quality (914/880/901 per game).
- **Parallel setup, sequential-ish refresh:** Setup can run parallel (one-time page creation), but ongoing refreshes need throttling.

**Lessons Learned:**
- **bet365 anti-scraping triggers on concurrent navigation:** 15 simultaneous `Page.navigate` calls causes half the responses to come back empty. Sequential with 1s gaps (production pattern) or bounded semaphore (sem=3) avoids this.
- **Sequential setup is a bottleneck:** 2s launch delay × 15 games = 8+ min. Parallel setup cut to ~90s with no issues (setup is one-shot, not repeated).
- **`asyncio.gather` for refresh is dangerous:** Unlike setup, refresh happens every cycle — bet365 rate-limits the repeated concurrent hits.
- **Register scrapers before async setup completes:** Otherwise a kill mid-setup orphans tabs not yet in `self.scrapers` dict.

**Action Items:**
- Capability 7 run with concurrency=3 still in progress — validate full slate + stable refresh counts
- Capability 11 (error recovery validation) harness written, needs execution
- Capabilities 12-13 still remaining after 11
- Faster refresh interval (10-15s per game) identified as possible improvement, not yet implemented

