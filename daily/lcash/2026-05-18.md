# Daily Log: 2026-05-18 (lcash)

## Sessions

## Memory Maintenance

### Session (08:59) [value-betting-scanner]

**Context:** Running /vb health check on the value betting infrastructure (NBA + MLB focus)

**Key Exchanges:**
- Full stack health check performed across mini PC, Eve v3, and VPS relay
- Mini PC running legacy 4-sport architecture (not v3) — ports 8800-8803
- NBA (:8800) and MLB (:8803) healthy; NRL (:8801) and AFL (:8802) returning 503
- Eve v3 alive with 9858 markets, 16h uptime, 25% CPU, 800MB RAM
- VPS relay degraded: `last_picks=1` flagged, 8658 markets, pulse=4015902

**Decisions Made:**
- NRL/AFL being down is acceptable (not current priority — NBA/MLB focus)
- Flagged news agent `--live` as potentially unauthorized (running ~28h, PID 9176, since 17/05 5:07am)

**Lessons Learned:**
- VPS missing Bet365 (365) from soft books despite mini PC bet365 workers being healthy — push worker may not be relaying correctly or VPS rejecting it
- Two push_worker instances running on mini PC (minor duplication, not critical)
- Mini PC bet365 NBA + MLB workers both healthy (PIDs 468 + 5696, started 17/05 ~5am)

**Action Items:**
- Investigate why VPS isn't reflecting Bet365 data from mini PC push worker
- Confirm whether news agent `--live` was intentionally started or needs stopping
- Consider killing duplicate push_worker instance

### Session (09:30) [value-betting-scanner]

**Context:** Debugging and restoring NBA bet365 scraper on Eve VPS after discovering zero NBA market production since May 15 restart.

**Key Exchanges:**
- NBA Chrome had been running since May 8 (10 days) — v3 restarted May 15, saw Chrome "already running," skipped login verification entirely. Session cookies expired → discovery returned nothing → orchestrator sat silent with zero log entries.
- First restart attempt failed: AdsPower API returned success for `browser/start` but no Chrome process actually spawned (port ECONNREFUSED). API reported "Active" — phantom state.
- Root cause of phantom start: AdsPower daemon couldn't spawn Chrome via API alone without inherited GUI session environment variables (Wayland/X11 display context).

**Decisions Made:**
- Killed only NBA Chrome (PID 71621), left MLB alone since it was working (7227 markets flowing)
- Chose "harvest GUI session env" approach over restarting entire AdsPower daemon (which would have killed MLB too)
- After fix confirmed, saved recovery procedure to memory for future reference

**Lessons Learned:**
- v3 startup has a dangerous assumption: if Chrome is already running, it presumes logged-in state without verification (`startup.py:258`). Stale sessions produce zero data silently.
- AdsPower API `browser/start` can return success with a port number, but Chrome never actually launches — always verify with `pgrep` or CDP `/json` endpoint before trusting API response.
- **Wayland recovery pattern**: harvest GUI session env (DISPLAY=:0, XAUTHORITY=mutter xauth path, DBUS_SESSION_BUS_ADDRESS) from active gnome-shell process, then `browser/stop` → `browser/start` under that env. Works for individual profile re-launches, not just full daemon restart.
- v3 output goes to tmux pty (`/dev/pts/0`) not `v3.log` when using `start-v3.sh` — must use `tmux capture-pane` for log inspection.
- Tab teardown is normal behavior (`TAB_TEARDOWN_AFTER_START_MIN=5`) — game tabs open/close per refresh cycle, so seeing only homepage in Chrome tabs doesn't indicate failure.

**Action Items:**
- Consider patching `v3/startup.py` to verify login state even when Chrome is "already running" (add `_is_logged_in` check to the "presumed logged in" path)
- Check MLB Chrome session age — same May 8 launch, may expire similarly within days
- Monitor for AdsPower phantom-active state recurring after future VPS reboots or display server restarts

### Session (10:06) [value-betting-scanner]

**Context:** Porting BetIT scraper to v3 architecture and reverse-engineering Neds (Entain) API for a new direct scraper

**Key Exchanges:**
- BetIT (book 961) was identified as never ported from legacy `direct_scrapers.py` to v3 — gap, not a bug
- Brain memory claimed BetIT/123bet was redundant with other Black Stream brands — **turned out wrong**: 123bet API works without auth and returns unique data
- BetIT legacy parser regexes outdated — market type naming evolved (e.g., `Player points + assists 25+` vs legacy `Pts + Ast Over X`)
- Neds API (`api.neds.com.au`) returns 500 to direct curl — requires SPA-driven browser session context
- Neds live updates flow via Socket.IO at `push.neds.com.au` (EIO=4)

**Decisions Made:**
- Ported BetIT to v3 despite initial brain memory suggesting redundancy — confirmed 210 unique markets across 8 prop_types
- BetIT gated behind `ENABLE_BETIT=1` env var, same pattern as Betr/TAB/TabTouch
- Neds scraper will piggyback on AdsPower-backed Chrome (in-browser `fetch()`) since direct API access is blocked
- NRL and AFL deprioritized; focus stays on NBA + MLB

**Lessons Learned:**
- Brain memory can be stale/wrong — always verify with live probes before trusting cached assessments
- BetIT's 123bet API endpoint pattern: `https://123bet.com.au/api/...` — no auth needed, JSON responses
- v3 log writes to pty after restart, not to v3.log file — misleading when checking if a new scraper is running
- Neds is Entain platform — same as Ladbrokes AU (902), meaning one scraper could serve both brands with a brand selector
- AdsPower browser on Eve needed Wayland env vars harvested to come alive (k19yb91n on CDP port 38101)

**Action Items:**
- Build `v3/scrapers/neds/` scraper using AdsPower browser context (finding doc at `brain/findings/2026-05-18-neds-recon.md`)
- Investigate whether Neds login unlocks additional markets beyond anonymous browsing
- Ladbrokes (902) — confirm same Entain API surface to get it free with Neds scraper
- 5 open questions in the Neds finding doc to resolve before scraper build
- Confirm BetIT market coverage for MLB (currently NBA-only — matches legacy behavior)

