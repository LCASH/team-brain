# Daily Log: 2026-04-26 (lcash)

## Sessions

## Memory Maintenance

### Session (07:42) [value-betting-scanner]

**Context:** Deploying multi-league EV betting scanner expansion across basketball and baseball leagues on VPS

**Key Exchanges:**
- Expanded from NBA/MLB to 100 basketball + baseball leagues (94 basketball, 6 baseball) with auto-discovered Pinnacle moneyline theories
- User caught that 408 prediction market leagues (soccer, tennis, hockey, esports) can't be accessed via Optic Odds API — only basketball and baseball SSE streams work
- System successfully found EV picks across 6 leagues: NBA (568), MLB (412), Japan B1 (13), KBO (5), Germany BBL (1), Greece A1 (1)
- Morning check revealed ~43% of picks (13/30) have zero trail entries despite being tracked

**Decisions Made:**
- Deactivated 408 prediction market theories — harmless but noise; left them for potential future API upgrade
- Restricted SSE streams and fixture cache to only `["basketball", "baseball"]` to eliminate 16 blocked-sport 400 errors and timeout spam
- Theory existence check changed to look for ANY theory (not just `is_active=true`) to prevent duplicate creation on restart
- Added auto-login self-heal to both bet365 scrapers (NBA + MLB) — detects session expiry via CDP tab URLs, triggers `login_bet365()`, resets on success

**Lessons Learned:**
- Optic Odds SSE multiplexes all leagues within a sport on one connection — 100 leagues need only 2 streams
- Bulk Supabase INSERTs (~500 individual calls) are slow (~2-3 min) but only needed on first run; subsequent restarts skip existing theories
- `auto_discover_all_leagues()` was re-creating deactivated theories every restart because the active-only check missed them — fixed by checking for ANY existing theory
- Fixture cache was calling `/fixtures/active?sport={sport}` for every discovered sport (500+), each timing out — needed explicit sport filtering

**Action Items:**
- Investigate why ~43% of picks have zero trail entries (created but never got trail updates — timing issue or market disappearing?)
- Monitor overnight: auto-login self-heal, SSE stream stability, trail coverage across non-NBA/MLB leagues
- Files changed: `sport_config.py`, `server/main.py`, `bet365_game.py`, `bet365_mlb_game.py`

### Session (08:20) [value-betting-scanner]

**Context:** Debugging and fixing the VB dashboard trail chart feature and discovering a critical market data bug where all NBA market lines were null.

**Key Exchanges:**
- Trail chart (📈) buttons weren't rendering for all picks → `pick_id` lookup via `pickIdByKey` only matched when the pick was in the pre-loaded cache; fixed by allowing Supabase fallback query by player/prop/side/line
- Charts kept closing on click → SSE auto-refresh (every 3s) calls `render()` which rebuilds the entire table, destroying open chart rows; fixed by tracking open charts and restoring after render
- Charts rendering empty despite picks existing → timezone mismatch: browser (AEST) = Apr 26, Supabase picks stored as Apr 25 (US game dates); fixed by loading yesterday + today + tomorrow in cache
- One specific pick (NAW Blocks Under 0.5) had no Supabase pick at all → led to discovering the root cause

**Decisions Made:**
- Added top-level `line` field to `MatchedMarket.to_dict()` derived from the first main-line book entry, rather than restructuring how the tracker reads lines from per-book data. Simpler change with broader fix.
- Cache loads 3 days of picks (yesterday/today/tomorrow) to handle timezone gaps between AEST browser and UTC-based game dates.

**Lessons Learned:**
- **Critical bug found:** ALL 3,101 NBA markets had `line=None` at the top level. The `line` field only existed per-book inside `BookOdds`, never on `MatchedMarket`. This meant the tracker couldn't create picks for ANY market (pick_id requires a line value). Trail charts, pick tracking — everything downstream was broken.
- Timezone mismatches between browser locale (AEST, UTC+10) and data storage (US game dates) are a recurring issue — always load a window of dates, not just "today"
- When debugging empty data, trace the full pipeline: dashboard showed picks fine (client-side EV calc doesn't need top-level line) but tracker couldn't create them (needs line for pick_id key). Same data, different consumers, different failure modes.
- Auto-refresh that rebuilds DOM will destroy any dynamically inserted UI (charts, modals, expanded rows). Must track and restore state across renders.

**Action Items:**
- Monitor that trail entries are now being recorded for all picks going forward (the fix just went in, so historical data is missing)
- Check if other sports (not just NBA) also had null lines — same `to_dict()` fix should cover them but worth verifying
- Consider whether binary props (Blocks, Steals, Double Double) that always have line=0.5 should have a hardcoded fallback if line is still missing from source data

### Session (11:03) [value-betting-scanner]

**Context:** Debugging and fixing the value betting scanner's trail recording pipeline and scraper configuration

**Key Exchanges:**
- Root cause of empty trail charts identified: `to_dict()` returned `line=None` → no pick_id in Supabase → no trail entries. Fixed by deriving line from first main-line book entry at serialization time.
- User asked about flaws of the `to_dict()` line fix — key issues: line is computed not persisted, different books can have different lines, no `line` field on `MatchedMarket` dataclass, market_key doesn't include line (so two markets at different lines share one key).
- Trail charts were showing "default sharps" instead of theory-specific sharps because `triggered_by` wasn't set on live-computed picks — only on Supabase-loaded picks. Fixed to use best passing theory name.
- TAB scraper wasn't running because `ENABLE_DIRECT_SCRAPERS=1` env var was missing from `start_nba.bat`. Direct scrapers require both sport config AND the env var (line 1445).

**Decisions Made:**
- Old data is NOT poisoned — results/ROI/CLV are valid; only trail depth is missing for ~40-50% of old picks that had `line=None`. No rebuild needed.
- Trail charts should show only the detecting theory's sharps vs soft odds, not all sharps.

**Lessons Learned:**
- The trail pipeline was architecturally correct (3hr gate → change detection → trail write) but the entry point (pick creation) was silently failing due to null lines — a classic "correct pipeline, broken input" pattern.
- `curl_cffi` was missing on scanner-ms, needed for TAB direct scraper.
- Batch file env vars are easy to miss — `start_nba.bat` lacked `ENABLE_DIRECT_SCRAPERS=1` which silently skipped the entire direct_scrapers task.

**Action Items:**
- Consider making `line` a proper field on `MatchedMarket` dataclass instead of only deriving it in `to_dict()` (leaky abstraction risk flagged but not yet addressed).
- Monitor that all new picks going forward generate trails (confirmed 20/20 for both NBA and MLB immediately after fix).

### Session (13:19) [superwin]

**Context:** Morning operations for the value betting scanner — fixing startup reliability, preventing false picks, and monitoring race day performance.

**Key Exchanges:**
- Sandown venue mismatch resolved via fuzzy matching ("Sandown" → "Sandown Park"), clearing a 44-hour stuck pick
- Unsettled picks dropped from 21 → 15, then 0 stuck — all remaining were legitimately waiting to jump
- Discussed false pick risk on startup: bookies ramp up at different speeds, creating fake EV gaps from data lag

**Decisions Made:**
- **Warmup guard added**: Scanner won't persist picks until 2+ racing bookies have 50+ races each. Opportunities still stream to TAKEOVER during warmup but nothing hits the journal until data is reliable.
- **Cron restart at 9am**: Changed from just toggling the DB `enabled` flag to doing a full service restart. Fixes the recurring "enabled in DB but adapter not registered" bug where the service started while bookies were disabled.
- **Warmup threshold**: 2 bookies × 50 races chosen as the gate — balances speed-to-market vs data reliability.

**Lessons Learned:**
- Toggling a DB flag doesn't register adapters if the service started without them — adapters only load at service startup, not on flag change
- Startup data lag between bookies (TAB takes 20-40s REST discovery, TabTouch needs MQTT connect, Betfair resubscribes) creates artificial EV gaps that look like real opportunities
- The pattern of "stream to UI but don't persist" during warmup lets operators see what's happening without creating false journal entries

**Action Items:**
- 9am cron automation should handle bookie startup from tomorrow — no more manual intervention needed
- Monitor CLV quality: today showed -13.1% CLV on 19 picks (1 win, -11.5u) — bookies pricing correctly on these, worth reviewing if EV thresholds need tightening
- Saturday afternoon ramp (Randwick, Flemington, Eagle Farm) is the real test for the system under load

### Memory Flush (13:50) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (15:58) [value-betting-scanner]

**Context:** Building a multi-account Twitter cookie rotation system for a news scraper to avoid rate limits

**Key Exchanges:**
- AdsPower (anti-detect browser, ID: k19yb91n) successfully logs into Twitter when programmatic curl_cffi login gets rate-limited — Twitter can't distinguish it from a real user
- Twitter deprecated `verify_credentials` endpoint (returns 404); switched to GraphQL `UserByScreenName` for cookie validation
- Twitter changed response structure from `timeline_v2` to `timeline` — required scraper patch

**Decisions Made:**
- Use AdsPower as primary login method when curl_cffi gets rate-limited (fingerprinting defeats Twitter bot detection)
- Implement multi-account cookie rotation to avoid rate limits (~300 requests/15min per account, 3-4 accounts = uninterrupted polling)
- Cookie validation now uses GraphQL instead of deprecated REST endpoint

**Lessons Learned:**
- Twitter API `verify_credentials` is dead (404) — use GraphQL `UserByScreenName` instead
- Twitter GraphQL response changed `timeline_v2` → `timeline` — fragile, monitor for future changes
- `validate_cookies` needs proper `features` param or returns empty/fails
- AdsPower anti-detect browser bypasses Twitter rate limits that block programmatic logins
- pycache can cause stale code to run — clear it when debugging persistent issues

**Action Items:**
- Add 2-3 more Twitter accounts to the rotation pool for uninterrupted polling
- Consider making AdsPower the fallback login method in `twitter_auth.py` for cookie refresh
- Monitor Twitter GraphQL schema changes (field names are fragile)

### Session (16:31) [value-betting-scanner]

**Context:** Building a multi-account Twitter scraping system with cookie rotation and automated AdsPower login pipeline

**Key Exchanges:**
- Twitter changed `timeline_v2` to `timeline` in API responses — scraper needed patching
- `validate_cookies` was hitting a dead endpoint; switched to GraphQL validation
- User proposed rotating multiple Twitter accounts to avoid rate limiting (~300 req/15min per account)
- User provided 100 Twitter account credentials for mass onboarding via AdsPower

**Decisions Made:**
- Round-robin cookie rotation across accounts rather than single-account polling — avoids Twitter rate limits entirely
- `ctx.clear_cookies()` for browser-side logout so exported session tokens stay alive for the scraper
- AdsPower browser automation for login flow (username → email challenge → password → 2FA → cookie export)
- Each account login takes ~25s; full pipeline for 100 accounts ≈ 40 min

**Lessons Learned:**
- Twitter API responses can silently rename keys (`timeline_v2` → `timeline`) — scraper needs resilient field lookup
- `validate_cookies` must send full `features` params to GraphQL or it returns empty/fails
- Browser-side `clear_cookies()` doesn't invalidate server-side auth tokens — exported cookies survive logout
- Duplicate init code in `_do_login` caused subtle failures; watch for copy-paste bugs in auth flows

**Action Items:**
- Monitor the 100-account AdsPower login pipeline for completion and failures
- Consider adding AdsPower as a fallback login method in `twitter_auth.py` for when curl_cffi API login is rate-limited
- Verify cookie pool after mass login — confirm all 100 accounts have valid `auth_token` + `ct0`
- Architecture files: `.twitter_accounts.json` (creds), `.twitter_cookies_pool.json` (cookie pool), `twitter_auth.py` (CookieRotator), `twitter_scraper.py` (auto-rotation)

### Session (19:32) [superwin]

**Context:** Value betting scanner — added Cash Multiplier edge, analyzed SuperPicks profitability, and wired per-selection Betfair liquidity data.

**Key Exchanges:**
- TAB Cash Multiplier is a flat `odds * 1.1` boost (not a lookup table like SuperPicks). Added as new edge `racing-cash-multi` with `boost_multiplier` field in criteria.
- SuperPicks profitability deep-dive: **+70.0u, +38.6% ROI** across 181 settled picks over ~1.5 days
- Harness races massively outperform: +157% ROI vs +15.6% thoroughbred, ~0% greyhound
- EV band analysis: sub-12% EV picks lose money (-18.7% ROI); 12%+ EV is where all profit sits (+79.2% ROI on 106 picks)
- Detection count sweet spot: 4-5 scans = +101% ROI, single-scan picks underperform
- Counterintuitive liquidity finding: $5K+ liquidity picks are worst (-40.5% ROI) — efficient markets leave no edge. $200-$1K is the profitable zone.

**Decisions Made:**
- Keep min liquidity at $200 (don't raise it — low-mid liquidity is where the edge lives)
- Strong case to raise SuperPicks `min_ev` from 10% to 12% (cuts 75 losing picks worth -14u)
- Scanner `liquidity` column now uses `selection_matched` (per-runner) instead of `total_matched` (per-market) — more actionable signal

**Lessons Learned:**
- High Betfair liquidity = efficient market = less edge for boosted odds. The soft-book mispricing exists precisely where Betfair is thin.
- `total_matched` from Betfair stream was being stored at market level for every runner — misleading. The `trd` field on runner changes gives per-selection traded volume.
- Betfair stream provides `trd` (traded volume ladder per runner as `[price, size]` pairs), REST provides `runner.totalMatched` directly.
- `boost_multiplier` pattern (flat %) vs `boost_table` pattern (lookup table) — scanner now supports both via edge criteria.

**Action Items:**
- Consider updating SuperPicks edge to `min_ev=12` (discussed but not yet applied)
- Monitor Cash Multiplier edge performance as picks accumulate
- Sample is still early (181 picks) — need several more days to confirm SuperPicks signal is real vs hot streak

### Memory Flush (20:07) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (20:37) [value-betting-scanner]

**Context:** Batch Twitter/X account login pipeline — warming up a pool of accounts with automated browser logins.

**Key Exchanges:**
- Monitored ~30 login attempts with 60s cooldown; ~27 succeeded, ~3 failed (single-streak failures = bad accounts, not throttling)

**Decisions Made:**
- 60s cooldown confirmed as the sweet spot for Twitter login pacing — no throttle-induced failure streaks observed
- Single login failures diagnosed as bad accounts (not rate limiting) when the streak counter resets to 1 immediately

**Lessons Learned:**
- Distinguish throttle vs bad-account failures by watching the failure streak counter — single fails that reset are account-quality issues, consecutive fails would indicate throttling
- Empty page title on login (`title: ` blank) is benign — cookies still export successfully
- Title variations ("Home / X" vs "X" vs empty) are all valid login confirmations

**Action Items:**
- Pipeline still running — ~75 accounts remaining to process
- Monitor for consecutive failure streaks which would signal throttle issues requiring cooldown adjustment

### Session (21:09) [value-betting-scanner]

**Context:** Batch X/Twitter account login pipeline — monitoring automated logins with 60s cooldown

**Key Exchanges:**
- Monitored accounts #28–40 in the login queue; reached the halfway mark (~40 of ~80 total)
- Three accounts failed on first attempt (streak:1) and were skipped as bad accounts: @FairJoey64240, @manysnake48606, @enoughdory29246

**Lessons Learned:**
- 60-second cooldown between logins eliminated throttle issues — zero throttling observed across the entire run
- Failed accounts all showed streak:1 (first-time failures), indicating bad credentials rather than rate-limiting

### Session (22:10) [superwin]

**Context:** Reverse-engineering TabTouch sports betting API for player props (NBA), discovering it's powered by Kambi white-label platform.

**Key Exchanges:**
- Navigated AdsPower browser to TabTouch basketball page and captured network traffic
- Discovered TabTouch sports betting is powered by **Kambi** (white-label sportsbook), completely separate from their racing API
- Captured 426 betOffers including 242 player prop markets from a single NBA game endpoint
- Captured 763 WebSocket push messages with live odds updates from Kambi push service

**Decisions Made:**
- Document the Kambi API structure in `docs/TABTOUCH_PLAYER_PROPS.md` since it's a completely different platform from TAB's custom API
- Focus on the open REST API rather than WebSocket for initial implementation since the REST API requires no auth at all

**Lessons Learned:**
- **TabTouch sports = Kambi white-label** — the API is at `ap.offering-api.kambicdn.com`, completely public, no auth/cookies/TLS fingerprinting needed. Much easier than TAB.
- **Kambi uses full player names** ("Anthony Edwards" not "A Edwards") — eliminates the name normalization problem TAB had
- **Kambi provides stable `participantId`** values across all markets for the same player — makes cross-market correlation trivial
- **Kambi betOffer types**: type 2 = over/under (line-based), type 1 = 1x2/moneyline — player props are type 2 with `criterion.id` identifying stat category
- **Kambi push** uses Socket.IO at `push-ap.offering-api.kambicdn.com` — sends real-time odds/line updates (~737 messages in 120s)
- **Two WebSocket streams**: AWS IoT MQTT (racing/tote) and Kambi push (sports odds) — completely independent systems
- AdsPower browser session management is finicky — profile WebSocket endpoints go stale when browser restarts, and the browser may open to a cached page (Twitter) instead of the requested URL

**Action Items:**
- Build Kambi scraper adapter for value betting scanner (simpler than TAB — no auth, full names, stable IDs)
- Capture MLB data from Kambi to confirm player prop structure (`baseball/mlb` endpoint)
- Map Kambi `criterion.id` values to stat categories (points, rebounds, assists, etc.) — partial mapping done in the doc
- Consider using Kambi WebSocket push for real-time odds instead of polling REST API

