# Daily Log: 2026-04-26 (lcash)

## Sessions

## Memory Maintenance

### Session (07:42) [value-betting-scanner]

**Context:** Deploying multi-league EV betting scanner expansion across basketball and baseball leagues on VPS

**Key Exchanges:**
- Expanded from NBA/MLB to 100 basketball + baseball leagues (94 basketball, 6 baseball) with auto-discovered Pinnacle moneyline theories
- User caught that 408 prediction market leagues (soccer, tennis, hockey, esports) can't be accessed via Optic Odds API — only basketball and baseball SSE streams work
- System successfully found EV picks across 6 leagues: NBA (568), MLB (412), Japan B1 (13), KBO (5), Germany BBL (1), Greece A1 (1)
- Morning check revealed ~43% of picks (13/30) have zero trail entries despite being tracked

**Decisions Made:**
- Deactivated 408 prediction market theories — harmless but noise; left them for potential future API upgrade
- Restricted SSE streams and fixture cache to only `["basketball", "baseball"]` to eliminate 16 blocked-sport 400 errors and timeout spam
- Theory existence check changed to look for ANY theory (not just `is_active=true`) to prevent duplicate creation on restart
- Added auto-login self-heal to both bet365 scrapers (NBA + MLB) — detects session expiry via CDP tab URLs, triggers `login_bet365()`, resets on success

**Lessons Learned:**
- Optic Odds SSE multiplexes all leagues within a sport on one connection — 100 leagues need only 2 streams
- Bulk Supabase INSERTs (~500 individual calls) are slow (~2-3 min) but only needed on first run; subsequent restarts skip existing theories
- `auto_discover_all_leagues()` was re-creating deactivated theories every restart because the active-only check missed them — fixed by checking for ANY existing theory
- Fixture cache was calling `/fixtures/active?sport={sport}` for every discovered sport (500+), each timing out — needed explicit sport filtering

**Action Items:**
- Investigate why ~43% of picks have zero trail entries (created but never got trail updates — timing issue or market disappearing?)
- Monitor overnight: auto-login self-heal, SSE stream stability, trail coverage across non-NBA/MLB leagues
- Files changed: `sport_config.py`, `server/main.py`, `bet365_game.py`, `bet365_mlb_game.py`

