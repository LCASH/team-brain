# Daily Log: 2026-04-25 (lcash)

## Sessions

## Memory Maintenance

### Session (07:28) [value-betting-scanner]

**Context:** Value betting scanner — testing new OpticOdds API key, discovering baseball unlocked, deploying updates, verifying overnight stability.

**Key Exchanges:**
- Tested new OpticOdds API key (`97f35e23...`) against current key — initially appeared identical (basketball + football only)
- User insisted on testing every sport — thorough test confirmed only basketball + football SSE streams active at first
- **Baseball suddenly started working** — fixtures, SSE streams, and player-results all accessible on the new key
- 111 basketball leagues with active fixtures (not just NBA) — Australia WNBL1, Japan B1, France LNB Pro B, etc.
- 15 baseball leagues: MLB, KBO, NPB, College Baseball, MiLB AAA, CPBL, plus international leagues
- VPS access instructions shared: `sshpass -p 'TAkeover69$T' ssh root@170.64.213.223`, files at `/mnt/takeover_data/value-betting/`

**Decisions Made:**
- Swapped to new API key on VPS + mini PC (both scanner-new and scanner-ms)
- Kept MLB fallback resolver code but OpticOdds `market_stats` now primary — all 17 prop mappings confirmed (hits, bases, strikeouts, HRs, earned runs, etc.)
- Disabled reconciliation in relay mode to fix pick flashing on dashboard
- Added sport param to `/api/v1/resolve` endpoint

**Lessons Learned:**
- OpticOdds API access can change without notice — sports that were blocked can become enabled (baseball went from blocked to fully working)
- Always test every sport individually when checking API key permissions — initial spot checks missed working sports
- The 22 SSE streams that appeared "alive" were actually in retry loops getting rejected — tasks staying alive ≠ receiving data
- OpticOdds baseball `market_stats` uses same format as basketball — resolver code works as-is without changes
- Chunk size of 50 needed for resolver to avoid API overload

**Action Items:**
- NRL and AFL servers still not running on mini PC (ports not listening)
- Auto-resolver found 4 stale MLB dates (Apr 19-22) pending resolution
- VPS health shows "degraded" despite all components healthy — threshold check may need tuning
- First MLB resolve attempt hit Supabase disconnect — monitor auto-resolver hourly cycle for successful completion

### Session (07:58) [value-betting-scanner]

**Context:** Morning health check and deep dive into the value betting scanner system — OpticOdds API access, bet365 status, trail recording architecture

**Key Exchanges:**
- Confirmed bet365 is logged in on mini PC — identified indicators (tab titles showing "Online Sports Betting" not "Login", URLs on `#/AC/` sports pages not `members.bet365.com`, 5,440 odds streaming)
- TAB soft book comes through OpticOdds (not a direct scraper) — 546 markets, 58% fresh within 2 min
- Celtics vs 76ers has 479 markets across two fixture name variants — fewer +EV picks means the market is more efficient (higher-profile game), not a data problem
- Trail health: 90% of 500 picks have trail entries; sharp trails avg 7.1 entries/pick (best gap 12s), soft trails avg 3.0 entries/pick (best gap 37s); distribution is bimodal

**Decisions Made:**
- None yet — user was presented with the option to add periodic heartbeat trail entries (every 10-15 min even if unchanged) but hasn't decided

**Lessons Learned:**
- Trail recording is **change-only** — soft trails write when odds/line change by >0.001, sharp trails write when any sharp book's odds hash changes
- This creates blind spots for timing analysis: can't distinguish "odds stable" from "stopped watching"; closing odds may be hours stale if nothing moved
- Sharp trails update ~7x more than soft because 5+ books means more frequent hash changes
- For ROI backtesting change-only is fine (opening/closing captured); for "optimal bet timing" analysis you'd need interpolation or heartbeat entries
- Bimodal trail distribution: some picks have 50-700 entries (hours of tracking, active odds movement), many have just 1-2 (new or illiquid props)

**Action Items:**
- Consider implementing periodic heartbeat trail entries for better timing analysis (proposed but not yet decided)
- NRL and AFL servers still not running on mini PC
- MLB resolver had a Supabase disconnect — auto-resolver should retry within the hour

### Memory Flush (08:28) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:29) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:30) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (08:32) [value-betting-scanner]

**Context:** Overhauling the VB scanner's news-driven pipeline — pre-computing EV in the workspace and redesigning the pick philosophy from reactive to predictive.

**Key Exchanges:**
- Full system audit identified 7 flaws ranked by severity — EV mismatch between workspace and pick_writer, hardcoded CLI path, fake freshness check, fragile output parsing
- User clarified the core thesis: **we're beating sharps to news**, not reacting to current EV. A pick that looks -3% EV now may become +8% once Pinnacle adjusts. Resolver validates over time.
- Tested Claude Code's built-in WebSearch — works well for real-time sports news ($0.05 with Haiku, $0.15-0.30 with Sonnet). No Brave API needed.

**Decisions Made:**
- **Removed EV validation gate** — agent picks based on conviction, not current sharp EV. Picks stored with reasoning, resolver validates over time.
- **Stage 1 = pure binary gate** — just "worth analyzing?" + extract team/player/event_type. No impact scoring (Stage 2 handles that).
- **Killed freshness.py** — redundant since Claude Code session can web search itself during Stage 2 analysis.
- **Added `news_source` + `agent_id` to every pick** — enables multi-agent A/B testing down the line (e.g., `--agent news_v2_aggressive`).
- **Pre-compute all EV in workspace** using Pinnacle devig (multiplicative) + Poisson/logit interpolation — agent reads a flat sorted table, zero calculation.

**Lessons Learned:**
- `subprocess.run(..., cwd=path)` is the correct way to set working directory for Claude CLI — positional arg doesn't work with `-p` flag. `analyst.py` already does this correctly.
- Each `subprocess.run()` spawns a fresh Claude Code session — no memory between events. Good for isolation, ~30s startup overhead.
- DuckDuckGo instant answer API returns Wikipedia-style abstracts, not real-time news — useless for breaking sports news like injury tweets.

**Action Items:**
- Commit the cwd fix and run a full end-to-end test
- CLI path (`shutil.which("claude")`) needs fixing before deploying to VPS
- Consider keeping Claude Code sessions alive to reduce startup overhead (future optimization)
- Build out multi-agent comparison once enough picks are stored for statistical significance

### Memory Flush (08:34) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:40) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (08:41) [value-betting-scanner]

**Context:** Monitoring NBA injury news for value betting impact during Thunder vs Suns playoff series.

**Key Exchanges:**
- Ingested Shams Charania report: Jalen Williams (OKC) ruled out Game 3 vs Phoenix — Grade 1 hamstring strain from Game 2. Classified as `injury_out` event.

**Lessons Learned:**
- Grade 1 hamstring strains typically carry a 1-3 week recovery window — worth monitoring for Game 4+ availability and line movement across the series.

### Memory Flush (08:48) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:49) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:49) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (08:50) [value-betting-scanner]

**Context:** Monitoring NBA injury news for value betting implications during playoffs.

**Key Exchanges:**
- FantasyLabsNBA reported Kevin Durant (ankle) questionable for Game 3 tonight (2026-04-25), Rockets vs Lakers. Rockets down 0-2 in series.
- Classified as a line-moving injury event — star player questionable in a playoff game with confirmed tip time.

**Decisions Made:**
- Flagged as relevant for the VB scanner pipeline — fresh injury designation on a star player in an active playoff series warrants line monitoring.

**Action Items:**
- Monitor for Durant's final status update (probable/out) closer to game time — line movement opportunity window.
- Check if VB scanner picks up any value on Rockets/Lakers lines shifting from the questionable designation.

### Memory Flush (08:50) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:53) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:59) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (09:04) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (09:08) [value-betting-scanner]

**Context:** Building and testing the VB scanner's news-driven picks pipeline — specifically the Sonnet agent that analyzes injury news + full sportsbook odds to generate picks.

**Key Exchanges:**
- Sonnet initially timed out at 300s with 0 picks when given the full 90KB workspace (1500+ market rows)
- Discussed that the agent needs ALL odds per game (not just pre-filtered +EV), grouped by player like a sportsbook page — the agent should reason about which markets will shift
- Discussed prioritization: not just "closest game first" but **most recent news + closest to tipoff = highest urgency**

**Decisions Made:**
- Increased Sonnet timeout from 300s to 600s (10 min) — 300s too tight for 100KB workspace + web searches
- Rewrote odds.md format: full player prop cards (every prop, every book, Pinnacle true line, EV column) grouped by player — 764 lines, 48KB for a single game
- Keep the full odds but trim the format — agent sees the complete sportsbook picture, not a pre-filtered EV table
- Smart pick that's currently -EV is valid if agent explains it's "ahead of the market" — this validates the news-driven thesis

**Lessons Learned:**
- Sonnet + 100KB workspace + web searches takes ~3-4 minutes — 5min timeout is too tight, 10min works
- Workspace trimming matters: 90KB → 48KB odds made the difference between timeout and success
- The agent found real Game 1 precedent data (KD out April 19) and used it to justify picks — historical context in the workspace pays off
- Marcus Smart pick demonstrates the core thesis: news-driven play that's -EV now but will become +EV once books reprice

**Action Items:**
- Monitor tonight's Rockets vs Lakers picks for resolution/grading
- Consider further workspace optimizations if multi-game scenarios cause timeouts again
- The resolver will grade picks post-game via `triggered_by=news_v1_injury_questionable`

### Session (09:38) [value-betting-scanner]

**Context:** Wiring the news agent pipeline end-to-end — OpticOdds box scores, workspace cleanup, and backtest data model.

**Key Exchanges:**
- Discussed how the news agent spawns a fresh Claude session each run (`subprocess.run`), with no memory between events
- Workspace files pile up in `/tmp/news_agent/` (~100KB per run); decided to delete immediately after picks stored in Supabase
- Identified that reasoning audit trail (per-pick `ai_reasoning`, `ai_impact_chain`, per-event `analysis_summary`) must live in Supabase to replace temp files
- Reviewed backtest chain: tweet → `news_agent_events` → `nba_tracked_picks` via `news_event_id` FK

**Decisions Made:**
- **Delete workspaces immediately** after Supabase storage (`shutil.rmtree`), not 7-day TTL — reasoning columns in DB are the audit trail
- **No RLS on `news_agent_events`** — consistent with all other tables, accessed via service key
- OpticOdds team names live in `home_competitors[0].name` / `away_competitors[0].name`, not `home_competitor_name`

**Lessons Learned:**
- `triggered_by` had double-prefix bug: agent_id already contains `news_` so it became `news_news_v1_injury_out`
- Agent instructions didn't ask for `ev_pct` output, so `triggered_ev` was always 0 — instructions must explicitly request every field you want stored
- `player_minutes` stat key from OpticOdds returns `None` — key name likely different, needs investigation

**Action Items:**
- Run migration `029_news_agent.sql` in Supabase SQL editor (creates `news_agent_events` table + adds reasoning columns to `nba_tracked_picks`)
- Fix `player_minutes` stat key mapping from OpticOdds box scores
- Verify agent output includes `ev_pct` field after instructions update

### Memory Flush (09:41) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (09:41) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (10:10) [value-betting-scanner]

**Context:** Setting up twscrape (Twitter scraper) for the value-betting-scanner project, hitting Cloudflare/anti-bot blocks.

**Key Exchanges:**
- twscrape installed but `python3` was pointing to a different Python than the one with twscrape — needed full path `/opt/homebrew/opt/python@3.14/...` or `pip3 install twscrape`
- twscrape's programmatic login to X.com returns 403 Cloudflare block from Australian IP (same pattern as bet365)
- Attempted Playwright with real browser to bypass Cloudflare — X's anti-automation JS detects Playwright and clears input fields, preventing login

**Lessons Learned:**
- twscrape's built-in `login_all()` is blocked by Cloudflare from AU IPs — cannot do headless/programmatic X.com login
- Playwright with `channel: "chrome"` still gets detected by X.com's anti-bot JS (unlike bet365/TAB where it worked) — X clears typed input fields even in real browser mode
- For X.com specifically, manual browser login + cookie extraction (`auth_token` + `ct0`) is the viable path, or potentially `curl_cffi` with Chrome TLS fingerprinting

**Action Items:**
- Get X.com cookies (`auth_token`, `ct0`) from manual browser session to feed into twscrape
- Change the `hgdluke` Twitter account password (credentials were shared in terminal output)

