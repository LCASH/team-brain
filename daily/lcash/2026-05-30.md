# Daily Log: 2026-05-30 (lcash)

## Sessions

## Memory Maintenance

### Session (09:44) [value-betting-scanner]

**Context:** Running full v3 value-betting pipeline health check via /VB-V3-Healthcheck skill

**Key Exchanges:**
- Ran end-to-end health check across all pipeline layers (Eve scraper, VPS proxy, dashboard, cache, AdsPower browsers, per-sport breakdown)
- JSON truncation issue on 14MB per-sport response when piping through SSH — had to retry directly from Eve

**Decisions Made:**
- MLB stale count of 51 (threshold 50) deemed acceptable — likely in-progress games, not a real issue
- Cache first-request timeout (30s) classified as transient cold-cache stall, not a bug — monitor for recurrence

**Lessons Learned:**
- SSH-piped Python commands can silently truncate large JSON responses (~14MB) — run large queries directly on Eve instead
- Bet365 MLB wizard parser is missing the `market_id` stamping code path that NBA has — results in 0% stable-ID coverage for MLB
- One-sided EV >100% picks (453 in 24h, threshold >20) are the known "anytime hit → 3+ hits" extrapolation phantom-EV pattern — need to verify none are reaching dashboard/picks views and check for missing `max_ev` cap on theories
- BetIT MLB showing 0 markets when 100-400 expected — needs investigation whether it's a no-slate day or adapter regression

**Action Items:**
- Fix Bet365 MLB market_id in `ws_mlb_v4.py` (estimated 5-10 LOC fix to match NBA wizard's MI-stamping)
- Investigate one-sided >100% EV cohort — sample 10, confirm they're not surfacing in dashboards/picks
- Check BetIT MLB adapter liveness: `grep "BetIT.*mlb" ~/value-betting-scanner/v3.log | tail -10` on Eve
- Monitor cache cold-start timeout for recurrence

### Session (09:53) [superwin]

**Context:** Reviewing SuperWin racing betting system health and confronting overconfident edge analysis after resolver math fix revealed inflated PnL

**Key Exchanges:**
- Full system diagnosis run: TAB browser dead (120s timeouts), settlement scanner stuck at 108 pending markets (444 picks unresolved), Eve/bet365 still down
- Today's honest PnL: **-339u / -34.1% ROI** on 994 settled picks — first full day after resolver fix vs yesterday's reported +20% ROI
- Racing superpicks biggest loser: -77.8u on 185 picks (11.4% strike vs ~30% expected)
- Catalogue snapshot "missing" alert was false positive during 9am cron restart window

**Decisions Made:**
- Acknowledged that yesterday's +198u/+20% ROI day was **not a clean signal** — it was partial-day data overlaying picks placed under broken math
- The "winning formula" for BoostBet run-2nd-3rd (+44.9% ROI on 127 picks) was **over-fitted pattern matching** from ~100 single-signal slices with no out-of-sample validation — should be treated as hypothesis, not finding
- Edge-research skill catalog's 9 "learnings" were pre-seeded from one day's data — should be marked "hypotheses pending validation"
- **Pause new staking decisions** until honest math accumulates for 7-14 days

**Lessons Learned:**
- Correcting measurement (resolver fix) can reveal that previously "profitable" edges were actually losing — don't celebrate math fixes as wins until recalibrated data accumulates
- Multiple comparison problem: testing ~100 signal slices then ~30 compound rules will surface false positives by chance — demand out-of-sample validation before calling anything a "formula"
- Anti-pattern identified: "every time you asked a question, I produced an answer" — honest uncertainty > confident wrong answers
- Snapshot-missing alert fires during cron restart window (ExecStartPre deletes file, first snapshot write is 30s later) — consider widening watcher threshold to 90s during first 60s after service start

**Action Items:**
- Let 7-14 days of honest-math data accumulate before drawing edge conclusions
- Do NOT deploy "winning formula" to Venom without out-of-sample validation on held-out time window
- Revisit edge-research skill catalog after sufficient sample size — mark all entries as hypotheses
- Fix TAB browser (restart superwin service)
- Investigate 108 stuck settlement markets (possibly NZ/venue/race-type specific)
- Clean up 86 leaked SSH sessions (add ClientAliveInterval 60 to sshd_config)
- Diagnose Eve separately for DNS resolution / bet365 failures

### Session (09:54) [value-betting-scanner]

I'll investigate both codebases carefully before making any changes. Let me start by understanding the current implementations.Both investigations complete. Here's what we found:

---

## Fix 1: Bet365 MLB `market_id` — Root Cause & Fix

**Root cause:** `ws_mlb_v4.py` never reads the `MI` field from `MA` segments. NBA's `ws_nba.py` does this with a `current_mi` state variable.

**4 targeted changes in `ws_mlb_v4.py`:**

1. Add `current_mi: str = ""` state variable (after `current_fi`)
2. Reset `current_mi = ""` when entering new EV section
3. Extract MI from MA segments: `mi = f.get("MI", ""); if mi: current_mi = mi`
4. Pass `market_id=current_mi` to both `ScrapedOdds()` constructors

Exact mirror of how NBA does it. ~6 LOC, no behavioral change except populating the field.

---

## Fix 2: MLB 4-Book Power EV Cap — Safest Path

The agent found a **zero-code-change shortcut**: tracker.py already has a `max_ev_if_sharps_gte` filter (lines 1403-1408). We can abuse it with `sharps_gte: 1` (always true) to cap EV at 50%:

```sql
UPDATE public.nba_optimization_runs
SET weights = jsonb_set(
    weights,
    '{max_ev_if_sharps_gte}',
    '{"sharps_gte": 1, "max_ev": 50}'::jsonb
)
WHERE name = 'MLB 4-Book Power';
```

This blocks all 186-476% phantom picks while letting real EV (3-20%) through. No deploy needed — takes effect on next theory reload.

---

Want me to apply both? I'll do the `ws_mlb_v4.py` code change first (can test locally), and the SQL can be run against Supabase directly.

### Session (10:17) [superwin]

**Context:** Diagnosing and fixing production outages in the superwin value-betting scanner, then running venue-linking maintenance.

**Key Exchanges:**
- REST timeouts (121-123s) cascading to kill WS subscriptions on TAB adapter, zeroing out picks during racing window
- Neds "browser may have crashed" error traced to generic 120s timeout wrapper in main.py:150 — Neds is pure REST, message was misleading
- Betfair settlement fix: `place_bsp_by_sel` lookup in betfair.py:1473 was broken — 3 of 4 call sites don't pass `place_bsp`/`place_ltp` in runner dicts. Fix: fall back to `rd.get("place_bsp")` as before
- Venue-linking scan found **65 unmapped TAB abbreviations** (49 unambiguous, 16 ambiguous due to multi-discipline venues sharing codes)

**Decisions Made:**
- Deploy betfair settlement fix + restart to clear stuck connection pools (Neds, TAB) — low risk, 2 min downtime
- Skip 16 ambiguous TAB abbreviations because link API doesn't support race-type-keyed aliases — linking e.g. `BEN` to one canonical would cross-merge greyhound/thoroughbred races
- Adapter bugs (race names leaking into venue field for betr/neds/pointsbet) flagged as separate follow-ups, not venue-alias work

**Lessons Learned:**
- The duplicate-venue scan script has false positives: it flags races sharing (race_type, race_number, date) at *different real venues* as duplicates
- TAB sends uppercase abbreviations (e.g. `MOE`) which become the canonical_id if TAB ingests first — downstream bookies sending `Moe` then fail to merge
- The race catalogue isn't cleaning resulted races aggressively enough — found 2-day-old stale race data in cache
- Generic error wrappers (like the 120s timeout → "browser may have crashed") make triage harder — the message should reflect the actual transport (REST vs browser)

**Action Items:**
- Auto-link the 49 unambiguous TAB abbreviation → canonical venue mappings (pending user confirmation)
- Fix adapter parsing bug: race names appearing in venue field for betr/neds/pointsbet (`@Thegardensgrnsw Maiden`, `Syngenta Maiden Plate`, etc.)
- Add race-type key to venue link API so ambiguous abbreviations (ADD, BEN, GBN, etc.) can be linked per discipline
- Investigate stale race cleanup — resulted races persisting 2+ days in cache
- After restart: verify settlement scanner draining 108 pending markets, TAB streaming, picks firing, Neds WS receiving frames

### Memory Flush (10:47) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (11:47) [superwin]

**Context:** SuperWin VPS diagnosis, venue pollution cleanup (Thread 2), and TAB cold-start reliability root-cause analysis

**Key Exchanges:**
- Full diagnose run showed system mostly healthy but TAB at 0 races despite discovery recovering 578 races, bet365/Eve dead, and 335 stuck picks from old markets
- Thread 2 cleanup: identified and deleted 33 polluted aliases + 25 polluted canonicals (race titles like "Syngenta Maiden Plate" incorrectly stored as venue names). Real venues (Moe, Rosehill, Awapuni) were already in DB — aliases were just pointing to wrong canonicals
- TAB reliability root cause found: cold-start fetches all 578 races in one cycle → exceeds 120s timeout wrapper → no races ever land → next cycle repeats because cached_specs never got assigned

**Decisions Made:**
- Thread 2 executed immediately: aliases deleted first (FK dependency), then canonicals, then cache reload. 364 canonicals remain, 0 pollution
- TAB fix proposed but **not yet shipped** — awaiting user confirmation. Fix: skip cold races on first cycle, backfill in chunks of 80 from cycle 2 onward (COLD_CHUNK pattern)

**Lessons Learned:**
- TAB cold-start reliability failure is a **load-budget problem**, not a discovery problem. Discovery works fine (Tier 3 rescue via `page.evaluate`). The bottleneck is detail-fetch trying to process 578 races in one 120s window
- The `needs_discovery` flag causes a vicious cycle: timeout before assignment → flag stays true → next cycle tries all 578 again → never recovers
- Venue pollution came from `auto_learn` using race name instead of meeting name when creating canonicals — Thread 1 fix (regex filtering Sprint/Maiden/Plate in race names) prevents new pollution; Thread 2 was pure DB cleanup

**Action Items:**
- Ship TAB cold-start fix (`tab.py:504` — COLD_CHUNK=80 backfill pattern) once user confirms
- Eve restart needed for bet365 recovery (separate Tailscale session)
- 335 stuck picks will auto-void via 72h orphan sweep — no manual action needed
- 86 stale SSH sessions still accumulating (same as yesterday, not yet addressed)

### Session (11:58) [superwin]

**Context:** Monitoring and optimizing the TAB WebSocket reconnect pipeline and fixing noisy regression alerts in the v3 value-betting scanner.

**Key Exchanges:**
- Monitored TAB worker reconnect timing live — observed a single disconnect/resubscribe cycle completing in **3 seconds** (down from 6-15s pre-optimization)
- Full validation of the no-sleep reconnect: 3s gap, no thrashing, 118 TAB races populated, no new error patterns
- System stable for 68 min straight post-deploy; all 9 bookies online

**Decisions Made:**
- **Replaced per-cycle regression detection with catalogue-coverage-based checks.** Rationale: tiered-fetch bookies (betr/boostbet/tab) rotate hot/warm/cold subsets each cycle, causing wildly different `valid_count` per poll. Coverage (how many races the bookie has data on in the catalogue) is the semantically correct metric and is stable across tiered fetches.
- **Baseline uses rolling MAX** so transient ingest-ordering blips can't lower the bar and trigger false positives.
- Decided remaining 3s reconnect gap (vs 2s target) is acceptable — further optimization would require refactoring TLS/EngineIO/namespace handshake parallelism for diminishing returns.

**Lessons Learned:**
- TAB rotation cadence is **variable**, not fixed at 3 min — observed 7+ min uptime stretches. Fast reconnect may reduce apparent rotation frequency.
- Catalogue persistence is already "once added, never removed until day rollover" — but a **service restart wipes the catalogue** (no persistence/replay). `BookieStatus.race_count` also swings cosmetically for tiered-fetch adapters even though actual odds in `/api/v1/odds` don't fluctuate.
- Alert noise was masking real issues — after refactor, alerts dropped from 100 to 1 (the legitimate edge-resolver backlog), making the dashboard actionable.

**Action Items:**
- Investigate persistence/replay logic so service restarts don't wipe the day's catalogue
- Fix cosmetic `race_count` swinging for tiered-fetch adapters on dashboard sidebar
- Run `/check-results` pass for the 268 picks stuck across 31 races (Betfair settlement scanner missed those markets)
- Continue observing TAB reconnect timing over more disconnect samples (current sample size = 1)

### Session (11:58) [superwin]

**Context:** Deep audit of racing-mult ("The Mult") settlement bug in the value-betting scanner, following discovery that it was the worst-performing edge at −26.5% ROI.

**Key Exchanges:**
- User flagged that racing-mult was missing from the 5-mode confirmation score analysis
- Initial hypothesis was a settlement bug (CLV using WIN-market BSP instead of PLACE-market BSP) — turned out PnL was correct but CLV was blind
- `_handle_race_result()` fetches `place_bsp_by_sel`/`place_ltp_by_sel` correctly, but the result-builder loop reads `rd.get("place_bsp")` from settlement scanner data which never populates those keys → all racing-mult CLV was null
- 3 of 34 VOIDs were misclassified (Nowra R2 anomaly) — only +2.3u impact, immaterial

**Decisions Made:**
- Fixed CLV blindness in `betfair.py` — read from locally fetched place BSP/LTP dicts instead of settlement scanner's empty fields. Deployed live.
- Racing-mult PnL (−120.4u, −26% ROI) is the real economic outcome, not a bug — confirmed by auditing 15/15 WIN picks
- Cannot backfill historical CLV — Betfair `listMarketBook` returns no SP data on markets settled >24h ago
- Saved full audit as THEORY T-MULT in edge-pick-theories.md

**Lessons Learned:**
- Racing-mult's `min_ev: 1%` gate is too loose for place markets (median raw odds ~$1.50). Below 5% EV structurally loses. Above 10% EV is detection noise (inverse correlation = smoking gun for noisy edge).
- High detection EV being the WORST ROI band (−57.9% at ≥20%) is a reliable indicator that the "edge" is noise, not signal
- Place-market edges need their own CLV pipeline — can't assume WIN-market result fields carry place data
- The "wait for confirmation" rule from T-CONFIRM does NOT apply to racing-mult — it's a "fast" mode like cash-multi where TAB corrects place odds in seconds

**Action Items:**
- Verify CLV populates on next AU racing-mult settlement (tonight)
- Bump racing-mult `min_ev` from 1% to 5%
- Apply confirmation score logic: stake on `trail ≤ 2 AND ev_last ≥ 8` (fast mode, don't wait)
- Don't run racing-mult and racing-cash-multi simultaneously on the same race (correlated exposure)
- Investigate the `same_race_count=3` cursed bucket from run-2nd-3rd (still open from earlier analysis)
- Saved 426-line in-depth mode-specific theory to edge-pick-theories.md as THEORY T-CONFIRM covering all 5 modes + unified confirmation_score dispatcher

### Session (12:31) [superwin]

**Context:** Debugging TAB sportsbook cold-start reliability failures on VPS — discovery timeout loop preventing race data from flowing

**Key Exchanges:**
- Initial diagnosis identified detail-fetch overload at tab.py:504 as the bug (hot+warm+cold fetching all 578 races on cold start, exceeding 120s wrapper timeout)
- Shipped a fix (rotate 80 cold races per cycle instead of all-at-once), but it didn't help because **discovery itself was returning 0 races** — the fix targeted a real but downstream problem
- Ran 3 parallel probes: (1) journal page-swap events, (2) VPS IP-level connectivity, (3) diff between working vs broken windows
- Probes revealed: not IP-blocked, no page swaps, both windows had identical code paths — the "working" window just got lucky after 11 min of Akamai warmup
- User's key insight: "as long as odds are quick… discovery doesn't have to be so quick because WS is what matters"

**Decisions Made:**
- Rejected "stale interceptor flag" hypothesis from Explore agent — evidence showed no page swaps occurring
- Identified real root cause: `DISCOVERY_INTERVAL = 60s` + no backoff on failure = 60 Akamai requests/hour during cold-start failure, triggering cookie/fingerprint throttling
- Proposed 3-line fix: discovery interval 60s→3600s, exponential backoff on failure (1→5→15→30 min), interceptor wait 15s→60s
- Thread 2 cleanup also shipped: 25 polluted canonicals + 33 aliases deleted from Supabase

**Lessons Learned:**
- TAB cold-start thrashing pattern: when `_cached_specs` is empty, discovery retries immediately with no backoff → Akamai flags the bot-shaped traffic → recovery takes hours
- Akamai throttling is at cookie/fingerprint layer, NOT IP layer — homepage loads fine, browser data fetch is gated
- WS is the real odds pipeline; discovery is just bootstrap for the proposition_id list. Architecture should reflect that priority
- Don't chase code bugs when the symptom is "works after 10-15 min warmup" — that's anti-bot behavioural gating
- The detail-fetch overload (all 578 races on first cycle) IS a real bug but only manifests after discovery succeeds — fix is correct but was targeting wrong failure mode for today's outage

**Action Items:**
- Ship the 3-change discovery interval + backoff + interceptor wait fix
- Verify TAB recovers after Akamai cooldown period
- Monitor WS subscription count vs new races over time to validate WS self-sufficiency assumption
- Documentation already shipped: `tab-cold-start-reliability.md`, `venue-meeting-name-resolver.md`, MEMORY.md updated, diagnose skill pitfalls 7a/7b added

### Memory Flush (13:07) [server]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

