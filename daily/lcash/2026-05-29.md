# Daily Log: 2026-05-29 (lcash)

## Sessions

## Memory Maintenance

### Session (00:09) [superwin]

**Context:** Overnight VPS monitoring session — SuperWin crash watcher running on remote server, tracking SSH stability and service health.

**Key Exchanges:**
- SSH drops started occurring every ~15-25 min; initially seemed random but pattern emerged after 5th drop
- Root cause diagnosed: **53 stale SSH sessions** piled up from same IP, no `ClientAliveInterval` in sshd_config, server load elevated at 3.92

**Decisions Made:**
- Watcher auto-reconnect is working fine — drops are cosmetic, not causing missed events
- Two fix options proposed but **not actioned** (awaiting user confirmation since it's a production sshd config change):
  1. Quick: kill stale pts sessions
  2. Permanent: add `ClientAliveInterval 60` + `ClientAliveCountMax 3` to `/etc/ssh/sshd_config` + reload

**Lessons Learned:**
- Long-lived SSH sessions without `ClientAliveInterval` pile up and cause periodic churn/drops on the server
- Pattern recognition on drop intervals (widening = normal aging, tightening = real issue) is useful for diagnosing SSH stability

**Action Items:**
- [ ] Get user confirmation to apply sshd_config fix (`ClientAliveInterval 60`, `ClientAliveCountMax 3`) on VPS
- [ ] Clean up ~53 stale SSH sessions once fix is in place

### Session (00:41) [superwin]

**Context:** Monitoring SuperWin VPS crash watcher overnight; diagnosing recurring SSH drops

**Key Exchanges:**
- SSH drops accelerated from ~77 min apart to a steady ~13-16 min cadence overnight
- Root cause identified: 53 stale SSH sessions piling up on VPS (all from home IP), no `ClientAliveInterval` in sshd_config, server load avg 3.92
- Two fix options proposed: (1) kill stale sessions manually, (2) add `ClientAliveInterval 60` + `ClientAliveCountMax 3` to sshd_config and reload
- User hasn't confirmed either fix yet — treated as production-config change requiring explicit approval

**Lessons Learned:**
- Long-lived SSH watchers accumulate dead sessions if server lacks `ClientAliveInterval` — eventually causes periodic churn that drops active connections
- The resilient watcher pattern (auto-reconnect on SSH drop) proved its value — zero service impact despite 10+ drops overnight

**Action Items:**
- [ ] Awaiting user decision on sshd_config fix vs manual session cleanup on SuperWin VPS
- [ ] Consider making `ClientAliveInterval` part of standard VPS setup checklist to prevent recurrence on other servers

### Session (00:57) [superwin]

**Context:** Settlement scanner backfill fix for VB system + SSH connection stability investigation on VPS

**Key Exchanges:**
- Settlement backfill shipped: seeds `_market_start_times` + `_persistent_races` + rebuilds `_persistent_sel_map` on startup so yesterday's races settle properly
- Unsettled picks reduced 952 → 612 (340 resolved via voider + fresh settlements)
- Historical gap (2+ day old picks) won't fully resolve via Betfair API — accepted as VOID at 72h via orphan sweep
- SSH drop pattern diagnosed: 53 stale SSH sessions piling up on VPS, no `ClientAliveInterval` configured, load avg 3.92

**Decisions Made:**
- Don't fix historical gap — system is healthy going forward, orphan voider bounds damage at 72h
- Settlement backfill is sufficient: today's stuck markets resolve on restart, fresh races settle in real-time
- Memory note saved at `.claude/memory/settlement-scanner-backfill.md` for future reference

**Lessons Learned:**
- Betfair `listMarketBook` with `SP_TRADED` projection doesn't return position data for OLD CLOSED markets — only runner status
- `listMarketCatalogue` returns empty for most old markets — sel_map rebuild only worked ~1/3 of attempts
- sel_map (selectionId → cloth number) not persisted across restarts was the root cause of "no cached race" settlement failures
- Name matching fallbacks needed: strip "N. " prefix from runnerName + sortPriority as secondary lookup
- SSH drops on long-lived connections caused by stale session accumulation — fix is `ClientAliveInterval 60` + `ClientAliveCountMax 3` in sshd_config

**Action Items:**
- Consider adding `ClientAliveInterval 60` + `ClientAliveCountMax 3` to VPS sshd_config to prevent stale SSH session buildup
- Future enhancement: persist sel_map to disk alongside catalogue snapshot (would eliminate need for orphan voider on 24-48h old picks)
- Alternative future enhancement: secondary results source (punters.com scraper, Racing Australia results API)**Context:** Value betting scanner settlement backfill debugging + VPS SSH stability investigation

**Key Exchanges:**
- Settlement backfill was shipped: seeds `_market_start_times` + `_persistent_races` + rebuilds `_persistent_sel_map` on startup. Reduced unsettled picks 952 → 612 (340 resolved via voider + fresh settlements)
- Backfill limitation identified: Betfair API doesn't return position data for OLD CLOSED markets — `listMarketBook` with `SP_TRADED` returns runner status but not winner. 2+ day old picks will VOID at 72h via orphan sweep
- User decided NOT to fix the historical gap — just wants it healthy going forward
- Fresh settlements confirmed working with full data (e.g., `Warrnambool R3 G (bsp=True ltp=True positions=3)`)

**Decisions Made:**
- Historical settlement gap accepted as-is — orphan voider bounds damage at 72h, system recovers cleanly going forward
- Memory note saved at `.claude/memory/settlement-scanner-backfill.md` documenting what backfill does and its limits

**Lessons Learned:**
- Betfair `listMarketCatalogue` returns empty for most old closed markets — sel_map rebuild only succeeded ~1/3 of attempts
- Betfair `listMarketBook` SP_TRADED projection doesn't include positions for old closed markets — returns runner status only
- To fully solve historical gap would need: persist sel_map/races to disk, or secondary results source (punters.com, Racing Australia API)
- SSH drops on VPS caused by 53+ stale sessions piling up (no `ClientAliveInterval` in sshd_config) + load avg 3.92. Fix: add `ClientAliveInterval 60` + `ClientAliveCountMax 3` to sshd_config

**Action Items:**
- Consider adding `ClientAliveInterval 60` + `ClientAliveCountMax 3` to VPS sshd_config to prevent SSH session pile-up
- Consider killing 53 stale SSH sessions on VPS (all from home IP)
- Future enhancement: persist `_persistent_sel_map` + `_persistent_races` to disk if historical settlement gap becomes a recurring pain point

### Memory Flush (01:13) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (01:44) [superwin]

**Context:** Overnight SuperWin crash watcher monitoring via resilient SSH connection

**Lessons Learned:**
- SSH connection to SuperWin VPS drops on a consistent ~16-minute cadence — likely a keepalive/timeout issue on the VPS or network layer, not a service problem
- Resilient watcher auto-reconnects reliably every time; all services (main=0, sports=0, bk=0) stayed clean throughout — no actual crashes

**Action Items:**
- Investigate the ~16-min SSH drop pattern — likely fixable with `ServerAliveInterval`/`ClientAliveInterval` SSH config tuning or a network-level keepalive adjustment on the VPS

### Memory Flush (02:16) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (03:03) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (03:19) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (03:59) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (04:32) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (05:03) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (05:38) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (06:16) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (07:17) [superwin]

**Context:** Monitoring SuperWin crash watcher SSH stability on VPS

**Key Exchanges:**
- SuperWin crash watcher experienced repeated SSH drops throughout the session, with auto-reconnect recovering cleanly each time (services 0/0/0)

**Lessons Learned:**
- SSH connection to VPS is dropping every ~13–22 minutes (median ~16 min) — consistent periodic pattern suggests a keepalive/timeout misconfiguration rather than server instability
- All reconnects succeeded on first retry (~11s turnaround), so the resilient watcher design is working as intended
- Services remained at 0/0/0 across all reconnects — no actual SuperWin crashes detected

**Action Items:**
- Investigate SSH keepalive settings (ClientAliveInterval / ServerAliveInterval) — the ~16-min drop cadence is suspiciously close to a default timeout value
- Consider tuning SSH config on client or server side to prevent unnecessary disconnects

### Memory Flush (07:33) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:03) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (08:41) [server]

**Context:** SuperWin v3 value-betting scanner — overnight health check, cron fix, boostbet competitor_id feature, and sportsbet crashloop fix

**Key Exchanges:**
- Full system diagnosis at 07:54 AEST: memory 89%, TAB poll 108s, settlement scanner stuck on 43 markets, 86 stale SSH sessions
- Explained `db.enabled` flag — flipped by racing schedule cron (`racing_schedule.sh`) at 13:00 UTC (stop) / 23:00 UTC (start)
- Discovered boostbet, neds, pointsbet were **missing from the cron script's hardcoded SLUGS list** — they were added to the platform on 2026-05-26 but nobody updated the cron. Result: 24/7 polling (~28k extra req/hour) during off-hours

**Decisions Made:**
- Added boostbet/neds/pointsbet to `racing_schedule.sh` SLUGS list (now 9 bookies). Didn't toggle them off immediately to preserve NZ greyhound coverage during morning hours — normalises from next 11pm AEST cycle
- Surfaced boostbet `competitor_id` as `runner.bookie_ids.boostbet` — enables placing BoostBet bets off the board

**Lessons Learned:**
- **Cron script drift**: when onboarding new bookies, the `racing_schedule.sh` SLUGS list must be updated too — easy to miss since the adapter works fine without it
- **`bookie_worker.py` had a fatal exit on disabled state** (`sys.exit(5)`) which, combined with systemd `Restart=always`, caused a crashloop when the DB said `enabled=False`. Deploy restart during off-hours exposed this latent bug (33 restarts before caught)
- Fix: changed to `while not enabled: sleep(60); re-check` loop — worker waits quietly and auto-recovers when cron re-enables
- `runner.bookie_ids` pattern (`Dict[str, str]`) is now generic — other bookies (TabTouch propositionId, Sportsbet outcomeId) can add their runner-level IDs the same way without model changes
- Catalogue merge (`race_catalogue.py`) needed updates in **two paths** (line ~430 canonical match + line ~447 collision path) to preserve `bookie_ids` during cross-bookie runner matching

**Action Items:**
- Monitor sportsbet worker picks up cleanly after 23:00 UTC cron re-enable
- Memory at 89% — auto-restart at 23:00 UTC should clear Chrome leaks; monitor post-restart
- 86 stale SSH sessions still accumulating — consider `ClientAliveInterval` in sshd_config (flagged yesterday too, still not done)
- Settlement scanner stuck on 43 markets — 72h orphan voider will handle, but pattern keeps recurring (pitfall #18)

### Memory Flush (09:14) [Knowted admin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (09:45) [server]

**Context:** Debugging and fixing PnL calculation bugs in the value-betting scanner's resolver, plus adding a Mode Library UI to the dashboard.

**Key Exchanges:**
- User noticed mug mode showing as most profitable — which contradicts its design as a qualifying-loss edge
- Investigation revealed two resolver bugs affecting PnL accuracy across 592 historical picks

**Decisions Made:**
- Added Mode Library section to existing dashboard Modes tab (not a separate `/modes` route) — cards with plain-English description, formula, and live example per mode
- Mug PnL formula changed from back-only to equalised lay-leg calculation (the correct matched-betting formula)
- Run-2nd-3rd position settlement changed from hardcoded `racing-run-2nd-3rd-sportsbet` slug match to mode-slug match, so all bookies (boostbet, neds, pointsbet) now settle PLACE_REFUND correctly

**Lessons Learned:**
- **Resolver was computing back-only PnL for all modes** — fine for value/boost modes, but fundamentally wrong for mug (matched betting) which requires lay-leg offset
- **Hardcoding edge slugs instead of matching on mode_slug** causes silent bugs when new bookies are added to an edge — run-2nd-3rd settlement only worked for sportsbet, silently broke for every other bookie
- **Mug** swung from +60.2u → −10.7u (−70.9u swing); **run-2nd-3rd** swung from −258.7u → −138.7u (+120u swing) — these are massive reporting errors that would mislead edge-config decisions
- boostbet and pointsbet run-2nd-3rd edges are actually profitable (+24.6u, +34.7u) — previously hidden by the bug
- Mode formulas in the Mode Library UI are static strings — will drift if scanner code changes; flag for future auto-pull from source

**Action Items:**
- Monitor corrected PnL going forward to validate the fix on new picks
- Consider auto-syncing Mode Library formula text from `scanner.py` source so it can't drift
- Re-evaluate edge-config decisions that were made based on the old (incorrect) PnL data — especially any edges that were disabled or tuned down due to misleading losses

### Memory Flush (10:17) [Knowted admin]

FLUSH_OK - Nothing worth saving from this session

### Session (10:48) [value-betting-scanner]

**Context:** Fixing MLB resolver data corruption from a deploy-gap (5-21 → 5-25) that mis-graded ~6,700 historical picks.

**Key Exchanges:**
- Phase 1 re-resolve went through 3 passes: v1 narrow (393), v1 wider (2,680), v2 final (2,925). First two had a bug — `et_date` was derived from `game_date` instead of `game_start`, breaking night-game picks by 1 day.
- Phase 2 dry-run caught a critical bug before execution: `_normalize_name` (podcast.py) doesn't NFD-strip accents, but MLB Stats API returns accented names (Andrés Giménez, Mauricio Dubón). Would have incorrectly voided 571 legitimate picks for star players.
- Ohtani edge case: two-way player who only pitched on certain days — batting prop picks should be voided, not left unresolved.
- Team Total props (e.g., "Los Angeles Dodgers") appeared in player_name slot — added to skip list to avoid wrongly voiding fixture-score props.

**Decisions Made:**
- Phase 2 categorized 741 picks: 170 voids (legitimate DNPs) + 571 grades (accent-canon misses). Executed successfully.
- Did NOT fix the underlying code bug in `_normalize_name` — only fixed historical data. This caused overnight regression (M8 back to 6.36%, stale from 33→141).

**Lessons Learned:**
- **Accent canonicalization mismatch is the root cause**: `_normalize` (resolver) NFD-strips accents, `_normalize_name` (podcast.py) does not. Any accented player silently stays `result=null` forever.
- **Fixing data without fixing code = regression next cycle.** Should always ship the code fix first, then clean historical data.
- **Dry-runs before bulk writes are essential** — Phase 2 would have voided star players (Ramirez 34x, Gimenez 44x) without the sanity check.
- **Pipe buffering (`| tail -20`) hides progress** — long-running scripts don't flush output until completion. Use Supabase count queries to monitor progress instead.

**Action Items:**
- **URGENT**: Patch `_normalize_name` in `ev_scanner/podcast.py` to NFD-decompose like `_normalize` does, deploy to Eve, restart v3
- Re-resolve the new overnight accumulation of accent-mismatched picks (~141 stale)
- Investigate the 6 remaining audit stragglers (night-game et≠game_date edge cases, possible doubleheader stat merge)
- Consider adding accent-stripping regression test to prevent recurrence

### Memory Flush (15:59) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (17:22) [value-betting-scanner]

**Context:** Fixing historical pick grading bugs in the value-betting scanner — accent-canon code bug, DNP voids, and Phase 1 corruption cleanup.

**Key Exchanges:**
- Phase 2 completed: 170 voids + 571 accent-canon grades, bringing M8 audit disagreement from 25.5% → <1%
- Overnight regression discovered: M8 back to 6.36%, MLB stale jumped 33→141 because the **code bug** wasn't fixed, only the data
- Root cause: `_normalize_name` in `podcast.py` doesn't NFD-strip accents, but `_normalize` in the resolver does — accented players (Acuña, Giménez, Ramírez) silently fail to match every cycle
- Shipped fix: patched `_normalize_name` to NFD-decompose, validated with 10 test names, deployed to Eve, committed as `e72d26e`
- Post-deploy: stale dropped 141→51 immediately, but M8 still 6.36% — discovered a THIRD issue: Phase 1's early buggy run corrupted some picks (e.g., Carroll game_date=5-22) that then fell outside the correction window due to updated_at filtering

**Decisions Made:**
- Patch `_normalize_name` in podcast.py to match resolver's NFD behavior (root cause fix, not just data patch)
- Re-scope final cleanup by `game_date` instead of `updated_at` to catch the Phase 1 corruption cohort (55,428 picks)
- Deploy immediately rather than wait for scheduled release

**Lessons Learned:**
- Fixing data without fixing the underlying code bug = regression on next cycle. Always fix code first
- Using `updated_at` as a filter for correction passes can miss records that were modified by earlier buggy passes (their updated_at moves past the cutoff)
- Unicode normalization mismatches between components are silent failures — picks just stay null with no error
- Multi-phase data fixes can have cascading scope issues: Phase 1 bug corrupted data → Phase 2 window missed it

**Action Items:**
- Dry-run of full 55,428-pick re-scope still running — needs verification and execution
- Confirm M8 drops below 1% after this final correction pass
- Monitor next few resolver cycles to confirm accent-canon fix is working in production (no new stale accumulation)
- Investigate the 6 remaining audit stragglers (night-game cohort, possible doubleheader stat merge edge case)

