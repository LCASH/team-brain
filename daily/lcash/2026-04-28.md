# Daily Log: 2026-04-28 (lcash)

## Sessions

## Memory Maintenance

### Session (08:16) [value-betting-scanner]

**Context:** Rewriting MLB bet365 scraper from worker-cycling to persistent-page-per-game architecture; fixing NBA Chrome tab bloat

**Key Exchanges:**
- MLB `_MLBGamePage` class was already 90% ready; main rewrite was replacing `MLBGameBet365Scraper`'s `_WorkerPage` cycling with direct `_game_pages` dict + `_refresh_loop`
- After deploy, new workers kept crashing on startup — diagnosed as stale CDP connections from dead workers blocking new Playwright `connect_over_cdp` to the same Chrome instance
- NBA also hit same pattern: 41 tabs accumulated on Chrome 9223, EPIPE crash, worker unable to scrape since 22:03

**Decisions Made:**
- Always kill existing Chrome and launch fresh on worker start instead of trying to reuse (`_kill_stale_chrome()`) — cookie profile preserves login session so no re-auth needed
- Persistent page architecture: each game gets its own tab that stays open forever; `scrape_cycle()` reads cached odds instantly; `_refresh_loop()` re-captures stale pages in background
- Removed the `if True:` indentation hack after fixing Chrome reuse logic

**Lessons Learned:**
- When a Playwright worker process dies, its CDP connection to Chrome becomes stale — a new worker can `connect_over_cdp` to the same Chrome but can't reliably create/manage pages due to the ghost session
- `_kill_stale_chrome()` must match the exact profile path string in the Chrome process command line; mismatches let stale Chromes survive
- CDP tab cleanup every 60s wasn't sufficient to prevent tab accumulation — the root cause was the cycling architecture creating tabs faster than cleanup could close them
- Fresh Chrome launch per worker start is cheaper than debugging stale CDP state; ~30s setup cost per game page is acceptable given it only happens once

**Action Items:**
- Monitor MLB persistent pages to confirm all 8 games set up successfully and odds reach 7,000-8,000 total
- Verify NBA discovers all 8 fixtures and starts pulling 5,000+ odds on fresh Chrome
- Consider porting the same "always fresh Chrome" pattern to NBA scraper if not already applied

### Memory Flush (08:19) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:20) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:21) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (08:22) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (08:22) [value-betting-scanner]

**Context:** Monitoring NBA playoff injury news for value betting impact

**Key Exchanges:**
- Austin Reaves (oblique) ruled out for Lakers vs Rockets Game 4 (2026-04-28)
- Flagged as high urgency — starting guard absence expected to move lines immediately

**Decisions Made:**
- Classified as PASS for the value betting pipeline — confirmed official status change, fresh news with immediate line movement implications

**Action Items:**
- Monitor Lakers-Rockets Game 4 lines for value opportunities post-Reaves ruling

### Session (08:47) [value-betting-scanner]

**Context:** Stabilizing bet365 NBA and MLB scrapers on persistent-page Chrome architecture after overnight failures

**Key Exchanges:**
- Diagnosed remaining flaws after fixing orphan prevention, shutdown cleanup, and MLB persistent-page rewrite
- User requested smarter rediscovery logic: one discovery tab checks existing tabs, skips already-open games, closes tabs for finished games (sport-scoped — never close the other sport's tabs)
- Identified MLB setup bottleneck: 15 games × 2 min = 30 min blocking before any odds served

**Decisions Made:**
- Tab cleanup now matches by `event_id` not URL, scoped per sport (`/B18/` for NBA, `/B16/` for MLB) — prevents cross-sport tab deletion
- MLB `_open_game_pages` made non-blocking — `scrape_cycle()` returns odds incrementally as each game finishes setup instead of waiting for all 15
- No artificial game cap (`MAX_GAMES = 20`) — let all games with props through
- Dedup by fixture_id already exists; duplicate fixture names (today vs tomorrow) are expected since they have different fixture IDs

**Lessons Learned:**
- Chrome tab leaks came from `_discover_games()` creating pages that weren't closed, not from the game page logic itself
- URL-based tab matching is unreliable — event_id matching is more robust for cleanup
- Blocking setup for all games before serving any odds is a bad pattern for scrapers with many fixtures; incremental serving is essential
- After deploying new code, old workers can use cached modules — need to force-kill workers to pick up changes
- Chrome gets overwhelmed at 30+ tabs and causes EPIPE crash loops; fresh Chrome restart is the recovery path

**Action Items:**
- Monitor MLB persistent-page architecture over next 3 days (day 1 of rewrite)
- Implement hard tab cap in NBA scraper (kill Chrome if >20 tabs via CDP `/json`)
- Add VPS health cron (check endpoint every 10 min, restart on consecutive 503s)
- Build alerting webhook (Telegram/Slack) when scraper dies or odds drop to 0 for 5+ min
- Add Chrome memory watchdog (kill if >2GB)
- Add login-check script for both Chrome ports (9223, 9224) to alert on cookie expiry

### Session (09:18) [value-betting-scanner]

**Context:** Debugging and stabilizing bet365 NBA/MLB scrapers that were crash-looping, plus diagnosing dashboard showing no picks

**Key Exchanges:**
- NBA scraper was crash-looping because it tried to attach to existing Chrome via CDP, which fails when a previous worker dies and poisons the CDP state
- MLB scraper was stuck in a loop only setting up TB Rays game repeatedly
- Dashboard showed no picks despite scrapers eventually running — root cause was **12 orphaned push workers** overwhelming the VPS, not scraper code

**Decisions Made:**
- **Always kill and relaunch Chrome with a persistent profile dir** for NBA (matching MLB's approach) instead of trying to attach to existing Chrome. Eliminates Playwright deadlock crash loop
- `_kill_stale_chrome` updated to match `bet365_game_profile` substring which catches both old temp dirs and new persistent profile
- Killed all 12 orphaned push workers, restarted 1 clean instance

**Lessons Learned:**
- Playwright CDP attach-to-existing-Chrome is fragile — if a previous worker dies, the CDP connection state is poisoned and new workers can't cleanly attach. Fresh Chrome with persistent profile is the reliable pattern
- Push worker orphans can silently accumulate and starve the system while scrapers appear healthy — symptom is dashboard showing stale/no data despite active scraping
- `_started = True` flag is set before all pages finish opening, so `scrape_cycle()` serves partial odds while setup continues (non-blocking setup pattern)
- MLB game setup is slow (~2 min per game for SPA boot + market expansion) — 19 games means 30+ min for full setup

**Action Items:**
- Watchdog should prevent push worker orphan accumulation going forward — monitor to confirm
- Need a full stable day to validate the fresh-Chrome-always + persistent-profile + tab-cleanup-by-event_id architecture
- Consider adding push worker count monitoring/alerting to catch orphan buildup early

