# Daily Log: 2026-07-24 (lcash)

## Sessions

## Memory Maintenance

### Session (11:13) [Knowted admin]

**Context:** Building and refining a Knowted app intro animation (logo click → ripples → linen wipe), then rendering it to video.

**Key Exchanges:**
- Replaced placeholder logo with real Knowted brain-check mark (white, base64-embedded) in both `intro-preview.html` and `IntroLogoClick.tsx`
- Fixed cursor animation smoothness — removed mid-path waypoint, made it fade in at rest then glide up in one continuous cubic-bezier ease
- Rendered animation to video using headless Chrome (puppeteer-core) + ffmpeg: deterministic frame-by-frame capture at 1080p60, 4.2s, H.264
- Removed "knowted" wordmark text (was system font, looked generic) and recentered the logo mark as a standalone app icon
- Adjusted background green darker (~20% deeper than original) after initially going the wrong direction (lighter)

**Decisions Made:**
- **Logo-only intro (no wordmark)** — rather than sourcing the correct brand font, cleaner to drop the text entirely and let the mark stand alone
- **Project-scoped `.mcp.json`** for Linear MCP — safer than editing `~/.claude.json` while Claude Code is running

**Lessons Learned:**
- Puppeteer frame-by-frame rendering (stepping animation clock) produces perfectly smooth video from CSS animations — no dropped frames vs. screen recording
- When user says "the green is a bit lighter," they may mean "it *should be* darker" not "make it lighter" — clarify direction before changing
- `.mcp.json` at project root is the safe, reversible way to add MCP servers; editing `~/.claude.json` live risks the app overwriting on exit

**Action Items:**
- Complete Linear MCP OAuth flow in an interactive Claude Code session (reload → approve → `/mcp` → login)
- Consider wiring up a `/log-bug` skill once Linear is connected
- Potential video variants still available if needed: ProRes, 4K, transparent, or logo-hold (no wipe)

