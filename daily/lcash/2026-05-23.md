# Daily Log: 2026-05-23 (lcash)

## Sessions

## Memory Maintenance

### Session (09:34) [TAKEOVER]

**Context:** Building out Discord message indexer — date cutoffs, server whitelisting, pruning, and backfill management

**Key Exchanges:**
- Implemented `minDate` cutoff (2025-01-01) to stop indexing pre-2025 messages; required stop→prune→restart sequence because running backfill doesn't pick up config changes mid-run
- Added server discovery UI (`GET /actions/list-guilds`) with checklist + gateway filter so live messages from non-whitelisted guilds are dropped
- Fixed multiple bugs in the save/backfill flow before landing on a working state
- Final corpus: **808,707 messages**, 742 channels, 11 servers, all post-2025-01-01

**Decisions Made:**
- Cutoff date: 2025-01-01 — balances recency vs corpus size
- Excluded servers retain existing indexed data but stop receiving new messages (no auto-purge on untick)
- Whitelisted 10 servers (Bonusbank, Unabated, ProfitDuel, EndGame, Boosts Server, BetIQ, 8rain Station, Diamond Squid, SEVA Sports Betting, red pill, [ ∴ ]); intentionally skipped TAKEOVER and MB Summit

**Lessons Learned:**
- **FTS5 contentless tables reject standard `DELETE`** — must use `INSERT INTO fts_table(fts_table, rowid) VALUES('delete', ?)` syntax
- **SQLite BUSY under concurrent writes** — gateway + backfill hammering the DB simultaneously caused crashes; both auto-restarted but the UX was confusing
- **Backfill processes don't pick up config changes** — must stop, update config, then restart; sequencing matters
- **UI state ambiguity:** "all checkboxes ticked" was indistinguishable from "no filter" (both → empty array → ingest all); fixed by adding explicit status indicator and amber warning for empty selection
- **409 "already running"** on backfill is expected when clicking Run twice — UI didn't communicate this clearly, looked like a real error

**Action Items:**
- Consider adding a "delete excluded server data" button for cleaner server removal
- Monitor live gateway for stability now that whitelist filter is active
- Could add visual backfill progress per-server in the UI (currently just a streaming log)

### Session (11:06) [discord-brain]

**Context:** Building out Discord Brain — server selection UI, backfill management, and database backup features

**Key Exchanges:**
- Added `GET /actions/list-guilds` endpoint + Settings UI checklist for discovering and selecting which Discord servers to index
- Gateway now respects the server whitelist — messages from non-selected servers are dropped on arrival
- Backfill completed: **808,707 messages** across **742 channels** in **11 servers**, all post-2025-01-01
- Added backup functionality: API endpoint + UI button in Settings, stored at `data/backups/`

**Decisions Made:**
- Server whitelist uses explicit ID list; empty array = "ingest all" (not "ingest nothing") — this caused a UX bug that was fixed
- Backup is a raw SQLite file copy (~1 GB per snapshot) — simple and fully restorable
- Servers removed from whitelist keep their existing indexed messages; only new ingestion stops
- Automatic daily backup deferred — manual/UI button for now

**Lessons Learned:**
- SQLite `BUSY` errors when gateway + backfill both write at full tilt — concurrent heavy writes need WAL mode or write serialization
- UI "select all" toggle → save as empty array → server interprets as "no filter" is a common footgun; need explicit "none selected" warning state
- Backfill endpoint should return current status (already running) gracefully instead of a bare 409 error — UI needs to distinguish "already running" from real failures

**Action Items:**
- Consider automatic daily backup (setInterval in api.js or launchd plist)
- Consider adding "delete excluded server data" button for clean pruning
- Consider launchd plist for persistent gateway+api on Mac boot

### Session (11:06) [superwin]

**Context:** Debugging racing-mult pick detection and deploying frontend update with all 5 racing modes to Vercel production

**Key Exchanges:**
- Racing-mult appeared to have 0 picks, but investigation revealed 12,377 all-time picks (15 today) — initial Supabase query was missing auth headers
- PRIDE TO FOLLOW detected 36 times at ~6% EV — racing-mult IS firing, just wasn't visible in UI
- Frontend bundle was stuck pre-fix due to GitHub Actions storage quota blocking CI deploy

**Decisions Made:**
- Deploy via Vercel CLI directly to bypass GitHub Actions quota (rather than waiting 6-12h for reset)
- Added `.vercelignore` files to both repo roots to prevent node_modules upload (hit 100MB/1.6GB limit on first attempt)
- Deployed from `TAKEOVER/app` dir after initially deploying to wrong Vercel project (`app` vs `takeover`)

**Lessons Learned:**
- Racing slugs are code-split into `SuperWinApp-BIRgmx11.js` (185KB), NOT the main index bundle — grepping the wrong chunk gave false negatives
- Vercel CLI deploys need correct project linking (`vercel link`) — deploying from a subdir can target the wrong project
- Supabase API queries require proper auth headers; missing them returns `count=0` even when data exists
- First "failed" deploy (b7m5ahjex) actually succeeded — the bundle was live the whole time

**Action Items:**
- Uncommitted files in TAKEOVER (`.gitignore` edits, `supabase/migrations/`, `seed_tabtouch_superpick_edge.py`, `useUnreadMessageCount.ts`) are from unrelated work — may need attention later
- Monitor GitHub Actions storage quota reset so CI pipeline unblocks naturally

### Session (11:36) [TAKEOVER]

**Context:** Discord Brain — fixing ingestion bugs, adding backup/parallel-chat features, and improving AI research quality

**Key Exchanges:**
- Server whitelist save logic was broken: unchecking "select all" sent empty array → server interpreted as "ingest all", causing runaway backfill. Fixed JS save handler to distinguish "nothing selected" from "all selected"
- SQLITE_BUSY errors occurred when gateway and backfill both wrote simultaneously — SQLite locked, both crashed and auto-restarted
- Backfill completed: 808,707 messages, 742 channels, 11 servers, all post-2025-01-01

**Decisions Made:**
- Backed up SQLite DB (~1GB) to `data/backups/` so backfill never needs re-running; added a UI "Back up DB now" button
- Switched default model from Sonnet to **Opus 4.7** for deeper multi-step research queries (~$0.50-$1.50 vs ~$0.30 per deep dive)
- Conversation context uses ±10-minute time windows, not Discord reply chains (reply-chain walking noted as easy future add)

**Lessons Learned:**
- Claude was auto-scoping `search_messages` to a single server even when no filter was set — prompt had to explicitly say "DO NOT pass a server argument" to fix
- Claude tends to synthesize too early off few hits; baking a "build running findings doc → summarize at end" pattern into the prompt forces proper breadth (min 8 searches for deep dive, target 10-15)
- Opus confirmed better at multi-step research: smoke test showed 31 tool calls, 0 auto-scoped, proper breadth exploration with rephrasing on empty results

**Action Items:**
- Consider adding automatic daily backup (setInterval in api.js or launchd plist) — user said manual is fine for now
- Could add Discord `reply_to_id` chain walking for true conversation threading (currently time-window only)
- Per-conversation logging now at `data/ask-logs/<session_id>.jsonl` — ready for post-query analysis of search strategy and synthesis quality

### Memory Flush (12:08) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (12:39) [value-betting-scanner]

**Context:** Deep investigation into CLV side-encoding bugs in the v3 value-betting pipeline — why ~85% of Under pick trail rows store pick-side decimals instead of canonical (Over) convention, and a real data race producing alternating Over/Under decimals in trail entries.

**Key Exchanges:**
- Confirmed Bug A (live-play leak via `anchored_closing_bundle`) was already fixed and deployed
- Resolver functions `_build_closing_sharps` and `_compute_closing_true_prob` read the wrong field for ~85% of Under picks — ~17,000 picks have contaminated CLV values
- `optic_clv_pct` (OO-direct path) is clean — independent calculation path unaffected
- Trea Turner trail data shows real alternation between 1.353 ↔ 2.57 on same market_key — confirmed race condition, not classifier noise

**Decisions Made:**
- Root-cause investigation prioritized over defensive fix — user explicitly said "we need to go to the root"
- Exhaustive mechanism enumeration approach chosen over hypothesis-driven testing
- Top 3 priors identified: (1) dual pipeline writing to state (Eve + VPS both running), (2) OO sending pathological SSE events, (3) state.py `is_main` guard race

**Lessons Learned:**
- Classifier tolerance of 0.4 decimal was too loose — produced false "5 flipping picks" finding; tighter filter reduced to 2 real flips
- Used wrong OO market name (`batter_*` vs `player_*`) which gave false "OO has no data" conclusion
- Static code analysis hit its ceiling — every parser path looks clean, yet empirical race exists. Need instrumented logging on Eve to go further
- Premature fix proposals are dangerous — initial one-liner would have only papered over 50% of cases

**Action Items:**
- Run `pgrep -f v3.startup` on both Eve and VPS to check for duplicate pipeline instances (mechanism #13/#14 — highest prior)
- Add temporary debug logging to Eve's OO SSE consumer to capture raw payloads and verify OO isn't sending pathological events (mechanism #6)
- Check if both REST seed and SSE consumer run concurrently for same (sport, source) — potential overwrite race (mechanism #1/#2)
- Two progress docs maintained: `brain/findings/2026-05-23-clv-side-encoding-investigation.md` and `~/.claude/plans/woolly-imagining-flurry.md`

### Session (13:14) [TAKEOVER]

**Context:** Iterating Discord Brain Army pipeline v5→v6, fixing worker failure patterns and launching first multi-bookie battery test.

**Key Exchanges:**
- v5 validated safe/noisy vocab split: cost $7.36→$6.33, rescued 8rain, but synth1 generated sniper-precise FTS queries (full sentences) that returned 0 hits — became the new dominant failure mode
- v6 validated three simultaneous fixes: (1) user-injected aliases override noisy classification (b3 now safe), (2) synth1 constrained to 1-3 token FTS-friendly queries with fallback_query field, (3) followup workers self-broaden on zero hits via 5-step strategy
- v6 followup success rate jumped 25%→60%, synth output longest yet (12,835 chars), cost $7.05 (slight bump from broadening extra turns)
- Launched 7-bookie battery test in parallel (betr, tabtouch, pointsbet, sportsbet/sportys, tab, unibet, boostbet) — ~$40-50 total, peak ~70 simultaneous Claude calls

**Decisions Made:**
- Pipeline iteration order validated: sharding (v3) → safe/noisy split (v5) → broader queries (v6) — each fix exposed the next bottleneck layer
- Created 4-tier fix plan; Tier 1 quick wins to ship immediately: (1) bump followup maxTurns 6→8 + "stop searching, write now" at turn N-2, (2) skip workers for thin-corpus servers (<15 hits threshold), (3) skipVocab toggle for non-bookie queries
- Acknowledged diminishing returns on orchestration tuning — bigger wins now come from architectural changes (semantic embeddings, quote verification, sub-sharding)

**Lessons Learned:**
- Remaining followup failures (2/5) are workers that search to budget exhaustion without ever writing synthesis — not a query problem, it's a turn-management problem. Fix: explicit "write by turn N-2" instruction
- FTS query design matters enormously: full-sentence queries → 0 hits; 1-3 token queries → 60%+ success. AND-joined tokens in FTS5 need to be individually high-recall
- `tab` identified as trickiest vocab test case — ultra-noisy 3-char word that matches everywhere; good canary for vocab agent robustness
- 7 parallel armies (~70 concurrent Opus workers) is a real load test for Anthropic rate limits — orchestrators handle throttling gracefully

**Action Items:**
- Ship Tier 1 fixes (followup turn management, thin-corpus skip, skipVocab toggle)
- Review battery results when 7 parallel runs finish — cross-bookie comparison and throttling analysis
- Pick one Tier 2 fix: quote verification vs sub-sharding vs per-server worker context
- Consider semantic embedding pre-pass ($10 one-time) as highest-leverage Tier 3 investment if daily usage justifies

