# Daily Log: 2026-05-20 (lcash)

## Sessions

## Memory Maintenance

### Memory Flush (07:57) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (08:29) [superwin]

**Context:** SuperWin platform health check and dashboard UI investigation

**Key Exchanges:**
- Full diagnostic report run on SuperWin VPS: system under heavy resource pressure (load avg ~8 on 2-vCPU box), primarily caused by uptime-kuma consuming 62.9% CPU and 1.8GB RAM — not SuperWin itself
- User flagged confusing bookie chips and orange dots in the BookieView dashboard component

**Decisions Made:**
- Confirmed the 3-12h MTJ gate was correct — historical data still shows -14.7% ROI in that band, validating the earlier gate decision
- Identified two display issues in `BookieView.tsx`: (1) `.slice(0, 3)` abbreviation causes TAB/TabTouch and betr/bet365 chip collisions ("TAB + TAB" looks duplicated but means TAB + TabTouch), (2) orange dot only indicates "no Betfair market" not "unlinked bookies" — misleading without context
- Harness orange dots confirmed as real data, not a bug — Betfair only listed 6 harness venues today (Albion Park, Gloucester Park, Menangle, Port Pirie, Shepparton, Wagga). Bathurst/Bendigo/Redcliffe are too small for Betfair to run markets
- Venue aliases verified correct in DB — if Betfair ever lists those meetings, resolver will match automatically

**Lessons Learned:**
- `BookieView.tsx:193` chip abbreviation logic (`b.slice(0, 3).toUpperCase()`) creates visual collisions — TAB/tabtouch both render as "TAB", betr/bet365 both render as "BET"
- `BookieView.tsx:187` dot color is binary: green = has Betfair, amber = no Betfair. Doesn't reflect multi-bookie coverage or linking status
- Betfair harness coverage is selective and volume-driven — smaller country meetings (Bathurst, Bendigo, Redcliffe) routinely excluded

**Action Items:**
- Fix chip abbreviations in BookieView.tsx: use explicit map (`TT` for tabtouch, `B365` for bet365, `BETR` for betr) — 3-line change, proposed but not yet shipped
- Consider richer dot color scheme (green=BF+multi, blue=multi no BF, orange=single, grey=BF only) or at minimum add tooltip explaining orange
- Investigate uptime-kuma resource usage — restart it or lower monitor frequency to relieve CPU/memory pressure on VPS
- Verify the 11pm AEST racing-stop cron script is actually stopping bookies (they were still streaming at 8:17am during off-hours)
- Monitor TabTouch proxy pool — HTTP 000 on sample test suggests possible broader proxy degradation; may need rotation

### Session (09:00) [superwin]

**Context:** SuperWin racing scanner — diagnosing downtime, adding THE MULT place-market edge, debugging Hitters feed filter

**Key Exchanges:**
- Betfair Exchange has **zero AU harness markets** in their API catalogue. AU thoroughbred and greyhound yes, harness no. What looks like harness coverage on betfair.com.au is their separate Sportsbook (fixed-odds) product or results/form pages — not Exchange markets with lay prices.
- Service was down because it restarted while racing bookies were DB-disabled by the 11pm AEST cron-stop. Only bet365/betr loaded. Fixed by manually running `racing_schedule.sh start`.
- Uptime-kuma process consuming 62.9% CPU / 1.7GB RAM on the VPS — unrelated to SuperWin but biggest CPU hog.

**Decisions Made:**
- Added `racing-mult` ("THE MULT") edge: 10% boost on TAB **place** odds, EV = `(tab.place × 1.1) / bf.place_lay - 1`. TAB-only, min_ev 5%, min liquidity $200. Committed as `875f4ff`.
- Implemented via a single `place_market` boolean flag in `scanner.py` — swaps bookie price to `.place`, Betfair price to `.place_lay`, liquidity to `.place_total_matched`. All existing logic (MTJ gate, confidence scorer, persistence, EV trail) applies unchanged.
- Decided NOT to add a `market` column to the DB — market is already implicit via `edge_slug='racing-mult'` for filtering.

**Lessons Learned:**
- **Hitters feed opt-in trap:** When you configure thresholds for ANY single edge in `edgeSettings`, every unconfigured edge is silently hidden (`CommandFeed.tsx:209`). This is the most common reason Hitters shows nothing despite visible 5%+ EV elsewhere. New edges like `racing-mult` need explicit entries in edgeSettings to appear.
- Local MODE items (frontend-computed) don't have `edge_slug` after the recent revert, so they bypass the per-edge filter entirely — they only hit per-mode settings. This explains why items appear in ALL view but not Hitters.
- `racing_schedule.sh` has a Python syntax error in its inline confirmation block (escaped quotes in f-string). Script still works but produces noisy logs.

**Action Items:**
- Add `racing-mult` entry to Hitters preset edgeSettings so it actually surfaces in the feed
- Consider adding a "Show edges I haven't configured" toggle or reset button in FeedSettings to prevent the opt-in trap from silently hiding new edges
- Fix `racing_schedule.sh` Python syntax error (non-blocking but noisy)
- Evaluate pulling Betfair Sportsbook odds for harness/minor meetings (significant work — new scraper needed)
- Monitor uptime-kuma resource usage if VPS load avg climbs during racing hours

### Session (09:36) [value-betting-scanner]

**Context:** Designing how topdown journal "Take" picks should integrate into the existing `nba_tracked_picks` pipeline instead of building a parallel NBA resolution system.

**Key Exchanges:**
- Walked through why the current PR's approach (reading from `nba_tracked_picks` for journal resolution) has a fundamental gap: manual journal picks that no theory fired on have no row → never resolve
- Evaluated NBA Stats API as independent resolver vs writing journal picks directly into `nba_tracked_picks` — landed on the latter as architecturally cleaner
- Confirmed `optic_clv_pct` backfill script (`scripts/backfill_optic_clv.py`) only needs row identity fields (player/prop/side/line/sport/fixture/game_date) — no trail data or sharp snapshot required

**Decisions Made:**
- `triggered_by='manual_topdown'` marker on rows, not a new column — follows existing convention, less schema churn
- No sharp_snapshot capture at take time — `optic_clv_pct` from OpticOdds historical API is sufficient and already the most reliable CLV number; `sharp_clv_pct_true` and `synthetic_clv_pct` will be null for journal picks (fine)
- Pick ID hash must include origin marker to prevent collision with theory-fired rows on same market — ~1 line change in `_generate_pick_id`
- localStorage of pick_ids as journal index for v1 (just IDs, data lives in Supabase)
- Skip trails for manual picks (tracker guard)
- Exclude manual picks from theory calibration/promotion datasets

**Lessons Learned:**
- The original PR's parallel resolution system (ET/UTC date dance, localStorage auto-resolve loop, void-retry) was solving symptoms of a design gap — journal picks should be first-class rows in the existing pipeline, not a parallel universe
- "Context awareness" across tracker/engine/analytics is carried by a single column marker — existing resolver and CLV machinery work unchanged
- Components needing awareness: tracker (skip trails), engine (skip re-eval), analytics (separate bucket), calibration (exclude), pick_id hash (disambiguate)

**Action Items:**
- 8-step implementation plan for journal-into-pipeline refactor (each step independently revertable):
  1. Hash marker in `_generate_pick_id`
  2. Tracker guard to skip trail_entries for manual picks
  3. `POST /api/v1/journal/take` endpoint on VPS
  4. OpticOdds backfill — no change needed
  5. Analytics bucketing for manual picks
  6. Swap `topdown.html` to call new endpoint
  7. Deprecate old `/api/v1/journal/resolve` NBA branch and ET/UTC date dance
  8. Filter `triggered_by != 'manual_topdown'` in theory promotion/calibration
- Plan doc before any code

### Session (10:09) [value-betting-scanner]

**Context:** Designing the architecture to make journal/manual "top-down" picks first-class citizens in the existing `nba_tracked_picks` pipeline instead of maintaining a parallel journal system.

**Key Exchanges:**
- Confirmed `optic_clv_pct` (from OpticOdds historical backfill) populates automatically for manual picks with no trails needed — eliminates the need to capture `sharp_snapshot` at take time
- Clarified there is no single `hybrid_clv` column — the resolver writes multiple CLV columns (`sharp_clv_pct_true`, `synthetic_clv_pct`, `optic_clv_pct`, `clv_pct`) and hybrid is a display-time COALESCE decision
- Identified that the original ET/UTC date mismatch bug becomes irrelevant once manual picks flow through the standard resolver

**Decisions Made:**
- **`triggered_by='manual_topdown'`** as the discriminator column (no new column, follows existing convention)
- **Option A for pick_id hash**: append `origin` to hash input string with `if origin:` guard — theory path unchanged (`origin=""`), manual picks get distinct IDs. Backwards compatible, 3 lines.
- **No localStorage index** — journal queries Supabase directly with `WHERE triggered_by='manual_topdown'`. Cross-device, no drift, no index maintenance.
- **Hybrid CLV at display time**: `COALESCE(sharp_clv_pct_true, optic_clv_pct, clv_pct)`. Manual picks naturally fall through to `optic_clv_pct` since trail-derived columns stay null.
- **No sharp_snapshot capture needed** — `optic_clv_pct` backfill only needs row identity fields (player/prop/side/line/sport/fixture/game_date), all present at take time.
- **Tracker must skip `trail_entries` writes** when `triggered_by='manual_topdown'` — snapshot is frozen at take time.
- **Analytics/calibration must exclude** `triggered_by='manual_topdown'` rows from theory ROI and calibration datasets.

**Lessons Learned:**
- When adding a new pick origin to an existing pipeline, a single discriminator column (`triggered_by`) plus a hash-input marker gives full separation with minimal schema churn
- CLV computation independence matters: `optic_clv_pct` works without trails because it queries an external API (OpticOdds historical), making it the natural fallback for picks without trail data
- Querying by a column value (`triggered_by`) is simpler than maintaining a separate index (localStorage or join table) when there's a single user

**Action Items:**
- Write plan doc (`brain/findings/2026-05-20-journal-as-first-class-picks.md`) before any code changes
- Implementation sequence: (1) hash marker in `_generate_pick_id`, (2) tracker guard for trail_entries, (3) `POST /api/v1/journal/take` endpoint, (4) analytics bucketing, (5) swap topdown.html to new endpoint, (6) deprecate old journal resolve + ET/UTC workarounds, (7) filter manual picks from calibration

### Session (10:43) [value-betting-scanner]

**Context:** Deploying a multi-phase manual journal pick system for the value-betting scanner (v3), carefully layering changes to avoid disrupting the existing theory architecture.

**Key Exchanges:**
- Walked through Phases 1-3 of manual pick journaling: hash marker change (Phase 1), tracker cache guard (Phase 2), and the `POST /api/v1/journal/take` endpoint (Phase 3)
- Each phase was validated offline before deployment, with explicit row-count comparisons and hash determinism checks
- Full deploy to Eve (scraper) and VPS (dashboard) with comprehensive smoke testing

**Decisions Made:**
- `origin` parameter added to `_generate_pick_id` as optional, default `""` — ensures theory picks are byte-identical to pre-change (confirmed 20/20 match on real DB rows)
- Phase 2 filter applied at `_load_cache` Supabase query level (`triggered_by != 'manual_topdown'`) rather than downstream — single entry point, cleanest guard
- `game_date` derived server-side in **UTC** (not ET) to match rest of system and avoid b8steel's date-boundary bug
- Hash algorithm duplicated inline in `v3/vps/server.py` rather than importing from `server/tracker.py` — intentional decoupling since v3/vps has zero imports from server/*
- UPSERT strategy for idempotent take submissions (same payload → same pick_id, no duplicate rows)

**Lessons Learned:**
- Legacy v1+sport hash artifact found (Schroder row, pre-2026-05-15) — old writer embedded line in raw string. Not caused by changes, just a fossil. Out of scope but noted
- The tracker's `_tracked_ids` set is the single gatekeeper for trail writes — filtering at cache load is sufficient because the only other `.add()` call is post-Phase A (theory only)
- Validating row counts before/after filter changes (570 = 570) is a clean "no-op proof" when zero manual rows exist yet

**Action Items:**
- Phase 4 still pending: `GET /api/v1/journal/picks` list endpoint
- Remaining phases beyond that to complete the journal feature (grading, dashboard UI integration)
- Monitor production to confirm theory pipeline unaffected post-deploy

### Session (11:38) [value-betting-scanner]

**Context:** Shipped Phase 5 of the journal refactor — migrating the topdown dashboard's manual pick journal from localStorage to Supabase-backed server endpoints.

**Key Exchanges:**
- Assistant initially overestimated Phase 5 complexity, proposing a 3-substep rollout (5a/5b/5c). User pushed back: the resolver already handles grading, CLV, etc. — so the swap is simpler than presented.
- User's key insight: "the resolving has been offloaded to the nba tracked picks resolver" — meaning autoResolveLoop, fetchProdCLVForPick, void-retry logic, and ET date juggling are all obsolete. Just need: write (POST take), read (GET picks showing today's takes), display (show as taken if exists in nba_tracked_picks with triggered_by='manual_topdown'), and delete.

**Decisions Made:**
- Full Phase 5 swap in one shot (not the phased 5a/5b/5c approach) — user correctly identified the resolver already does the heavy lifting
- Manual pick ID computed via SHA-256 hash for deterministic matching (verified hash parity between Python and browser Web Crypto)
- Hybrid CLV display: `sharp_clv_pct_true ?? optic_clv_pct ?? synthetic_clv_pct ?? clv_pct` — reads whatever the resolver has populated
- DELETE endpoint guarded by `triggered_by='manual_topdown'` so theory rows can never be deleted from the dashboard
- 60-second `periodicJournalRefresh` while journal view is active
- Removed: W/L/V buttons, Auto button, "Clear settled" button, all localStorage write paths, autoResolveLoop, fetchProdCLVForPick, syncProdCLVs, ET date juggling

**Lessons Learned:**
- When a background resolver already populates result/CLV columns on the same table, the client journal doesn't need its own resolve/CLV-fetch loops — just read the row
- The assistant's initial survey found 5 loops and ~6 endpoints — but most were already superseded by the tracked picks resolver. Always check what's actually still needed before planning removals.
- `pickJournalKey` and all the localStorage machinery were the real legacy — the "complexity" was in code that should have been deleted earlier

**Action Items:**
- Phase 6: Analytics/calibration filter — must land before running calibration sweeps (manual picks could contaminate the dataset)
- Phase 7: Deprecate `/api/v1/journal/resolve` NBA branch + `tracked-clv` endpoint (now dead code server-side)
- Browser verification: confirm Take → Journal → Refresh → Remove flow works end-to-end in real browser (smoke tests passed via curl)
- Legacy localStorage data still lingers in user's browser but is no longer read — can clean up eventually

### Session (12:08) [value-betting-scanner]

**Context:** Shipping the journal Supabase cutover for the value-betting topdown dashboard — migrating manual picks from localStorage to server-side Supabase rows (Phases 1-5 of a multi-phase plan).

**Key Exchanges:**
- Full Phase 5 implementation and deployment: rewrote topdown.html (~370 lines changed), added DELETE endpoint to server.py, removed all localStorage-based pick management (W/L buttons, auto-resolve, clear-settled)
- User reported dashboard broken via screenshot — turned out to be HTTPS on an HTTP-only port (8803)
- Browser testing via AdsPower/Playwright uncovered a real bug: `crypto.subtle` is undefined on plain HTTP (not a secure context), breaking `computeManualPickId`

**Decisions Made:**
- Dropped SHA-256 hashing on the client entirely; switched to identity-key matching (e.g. `nba|player|market|side|date|book_id`) instead of crypto pick_ids — server still uses SHA-256 internally, client never needs it
- No sharp line snapshot at take-time, no manual W/L resolution — resolver handles everything automatically
- localStorage data preserved but no longer read — clean rollback path if needed
- Do NOT run `calibrate_weights.py` or `promote_theories.py` until Phase 6 (analytics filter) lands to avoid manual picks contaminating calibration data

**Lessons Learned:**
- `crypto.subtle` (Web Crypto API) only works in secure contexts (HTTPS or localhost) — plain HTTP on a VPS will silently fail. Always check context requirements for browser APIs.
- Identity-key matching is simpler and more robust than client-side hashing when the server already manages canonical IDs
- Phase 2 filter (`triggered_by != 'manual_topdown'`) is critical — without it manual picks leak into the theory tracker pipeline

**Action Items:**
- Monitor with M1-M7 queries per the monitoring plan (daily for first week, weekly after)
- Phase 6: analytics/calibration filter (HIGH priority — blocks safe calibration runs)
- Phase 7: deprecate `/api/v1/journal/resolve` NBA branch + `tracked-clv` endpoint
- Success criteria in 2 weeks: M2 stays at 0, M5 empty, M6 ≥80% optic_clv coverage on resolved manual picks
- Commits are local-only — need to push to remote when ready

### Session (12:38) [value-betting-scanner]

**Context:** Imported Jay's localStorage journal into Supabase as Phase 5 validation test; discovered a critical MLB resolver bug in the process.

**Key Exchanges:**
- Imported 66 of Jay's picks via `POST /api/v1/journal/take` — 64 unique rows landed (2 correctly collapsed as re-takes with drifted odds)
- Resolver graded 44/64 (69%), concordance with Jay's recorded W/L was 21/23 (91%)
- 14 of Jay's 25 NBA "Gap #1" failures now have verdicts through our pipeline
- User correctly challenged claim that MLB resolver uses OpticOdds player-results — it does NOT; MLB Stats API is primary, OpticOdds is fallback-only, and OpticOdds historical odds are only for `optic_clv_pct`

**Decisions Made:**
- Phase 2 trail guard validated at scale (0 trail entries for manual picks) — working as designed
- Multi-user journal ownership deferred (no `journal_user_id` column yet; Jay's picks share `triggered_by='manual_topdown'`)
- Re-take semantics: current last-write-wins via UPSERT; could switch to immutable-first-take later

**Lessons Learned:**
- **Critical bug found in `server/resolver.py:1422-1428`:** `dates_to_fetch` iterates `[day-1, target, day+1]` with first-occurrence-wins, so any MLB player who played the day before gets **yesterday's stats** stamped as their result. Confirmed via MLB Stats API: Perez had H=2 on 5/17 and H=0 on 5/18 — resolver returned 2 (wrong). Same for Realmuto (H=1 on 5/17, H=0 on 5/18).
- This bug affects **every MLB pick where the player played on consecutive days** — essentially most MLB picks. Corrupts `result`, `actual_stat`, and downstream calibration metrics silently.
- Bug was only caught because Jay's snapshot provided ground-truth comparison. Without external validation, the error is invisible — wrong enough to corrupt Brier/ROI but not enough to look obviously broken.
- Initially misdiagnosed as "MLB Stats API vs OpticOdds disagreement" — corrected to "same MLB Stats API source, date-iteration ordering bug."

**Action Items:**
- Ship fix Option A: reorder `dates_to_fetch` to `[target, day-1, day+1]` so target-date wins — smallest correct fix
- Re-resolve all affected MLB picks after deploying the fix
- Update findings doc (`brain/findings/2026-05-20-journal-snapshot-import-results.md`) — currently has wrong root-cause framing
- Add M8 to monitoring plan: weekly cross-check of random sample of resolved MLB picks against MLB Stats API directly
- 20 pending picks from Jay's import will resolve as games complete via hourly resolver cycle
- Commits `e236021` + findings doc are local on `main`, not yet pushed

### Session (13:34) [value-betting-scanner]

**Context:** Debugging and fixing a critical bug in the value-betting scanner's MLB pick resolver (`server/resolver.py`) where adjacent-day stat merging caused ~16% of MLB batting picks to have wrong results.

**Key Exchanges:**
- Jay's snapshot flagged Salvador Perez and J.T. Realmuto as having wrong `actual_stat` values — web search against MLB Stats API confirmed Jay was right and the resolver was wrong
- Root cause identified: `dates_to_fetch = [day-1, target, day+1]` with a "first-wins" dict merge meant any player who played consecutive days got their day-before stats stamped on target-date picks
- Three fix options presented (reorder, last-wins, per-date lookup); user chose Option A (reorder to `[target, day-1, day+1]`)
- Historical re-resolution scoped: user chose Option A (MLB batting only, ~809 picks) over Option B (all MLB + NBA, ~3315 picks)

**Decisions Made:**
- **Fix approach: Option A (reorder `dates_to_fetch`)** — smallest surgical change, target-date stats win, adjacent days only fill gaps. Shipped and deployed to Eve.
- **Re-resolution scope: MLB batting only** — 809 picks cleared and re-resolved. 133 (16.4%) had wrong `actual_stat`, 99 (12.2%) had flipped W/L result.
- **M8 added to monitoring plan** — weekly cross-check of resolver output vs MLB Stats API ground truth. This is the signal that would have caught this bug; M1-M7 all relied on internal consistency only.

**Lessons Learned:**
- **"First-wins merge from multi-source iteration" is a dangerous anti-pattern** — when iterating over multiple date windows and merging into a dict, iteration order silently determines correctness. Grep the codebase for similar patterns.
- **Internal consistency checks (M1-M7) can't catch grading bugs** — only external ground-truth comparison (M8) catches cases where the resolver confidently returns the wrong answer. Monitoring plan was incomplete without this.
- **Calibration data is corrupted** — any Brier/ROI sweep run pre-fix against MLB data used 16%-wrong results. Those results should be discarded. Hard stop on `calibrate_weights.py` until M8 runs cleanly.
- **NBA picks (2408 resolved) almost certainly have the same bug** — same code path, same merge order. Can't ground-truth easily (no free NBA API like MLB Stats API).

**Action Items:**
- Re-audit the 809 just-resolved MLB batting picks to confirm they now agree with MLB Stats API
- Sweep NBA picks (2408 resolved) — same code path, same bug, needs re-resolution
- Full MLB pitcher audit (all ~98, not just sampled 18)
- Git blame `dates_to_fetch` to determine how far back the bug goes (pre-2026-05-15 scope)
- Build M8 as real infra (daily script + alerting on >1% disagreement), not just a monitoring plan bullet
- Grep codebase for same "first-wins merge" anti-pattern elsewhere (at least one more at `resolver.py:1391-1394`)
- Investigate the 43 "re-pending" picks that returned `skip_no_player` after re-resolve
- Do NOT run `calibrate_weights.py` or theory promotion against MLB/NBA data until M8 has run cleanly for a week
- Commit `0e0afd0` contains: fix, findings doc (`brain/findings/2026-05-20-resolver-adjacent-day-merge-bug.md`), M8 in monitoring plan

### Session (14:05) [value-betting-scanner]

**Context:** Shipping and verifying the Journal Supabase cutover (Phases 1-5), importing b8steel's localStorage picks, diagnosing the CLV gap on imported manual picks.

**Key Exchanges:**
- Dashboard SSL error was just a URL typo (https → http on port 8803) — not a real bug
- Found real bug via AdsPower browser test: `crypto.subtle` is undefined on plain HTTP (not a secure context), breaking `computeManualPickId` silently on every Live-view render
- Imported b8steel (Jay)'s 66 localStorage picks (41 MLB, 25 NBA) into Supabase — resolver grading them correctly
- CLV (PROD) is blank/0% for all 47 resolved imported picks — OpticOdds backfill matched zero manual picks

**Decisions Made:**
- Replaced SHA-256 (`crypto.subtle`) with synchronous identity-key matching (e.g. `nba|Julian Champagnie|Assists|Over|2026-05-16|365`) — server keeps SHA-256 internally, client never needs it
- Monitoring plan established with 7 queries (M1-M7), daily ritual first week, weekly after
- Hard stop locked: do NOT run `calibrate_weights.py` or `promote_theories.py` until Phase 6 lands — manual picks would contaminate the dataset
- Recommended Phase 5.5: capture `sharp_snapshot` at Take time so future manual picks get `sharp_clv_pct_true` once resolver runs

**Lessons Learned:**
- `crypto.subtle` (Web Crypto API) only works in secure contexts — HTTPS or localhost. Plain HTTP on VPS doesn't qualify. This is a silent failure, not a thrown error.
- OpticOdds/Pinnacle doesn't quote many Bet365 prop lines (Bases O/U 1.5, Batting Strikeouts U 0.5, Walks U 0.5, RBIs O 0.5) — so `optic_clv_pct` backfill structurally cannot fill these
- CLV cannot be reconstructed retroactively for imported picks — the sharp pricing moment has passed
- CLV waterfall on dashboard: `COALESCE(sharp_clv_pct_true, optic_clv_pct, synthetic_clv_pct, clv_pct)` — when all null, falls back to naive 0%
- CLV (FLOOR) works for some picks because Jay's browser captured `closingSnapshot` from the live feed at game time

**Action Items:**
- Ship Phase 5.5: modify `POST /api/v1/journal/take` to fetch live sharp snapshot from `/api/v1/odds` and store it, so future takes get devig CLV
- Phase 6 (HIGH): analytics/calibration filter to exclude manual picks from theory calibration
- Phase 7: deprecate `/api/v1/journal/resolve` NBA branch + `tracked-clv` endpoint
- `git push origin main` still pending — two local commits (569e433, 4590c31) + Jay's snapshot (46375f2)
- Run daily monitoring ritual (M1+M2+M5 + browser smoke) for first week

