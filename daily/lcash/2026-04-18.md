# Daily Log: 2026-04-18 (lcash)

## Sessions

## Memory Maintenance

### Session (11:48) [value-betting-scanner]

**Context:** Debugging and fixing phantom EV bugs in the value betting scanner (Pinnacle devig + interpolation)

**Key Exchanges:**
- Traced soccer moneyline phantom EV to a specific code path: `_under` keys couldn't detect the draw market (line 563 only built `draw_key` from `_over` keys), causing fallthrough to 2-way devig
- 2-way devig on a 3-way market absorbs ~27% draw probability into both sides, inflating Motor Lublin from true 3.76 odds to phantom 2.77 odds (+18.6% fake EV vs actual -12.7%)
- Discovered NBA player prop interpolation was bridging a 4-point line gap (Pinnacle 5.5 → Kalshi 9.5), producing 15.6% true prob when Kalshi's own price said 7.5%
- Data showed all books cluster at line 5.5 — no intermediate lines available to build a curve

**Decisions Made:**
- Use **Poisson model** (not logit) for count props across any line gap — produces 4.2% true prob at 9.5 vs logit's 15.6%
- Added **cross-validation gate**: if soft book's own devigged price disagrees with interpolation by >5pp, skip the pick
- Kept 50% EV hard cap + tightened line gap as additional safety layers
- Voided 31 phantom soccer moneyline picks from Supabase (all showing 15-42% fake EV)

**Lessons Learned:**
- Stored picks in Supabase persist after code fixes — must void old phantom picks manually, not just fix the generation path
- 2-way devig on 3-way markets is a catastrophic error — redistributes draw probability and creates massive phantom edges
- Prediction market books (Kalshi, Polymarket) offer fundamentally different line structures than traditional sportsbooks — deep alt lines vs main lines
- Cross-validating interpolated true prob against the soft book's own devigged odds is a powerful sanity check

**Action Items:**
- Monitor new picks post-deploy to confirm Poisson + cross-validation gate working correctly
- Watch for edge cases where cross-validation 5pp threshold might be too tight or too loose
- Resolved pick data still thin (110 picks) — need 400+ before drawing statistical conclusions on profitability

### Session (12:31) [value-betting-scanner]

**Context:** Building and deploying a new "Crypto Edge" pill for the value betting scanner — targeting prediction market opportunities where Pinnacle has no coverage.

**Key Exchanges:**
- Game start times missing on some dashboard picks — confirmed data exists in Supabase, rendering cascade issue where `getGameTime()` returns null for stored picks not in SSE stream. Cosmetic only.
- Strategic thesis: everyone arbitrages Pinnacle → prediction markets, so edges close fast. Flip it: find markets prediction markets cover but Pinnacle doesn't, use DraftKings/FanDuel/Novig as sharps instead. Higher barrier to entry = longer-lasting edges.
- Scanned OpticOdds: **1,535 markets** where prediction markets exist but Pinnacle doesn't; **1,433** of those have other sharp books available.
- Goldmine is MLB (1,354 markets): Hits, Hits/Runs/RBIs prop types Pinnacle doesn't list at all. Also NBA Blocks/Steals/Double Double and NHL game lines.

**Decisions Made:**
- New "Crypto Edge" pill (separate from Pinnacle pill) with `exclude_sharp_books: [250]` filter to skip any market Pinnacle covers
- Sharp book hierarchy: DraftKings (1.0), Novig (0.9), FanDuel (0.8), Hard Rock (0.7), BetRivers (0.7)
- Soft books: Kalshi, Polymarket, Polymarket USA, DK Predictions, Underdog, Crypto.com
- Full tracking + trails + resolver enabled from day 1 for backtesting

**Lessons Learned:**
- Pinnacle prop coverage has major gaps: doesn't list MLB Hits or Hits/Runs/RBIs at all, covers only 74/249 MLB HR players
- The `exclude_sharp_books` theory config pattern works for filtering out markets already covered by specific books
- No immediate +EV picks on first cycle is expected — means filters are working, not letting garbage through

**Action Items:**
- Monitor Crypto Edge pill for first picks as games enter 24h window
- Fix dashboard time column display bug for stored picks (cosmetic, low priority)
- Backtest Crypto Edge results once sufficient pick history accumulates

### Memory Flush (13:02) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (13:31) [value-betting-scanner]

**Context:** Value betting scanner — fixing Pinnacle pill bugs, building Crypto Edge pill, discovering massive coverage gaps, and finding SSE streaming as the scaling solution.

**Key Exchanges:**
- Confirmed Pinnacle genuinely doesn't offer MLB Hits, HRIs, NBA Blocks/Steals/DD/TD on OpticOdds — Crypto Edge thesis validated with ~1,400 same-line markets
- User pushed back on "97% coverage" claim — correctly identified we're only polling 23 of 491 Kalshi leagues (~5% actual coverage)
- Kalshi covers 272 soccer leagues (we poll 22), 96 basketball leagues (we poll 4), plus tennis, esports, golf, cricket, MMA, handball, volleyball — all untapped

**Decisions Made:**
- Use OpticOdds SSE streaming (`/stream/odds/{sport}`) instead of REST polling — tested successfully, 459KB in 15s covering ALL leagues per sport
- Replace 28 pollers with ~15 SSE connections to cover 491+ leagues without hitting rate limits
- Plan documented in `brain/findings/2026-04-18-sse-scaling-plan.md` for next session pickup

**Lessons Learned:**
- OpticOdds REST polling doesn't scale — 491 leagues = 10K API calls/min, would hit rate limits
- SSE endpoint streams all leagues within a sport, includes league IDs, market types, odds, player props, and bet limits — far superior to polling
- Prediction market odds only appear close to game time — can't verify coverage for future events
- Crypto Edge pill showing 0 picks is expected when games are 14+ hours out; edges appear closer to tip-off

**Action Items:**
- Phase 1: Build `OpticOddsStreamConsumer` class (parse SSE events into market dicts)
- Phase 2: Auto-discover all 491 leagues from `/leagues/active`
- Phase 3: Replace 28 pollers with ~15 SSE streams in `main.py`
- Phase 4: Auto-create Supabase theories per league
- Phase 5: Dashboard auto-expansion for new leagues/sports

### Session (13:36) [superwin]



**Context:** Diagnosing and fixing SuperWin value-betting scanner issues across VPS and Dell Mini PC — CPU spikes, dead proxies, and TabTouch blocking.

**Key Exchanges:**
- Diagnosed bet365 racing worker hitting 99% CPU during runner map build phase; implemented three fixes (longer settle time, Chrome GPU flags disabled, BELOW_NORMAL process priority) — expected to drop peak from ~99% to ~70-80%
- Investigated TabTouch proxy failures: initial assumption was expired Webshare subscription, then discovered bandwidth limit error (402), then confirmed bandwidth was actually fine (6MB/1000GB used), then finally determined the real issue — **CloudFront is blocking all 100 Webshare proxy IPs** for TabTouch specifically (502 Bad Gateway through proxy, direct VPS IP also blocked)

**Decisions Made:**
- Deployed CPU throttling changes (settle time 3→5s first/2→3s subsequent, Chrome headless GPU flags, BELOW_NORMAL priority) — trade-off is ~1 min slower runner map build
- Confirmed proxies.json doesn't need updating — same IPs/creds from Webshare API download

**Lessons Learned:**
- Webshare `402 bandwidthlimit` error can persist across billing cycles — don't trust the error message without checking the dashboard (actual usage was 6MB/1TB)
- TabTouch/CloudFront has blocked the entire Webshare residential proxy IP range — not just individual IPs. Tested 10 across different ranges, all 000
- Direct VPS IP (170.64.213.223) is also blocked by TabTouch CloudFront
- IP Authorization in Webshare is separate from user:pass auth — both needed

**Action Items:**
- Replace Webshare proxy pool with fresh IPs (use "Replace Proxies" in Webshare dashboard) or switch to a different residential proxy provider for TabTouch
- TAB still broken on VPS (page-crash loop, possibly disk-related) — needs investigation
- Monitor bet365 CPU after next restart to validate the throttling changes work as expected

### Session (14:09) [superwin]



**Context:** Debugging why TabTouch API stopped working in the SuperWin value betting scanner; discovered domain migration and new architecture.

**Key Exchanges:**
- User asked to investigate why TabTouch was inaccessible — turned out `api.tabtouch.mobi` domain was killed (NXDOMAIN), not a proxy/blocking issue
- Used browser monitor script to reverse-engineer the new TabTouch site architecture
- User correctly pushed back on static-only approach ("there has to be a websocket or some sort of insight into how odds are updated")

**Decisions Made:**
- TabTouch adapter base URL must change from `tabtouch.mobi` → `tabtouch.com.au` (line 46 of adapter)
- Two-phase approach recommended: (a) simple SSR page-parse adapter first to get back online, (b) MQTT real-time streaming later
- Entire Webshare proxy subscription is unnecessary for TabTouch — new domain serves public, no-auth responses

**Lessons Learned:**
- The proxy infrastructure debugging (datacenter IPs, rotating residential, CloudFront blocking) was a complete red herring — the underlying domain had been retired
- TabTouch architecture: SSR HTML with `var model` JSON embedded → Knockout.js renders → AWS IoT MQTT (Cognito anonymous auth) pushes real-time price changes
- Key endpoints: `/api/raceinfo/nextraces` for discovery, `/racing/{date}/{meetingId}/{raceNum}` for full race card with odds in `pool.legs[0].starters` (fields: `fixedWinDiv`, `fixedPlaceDiv`)
- AWS IoT MQTT config lives in `globals.eventNotificationApiSettings` with Cognito identity pool `ap-southeast-2:06357b04-f346-4414-b089-...`
- Config reveals push capabilities: `pushdata-pricechange: True`, `pushdata-raceclose: True`, balance poll interval 30s

**Action Items:**
- Build page-parse TabTouch adapter (phase 1) — parse `var model` from SSR HTML, no proxy needed
- Later: implement MQTT WebSocket connection for real-time odds streaming (phase 2)
- Evaluate whether Webshare subscription is still needed for other adapters or can be cancelled

### Session (14:39) [value-betting-scanner]

**Context:** Debugging and scaling the value betting scanner's SSE odds pipeline to handle 491 niche leagues

**Key Exchanges:**
- Confirmed 215 Pinnacle picks across 14 leagues (NBA 95, MLB 89, plus niche leagues like Japan J1, Korea K1, MLS)
- Dashboard "SHARPS: 3" display was misleading — Novig is the only sharp book, but 3 Crypto Edge theories (NHL, MLB, NBA) each flag the same market, so the column shows 3 theory passes, not 3 sharps
- SSE data IS flowing for new sports (ATP Challenger, Dota 2, NPB, China CBA, etc.) — 16 sports with active markets
- Of 2,400 raw SSE entries across 52 leagues, only 54 made it through — **495 skipped because fixture not in cache**

**Decisions Made:**
- Increased fixture cache API limit from 100 → 500/1000 per sport to unblock moneyline market matching
- Decided to keep `market_map` as moneyline-only for now — 31 market types stream in (asian_total_goals, total_goals, asian_handicap, etc.) but moneyline is safest (no interpolation, 3-way devig works). Adding totals/handicaps later would multiply picks further
- The sharp count display issue is cosmetic (display clarity, not data) — pick EV is correct

**Lessons Learned:**
- Fixture cache was the primary bottleneck preventing niche league picks — API defaulted to 100 fixtures per sport, silently dropping markets that couldn't be mapped to home/away
- Odds are available well before kickoff — don't wait for game start to expect markets
- Theory existence check loop is slow (~0.2s × 462 theories = ~90s Supabase round-trips) — on restart it's instant since theories are cached, but first deploy is painful

**Action Items:**
- Monitor pick volume after fixture cache fix takes effect (SSE streams need to reconnect)
- Consider adding `total_goals` and `asian_handicap` to `market_map` to multiply picks
- Consider batching theory existence checks to avoid 462 serial Supabase GETs
- Fix sharp count display to show unique sharp books, not theory pass count

### Session (14:43) [value-betting-scanner]

**Context:** Debugging and optimizing the value betting scanner's SSE streams, fixture caching, and pick generation pipeline.

**Key Exchanges:**
- Investigated why pick volume didn't 10x after connecting 491 league SSE streams — turned out odds ARE flowing (52 leagues, 2,400 raw entries) but most get filtered out before becoming picks
- Sharp count display bug: "SHARPS: 3" shown for Alex Bregman pick when only Novig is the sharp book — the "3" actually means 3 Crypto Edge theories (NHL, MLB, NBA) all evaluated the same market, not 3 sharp books

**Decisions Made:**
- Increased fixture cache API limit from 100 → 1000 per sport to unblock moneyline market processing (495 markets were being skipped due to missing fixture cache entries)
- Keeping `market_map` as moneyline-only for now — 31 market types stream in (asian_total_goals, total_goals, asian_handicap, etc.) but moneyline is safest (no interpolation, 3-way devig works). Adding totals/handicaps later would multiply picks further

**Lessons Learned:**
- The SSE fixture cache was silently dropping ~90% of incoming markets because the `/fixtures/active` API defaults to limit=100 — a pagination issue masquerading as "no data"
- Theory existence checking on startup is slow (~0.2s × 462 theories = ~90s) because each check is a separate Supabase GET — could batch this
- Odds are available well before game time; don't conflate "game hasn't started" with "odds aren't available" — pre-game markets should be captured hours/days in advance

**Action Items:**
- Monitor post-deploy pick count to confirm fixture cache fix actually increases market throughput
- Consider adding `total_goals` and `asian_handicap` to `market_map` to further multiply pick volume
- Fix the sharp count display to show unique sharp book count rather than theory-pass count
- Batch the theory existence check on startup to avoid 462 sequential Supabase calls

### Memory Flush (15:20) [tmp]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

