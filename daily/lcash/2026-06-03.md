# Daily Log: 2026-06-03 (lcash)

## Sessions

## Memory Maintenance

### Memory Flush (07:00) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (07:31) [value-betting-scanner]

**Context:** Running the full v3 value-betting scanner healthcheck (Eve process, VPS, scrapers, cache, resolvers, picks overlay, canaries).

**Key Exchanges:**
- Full healthcheck completed across all layers — Eve, VPS, AdsPower, SPA routes, cache, fix markers, alt sentinels, AU scrapers, push-loop cadence, journal tracker, resolver, C3 canary, real-EV flow, picks overlay
- Per-sport breakdown: MLB 11138 markets (in-season), NBA 726 (wrapping season)
- Stable-ID coverage degradation identified on multiple books

**Decisions Made:**
- MLB resolver staleness (255, RED threshold) assessed as non-issue — OO fixture-completion delay, Layer-1 gate correctly skipping all 4424 picks; will self-clear in ~2h
- JournalTracker "no recent cycles" assessed as yellow-not-dead — 0 in-window manual picks means nothing to process
- Overlay drop rate ~65% MLB / 71% NBA flagged but may be explained by morning AEST timing (US games finished, lines pulled)

**Lessons Learned:**
- `/v1/picks` response is 19MB — default 10s timeout insufficient, need 60s for overlay checks
- Stable-ID coverage can silently regress when adapter emission paths miss `market_id=` assignment — PointsBet MLB at 46.7%, TabTouch MLB at 45.0% (both RED, below 50% threshold)
- Neds stable-ID also degraded to ~76% (was 100% baseline) — same `market_id` pattern
- Overlay drop rate metric needs context: time-of-day matters (morning AEST = post-game line pulls inflate drops)

**Action Items:**
- `git blame` PointsBet + TabTouch MLB adapters for commits after 2026-05-29 that touched emission flow — `market_id=` likely missing on MLB-specific code path
- Investigate Neds adapter for same `market_id` regression pattern
- Re-check MLB staleness in ~2h after OO fixture-completion catches up
- If overlay drop rate persists at 65%+ outside "games-just-finished" window, inspect canonicalization in `v3/pipeline/server.py` overlay block — sample a dropped pick and grep live store to confirm market actually vanished vs key divergence

### Session (08:47) [value-betting-scanner]

**Context:** Deep quantitative audit of prediction-market monitoring infrastructure in the value-betting scanner — coverage, signal quality, and next-EV investment.

**Key Exchanges:**
- Kalshi via OO SSE is the dominant PM volume source (6,150 picks/30d, 75% of PM-soft), despite prior code inventory incorrectly labeling it "no active scraper"
- Flagship MLB PM Edge — Strict theory: 5,060 picks, +2.66% ROI headline **collapses to t=0.36 at day-as-unit** (bootstrap 95% CI [−11.0%, +17.8%])
- Polymarket liquidity enrichment is completely dead — 0/605 fill rate despite module being wired into tracker. Largest infrastructure gap found.
- 414 Pinnacle niche-league theories auto-spawned, ~399 have zero picks in 30d — dead weight on pipeline evaluation cycles
- Only statistically significant PM cell: Underdog Predictions (65 picks, 6 days, +14.4% ROI, t=+2.18) — borderline n, needs forward holdout

**Decisions Made:**
- Polymarket liquidity fix is the #1 priority — unlocks liquidity-gating, per-book signal decomposition, and the execution path
- Underdog Predictions needs pre-registered forward-holdout before shipping (Part 5 discipline: t>2 on borderline n = hold, don't ship)
- Novig sharp weight 0.8 in PM Edge — Strict contradicts 2026-06-01 stake-window finding (dropped to NBA #11); needs resolution

**Lessons Learned:**
- OO SSE relay can be the dominant data source for a book without any direct scraper — code inventory audits that only check for scraper files will miss this
- The +2.7% PM Edge headline was a classic pick-count-CI artifact: √(5060/15) ≈ 18× naive CI tightening disappears when you correct for intra-day correlation → t=0.36
- Bases prop on PM books is t=−1.70 / ROI −9.6% — near-significant negative, mechanistically plausible (casual HR narratives juice Over lines)
- Pinnacle had only 52 markets total at check time — extreme drought means Pinnacle-weighted sharps (weight 1.0) are pulling from thin data into devig

**Action Items:**
1. Debug and fix `poly_liquidity` enrichment in `ev_scanner/polymarket_liquidity.py` (likely: condition_id lookup failure, Gamma API silent 4xx, or swallowed async exception)
2. Pre-register Underdog-only PM Edge as forward-holdout theory
3. Auto-deactivate 399 dormant Pinnacle niche-league theories (0 picks in 30d threshold)
4. Audit/resolve Novig 0.8 weight vs "worst tested (Brier 0.2887)" contradiction
5. Re-run full PM audit after fixes 1–4 land
6. 7 new entries appended to edge-research learnings catalog (SKILL.md L375-L387)

### Session (09:12) [value-betting-scanner]

**Context:** Building and deploying "Claude God" — an automated knowledge extraction system on Eve (VPS) that pulses hourly, reads Claude session JSONLs, extracts findings/decisions/skills via Claude SDK, and writes them to a shared team brain.

**Key Exchanges:**
- Designed and implemented full pipeline: JSONL scanner → transcript renderer → Claude SDK extractor → brain indexer → systemd timer
- Debugged `usage` being a dict not an object (needed `.get()` instead of `getattr`)
- Debugged empty extraction caused by SDK requiring a valid `cwd` — synthetic JSONL referenced `/shared/repos/vb-scanner` which didn't exist on Eve
- Discovered and fixed **recursion bug**: god's own SDK calls spawn new session JSONLs which get picked up on next pulse → infinite loop. Fixed with dedicated `god-sdk-cwd` path excluded from scan
- Errors were being silently swallowed in the extractor's catch block — improved error surfacing

**Decisions Made:**
- Use `uv` for clean venv management on Eve (over pip/pipx)
- Dedicated isolated cwd for god's SDK calls to prevent recursion (same pattern as original `flush.py`'s `CLAUDE_INVOKED_BY=memory_flush` guard)
- Delta-only processing: only extract from JSONL growth ≥500 bytes since last pulse, preserving original findings
- Hourly systemd timer (not more frequent) to balance cost vs freshness

**Lessons Learned:**
- Claude SDK spawns a subprocess that writes its own session JSONL — any tool that uses the SDK to process sessions must exclude its own spawned sessions or face infinite recursion
- Claude SDK requires a valid `cwd` for its subprocess; missing directories cause silent failures
- `usage` from Claude SDK is a dict, not an object with attributes — use `.get()` not `getattr()`
- Always surface errors from LLM SDK calls; silent catches hide critical failures

**Action Items:**
- God timer **disabled** at user's request — code remains installed, can re-enable with `sudo systemctl enable --now god.timer` or run `god pulse` manually
- Phase 2 queued: build `query-session` skill so one Claude session can read another's brain-summary without re-resuming the source

### Session (09:20) [value-betting-scanner]

**Context:** Deployed 4 new value-betting theories to production and discussed backtest findings (NBA sharps, devig methods, book mappings).

**Key Exchanges:**
- Mapped all previously-unidentified book IDs (804 Fanatics, 805 Betway, 806 Unibet, 809 Heritage, 810 theScore, 811 Bodog, 812 LowVig) — they were already in production code, just missing from local analysis scripts
- NBA sharps breakdown by prop: PointsBet AU (903) = combo specialist, TAB (908) = single-stat specialist, Sportsbet (900) = Points/Threes anchor, Hard Rock (803) = combo + Threes player-correlation, BetMGM (800) = Assists specialist, Novig (192) drops from #1→#11 at stake window (late-sharpener, useless for live betting)
- Power devig won the overall bake-off (~71% of #1 spots across sport × prop matrix). Only exception: MLB Home Runs where additive won due to extreme-longshot regime
- Claude Code supports custom subagent definitions at `~/.claude/agents/<name>.md` or `.claude/agents/<name>.md` with frontmatter for tools/model — no built-in "team mode" but parallel agent spawning achieves the same effect

**Decisions Made:**
- Added Coolbet (397), Sportsbet (900), Neds (901), PointsBet AU (903), TAB (908) to `SHARP_BOOK_IDS` in both `v3/core/models.py` and `ev_scanner/models.py`. Safety: existing theories default to weight 0.0 for new books — zero side effects
- Shipped 4 theories: MLB-Counts-Coolbet-v1 (1% min_ev), MLB-Batters-BetMGM-v1 (3% min_ev), NBA-Single-TAB-Sportsbet-v1 (0% min_ev), MLB-Hits-MultiBook-v1 (10% min_ev). All use power devig
- Did NOT ship per-prop NBA theories for Assists, Combos, or Rebounds-only — backtest n was too thin (<10 picks each). Next candidates once 2-3 weeks of data lands
- Using active theories as shadow tracker rather than building separate infrastructure — picks flow through existing engine + tracker pipeline, can choose to bet or just observe

**Lessons Learned:**
- Adding books to `SHARP_BOOK_IDS` is safe because `engine.py:294` defaults to `weight=0.0` for any book not explicitly weighted by a theory
- `v3/core/models.py` and `ev_scanner/models.py` had drifted (v3 was missing BetOnline 813) — need to keep these in sync or consolidate
- For devig: over-fitting a per-prop method on noise-level differences has negative EV vs. a single consistent choice (power everywhere)
- Interpolation routing already correct: logit for continuous/Points-containing props, Poisson for pure count props

**Action Items:**
- Monitor pick volume per new theory over next 24-48 hours (`nba_tracked_picks WHERE triggered_by IN (...)`)
- Track per-theory PnL after resolution + CLV per pick
- Compare live numbers to backtest projections (Coolbet-counts +14%, BetMGM-batters +31%, TAB-Sportsbet-NBA +25%, Hits-MultiBook +7%)
- After ~2 weeks: promote if within 5pp of backtest, retire if meaningfully negative
- End of 3 weeks: re-run full backtest with 3× cohort, true out-of-sample test (pre-2026-06-15 train / post test)
- Consider adding `is_shadow=true` flag if log-only mode needed (small schema + dashboard filter change)
- Consider creating custom agent files for VB workflow (`vb-data-puller.md`, `vb-backtester.md`, `vb-deploy-engineer.md`)

### Session (09:20) [value-betting-scanner]

**Context:** Deploying 4 winning value-betting theories to production and reviewing sharp book/devig findings from the 5-window backtest.

**Key Exchanges:**
- Mapped all previously-unidentified book IDs (804-813, 961) — they were already in production code, just missing from local analysis scripts
- Confirmed adding books to `SHARP_BOOK_IDS` is safe for existing theories — engine defaults to weight 0.0 for books a theory doesn't explicitly weight (engine.py:294)
- Discovered `v3/core/models.py` and `ev_scanner/models.py` had drifted — v3 copy was missing BetOnline (813); synced both
- NBA sharp breakdown by prop: PointsBet AU (903) = combo specialist, TAB (908) = single-stat specialist, Sportsbet (900) = Points/Threes anchor, Hard Rock (803) = combo + Threes, BetMGM (800) = Assists specialist
- Power devig wins ~71% of all sport×prop cells; only exception is MLB Home Runs where additive wins (extreme-longshot regime)
- Discussed Claude Code custom agent definitions (`~/.claude/agents/<name>.md`) — no built-in team mode but can define personas and spawn in parallel

**Decisions Made:**
- Ship only `NBA-Single-TAB-Sportsbet-v1` for now; per-prop NBA theories (Assists/BetMGM, Combos/PointsBet AU, Rebounds-only/TAB) deferred until n > 10 picks accumulate
- Use power devig universally (including props where multiplicative or additive tie within bootstrap noise) to avoid overfitting
- Shadow tracker = the 4 active theories themselves; defer `is_shadow` flag unless needed later
- Interpolation routing already correct: logit for Points-containing props, Poisson for pure count props

**Lessons Learned:**
- Novig (192) dominates at closing but drops from #1 → #11 at the stake window — late-sharpener, useless for live betting
- Per-prop devig optimization has negative EV when differences are within standard error of the estimate
- Two separate `models.py` files can drift silently — need a single source of truth or a sync check

**Action Items:**
- Monitor pick volume per new theory over 24-48h (`nba_tracked_picks WHERE triggered_by IN (...)`)
- Track per-theory PnL + CLV after resolution; compare to backtest projections (Coolbet-counts +14%, BetMGM-batters +31%, TAB-Sportsbet-NBA +25%, Hits-MultiBook +7%)
- At 3-week mark: re-run backtest with 3× cohort, true out-of-sample split (pre/post 2026-06-15), promote/demote based on OOS ROI
- Build per-prop NBA theories once fresh data accumulates (Assists, Combos, Rebounds-only)
- Consider drafting custom agent files for VB workflow team (`vb-data-puller`, `vb-backtester`, `vb-deploy-engineer`)

### Session (09:23) [value-betting-scanner]

**Context:** Deploying 4 winning value-betting theories to production and discussing sharp book / devig analysis results.

**Key Exchanges:**
- Mapped all previously-unidentified book IDs (804 Fanatics, 805 Betway, 806 Unibet, 809 Heritage, 810 theScore, 811 Bodog, 812 LowVig) — they were already in production code, just missing from local analysis scripts
- NBA sharps per prop: PointsBet AU (903) = combo specialist, TAB (908) = single-stat specialist, Sportsbet (900) = Points/Threes anchor, Hard Rock (803) = combo + Threes, BetMGM (800) = Assists specialist, Novig (192) drops from #1 → #11 at stake window (useless for live betting)
- Power devig won ~71% of #1 spots across the full sport × prop matrix; Home Runs is the sole exception where additive/Shin wins (extreme-longshot regime)
- Interpolation routing: logit for continuous props (Points, combos containing Points), Poisson for pure count props (Rebounds, Assists, Threes, R+A)

**Decisions Made:**
- Added Coolbet (397), Sportsbet (900), Neds (901), PointsBet AU (903), TAB (908) to `SHARP_BOOK_IDS` in both `v3/core/models.py` and `ev_scanner/models.py` — safe because existing theories default to weight 0.0 for unknown books
- Shipped only 1 NBA theory (`NBA-Single-TAB-Sportsbet-v1`) because per-prop NBA theories (Assists/BetMGM, Combos/PointsBet AU, Rebounds-only/TAB) had n < 10 — wait for 2-3 weeks of fresh data before building those
- Used `is_active=true` instead of a shadow tracker — the existing engine + tracker pipeline already tracks picks and PnL; `is_shadow=true` flag deferred as optional future work
- Used power devig uniformly for NBA (per-prop differences are within bootstrap noise)

**Lessons Learned:**
- `v3/core/models.py` and `ev_scanner/models.py` had drifted (v3 was missing BetOnline 813) — need to keep these in sync or consolidate
- Adding books to `SHARP_BOOK_IDS` is safe for existing theories due to the `book_weights.get(book_id, 0.0)` default at engine.py:294
- Novig's closing-line dominance doesn't translate to stake-window utility — it's a late-sharpener, not a predictive sharp
- Claude Code custom agents live in `~/.claude/agents/<name>.md` or `.claude/agents/<name>.md` with YAML frontmatter (tools, model, description); no built-in "team mode" but parallel Task calls + background execution achieves the same thing

**Action Items:**
- Monitor 4 new theories over 24-48h: pick volume, per-theory PnL, CLV per pick
- Compare live numbers to backtest projections after ~2 weeks (Coolbet-counts +14%, BetMGM-batters +31%, TAB-Sportsbet-NBA +25%, Hits-MultiBook +7%)
- At end of 3 weeks: re-run full backtest with 3× cohort, true out-of-sample test (pre-2026-06-15 train, post-2026-06-15 test)
- Build per-prop NBA theories (Assists/BetMGM, Combos/PointsBet AU, Rebounds/TAB) once fresh data accumulates n > 10
- Optionally add `is_shadow=true` schema + dashboard filter if log-only mode is needed
- Consider drafting custom Claude agent files for VB workflow (data-puller, backtester, deploy-engineer)

### Session (09:28) [value-betting-scanner]

**Context:** Deployed 4 new sharp-book theories to the v3 value-betting engine and discussed devig/sharpness findings from the NBA/MLB bake-off.

**Key Exchanges:**
- Catalogued NBA sharps per prop: PointsBet AU (903) = combo specialist, TAB (908) = single-stat specialist, Sportsbet (900) = Points/Threes anchor, Hard Rock (803) = player-correlation modeller, BetMGM (800) = Assists specialist, Novig (192) drops from #1→#11 at stake window (late-sharpener, useless live)
- Power devig won ~71% of #1 spots across the full (sport × prop) matrix; MLB Home Runs was the sole exception where additive/Shin won (extreme-longshot regime)
- MLB Strikeouts is NOT a clean power win — multiplicative came within overlapping bootstrap CIs; all books were under-confident by ~+12pp on this prop (hidden edge); need ~3× current n to distinguish methods
- Novig dominance at closing but collapse at stake window is a key finding — "late-sharpener" pattern

**Decisions Made:**
- Use power devig universally across NBA props — differences between methods are smaller than standard error
- Did NOT ship per-prop NBA theories for Assists, Combos, or Rebounds-only due to thin backtest n (<10 picks each) — waiting for 2-3 weeks of fresh data
- Treat Strikeouts as a wider-CI cell; default to power but don't over-fit

**Lessons Learned:**
- Claude Code custom agents live in `~/.claude/agents/<name>.md` or `.claude/agents/<name>.md` with frontmatter (name, description, tools, model); no built-in "team mode" but parallel Agent spawning + background execution achieves the same thing
- Orchestration pattern: define persona files → spawn parallel via Task calls → `run_in_background: true` → resume via SendMessage with agent ID
- Persistence options: `/schedule` for cron, `/loop` for interval, hooks for session events
- An external agent session (8616f...) queried this session about Strikeouts devig — demonstrates cross-session knowledge retrieval working

**Action Items:**
- Monitor 4 new theories over 24-48h: pick volume, per-theory PnL, CLV per pick
- Compare live numbers to backtest projections after ~2 weeks (Coolbet-counts +14%, BetMGM-batters +31%, TAB-Sportsbet-NBA +25%, Hits-MultiBook +7%)
- Consider adding `is_shadow=true` flag if want log-only/never-recommend mode
- Build next NBA theories once fresh data lands: Assists (BetMGM), Combos (PointsBet AU), Rebounds-only (TAB)
- Potentially draft custom agent files for VB workflow team (data-puller, backtester, deploy-engineer)

### Session (09:41) [value-betting-scanner]

**Context:** Deployed 4 new sharp-book theories to the v3 value-betting engine and answered follow-up questions about the devig bake-off results.

**Key Exchanges:**
- Mapped all unidentified book IDs (804 Fanatics, 805 Betway, 806 Unibet, 809 Heritage, 810 theScore, 811 Bodog, 812 LowVig) — they were already in production code, just missing from local analysis scripts
- Identified NBA sharp specialists: PointsBet AU (903) = combo specialist, TAB (908) = single-stat specialist, Sportsbet (900) = Points/Threes anchor, Hard Rock (803) = combos + Threes, BetMGM (800) = Assists specialist, Novig (192) = useless at stake window (drops #1→#11)
- Confirmed power devig won the overall bake-off (~71% of #1 spots), stable across all 5 measurement windows — method ranking rotates by PROP not by window
- MLB Home Runs is the single prop that broke the power pattern (additive/shin wins due to extreme-longshot regime)
- Custom agent definitions live at `~/.claude/agents/<name>.md` or `.claude/agents/<name>.md` with YAML frontmatter (name, description, tools, model); no built-in "team mode" but parallel Task calls achieve it

**Decisions Made:**
- Added Coolbet (397), Sportsbet (900), Neds (901), PointsBet AU (903), TAB (908) to SHARP_BOOK_IDS — safe because engine defaults to weight 0.0 for books not in a theory's config, so existing theories unaffected
- Used power devig for all 4 new theories rather than per-prop devig — over-fitting on noise-level differences has negative EV
- Did NOT ship per-prop NBA theories for Assists, Combos, or Rebounds-only due to thin backtest n (<10 picks each) — revisit after 2-3 weeks of fresh data
- Skipped `is_shadow=true` flag for now — existing pick pipeline already works as shadow tracker; can add the flag later if needed

**Lessons Learned:**
- `v3/core/models.py` and `ev_scanner/models.py` had drifted (v3 was missing BetOnline 813 from SHARP_BOOK_IDS) — need to keep these synced or consolidate
- Novig (192) dominates at closing but is useless at the live stake window — a "late-sharpener" that can't help real-time betting
- Devig method rankings are prop-driven not window-driven — cross-window stability is the strongest evidence the differences are real

**Action Items:**
- Monitor pick volume, per-theory PnL, and CLV for the 4 new theories over 24-48 hours
- Compare live numbers to backtest projections after ~2 weeks (Coolbet-counts +14%, BetMGM-batters +31%, TAB-Sportsbet-NBA +25%, Hits-MultiBook +7%)
- Build per-prop NBA theories for Assists (BetMGM), Combos (PointsBet AU), Rebounds-only (TAB) once fresh data lands
- Consider drafting custom agent files for VB workflow team pattern (data-puller, backtester, deploy-engineer)

### Session (09:44) [value-betting-scanner]

**Context:** Multi-issue investigation and fix deployment for the v3 value-betting scanner pipeline

**Key Exchanges:**
- Diagnosed MLB fixture resolution failure — only "Oakland Athletics vs New York Yankees" failed out of 14 fixtures due to 2025 MLB team rename
- Confirmed via MLB Stats API that the canonical name is now just "Athletics" while bookies still emit "Oakland Athletics"
- tracker.py sharp-alt investigation turned out to be a FALSE ALARM — SSH transient caused a misleading sentinel-missing read; all 6 sentinels were present on Eve

**Decisions Made:**
- Added `_TEAM_ALIASES` dict + `_canonicalize_fixture` helper for symmetric team-name resolution (works regardless of which side has the legacy name)
- Issue 4 (diff-key collision instrumentation) DEFERRED — no data loss since echoes have identical odds
- Issue 5 (phantom-EV path 2) DEFERRED — root cause documented (`POISSON_PROPS` missing MLB count props) but no cap applied yet
- Committed in 4 focused commits rather than one monolith: resolver fix, MLB diff_key fix, engine one-sided fallback gate, alt-rollout cluster
- Always push to remote for history — "we must always deploy so we have the history of updates"

**Lessons Learned:**
- 2025 MLB rename (Oakland → Athletics, no city prefix) breaks fixture matching; bookies lag behind official name changes — alias maps needed
- SSH transient reads can mimic real deployment drift — always verify with hash comparison, not single reads
- Alt-rollout code was live on Eve but not in git for 4 days — git-Eve alignment is a hygiene gap to watch
- Rebase conflicts in append-only log files (brain/log.md) resolve by keeping both entries in chronological order

**Action Items:**
- Validate Athletics resolution on next hourly resolver cycle — expect `skip_no_player` to drop by ~60 picks/cycle
- AU bookie adapter rollout files still uncommitted (betit, neds, pointsbet, tab, tabtouch adapters + tests)
- Name canon audit and PM Edge theories findings docs still uncommitted
- Phantom-EV path 2 fix still open — needs `POISSON_PROPS` MLB count props added in `v3/core/interpolate.py:154`

### Session (09:46) [value-betting-scanner]

**Context:** Debugging frozen EV pick display on the VPS-hosted NBA theory picks dashboard (`/nba` at port 8803)

**Key Exchanges:**
- Friend reported sharp book odds moving on `/topdown` but EV column frozen on `/nba` theory picks page
- Root cause traced through three layers: VPS proxy hardcoded `pulse: 0` → JS falsy guard `if (data.pulse)` never fires on `0` → `computeEVPicks()` memo cache key always matches → EV never recomputes after first poll

**Decisions Made:**
- Applied browser-side cache-bust as the load-bearing fix (bust `_evPicksCache.pulse = -1` after every poll/SSE frame) rather than relying solely on upstream pulse — safer if mini PC restarts and resets pulse counter
- Also fixed VPS `server.py` to emit `int(time.time() * 1000)` as defense-in-depth instead of hardcoded `pulse: 0`
- Fixed both SSE and polling branches in `index.html` (two separate code paths had the same falsy bug)

**Lessons Learned:**
- `if (data.pulse)` is a falsy trap — `0` is a valid pulse value but JS treats it as false. Use `!== undefined` for presence checks on numeric fields
- VPS proxy was building its own response envelope and discarding upstream metadata — proxy layers that reconstruct envelopes are a recurring source of data loss
- The mini PC `/v1/odds` endpoint returns a bare list with no pulse in the envelope anyway, so the VPS couldn't have preserved it even if it tried
- `/topdown` was immune because it renders straight from `markets[]` with no pulse-keyed memoization

**Action Items:**
- `MAX_SOFT_ODDS_AGE_S = 3600` still set to 60 min — stalled bet365 worker leaves picks on screen for an hour at last-seen price. Should be tightened back down now scraper is stable
- VB-V3-Healthcheck skill updated with pulse cache-bust marker check and symptom table entry for this failure mode

### Session (09:46) [value-betting-scanner]

**Context:** Shipped MLB player-prop scraping across all AU direct-scrape bookies, deployed to Eve, updated healthcheck skill, and diagnosed a BetIT discovery bug.

**Key Exchanges:**
- Rolled out MLB scrapers for PointsBet, BetIT, Neds, TabTouch — ~2,300 new MLB entries on Eve from books that had 0 pre-deploy
- All 7 books (bet365 + 6 AU direct) landing trail_entries at comparable rates (~34k trails/hour, ~565/min)
- 73% sharp trails, 27% soft trails — sharps dominate because Pinnacle/DraftKings lines move every few seconds pre-game
- Trail volume (~5M/month) is intentional for bet-timing backtester and within Supabase Pro I/O budget

**Decisions Made:**
- **Per-sport prop maps, not per-sport adapters** — same `parse_event` entrypoint dispatches on `sport` kwarg. Keeps adapter count low.
- **PointsBet "(Match)" marker** means opposite things by sport — skip on NBA, keep on MLB. Documented as a gotcha.
- **MLB skips combos that don't apply** — no DD/TD on MLB; NBA-only YES/NO dispatch table is empty for MLB.

**Lessons Learned:**
- **BetIT `base_competition_id` vs `competition_id` trap** — bmapollo's `nextUpMarkets` filter strictly differentiates the two. Passing competition_id where base was expected returns **silently zero events** (no error, just empty). The correct MLB base_competition_id is `2505f17b-84c1-5f87-b97f-3b3ff234dafe`, not `299024d3-…`. Added a comment in scraper.py to prevent recurrence.
- **BetIT loads MLB player props only ~2-3 hours before first pitch** — 0 rows from BetIT MLB during early discovery is expected upstream timing behavior, not a parser bug.
- **Push-loop heartbeat (120s)** re-pushes unchanged entries to keep `captured_at` fresh — visible in TAB logs where odds are slow-moving.
- **Direct scraper cadence is ~27-28s** (config: 30s); bet365 WS is 5-7s; bet365 wizard MLB is 17-20s. All healthy.

**Action Items:**
- Monitor BetIT MLB prop population as tonight's games approach first pitch — confirm adapter parses correctly in production (worked locally with 302 rows on Rangers-Astros yesterday)
- Healthcheck skill updated with: per-AU-scraper sport-aware liveness, stable-ID coverage check (≥90% market_id), push-loop cadence check (90s gap threshold)
- Watch for 522 errors if trail volume grows beyond current ~5M/month Supabase budget (CLAUDE.md gotcha #10)

### Session (09:52) [value-betting-scanner]

**Context:** Building the `/ask-session` skill — agent-to-agent communication between Claude Code sessions via `--fork-session`

**Key Exchanges:**
- User corrected initial architecture: must use Claude Code (not raw Anthropic API) so forks get full runtime (tools, hooks, MCP, skills, CLAUDE.md)
- Confirmed design: Session A spots a heavily-cited session UUID in brain docs → forks that session → asks question with `{caller-uuid}: {question}` prefix → gets answer without disturbing target session
- `claude --resume <uuid> --fork-session -p "<msg>"` confirmed working — target JSONL byte-identical after fork, fork lands as separate UUID file

**Decisions Made:**
- Agent-to-agent protocol uses `{session-uuid}: {question}` prefix convention so the fork recognizes it's an agent caller (not a human)
- Skill lives at `/shared/claude-skills/ask-session/` on Eve (canonical, team-wide) with symlink into `~/.claude/skills/` — mirrors `/shared/brain/`, `/shared/claude-god/` pattern
- Fork outputs stay on disk for Claude God to ingest as Q&A artifacts on next pulse

**Lessons Learned:**
- `--fork-session` flag exists natively in Claude CLI — makes the fork read-only against original session, creates new UUID
- Fork is a full Claude Code agent (not just API transcript replay) — has tools, hooks, MCP, CLAUDE.md, skills
- SSH key auth to Eve broke (likely during earlier gdm3 restart) — workaround was SCP + remote bash of install script

**Action Items:**
- Run `/tmp/install-ask-session-on-eve.sh` on Eve to deploy the skill team-wide
- Re-authorize SSH pubkey on Eve (`ssh-ed25519 AAAAC3...`) so Claude can reach Eve directly
- Update `/shared/brain/wiki/claude-god.md` Phase 2 status from "in-progress" to "shipped" after install confirmed
- Future: add UUID-by-topic lookup (`ask.sh --topic "devig" "<q>"` → greps brain for most-cited UUID)
- Future: re-enable Claude God so fork Q&A artifacts auto-fold into brain

### Session (09:56) [value-betting-scanner]

**Context:** Debugging why `/topdown` dashboard showed zero picks despite healthy scrapers and data pipeline

**Key Exchanges:**
- Ran full healthcheck with Gamma focus — everything GREEN except two observability YELLOWs (Gamma freshness lag from diff-only push, alt-line multi-game collapse now visible via new log format)
- User reported zero `/topdown` picks — root-caused to `captured_at` timestamps going stale (3+ hours) because the push loop only updated timestamps when odds *values* changed, not when the scraper confirmed the quote was still live
- Bet365 NBA median age was 13,426s (3.7h), MLB 11,115s (3h) — both far beyond `topdown.html`'s `MAX_SHARP_AGE_S = 600` freshness gate

**Decisions Made:**
- Chose **Option B** (heartbeat-refresh `captured_at` in push loops) over Option A (bump threshold) or Option C (per-book heartbeat field) — because `captured_at` semantically should mean "scraper still quoting this value at time T", not "odds last moved at time T"
- `PUSH_HEARTBEAT_S = 120` (env-tunable) — re-pushes unchanged odds every 2 minutes to keep `captured_at` fresh
- Applied fix to **both** push loops (`_bet365_push_loop` and `_direct_scraper_push_loop`) since all 8 books had the same dedupe structure

**Lessons Learned:**
- Diff-only push deduplication (`if prev == odds: continue`) silently starves downstream freshness gates when markets are legitimately static (closed NBA Finals markets, frozen MLB lines between scrapes)
- The scraper being alive ≠ data appearing fresh to consumers — the push layer is an invisible staleness amplifier
- Changed `last_pushed` cache from `{key: odds_value}` to `{key: (odds_value, last_push_time)}` tuple pattern to track both dimensions
- Push volume impact of heartbeat: ~4 extra entries/sec total across all books — trivial overhead

**Action Items:**
- Commit `30feff4` is on local `main` only — needs pushing to remote (either existing `claude/polymarket-gamma-and-engine-throttle` branch or new branch)
- Prior local commit `3489313 fix(bet365-nba): keep only the centre line per (player, prop, side)` also local-only — needs integration
- Option C (per-book heartbeat via `bookie_status` field) remains as future architectural improvement if needed

### Session (10:01) [value-betting-scanner]

**Context:** Deployed "Claude God" — an autonomous knowledge extraction system on Eve (VPS), plus validated the ask-session cross-agent query skill as Phase 2.

**Key Exchanges:**
- Built and deployed full Claude God pipeline on Eve: JSONL scanner → Claude SDK extractor → brain indexer, running as systemd hourly timer
- Debugged several non-obvious issues: `usage` is a dict not object (`.get()` not `getattr`), SDK requires valid `cwd` (synthetic JSONL's cwd didn't exist on Eve), errors were silently swallowed in catch blocks
- Discovered and fixed recursive session creation: God's own SDK calls spawn JONLs that would be scanned next pulse → infinite loop. Fixed with dedicated `god-sdk-cwd` path + `exclude_path_patterns`
- Reviewed existing `ask-session` skill — recognized it as complete Phase 2 implementation, not a rough sketch

**Decisions Made:**
- God runs hourly via systemd timer (not continuous), processes only delta (≥500 byte growth threshold)
- Recursion prevention via isolated cwd for God's own SDK subprocess, not env-var guard like flush.py's `CLAUDE_INVOKED_BY`
- Brain output is identity-detached (session UUID only, no developer name tagged)
- `--fork-session` approach for cross-agent queries beats the lock-based design from original plan — target JSONL stays read-only

**Lessons Learned:**
- Claude SDK spawns a subprocess that writes its own session JSONL — any tool using the SDK must account for this or risk recursive scanning
- SDK errors get swallowed silently if you're not careful with error handling in extractors — always surface errors in pulse summary
- `usage` object from Claude SDK is a dict, not a class instance — use `.get()` not `getattr()`
- The `cwd` field in JSONL must point to an existing directory on the machine running the SDK, not just the machine that created the session
- Fork-session is the elegant primitive for agent-to-agent: full runtime (tools/hooks/skills/MCP), read-only source, auto-provenance via `<caller-uuid>:` prefix

**Action Items:**
- Monitor first real (non-synthetic) God pulse to confirm production extraction works
- Brain will start compounding once real team sessions happen on Eve
- Consider adding per-user scan paths to `config.json` as team grows
- Doc created at `/shared/brain/wiki/claude-god.md` (canonical) and mirrored locally at `~/Documents/value-betting/value-betting-scanner/brain/infra/claude-god.md`

### Session (10:04) [value-betting-scanner]

**Context:** Investigating trail capture health in the v3 value-betting scanner, uncovering a critical trail-depth regression.

**Key Exchanges:**
- Trail *coverage* per book is healthy (99.25% of 400 recent picks have complete coverage) — Bet365 contamination from earlier audits has also cleared
- Trail *depth* has collapsed: median sharp trail rows per pick dropped from 61 (6-10 days ago) to 1 (today) — this is a confirmed regression, not a timing artifact, since even fully settled picks show median=1

**Decisions Made:**
- Trail coverage ≠ trail depth; coverage audit alone gives false confidence — depth must be checked independently
- The regression is real and recent (last 1-2 days), not a "picks haven't accumulated yet" issue

**Lessons Learned:**
- **Root cause identified:** Phase B (the loop adding trail rows to existing picks) only fires meaningfully on the first cycle after an Eve restart, then goes silent — subsequent cycles write 0 updates to existing picks
- Three plausible mechanisms: (1) Phase B only iterates +EV markets, not all markets with tracked picks; (2) `sharp_snap` is None for most markets due to theory weight mismatch; (3) `_should_append_sharp` hash check short-circuits because in-memory cache is stale
- **Likely trigger:** the 2026-05-28 alt-line rollout (`28ab39e`) restructured how `sharp_snap` is built and narrowed the Phase B iteration scope
- The tracker log pattern `0 updated` on every normal cycle (vs `2604 updated` on cold-start) is the telltale signature

**Action Items:**
- Read the Phase B outer loop to confirm whether iteration is over ALL markets or just +EV-this-cycle markets
- Fix Phase B to iterate all markets with tracked picks, not just current +EV markets — sharp trails should capture continuously regardless of +EV state
- Investigate whether alt-line rollout commit `28ab39e` narrowed iteration scope

### Session (10:28) [brain-test]

**Context:** Shipped Claude God indexer rewrite (provenance-rich brain format) and installed Gitea on Eve with webhook → brain integration.

**Key Exchanges:**
- Discussed inline provenance design: every claim carries its own date + source session, not just file-level metadata. Table-of-claims format with supersession, confidence decay, bi-directional links, and Q&A as first-class citizens.
- Evaluated local Git hosting options — Gitea chosen as lightweight single-binary solution over GitLab. Tailscale-only access, SQLite backend, no TLS needed.
- Identified that PR discussions are the brain's biggest gap — decisions crystallise in reviews but weren't being ingested.

**Decisions Made:**
- Brain findings use **slug-keyed dedup** so future sessions compound into existing findings rather than fragmenting per-date
- Gitea listens on Tailscale IP only (100.69.199.85:3000, SSH :2222) — no public exposure
- GitHub repos stay on GitHub for now; Gitea used for new internal stuff first, with `god repo-import` available for gradual migration
- No CI/CD on Gitea yet — tests run directly on Eve

**Lessons Learned:**
- LLM-driven backfill is inherently non-idempotent: re-extraction produces slightly different titles → different slugs → duplicate files. Pulse-path (delta-byte tracking) is properly idempotent. Semantic dedup (embedding-based slug lookup) needed eventually.
- Gitea install wizard can be bypassed via direct POST to `/install` endpoint — avoids manual browser setup
- Webhook receiver as a separate systemd service (`god-webhook.service` on :9000) keeps concerns separated from Claude God pulse timer

**Action Items:**
- Re-enable Claude God timer (`sudo systemctl enable --now god.timer`) — currently disabled
- Implement semantic finding dedup (embedding-based slug lookup) to handle backfill non-determinism
- Update `/shared/brain/wiki/claude-god.md` to reflect shipped status
- Set up admin token for `god repo-import` for non-interactive GitHub pulls
- Decide on GitHub mirror strategy after testing Gitea workflow with team

### Session (11:02) [value-betting-scanner]

**Context:** Deploying 4 winning value-betting theories to production and discussing NBA sharp book findings from the 5-window backtest.

**Key Exchanges:**
- Mapped all previously-unidentified book IDs (804 Fanatics, 805 Betway, 806 Unibet, 809 Heritage, 810 theScore, 811 Bodog, 812 LowVig) — they were already in production code, just missing from local analysis scripts
- Added Coolbet (397), Sportsbet (900), Neds (901), PointsBet AU (903), TAB (908) to `SHARP_BOOK_IDS` in both `v3/core/models.py` and `ev_scanner/models.py` — safety verified that existing theories ignore new books (default weight 0.0)
- Created and deployed 4 Supabase theories: MLB-Counts-Coolbet-v1, MLB-Batters-BetMGM-v1, NBA-Single-TAB-Sportsbet-v1, MLB-Hits-MultiBook-v1
- Confirmed v3 engine uses `v3/core/models.py` not `ev_scanner/models.py` — they had drifted (v3 was missing BetOnline 813)
- NBA sharp book specializations identified: PointsBet AU = combo specialist, TAB = single-stat specialist, Sportsbet = Points/Threes anchor, Hard Rock = combo + Threes, BetMGM = Assists specialist, Novig drops from #1→#11 at stake window (late-sharpener, useless live)
- Power devig won the overall bake-off (~71% of #1 spots). Only exception: MLB Home Runs where additive/Shin won due to extreme-longshot regime
- Interpolation routing: logit for continuous props (anything containing Points), Poisson for pure count props — already correct in `v3/core/interpolate.py`

**Decisions Made:**
- Ship power devig universally rather than per-prop optimization — differences are within bootstrap noise, over-fitting would have negative EV
- Did NOT ship per-prop NBA theories for Assists, Combos, or Rebounds-only due to thin backtest n (<10 picks each) — wait for 2-3 weeks of fresh data
- Shadow tracker = the 4 new active theories themselves; no separate `is_shadow` flag needed yet (can add later if desired)
- Committed as `ab1aed7`

**Lessons Learned:**
- Adding books to `SHARP_BOOK_IDS` is safe for existing theories because `book_weights.get(book_id, 0.0)` defaults to zero — new books are silently ignored by theories that don't weight them
- `v3/core/models.py` and `ev_scanner/models.py` can drift — need to keep them in sync or consolidate
- Novig (192) is a "late-sharpener" — dominates at closing but useless at stake window; lesson for future theory design
- Claude Code supports custom subagents via `~/.claude/agents/<name>.md` or `.claude/agents/<name>.md` with frontmatter (tools, model, description); no built-in "team mode" but parallel Task calls + background execution achieve the same thing

**Action Items:**
- Monitor pick volume per new theory over 24-48h: query `nba_tracked_picks WHERE triggered_by IN (...)` 
- Track per-theory PnL after resolution and compare to backtest projections (Coolbet-counts +14%, BetMGM-batters +31%, TAB-Sportsbet-NBA +25%, Hits-MultiBook +7%)
- After 2 weeks: promote if within 5pp of backtest, retire if meaningfully negative
- Build per-prop NBA theories (Assists/BetMGM, Combos/PointsBet AU, Rebounds/TAB) once fresh data lands
- Consider adding `is_shadow=true` flag to schema + dashboard if log-only mode is needed
- Consider drafting custom agent files for VB workflow team (data-puller, backtester, deploy-engineer)

### Session (11:02) [value-betting-scanner]

**Context:** Deploying 4 backtest-winning betting theories to production and reviewing sharp book / devig findings from the 5-window audit.

**Key Exchanges:**
- Mapped all previously-unidentified book IDs (804 Fanatics, 805 Betway, 806 Unibet, 809 Heritage, 810 theScore, 811 Bodog, 812 LowVig) — they were already in production code, just missing from local analysis scripts
- NBA sharps breakdown per prop: PointsBet AU (903) = combo specialist, TAB (908) = single-stat specialist, Sportsbet (900) = Points/Threes anchor, Hard Rock (803) = combo + Threes, BetMGM (800) = Assists specialist, Novig (192) drops from #1→#11 at stake window (late-sharpener, useless for live)
- Power devig won ~71% of #1 spots across full matrix; only exception is MLB Home Runs where additive wins (extreme-longshot regime)
- Explored Claude Code custom agent architecture — no built-in "team mode" but custom agents via `~/.claude/agents/<name>.md` with frontmatter (tools, model, description), invoked via Task tool, can run in parallel/background

**Decisions Made:**
- Added Coolbet (397), Sportsbet (900), Neds (901), PointsBet AU (903), TAB (908) to `SHARP_BOOK_IDS` in both `v3/core/models.py` and `ev_scanner/models.py` — safe because existing theories default to weight 0.0 for unknown books
- Shipped 4 theories: MLB-Counts-Coolbet-v1, MLB-Batters-BetMGM-v1, NBA-Single-TAB-Sportsbet-v1, MLB-Hits-MultiBook-v1 — all `is_active=true`, power devig
- Shadow tracker = the theories themselves; existing engine + tracker pipeline handles pick generation and resolution automatically
- Did NOT ship per-prop NBA theories for Assists, Combos, or Rebounds-only due to thin backtest n (<10 picks each) — wait for 2-3 weeks of fresh data

**Lessons Learned:**
- `v3/core/models.py` and `ev_scanner/models.py` had drifted (v3 was missing BetOnline 813) — need to keep these in sync or consolidate
- Power devig is safe as a universal default; per-prop devig over-fitting on noise-level Brier differences has negative EV
- Interpolation routing already correct: logit for continuous props (anything containing Points), Poisson for pure count props
- Custom Claude agents are defined as `.md` files with YAML frontmatter, not via settings.json — orchestrated by spawning multiple Task calls in parallel from the main thread

**Action Items:**
- Monitor pick volume per new theory over 24-48h: `nba_tracked_picks WHERE triggered_by IN ('MLB-Counts-Coolbet-v1', ...)`
- Track per-theory PnL + CLV after resolution; compare to backtest projections (Coolbet-counts +14%, BetMGM-batters +31%, TAB-Sportsbet-NBA +25%, Hits-MultiBook +7%)
- If live numbers diverge >5pp negative from backtest after ~2 weeks, retire; if within range, promote
- Consider adding `is_shadow=true` flag for log-only/never-recommend mode (small schema + dashboard filter change)
- Build next NBA theories (Assists/BetMGM, Combos/PointsBet AU, Rebounds/TAB) once fresh data lands
- Optionally draft custom agent files for VB workflow team pattern (`vb-data-puller`, `vb-backtester`, `vb-deploy-engineer`)

### Session (11:14) [Knowted admin]

**Context:** Luke was working on a co-founder accountability plan (v2) for his startup Knowted, incorporating feedback from a banker meeting on 2 Jun.

**Key Exchanges:**
- Drafted v2 of cofounder-accountability-plan with Jeffrey, capturing significant structural changes from v1 (content targets, outreach renaming, time allocation, strategic actions from banker feedback)
- Luke asked what he should do to commit — specifically whether to set up a LinkedIn outreach system
- Advised: ship video #1 first (founder POV, ~90 min effort), then build LinkedIn system on Thursday Knowted shift — "public video is the commitment signal; the system is what makes it scalable"

**Decisions Made:**
- Content target revised to 2 YouTube + 3 shorts/week (shorts cut from longs)
- Luke's schedule: Tues + Thurs as locked Knowted shift days
- Investor outreach rebranded as "Partnership Relations" expanding scope to investors + grants + co-founder conversations
- Pitch deck overhaul due by 16 Jun: cut to ~3 pages, lead with margin/cost leadership, kill Google-exit thesis
- Denny reactivation target: 9 Jun
- Co-founder search: open but equity-for-cash-only; 1 convo/week

**Lessons Learned:**
- Infrastructure without output is the classic founder trap — don't spend a week on LinkedIn automation before having anything public
- Banker feedback: de-emphasise operations in pitch, fix co-founder bio to highlight agency background, revise ask/valuation

**Action Items:**
- Ship video #1 this week (founder POV, no product demo needed)
- Build LinkedIn outreach system (Sales Navigator + sequence tool + templates) on Thursday shift
- Send v2 accountability plan to Jeffrey for review + Section 8 answers
- Jeffrey to accept banker's investor intros by 9 Jun, first meetings by 16 Jun
- Both propose revised raise size + valuation at 4 Jul review

### Memory Flush (11:14) [Knowted admin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (11:47) [value-betting-scanner]

**Context:** Deploying 4 new winning betting theories to production + fixing tracker movement bugs in the value-betting scanner on Eve VPS.

**Key Exchanges:**
- Mapped all 10 previously-unidentified book IDs (804-813, 961) — they were already in production code, just missing from local analysis scripts
- Confirmed adding books to `SHARP_BOOK_IDS` is safe — engine defaults to weight 0.0 for unweighted books, so existing theories unaffected
- Identified the "shadow tracker" is unnecessary as a separate build — the 4 new active theories ARE the shadow tracker via the existing pick/trail pipeline

**Decisions Made:**
- Added Coolbet (397), Sportsbet (900), Neds (901), PointsBet AU (903), TAB (908) to `SHARP_BOOK_IDS` in both `v3/core/models.py` and `ev_scanner/models.py`
- Created 4 theories: MLB-Counts-Coolbet-v1, MLB-Batters-BetMGM-v1, NBA-Single-TAB-Sportsbet-v1, MLB-Hits-MultiBook-v1
- Decided against building a separate `is_shadow` flag — just observe picks without betting for now

**Lessons Learned:**
- **prop_filter storage bug:** Engine reads `theory.get("prop_filter")` or `weights.get("prop_filter")` — storing it as a top-level Supabase TEXT column (stringified JSON `'["Hits"]'`) breaks parsing. Must nest inside `weights` JSONB as a real list, like Basic-A does.
- **Tracker heartbeat restart bug:** `_should_append_soft` requires `soft_last_ts is not None`, but DB-loaded picks on restart weren't seeded with `soft_last_ts` — only freshly created picks via `_staged_ids` got it. Fix: seed `"soft_last_ts": time.time()` in cached_state rebuild at `tracker.py:1645`.
- **v3/core/models.py vs ev_scanner/models.py drift:** These two copies of SHARP_BOOK_IDS had drifted (v3 was missing BetOnline 813). Need to keep them in sync or consolidate.
- **Eve restart fragility:** The SIGKILL + tmux relaunch sequence knocked Eve offline entirely (Tailscale went offline 26min after restart). `eve_watchdog` does NOT auto-respawn v3.

**Action Items:**
- Verify both fixes once Eve comes back online: (a) trail volume should jump to hundreds per 5-min window, (b) 4 new theories should fire picks
- Run `tmux new-session -d -s v3` restart command when Eve reconnects
- Monitor 24-48h: pick volume per new theory, per-theory PnL, CLV per pick
- Compare live numbers to backtest projections (Coolbet-counts +14%, BetMGM-batters +31%, TAB-Sportsbet-NBA +25%, Hits-MultiBook +7%) — promote if within 5pp after ~2 weeks
- Consider consolidating the two models.py files to prevent future drift

### Session (12:20) [value-betting-scanner]

**Context:** Fixing the v3 value-betting dashboard to show honest scraper health instead of falsely reporting "healthy" when scrapers (like bet365) weren't running.

**Key Exchanges:**
- User called out that the dashboard was saying "healthy" when bet365 scraper wasn't even running — fundamentally dishonest health check
- Discovered `_bookie_status` dict existed on v3 DataStore but was never populated — no setter was ever called
- Found 3 push loops in v3/startup.py (OpticOdds, Bet365, Direct) that needed heartbeat callsites

**Decisions Made:**
- Staleness threshold set at ≥90s = FAIL (3 missed cycles for direct scrapers at 30s cadence), with 3-band coloring: <30s green, 30-89s yellow, ≥90s red
- 6 callsites added (3 success + 3 error paths) in v3/startup.py to populate bookie status
- New `/api/v1/health/sources` endpoint on Eve, wired through VPS proxy to dashboard
- `collectIssues` updated so stale scrapers trigger FAIL not WARN — stale scraper is critical
- Per-book OpticOdds sub-health deferred (OO pushes ~40 books through one SSE channel, would need per-(book_id, sport) timestamps)

**Lessons Learned:**
- `set_bookie_status` already existed via inheritance — checked before writing duplicate code
- v3 binds to Tailscale IP `100.69.199.85:8900` only (not localhost), important for endpoint testing
- Organic proof during deploy caught the system working: bet365_nba went from `age=100s, healthy=False` to `age=10s, healthy=True` after a real push — no fake test needed
- Dashboard health panels are only as honest as the data feeding them — empty `sources: {}` stub was silently passing

**Action Items:**
- Per-book OpticOdds health tracking (currently all ~40 books aggregated under `opticodds_{sport}`)
- Tracker stats still showing dashes — v3/pipeline/server.py doesn't expose cycle counters yet
- No history sparklines — point-in-time only currently
- Remaining v3 port punchlist: heartbeat fix in tracker.py, trail streamer port, native health endpoints to drop VPS transformation logic

### Session (12:54) [value-betting-scanner]

**Context:** Shipped Phase B-2 tick-driven trail loop for the v3 value-betting tracker on Eve VPS

**Key Exchanges:**
- Diagnosed why existing picks produced zero trail updates — the per-cycle `books` dict didn't retain soft-book entries for already-tracked picks, so `_alt_aware_origins_for_soft()` skipped them every cycle
- Heartbeat removal (commit `55ca05a`) was a red herring cleanup — it wasn't the bottleneck, the real bug was the Phase B gate
- End-to-end throughput test confirmed system handles 1000+ trails in ~3.5 min with headroom

**Decisions Made:**
- Removed heartbeat entirely (dead code producing synthetic rows that pollute backtest data) — pure stream-driven now
- Shipped Phase B-2 same night despite earlier hesitation, because the fix was scoped and testable
- Phase B-2 approach: iterate `_tracked_ids - inserted_ids` directly using cached `market_key` + `soft_book_id` to bypass the per-cycle books dict gate

**Lessons Learned:**
- "0 updated" every cycle + trail rows = 2× new picks was the diagnostic fingerprint — all trail writes were creation-only
- The heartbeat was masking the real architectural gap; removing it made the actual bug obvious
- Staging `market_key` + `soft_book_id` in Phase A and deriving them on cache load (via `normalize_player_name` + `normalize_prop_type`) was necessary for the post-B loop to work without the per-cycle books dict
- tmux sessions on Eve are fragile — needed re-launch after deploy

**Action Items:**
- Monitor overnight trail volume stability (watch for Supabase rate limits or memory growth)
- Commit `c812931` is on Eve — ensure it's pushed to remote
- Trail volume ~250-300/min sustained — revisit if Supabase free-tier I/O becomes the cap under heavier load (more theories, more sports)

### Memory Flush (13:05) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (13:14) [value-betting-scanner]

**Context:** Porting the trail streamer module to v3/Eve and discovering it's redundant with the tracker's Phase B2

**Key Exchanges:**
- User shared status from sister session: heartbeat removed from tracker.py (commit 55ca05a), now pure stream-driven. Quiet periods correctly produce zero rows.
- Explained what the streamer port would do: capture post-creation trail rows at event resolution (50-500+ rows per pick in active window vs 2 baseline rows)
- Discovered during deploy that `server/tracker.py` Phase B2 already does tick-driven soft trail writes inline — making the standalone `trail_streamer.py` module redundant

**Decisions Made:**
- Dashboard reshaped: streamer OFF now shows as FAIL (was silently "DISABLED"), pipeline node says "NO trail capture" instead of "heartbeat covers"
- Heartbeat artifacts cleaned from dashboard, tests, and brain docs
- Leaning toward Option A: disable standalone streamer, keep Phase B2 as canonical trail-write path (it's already proven working)

**Lessons Learned:**
- Two parallel sessions built overlapping solutions: sister session added Phase B2 inside tracker.py (stream-driven inline writes), this session built trail_streamer.py (separate subscriber). Both are stream-driven but redundant.
- Eve runs `v3/pipeline/server.py`, not `server/main.py` — endpoints/tasks wired into main.py don't exist on Eve without explicit porting
- `ENABLE_TRAIL_STREAMER` wasn't set on Eve — dashboard correctly flagged this once updated to treat OFF as FAIL
- Over-capture (both paths running) is safer than under-capture for backtest data — dedup catches duplicates

**Action Items:**
- Decide canonical trail-write path: Phase B2 in tracker vs standalone trail_streamer module (recommended: keep B2, disable streamer)
- Investigate why streamer's `events_matched` never reaches batch (window gate strictness or B2 winning the race)
- Monitor tonight's games to confirm Phase B2 produces backtest-quality trail data
- 6 of 15 scrapers borderline stale (90-99s) — worth monitoring

