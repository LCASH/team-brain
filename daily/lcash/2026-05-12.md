# Daily Log: 2026-05-12 (lcash)

## Sessions

## Memory Maintenance

### Memory Flush (11:14) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (11:51) [value-betting-scanner]

**Context:** Debugging why MLB picks showed 0 in the value-betting dashboard despite 10,000+ markets being delivered from Eve v3.

**Key Exchanges:**
- Server-side `/v1/picks?sport=mlb` returned 9,522 picks, but client-side `computeEVForTheory()` returned 0 for all 7 MLB theories — the problem was between SSE delivery and client rendering
- Python offline replication of dropout logic (bypassing flaky Playwright/AdsPower) revealed **93% of markets filtered as "live"** by `isGameLive(m)` — 9,275 of 10,000 markets
- Sampling `game_start` times showed 30 of 31 MLB fixtures had `game_start = 2026-05-08` (3+ days stale) despite `captured_at` being 4 seconds old

**Decisions Made:**
- Used offline Python script to replicate dashboard dropout logic rather than continuing to fight Playwright/AdsPower browser automation — much faster diagnosis
- Fixed `game_start` with chronological comparison (`incoming > existing`) rather than always-overwrite, preserving the preference for full-datetime over date-only strings
- Set cleanup interval at 10 min with `max_age_hours=6` — aggressive enough to catch stale games but won't prune pre-game markets loaded early

**Lessons Learned:**
- **Root cause was two compounding bugs in Eve `server/state.py`**: (1) Lines 285-290 never refreshed `game_start` once it was a full datetime — same `market_key` (player+prop+side) reused across different game days meant odds updated but `game_start` stayed stuck on first-ever observation. (2) `cleanup_stale_markets()` existed but was never called from v3's `startup.py` (only wired in v2's `main.py`)
- The `market_key` design (player+prop+side, no date component) means keys collide across different game days for the same player — any field that doesn't get refreshed will go stale
- When browser automation keeps failing, switching to pure Python data fetching + logic replication is dramatically more productive for diagnosis
- The vig sanity gate commits (0de77c5, 632627a, a560ef9) were a red herring — the real filter was `isGameLive`

**Action Items:**
- Deployed two fixes: `server/state.py` game_start chronological refresh + `v3/startup.py` periodic cleanup task (commit `ae61bfd`, merged to `89032bb`)
- Verified fix live: all 31 MLB fixtures now show current `game_start` (2026-05-12/13), eligible picks per theory went from 4 → 93-97
- Previously deployed: pill→URL slug + SSE reconnect (`7e53b6b`), layered health pill + empty state (`35d8bfe`)
- Consider adding date to `market_key` to prevent cross-day collisions entirely (would be a larger refactor)

### Session (12:30) [value-betting-scanner]

**Context:** Building and deploying an odds-coverage dashboard on Eve to visualize prop market coverage across all books.

**Key Exchanges:**
- Audited all 26 NBA player prop markets visible in bet365 wizard against v3's `EV_NAME_MAP` — 15 collected, 13 missing (1Q props, Points High/Low, FG Made/Attempted, Threes Attempted)
- Missing markets are present in wizard body but not keyed into `EV_NAME_MAP`; each is a one-liner to add
- Built a full coverage dashboard (`coverage.html`) served from Eve at `/dashboard/coverage.html` via a new generic static-file route in `server.py`

**Decisions Made:**
- Serve dashboard from Eve directly (not VPS) because VPS `/api/v1/odds` only has ~195 markets vs Eve's ~10k — Eve is the canonical data source
- Dashboard mirrors existing `picks.html` static-file pattern — added generic `/dashboard/{filename}` route
- Deferred adding the 13 missing bet365 markets pending audit of whether sharp pairs (Pinnacle/FanDuel) exist for them

**Lessons Learned:**
- Restarting v3 clears AdsPower Chrome Bet365 sessions — login flow blocks ~5 min per sport with `No BET365_USERNAME/PASSWORD set` then times out. Must manually log into bet365.com.au in both AdsPower profiles (`k1bdk50e` NBA, `k1c9tptw` MLB) then restart v3
- VPS state is not a passthrough to Eve; they maintain separate market sets — always use Eve's API for full coverage analysis
- `premws_not_ready` WS overlay warnings are expected and harmless — pre-game props don't push via WS, wizard HTTP fetch is canonical

**Action Items:**
- Manually log into bet365 in both AdsPower Chrome windows on Eve to restore Bet365 odds
- Audit which of the 13 missing bet365 markets have OpticOdds sharp pairs before adding to `EV_NAME_MAP`
- Consider adding 1Q props and FG/Threes Attempted markets if sharp pairs exist

### Memory Flush (13:03) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (13:35) [value-betting-scanner]

**Context:** Reverse-engineering the Betr (BlueBet) sportsbook API for the value-betting scraper — discovering endpoints, testing rate limits, and checking for WebSocket usage.

**Key Exchanges:**
- Discovered Betr runs on BlueBet platform with API at `https://web20-api.bluebet.com.au`
- Confirmed **no anti-bot protection** — plain JSON API, no browser/AdsPower needed at runtime (massive simplification vs bet365)
- Confirmed **no WebSockets or SSE** — 100% HTTP polling, no protocol reverse-engineering needed
- Throttle tested from Eve: **30+ QPS sustained with zero throttling** — the earlier 403s were caused by Python's default `Python-urllib/x.x` User-Agent being on a blocklist, not volume-based rate limiting

**Decisions Made:**
- Betr scraper will be pure HTTP (no AdsPower at runtime), targeting `book_id=910`
- NBA is the best target (16 player-prop categories); AFL also valuable (Disposals, Goals); **MLB is worthless on Betr** (zero player props)
- Must use a browser-like User-Agent header to avoid 403s (Python default UA is blocklisted)

**Lessons Learned:**
- Betr's "rate limiting" was actually UA-based blocking — `Python-urllib` UA triggers 403 regardless of volume
- Per-game endpoint (`/MasterEvent?MasterEventId=X`) only returns "Popular Markets" (game-level) — player props require a **separate per-category fetch endpoint that hasn't been discovered yet**
- Player prop markets may only open a few hours before game time on Betr (unlike bet365 which opens days ahead)
- `GroupLinks` in the MasterEvent response lists 16 prop categories with `GroupTypeCode` values, but the actual fetch pattern to load those categories is still unknown
- League IDs: NBA=39251, MLB=29431, AFL=43735

**Action Items:**
- Discover the per-category player-prop endpoint — need to capture network traffic when clicking a prop tab (e.g., "Player Points") on a game with live prop markets
- Pick a future NBA game ~3-6h pre-start so prop markets are open, then capture the URL via AdsPower/Playwright
- Write the Betr Python scraper (~300 lines) once endpoint is confirmed
- Test additional league IDs for NRL, Soccer, etc.

### Session (14:14) [value-betting-scanner]

The Betr scraper's skeleton — `discovery.py` + `mappings.py` — is done:

- `discovery.py` (160 lines): discovers games, expands all GroupTypeCode tabs to grab player props, converts CO milestone markets ("20+ Points") into canonical ScrapedOdds (Over/19.5), handles discovery → prop_detail → emit loop
- `mappings.py`: GroupTypeCode→canonical market name + CO↔line conversion logic

Next up: `push_loop.py` (the async loop that wires discovery into the V3 store). Let me write that now and wire it into startup.py.

**User:** wait

**User:** sorry i'm back, I just wanna make sure we're considering all the betr/bluebet stuff before we run forward. We should add bluebet first to the existing system before doing the all the new betr scraping stuff. its not a new book in the system, its already there

**Assistant:**You're right — Betr (book 910) is already in the system via OpticOdds. Let me check what's actually wired up and what state it's in before doing anything new.Good call — here's the full picture:

## Betr (910) — Already In The System

| Layer | Status | Details |
|---|---|---|
| **OpticOdds mapping** | ✅ Active | 3 slugs registered ("betr", "betr_australia_", "betr (australia)") → book 910 |
| **Dashboard** | ✅ Registered | Both `index.html` and `picks.html` have `910: "Betr"` |
| **Book classification** | ⚠️ **AU_SHARP** not SOFT | Lives in `AU_SHARP_BOOK_IDS`, NOT in `SOFT_BOOK_IDS` — so it feeds consensus devigging for AFL/NRL but the EV engine **never finds +EV picks against it** |
| **OpticOdds data flowing** | ✅ Yes | SSE consumer pulls Betr odds every cycle |
| **Direct scraper** | 🟡 Built but NOT wired | 344-line `v3/scrapers/betr/scraper.py` exists — never imported or started in `startup.py` |
| **Health monitoring** | ❌ None | Rolls into generic "opticodds" — no per-book breakdown |

## The Real Question

Betr odds are **already flowing through OpticOdds** — but earlier today we proved OpticOdds only gives us ~3% of Betr's actual markets (9 NBA selections vs hundreds). So we have two separate issues:

1. **Make Betr useful NOW** — should it be SOFT (bet against) or stay SHARP (consensus only)? If you want to find +EV on Betr, it needs to move to `SOFT_BOOK_IDS`.

2. **Coverage gap** — OpticOdds Betr feed is paper-thin. The direct scraper we just built would give 30-50× more markets. But you're right that step 1 is making the *existing* pipeline healthy first.

**What do you want me to do?**
- Move 910 from `AU_SHARP_BOOK_IDS` → `SOFT_BOOK_IDS` so the EV engine starts generating Betr picks?
- Check the health of what OpticOdds is actually delivering for Betr right now (live store query)?
- Wire up the direct scraper into `startup.py` alongside OpticOdds (with dedup)?

Or — did you mean the *other* books (TAB, TabTouch, Surge, etc.) are the ones to get healthy first, and Betr stays as sharp consensus?

### Session (14:16) [value-betting-scanner]

**Context:** Reviewing Unabated API security, bet365 WS parser architecture, and verifying EV calculation math across scanner theories.

**Key Exchanges:**
- Analyzed Unabated's JWT (RS256) security — user worried permissions could be tampered; explained why asymmetric signing prevents this and outlined the vulnerable patterns to avoid in their own dashboard (alg:none, HS256 weak secret, client-only permission checks)
- Pulled 1,729 lines from Unabated odds API — full access, no blurring. Player IDs are opaque (P39221 etc.), `people` dict empty, separate player lookup endpoint exists but wasn't hit
- Synthesized three bet365 findings docs to answer whether player props can be scraped without navigating to individual games — answer is yes via `OV_POPULAR_30_0` auto-subscribed global WS feed, but likely only featured/promoted props (subset of full game prop tree)
- Identified that I19 coupon format embeds player name directly in `PA.NA` as `"Player Over Line"` — fundamentally different from I99 wizard's two-pass roster pattern
- Walked through full manual EV calculation verification for the Aggressive theory on Anthony Edwards Rebounds+Assists Over 9.5 @ 1.87 — math checked out

**Decisions Made:**
- Keep I19 coupon parser separate from I99 wizard parser — different structures, don't try to unify
- For dashboard auth: gate data at the API layer (Supabase RLS), never rely on frontend-only blurring/hiding
- `OV_POPULAR_30_0` global feed worth a 60-line probe to measure coverage vs full I43 coupon

**Lessons Learned:**
- RS256 JWTs: payload is readable (base64) and modifiable locally, but signature verification with public key rejects tampered tokens — security is in the signing, not the encoding
- Bet365 WS `F` (full) frames on subscribe contain `NA=` records with player names — no HTTP coupon call needed for initial mapping; `U` (update) frames are skinny deltas referencing those IDs
- Logit interpolation formula for cross-line EV: `logit_adjusted = logit + β × (L_sharp - L_target)` with β=0.28; verified FanDuel row: 48.14% at 10.5 → 55.12% at 9.5

**Action Items:**
- Build `NBA_MG_TO_PROP` dict by dumping 100 unique `MG.NA` values from a real I19 response and classifying them
- Write 60-line probe to test `OV_POPULAR_30_0` coverage (count distinct players×markets vs I43 coupon for same game)
- Track down Unabated's player name lookup endpoint (player IDs currently unmapped)
- Verify power devig formula (used by Calibrated theory) — not yet validated

### Memory Flush (14:20) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Session (14:20) [value-betting-scanner]

**Context:** Integrating Betr sportsbook (book ID 910) into the value-betting pipeline — scraper, orchestrator, startup/shutdown wiring.

**Key Exchanges:**
- Built BetrOrchestrator following the existing bet365 pattern (discover → parse → push loop)
- Wired Betr into `startup.py`: import, `_betr_push_loop` function, spawn block gated by `ENABLE_BETR` env var, shutdown handling
- Added book ID 910 to `BOOK_ID_TO_NAME` and SOFT book sets
- Smoke-tested end-to-end: 285 odds across 11 prop types parsed correctly

**Decisions Made:**
- Used same architecture as bet365 stack (orchestrator + push loop pattern) for consistency
- Gated behind `ENABLE_BETR` env var so it can be toggled without code changes
- CO milestones mapped to Over at N-0.5 (e.g., "18+ Points" → Over 17.5 at odds=1.1)

**Lessons Learned:**
- `make_market_key` accepts `ScrapedOdds` directly — no conversion needed
- Betr prop type mapping produces 11 distinct prop types from their milestone format

**Action Items:**
- Deploy and monitor Betr scraper in production
- Verify odds flow through to dashboard and value calculations look correct

### Session (14:20) [value-betting-scanner]

**Context:** Reverse engineering Bookie Bashing's Early Payout Tracker API and attempting to compare odds with bet365.com.au

**Key Exchanges:**
- Reverse-engineered Bookie Bashing EP Tracker: uses `POST /app/auth.php` with `user_key`, `session_token`, `hash` tokens; data from `ep_ratings`, `ep_comps`, `selections` tables; paginated REST-like API with filter syntax
- Authentication IS required for BB data — no public access, tokens last ~14 days
- Attempted to scrape bet365.com.au for matching game odds — blocked by React SPA dynamic loading + Cloudflare WAF protection

**Decisions Made:**
- Named project files with `2up_` prefix (API docs, scraper, token guide) stored in project directory
- BB scraper built with async Python, pagination support (200 pages × 500 records), 0.5s rate limiting
- Acknowledged bet365 direct scraping is impractical via browser automation due to heavy SPA + WAF

**Lessons Learned:**
- BB API uses double-JSON-encoded strings in responses — needs special parsing
- bet365.com.au games load dynamically behind obfuscated React endpoints, making direct API scraping very difficult without full browser context
- BB "Early Payout" games are a specific promotional bet type — may not map 1:1 to standard bet365 match odds
- Alternative odds sources (OpticOdds, Betfair API) may be better for comparison than scraping bet365 directly

**Action Items:**
- Extract BB authentication tokens via DevTools to test scraper
- Set up automated token refresh (~every 7 days)
- Decide approach for odds comparison: OpticOdds/Betfair API vs solving bet365 scraping
- Match game names between BB and a second odds source to enable side-by-side comparison

### Session (14:20) [value-betting-scanner]

**Context:** Debugging and documenting the V3 value betting scanner — fixing dashboard issues, investigating MLB picks, and writing comprehensive system documentation.

**Key Exchanges:**
- MLB showing 0 picks was a timing race, not a data issue — 4917 markets and 20 EV picks exist once polling completes (~3-5s after sport switch)
- Full latency breakdown: NBA worst case ~23s, MLB ~38s from line move to dashboard update. Bottleneck is Bet365 CDP scraper cycle, not dashboard polling.
- Existing docs (ARCHITECTURE.md, SYSTEM_GUIDE.md) are outdated — describe V2 architecture. Neither reflects V3 single-server design.

**Decisions Made:**
- Wrote `V3_SYSTEM_GUIDE.md` as the canonical system doc covering: 3-machine layout, single-process architecture, data flow with latency, book IDs, EV computation, key files, VPS routes, Supabase tables, deployment commands, common issues
- Three bug fixes deployed: picks disappearing (90s retention window), MLB 0 picks flash (loading state on sport switch), red age badges (thresholds now 5min/15min)

**Lessons Learned:**
- `_lastSeen` timestamp pattern prevents transient scraper gaps from wiping picks — retain markets for 90s even if absent from a poll
- `_sportSwitchPending` flag + "Loading markets..." prevents the confusing 0-picks flash during sport transitions
- Bet365 login code (`v3/scrapers/bet365/login.py`) auto-logs in with human-like input or falls back to manual login polling every 5s if CAPTCHA hit
- Env files live in 3 places: `config/.env.v3` (mini PC V3), `.env` root (old V2), `/mnt/takeover_data/value-betting/.env` (VPS)
- zsh handles `[ -gt ]` differently than bash when variable is empty — caused a background polling task to fail (non-functional)

**Action Items:**
- Update CLAUDE.md project instructions module map section to reflect V3 architecture (still describes V2)
- Consider updating ARCHITECTURE.md to point to V3_SYSTEM_GUIDE.md or deprecating it

### Session (14:20) [value-betting-scanner]

**Context:** Reverse-engineering bet365 API architecture for player props delivery, then reviewing/correcting the findings document.

**Key Exchanges:**
- Ran Playwright probes against bet365 content APIs — guest `swt` sessions return `HTTP 200 / 0 bytes` from all content endpoints; the gate is auth (session cookie), not bot detection
- Confirmed 6 live NBA game IDs from WS subscription topics (`<eventId>C1A_30` format)
- Mapped the complete player props data flow: `betbuilderpregamecontentapi/wizard` (logged-in HTTP, one call per game, all 17 prop types) vs `matchmarketscontentapi/markets?D=43` (HTTP structure + WS DELTA odds)

**Decisions Made:**
- BB wizard is the correct approach for player props — one HTTP call per game returns all 17 categories with fractional odds and player names, no WS needed. Existing scraper architecture is validated.
- `playercontentapi/playerprops` (type `PPP`) is the Player Parlay popup endpoint, not the main props tab — bypassed entirely via BB wizard

**Lessons Learned:**
- `premws` is the auth/handshake channel; `pshudws` is the primary market data channel (this was backwards in the original findings doc)
- Coupon HTTP responses include initial odds snapshots (OC records) with a logged-in session — not "structure only" as previously documented
- Auth tokens (`A_` prefix) are session-stable, reused across sequential WS subscribes — not per-message rotation as initially thought
- xcftr token and Cloudflare pass fine for guest sessions; the content gate is purely the session cookie

**Action Items:**
- 11 factual errors corrected in `bet365_re_findings.md` (WS role reversal, token byte counts, stale probe notes, coupon description, token rotation model)
- Remaining open question (C-type for props on WS when viewing D=43 page) is academic — BB wizard approach is simpler and already working

### Memory Flush (14:20) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (14:20) [value-betting-scanner]

**Context:** Deep reverse engineering of bet365 AU's security stack, auth tokens, WebSocket protocol, and odds delivery system.

**Key Exchanges:**
- Full mapping of bet365's bot protection (Cloudflare BM fingerprinting — headless Chrome fails, non-headless passes)
- Decoded 3-tier auth token system: `swt` (144-byte session cookie), `SST` (188-byte site session token from sports-config API), `x-net-sync-term` (996-byte network sync token containing SST as payload)
- Discovered `xcftr` → `xcft<N>` CustomEvent chain for token generation — the responder module is dynamically loaded at runtime, not in static bundles
- Fully decoded WebSocket handshake format: `<4 control bytes>__time,S_<pstk>,A_<x-net-sync-term>\x00`
- Decoded WS message format: `\x14<topic>\x01F|<type>;<k>=<v>;...|` with `\x14` = INITIAL_TOPIC_LOAD
- Market subscription format: `<event_id>C<market_type>_<zone>` for receiving delta updates

**Decisions Made:**
- Non-headless Chrome CDP confirmed as the only viable approach (Cloudflare BM cannot be bypassed without real browser)
- Browser handles all token generation automatically — scraper doesn't need to replicate `xcftr` chain

**Lessons Learned:**
- **NBA is sport B18, NOT B12** (B12 = NFL/American Football) — critical sport ID mapping fix needed
- `swt` is a 45-byte binary blob, not a JWT — can't be decoded/forged
- `x-net-sync-term` and `swt` are different tokens (996 vs 144 bytes) despite both being session credentials
- `/playercontentapi/playerprops` (type=`PPP`) exists as a dedicated endpoint with no sport filter — potential direct access path
- Page descriptor routing format: `#AC#B18#C<comp>#D48#E<event>#`
- Two separate WS endpoints: `premws-pt1.365lpodds.com/zap/` (betting data) and `pshudws.z1.365lpodds.com/zap/` (in-play/config)
- Attempting to actively stream live odds or interact with the betting platform triggers Anthropic's usage policy restrictions (cyber content)

**Action Items:**
- Fix sport ID routing in scraper (NBA = B18)
- Probe `/playercontentapi/playerprops` endpoint directly for player prop odds
- Full findings report saved to `/tmp/bet365_re_findings.md`
- User attempted to action the findings but hit API policy blocks — may need alternative approach or model switch

### Session (14:20) [value-betting-scanner]

**Context:** Diagnosing why bet365 scrapers fail silently and 0 soft book odds reach the VPS dashboard

**Key Exchanges:**
- Discovered NBA server had been dead 5 days (since Apr 27), MLB server crashing repeatedly (ConnectionResetError from OpticOdds API), push worker dead since April 30
- Push worker being dead was the primary reason 0 soft book data reached VPS — it reads from all 4 sport servers and POSTs to VPS every 5s
- Dashboard showed "Waiting for soft book + sharp book data to match" because literally 0 Bet365 markets existed on VPS
- After restarting all services and running sync, VPS showed 6,269 OpticOdds markets but still 0 soft books — bet365 scraper not contributing data at all
- Deep code exploration of the v3 scraper pipeline: `_refresh_loop` in orchestrator, `bet365_game_worker.py` writing to `.bet365_game_live.json`, state.py merge logic, market key matching

**Decisions Made:**
- Added Discord alerting (`_send_discord_alert()`) to both `bet365_nba_v3.py` and `bet365_mlb_v3.py` for login failures — previously only logged errors silently
- Added `ALERT_WEBHOOK_URL` to `.env` (found from git commit 777b902)
- Created `scripts/sync_and_verify.sh` and `scripts/verify_mini_pc_sync.sh` for ongoing mini PC health management
- Created `MINI_PC_LAYOUT.md` documenting directory structure, env vars, sync process

**Lessons Learned:**
- `market_name` vs `prop_type`: API response uses `prop_type` field, not `market_name` — caused confusion during debugging
- `scanner-ms/` is the ACTIVE directory; `scanner-new/` is legacy/unused
- Port 8800 conflict is a recurring issue — old NBA process holds port, must `netstat -ano | findstr :8800` then `taskkill /PID` before restart
- PowerShell commands via SSH have escaping issues with `$_` — need simpler command forms
- Push worker has no auto-recovery — silently dies and stays dead indefinitely
- The v3 scraper `_refresh_loop` asyncio Task can die silently, leaving the orchestrator frozen with stale odds that main.py rejects

**Action Items:**
- Complete the bet365 scraper resilience plan (was being written when interrupted) — address why `_refresh_loop` Task dies silently, WS disconnects not escalated, and market key mismatches
- MLB server stability fix — keeps crashing with exit code 1 from OpticOdds connection errors, needs retry/backoff
- Auto-recovery for push worker and sport servers when they crash
- Investigate whether 0 +EV picks is a matching problem or genuine market condition (edges too tight at >2% threshold)
- Port 8800 automatic cleanup before NBA server restart

### Session (14:20) [value-betting-scanner]

**Context:** Reviewing value-betting scanner theories and manually verifying EV calculation math for NBA player props.

**Key Exchanges:**
- Explored Unabated API security (JWT RS256 signing prevents permission tampering — `alg:none` and client-side-only checks ARE vulnerable)
- Pulled 1,729 lines from Unabated odds API — full access, zero blurred entries, but player IDs are opaque (empty `people` dict)
- Deep-dived three bet365 WS research docs to extract architecture for getting player props WITHOUT navigating to individual games
- Identified that I19/I43 coupon format is structurally different from I99 wizard format — needs a separate parser, not an extension of `ws_nba.py`
- Walked through full manual EV calculation verification for the Aggressive theory on an Anthony Edwards Rebounds+Assists pick

**Decisions Made:**
- Two parsers, not one: I19 coupon parser should be separate from the I99 wizard parser — formats are fundamentally different (inline `PA.NA` parsing vs two-pass roster lookup)
- `OV_POPULAR_30_0` global WS feed is the path to props without per-game navigation, but likely only covers featured/promoted markets (needs a 60-line probe to confirm coverage vs full I43 coupon)

**Lessons Learned:**
- **Unabated auth:** `isBlurred` is server-side computed, not just a frontend flag — safe pattern. JWT with RS256 means payload is readable but not forgeable
- **Bet365 WS:** The `F` (full) frame on subscribe contains `NA=` records with player names inline — no separate HTTP coupon call needed for the global feed
- **I19 coupon PA.NA format:** `"LeBron James Over 29.5"` — regex `^(.+?) (Over|Under) ([\d.]+)$` extracts player/side/line. No "Player " prefix like wizard format
- **EV calc pipeline verified:** multiplicative devig → logit interpolation (β=0.28) for line gap adjustment → weighted average across sharps → EV% = (odds × p - 1) × 100
- **Logit interpolation formula:** `logit_adjusted = logit + β × (L_sharp - L_target)` where β=0.28 for continuous props; a 1.0 line gap lifts probability by ~7 percentage points

**Action Items:**
- Build and test the 60-line probe for `OV_POPULAR_30_0` F-frame coverage vs full I43 coupon
- Build `NBA_MG_TO_PROP` mapping dict (dump 100 unique `MG.NA` values from real I19 response, classify)
- Write separate I19 coupon parser function
- Verify the power devig formula (used in Calibrated theory) — user hasn't confirmed yet
- Manual calc verification of all four theories (Aggressive, Pinnacle Only, Calibrated, NBA Crypto Edge) — user indicated they want to do this

### Session (14:20) [value-betting-scanner]

**Context:** Deploying v4 WebSocket scrapers (NBA + MLB) to the VPS mini PC for the value betting scanner

**Key Exchanges:**
- User provided a battle-tested 9-step deployment plan with lessons learned from prior failures (SCP -r nesting, pycache staleness, schtasks not killing python, env var stripping)
- First deploy attempt failed silently — startup.py SCP didn't land, so v3 code loaded instead of v4. Had to re-SCP and restart a second time
- V4 successfully booted: both WS endpoints (`premws-pt1` + `pshudws.z1`) accepting subscribe injections for the first time, NBA wizard fetches returning 248-279KB bodies (bot detection bypassed)
- Discovery bug surfaced: Pass-2 PD-path fallback over-collects 14 futures/championship/draft E-IDs (D43/D47/D48/D706 segments) alongside the 4 real games

**Decisions Made:**
- Deploy now (option a) rather than wait until 6 PM AEST — production already mid-restart, v4 validated locally (1172 NBA / 592 MLB ODs), and overnight gives time for NBA prime-time US delta validation by morning AEST
- Kill PID directly (`Stop-Process -Id`) before `schtasks /End` — learned that schtasks alone doesn't kill the underlying python process

**Lessons Learned:**
- `scp -r` creates nested directories on Windows — always SCP files individually
- Stale `__pycache__` silently no-ops module reloads — must clear before restart
- `schtasks /End` does NOT kill the underlying python process — need `Stop-Process` first
- Always verify `StartTime` on the new python process to confirm new code actually loaded
- SCP can silently fail (startup.py didn't land on first attempt) — must `cat` or checksum the remote file post-SCP
- v4 page body size-gate fires false alarm on homepage body (41936 bytes) vs actual wizard fetch (248-279KB) — threshold needs tuning for v4's hash-nav entry page

**Action Items:**
- Fix discovery.py Pass-2 fallback: restrict to known match-D prefixes (D19 for NBA matchups) to filter out D43/D47/D48/D706 futures markets
- Tune v4 page body size-gate threshold (homepage vs wizard body distinction)
- Investigate NY@PHI wizard fetch failure ("no wizard body")
- Validate WS delta flow during actual tipoff window (overnight NBA prime-time US)
- Status doc written to `brain/findings/2026-05-07-v4-deploy-status.md`

### Session (14:20) [value-betting-scanner]

**Context:** Debugging why VB dashboard showed 0 MLB picks despite 10k+ markets reaching the browser

**Key Exchanges:**
- Full pipeline health confirmed: Eve v3 up (12k markets), VPS proxy OK, SSE delivering 10,491 MLB markets to browser, server `/v1/picks?sport=mlb` returning 9,522 picks — but `computeEVForTheory()` client-side returned 0 for all 7 MLB theories
- Initial suspicion was vig sanity gate from friend's commits (`0de77c5`, `632627a`, `a560ef9`), but actual root cause was stale `game_start` dates
- Offline Python reproduction (bypassing flaky Playwright/AdsPower) revealed **93% of markets filtered as "live"** — 30 of 31 MLB games had `game_start = 2026-05-08` despite fresh `captured_at` (4s old)

**Decisions Made:**
- Fix 1: `server/state.py:285-290` — changed game_start update logic to overwrite when incoming is chronologically later (was: never update once full datetime existed). Same `market_key` (player+prop+side) gets reused across days; odds updated but game_start stayed stuck on first-ever observation
- Fix 2: `v3/startup.py` — wired `cleanup_stale_markets(max_age_hours=6)` every 10 min. Function existed in state.py but was only called from v2's main.py, never from v3
- Defense-in-depth: game_start refresh prevents new staleness; cleanup catches anything that slips through within 6h+10min

**Lessons Learned:**
- `market_key = player+prop+side` collides across different game days for the same player — line is NOT in the key, and neither is game date. This is a fundamental design gap that caused silent data corruption
- Eve's conditional `game_start` update (`len > 10` check) was a premature optimization that backfired catastrophically — "prefer full datetime" logic prevented ANY refresh once set
- When debugging "0 picks" in a multi-layer pipeline, the issue may not be in the devig/EV logic at all — check upstream data freshness first
- Offline Python replication of client-side logic is far more reliable than browser automation for diagnosing filter/dropout issues
- Two earlier fixes also deployed: pill→URL slug routing (`7e53b6b`) and layered health pill with context-aware empty state (`35d8bfe`)

**Action Items:**
- Monitor cleanup loop effectiveness in logs (currently silent = nothing to clean, which is expected)
- Consider adding `game_date` or `fixture_id` to `market_key` to prevent cross-day collisions permanently
- Pinnacle Additive theory only produces 8 eligible picks vs 550-658 for other theories — may need separate investigation

### Memory Flush (14:47) [value-betting-scanner]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (15:22) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (15:54) [value-betting-scanner]

**Context:** Pivoting the v3 value-betting dashboard from edge-first to coverage-first, then diagnosing a critical freshness-stamping bug in the Bet365 tracker pipeline.

**Key Exchanges:**
- User clarified the dashboard goal is **coverage visibility**, not edges — led to full dashboard reorientation (KPI strip, per-book footprint bars, coverage-first presets, matrix heatmap)
- User flagged tracker might be flawed due to Bet365 scraper slowness — investigation confirmed a real bug: `_bet365_push_loop` overwrites `captured_at` with `time.time()` every 5s regardless of whether wizard data actually refreshed, making the 300s staleness gate a permanent no-op
- User raised the philosophical question: how to handle multiple soft books with different refresh rates without bottlenecking on the slowest one

**Decisions Made:**
- Dashboard reoriented: coverage metrics (markets, books, freshness) are now headline; edge/gap pushed to background (still accessible via "Data anomalies" preset)
- Bet365 is now the widest-coverage book (59.7%), Pinnacle only ~2% (missing player props) — this is a real finding worth investigating
- Architecture direction: move from "one global fresh state" to **per-(soft book × market) asynchronous time series** — each soft book gets its own clock, EV eval is per-book, no slowest-book bottleneck

**Lessons Learned:**
- `captured_at` overwrite in push loops is a subtle but critical bug — makes staleness gates meaningless; any push loop that re-stamps time without verifying data actually changed is lying about freshness
- Adding soft books becomes "free" if pick identity is `(market_key, soft_book_id)` — schema already supports this but the tracker doesn't enforce it
- Cross-book contemporaneity (soft vs sharp within 60s) is not checked — stale-soft + fresh-sharp creates biased EV reads

**Action Items:**
- **Phase 0 (immediate):** Fix `captured_at` in `v3/startup.py:776` — use wizard parse time instead of push loop time
- **Phase 2:** Add pair-time gate in `_compute_true_prob` — skip (market, soft) combos where `|soft.captured_at - sharp.captured_at| > 60s`
- **Phase 3 (architectural):** Re-key tracker to iterate `markets × active_soft_books` — per-soft-book pick lineage, trail entries, change detection
- **Phase 4:** Per-soft-book refresh latency view in coverage.html for observability
- Recommended sequence: observability first (d) → fix foundation (a) → pair-time gate (b) → full re-key (c) over ~1 week

### Session (16:27) [value-betting-scanner]

**Context:** Building OpticOdds CLV (Closing Line Value) backfill pipeline for the value betting scanner, plus fixing data-loss migrations in the tracker.

**Key Exchanges:**
- Confirmed OpticOdds **does** expose CLV data via `/fixtures/odds/historical` (previous investigation missed it) — each entry has `olv` and `clv` objects with `price` and `points`
- `/grader/odds` is a paid-tier auto-grader endpoint (403 on our key) — **not needed** because `server/resolver.py` already replicates the same logic with more control (DD/TD edge cases, void handling)
- Cross-validated OO Pinnacle CLV vs our devigged-blend CLV on 580 LAL@OKC picks: **8.07pp mean absolute gap**, with some sign-flips — expected since our blend gives Pinnacle only ~23% weight
- First 40 backfilled picks show a counterintuitive pattern: when Pinnacle moves *with us* (>+2%), win rate is only 36% vs 60% when flat — needs n>200 to conclude anything

**Decisions Made:**
- Use OpticOdds CLV as an **independent audit signal**, not a replacement for our devigged blend CLV
- Store `optic_close_pinnacle_price`, `optic_close_pinnacle_points`, `optic_clv_pct`, `optic_clv_direction`, `optic_lookup_at`, `optic_lookup_status` — 6 new columns (migration 034)
- Migration 033 adds four tracker audit columns that were silently being dropped: `pick_filter_reason`, `model_take`, `model_take_reason`, `extra_sharp_data`

**Lessons Learned:**
- OpticOdds CLV is raw Pinnacle-only (not devigged) — always compare *shape* of disagreement, not magnitude
- Two writers into `nba_tracked_picks` (tracker + news_agent/pick_writer.py) with independent column lists and separate strip-retry band-aids — architectural debt, no shared schema
- Match rate is only 40/1000 (4%) due to: fixture name mismatches (349), unmapped markets / no Pinnacle history (510), alt-line differences (101)
- PATCH error logging was swallowing all errors as `patch_fail += 1` — now buckets by cause (`http_400`, `http_403`, `exc_TimeoutException`, etc.)

**Action Items:**
- Wire backfill script into daily resolver cron (`python3 scripts/backfill_optic_clv.py --days 7`)
- Improve fixture matching (fuzzy team-name matching to recover 349 unmatched)
- Build dashboard divergence cell for picks where `|clv_pct - optic_clv_pct| > 5pp`
- Re-run ROI bucket analysis monthly once n>100 per bucket
- Longer term: unify the two `nba_tracked_picks` writers behind a shared schema validator

### Memory Flush (21:29) [value-betting-scanner]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Session (23:53) [value-betting-scanner]

**Context:** Deploying native AU sportsbook scrapers (TAB, TabTouch, Betr) to Eve VPS to replace OpticOdds dependency for player props coverage.

**Key Exchanges:**
- TAB (908) confirmed: no WebSocket for sports — the `push.beta.tab.com.au` engine.io endpoint is for live racing tote only. REST scraper via `curl_cffi` is the correct path.
- TabTouch (909) WS protocol fully decoded: `wss://push-ap.offering-api.kambicdn.com/socket.io/` — no auth needed, subscribe to `v2018.rwwawa.en.ev.json` firehose. Odds are integer × 1000. `mt=6` = betOffer ADD, `mt=11` = outcome odds update.
- TabTouch WS firehose only carries **live in-play events** — pre-game NBA player props don't flow through it. Discovered during a 3-min smoke test at midday AU (= early morning US, no NBA live).
- Coverage dashboard (`coverage.html`) was missing book IDs 909, 910, 960, 961 from `SOFT_BOOK_IDS` and `BOOK_PRIORITY` — fixed and deployed.

**Decisions Made:**
- Go fully native for AU books instead of relying on OpticOdds — OpticOdds doesn't have all player props (user's explicit requirement)
- Betr discovery cadence bumped from 60s to 5 min to avoid 429 rate-limiting
- TabTouch pre-game NBA deferred (~2-4h work) — needs either Kambi REST cookie warmup via Playwright or per-event subscribe topic pattern; best done when NBA is actually live

**Lessons Learned:**
- `SIGTERM` doesn't reliably kill the v3 process — `SIGKILL` needed. Multiple restarts caused duplicate processes which had to be manually cleaned
- Betr 429s happen easily on rapid restarts because discovery fires immediately at boot; the 5-min throttle prevents this
- Eve runs Python 3.12 vs local 3.14 — AST check needed before restart to catch syntax incompatibilities
- TabTouch WS confirmed working for live MLB (24 fresh of 758 markets once games went in-play)

**Action Items:**
- TabTouch pre-game NBA: implement Kambi REST `listView` seeded by Playwright cookie warmup to get pre-game player props (documented in `brain/findings/2026-05-12-tab-tabtouch-ws-recon.md`)
- Investigate OpticOdds NBA sharps going stale (DraftKings/FanDuel/Hard Rock/Pinnacle showing near-zero fresh) — likely schedule-based but worth confirming
- Monitor Betr 429 recovery after restarts — current 5-min discovery cadence appears sufficient

