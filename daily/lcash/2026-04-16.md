# Daily Log: 2026-04-16 (lcash)

## Sessions

## Memory Maintenance

### Session (11:56) [value-betting-scanner]

**Context:** Diagnosed and fixed NBA bet365_game scraper going dark on the mini PC, then verified all prior work artifacts are preserved in memory.

**Key Exchanges:**
- Traced bet365_game missing from health endpoint through multiple red herrings: initially suspected multiple `server.main` processes (orphans), but they were 3 legitimate sport servers on different ports
- Discovered `nba_out.log` was from a **dead older process**, not the current PID 8724 — the real log was `server.log` showing only OpticOdds activity
- Root cause: watchdog (or equivalent) relaunched NBA server with bare `cmd /c python -m server.main` — no env vars (`ENABLE_BET365_GAME`, `ENABLE_DIRECT_SCRAPERS`, `ENABLE_BETSTAMP` all unset), so 7.2 hours of scraping with no bet365 data

**Decisions Made:**
- Kill-and-relaunch via `schtasks /Run /TN NBA_Server` (uses `start_nba.bat` which sets env vars correctly) rather than manually setting env vars on the running process
- Kill 15 orphaned Chrome processes tied to `bet365_game_profile` from even older sessions before restarting
- Stored 4 new memory entries covering WS probe verdict, coupon endpoint feature, Pinnacle feature, and watchdog env-var stripping

**Lessons Learned:**
- **Watchdog strips env vars on restart** — when the watchdog relaunches a process with a bare command instead of the batch file, all `ENABLE_*` flags are lost. Root fix = update `watchdog.py` to use the batch file
- **Always check which log file maps to the current PID** — `nba_out.log` vs `server.log` confusion wasted investigation time; the process's parent command (`cmd /c ... > server.log`) was the giveaway
- **`set_bookie_status()` being called in logs doesn't mean it's visible** — if you're reading a stale log from a dead process, the state isn't in the live AppState

**Action Items:**
- Morning health check one-liner saved in `squishy-honking-acorn.md` — run to confirm bet365_game still streaming
- Root fix: update `watchdog.py` to relaunch via batch file instead of bare command (not yet done)
- Feature branch `bet365-nba-coupon-endpoint` (3 commits) ready to deploy
- Pinnacle commit `9a0b19d` on main waiting for separate deploy window
- MLB scraper refinement uncommitted in working tree
- Yes/no parsing and quarter props still TODO on coupon endpoint

### Session (12:26) [superwin]



**Context:** Deploying the multi-fixture bet365 streaming system from local Mac test to Dell + VPS production

**Key Exchanges:**
- Ran the bet365 streaming system locally (Mac, no AdsPower) to validate multi-fixture runner map building across all 13 AU/NZ meetings
- Local test captured 502 participants, 24 fixtures, 13 meetings with 198/502 named (39%)
- Deployed to VPS and Dell in parallel after local validation succeeded
- VPS ingest endpoint wired up with INGEST_TOKEN, health check confirmed live
- Dell came up with 484 participants, 25 fixtures, 12 meetings, 221 named (44%)

**Decisions Made:**
- Deploy to both VPS and Dell simultaneously rather than sequentially — parallel deployment
- VPS ingest pipe architecture: Dell streams → HTTP POST to VPS `/api/v1/ingest/bet365` → TAKEOVER SSE
- Runner naming at ~40-44% on first pass; remaining runners get names on re-discovery cycles

**Lessons Learned:**
- AdsPower timeout adds ~9s before Chrome-via-CDP fallback kicks in — expected delay on both Mac and Dell
- Multi-fixture iteration (13 meetings × 5-8 races × 3s each) takes 2-3 minutes to build full runner map
- Dell found 12 meetings vs Mac's 13 — slight variance is normal depending on timing of meeting availability
- `build_runner_map` was patched to iterate all fixtures per meeting (not just first) — critical for full race coverage

**Action Items:**
- Build HTTP pusher module inside `bet365_stream.py` streaming loop to POST live odds to VPS every few seconds (Dell → VPS → TAKEOVER SSE pipeline)
- That pusher is the final piece to close the full pipeline loop

