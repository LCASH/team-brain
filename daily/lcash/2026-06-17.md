# Daily Log: 2026-06-17 (lcash)

## Sessions

## Memory Maintenance

### Session (00:11) [social-images]

**Context:** Generating and refining Knowted ad creative images (content set of 6 images across 4 avatars)

**Key Exchanges:**
- Added Google Meet grids to laptop screens in the three "call" scene images (sell, relax, stress) to make the "meeting being captured" use case unmistakable
- Decided Meet doesn't belong on promised (reflective, not mid-call) or student/free (in-person lecture)
- Identified skewed/garbled logo problem — nano banana redraws the logo instead of reproducing it pixel-perfectly

**Decisions Made:**
- **Deterministic logo compositing over AI rendering:** Stop letting the image model draw the Knowted logo. Instead, generate scenes with the top-left clean, then composite the real logo PNG on top using puppeteer. This guarantees pixel-perfect logos every time vs. hoping the model gets it right.
- **Google Meet only on call scenes:** stress, relax, sell get Meet grids; promised stays dark/reflective; student + free stay in-person lecture context.

**Lessons Learned:**
- Image models (nano banana) can render well-known UIs like Google Meet grids surprisingly well, but struggle with brand logos — they simplify/skew the icon even when given a reference image
- The "generate 2, pick clean" workflow catches text garbling and other artifacts reliably
- Passing a logo as a reference image helps the model "know" it but doesn't prevent re-drawing artifacts — deterministic post-processing (compositing) is the only reliable fix

**Action Items:**
- Validate the new puppeteer logo-overlay pipeline on stress image, then regenerate the full set of 6 with real composited logos
- Still pending: organic captions (post-body copy) for the six images, and potentially generating more lines from the copy bank

### Session (00:47) [Knowted admin]

**Context:** Knowted content pipeline — finalizing ad images with real logo overlay, brand copy guide PDF, and status check on raise/P0s.

**Key Exchanges:**
- Completed 6 post-ready images (stress, relax, sell, promised, student, free) with real logo via new `brand-overlay.js` pipeline — AI no longer draws the logo, it's composited with Puppeteer
- `free-v1` had a text glitch ("Record" rendered twice), so `free-v2` was the keeper
- Rendered `copy-bank.md` → `Knowted-Brand-Copy-Guide.pdf` (5 pages, branded) and placed in Downloads
- User asked about `copy-bank.md` status — confirmed intact, only change was moving "If it was said, Knowted noted it." to Retired list
- Status debrief: runway at 0.0 months, 3 of 4 P0s stalled on sends (Jeffrey re-chase, June investor update, Danny touch), not on drafts

**Decisions Made:**
- Logo is now always composited post-render via `brand-overlay.js` (never AI-drawn) — permanent fix for skew
- Recommended against activating full agent team since bottleneck is unsent drafts, not missing work
- Suggested a focused Drive-enabled agent run (IR + Finance + Sales) since Google Drive was newly connected

**Lessons Learned:**
- AI image models can't reliably render logos — composite them programmatically after generation
- `free-v1` text glitch ("Record" x2) is a recurring AI text-rendering issue; always generate variants
- §1 of copy bank drifted from actual built lines (two of six came from §5 avatar hooks, not §1 hero) — need to promote winners per §9 rule
- PDF exports are snapshots; need re-render after any copy-bank edits to stay in sync

**Action Items:**
- User to send: Jeffrey re-chase, June investor update, Danny retention touch, schedule 30 min with Jeffrey
- Promote 6 built lines into §1 of copy bank + re-render PDF
- Write organic captions for the six images (not yet started)
- Wire waitlist endpoint (user action)
- Optional: focused Drive-enabled agent run to set up data room + CRM in Google Drive

