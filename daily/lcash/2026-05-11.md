# Daily Log: 2026-05-11 (lcash)

## Sessions

## Memory Maintenance

### Session (08:49) [value-betting-scanner]

ran two health checks (Eve direct + VPS/dashboard) plus a sequential bandwidth test.

**Results:**

| Layer | Status | Detail |
|---|---|---|
| Eve v3 | ✅ | 1d 19h uptime, PID 74818, RSS 1.1 GB (~9 MB/hr drift — flat), 0 errors, pulse 1.63 M |
| Markets | ✅ | 11,756–11,762 across NBA + MLB |
| Tracker | ✅ | REST seeds + theory refresh every 5 min, calibration cells live |
| AdsPower / Chromium | ✅ | Both profiles firing wizards |
| Tailscale Eve↔VPS | ✅ | Cross-machine ping working |
| VPS service | ✅ | `mini_pc_ok=true`, `mini_pc_markets=11,762` |
| SPA routes | ✅ | `/nba`, `/mlb`, `/pinnacle`, `/crypto` all 200 |
| `/api/v1/odds` cache | ✅ | Server-side single-flight proven (40–270 ms warm). Client-side "slow" = bandwidth (17 MB ÷ ~700 KB/s ≈ 25 s) |
| All session fixes | ✅ | Sport-routing, thin-trail empty state, sparkline noise, SSE-leak, page-leak, byte cache, single-flight |

**One known non-urgent item:** The 17 MB initial JSON snapshot is slow for users on limited bandwidth. Fix = gzip middleware (6–8× compression expected). Not blocking anything.

**Bottom line: fully healthy, nothing broken.**

**User:** give me a rundown

**Assistant:**# Value Betting Scanner — Full Rundown

## What You Have Running

A **live odds aggregation pipeline** running 24/7 on Eve (your mini PC), serving a dashboard via VPS.

### Architecture

```
Bet365 (wizard scraper)  ──┐
                           ├──▶  Eve v3 (FastAPI :8900)  ──▶  VPS (:8803)  ──▶  Dashboard
OpticOdds (sharp books)  ──┘         │                         │
                              DataStore (in-memory)      SPA + SSE stream
                              = one centralized JSON     /nba /mlb /pinnacle /crypto
```

### The Centralized JSON (what you cared about most)

`GET /v1/odds?sport=mlb` returns **every market with every book's quote side-by-side**:

- **One dict per market** — keyed by `market_key` (player + prop + side)
- **All books in `.books{}`** — Bet365 (365), FanDuel (100), DraftKings (200), Novig (192), Hard Rock (803), Kalshi, Polymarket, Bovada, etc.
- **Same schema everywhere** — `line`, `odds`, `captured_at` per book
- **Live updating** — NBA every 30s, MLB every 60s, sub-second WS deltas for in-play

### MLB Wizard Refactor (just shipped)

**Before:** 26 G-ID fetches/game × 14 games = **364 requests/min** → rate-limit cascades
**After:** 1 wizard fetch/game × 14 games = **14 requests/min** → **26× drop**, zero rate limits

### Current Numbers

| Metric | Value |
|---|---|
| Uptime | 1d 19h+ |
| Total markets | 11,762 |
| Bet365 MLB markets | 1,358 (34% coverage) |
| Memory | 1.1 GB RSS (~9 MB/hr growth — flat) |
| Errors | 0 |
| Pulse | 1.63M |
| Data freshness | NBA ≤30s, MLB ≤60s, in-play <1s |

### Books Ranked by MLB Coverage

1. DraftKings — 2,546 markets
2. FanDuel — 1,977
3. Hard Rock — 1,376
4. **Bet365 — 1,358** ← your soft book target
5. Novig — 1,216

### What Downstream Consumers Do

The EV engine reads this centralized JSON and:
1. Pairs sharp Over/Under quotes (FanDuel/Novig/DraftKings)
2. Devigs → true probability
3. Compares to Bet365's odds → emits EV%
4. Tracker writes qualifying bets to Supabase

### Deployment Path

- **Deploy guide written** — `DEPLOY_GUIDE.md` with copy-paste rsync + restart commands
- **MLB wizard refactor committed locally** — ready to ship to Eve whenever
- **Not yet deployed** — Eve is still running the previous version (which is healthy)

### Only Known Issue

17 MB initial JSON payload is slow on limited bandwidth (~25s on 700 KB/s). Fix = gzip middleware for 6-8× compression. Not urgent since dashboard uses SSE streaming (small deltas), not repeated full polls.

### Session (08:50) [value-betting-scanner]

**Context:** Debugging and fixing the v3 value-betting dashboard pipeline — sport routing, trail charts, cache performance, and creating a reusable health-check skill.

**Key Exchanges:**
- Diagnosed `/mlb` URL showing NBA picks — root cause was `currentSport` hardcoded to `'nba'` for non-localhost; fix reads `window.location.pathname` and matches against `SPORT_INSTANCES`
- Diagnosed "click 📈 does nothing" — picks with only 1 trail snapshot made `tMin === tMax`, collapsing all points; fix detects timestamp span <30s and shows explainer text
- Diagnosed `/api/v1/odds` timing out — concurrent cold-cache requests (5×14MB) saturated the event loop; fix was adding single-flight dedup so only one upstream fetch runs at a time
- Merged friend's 200-line index.html changes + lab.html without conflicts

**Decisions Made:**
- Single-flight dedup on VPS cache is necessary (was removed earlier, had to be re-added) — without it, concurrent cold-cache requests starve the event loop
- 17MB payload slowness on client side is bandwidth, not a bug — gzip middleware would help but not urgent since dashboard uses SSE for real-time, not repeated `/api/v1/odds` polls
- Created `/VB-V3-Healthcheck` skill with three modes: full sweep, quick (Eve+VPS only), cache-only

**Lessons Learned:**
- `_trailChartCache` was `const` in local scope, not on `window` — probing `window._trailChartCache` was watching a non-existent global (wasted debugging time)
- When parallel requests all hit a cold cache, they all fetch upstream simultaneously — single-flight pattern is essential for large payloads
- Eve RSS growth ~9 MB/hr is stable/flat — 1.1 GB after 2 days is normal
- `premws_not_ready` warnings in AdsPower logs are expected pre-game behavior, not errors

**Action Items:**
- Consider adding gzip middleware to VPS for the 17MB `/api/v1/odds` payload (6-8× compression expected)
- Monitor Eve RSS growth rate — currently healthy at ~9 MB/hr

### Memory Flush (09:11) [superwin]

FLUSH_OK - Nothing worth saving from this session

### Session (09:42) [value-betting-scanner]

**Context:** Debugging v3 value-betting dashboard showing 0 picks despite healthy pipeline; deploying UX improvements for connection diagnostics.

**Key Exchanges:**
- Full pipeline health confirmed: Eve (2d14h uptime, 12k markets), VPS (`mini_pc_ok=true`), SSE chain (61s to Live), server-side `/v1/picks?sport=mlb` returns 9,522 picks (7,459 above 5%)
- Root cause of "no picks" display: `computeEVForTheory()` drops all 10,491 MLB markets client-side — likely the vig sanity gate (commits `0de77c5`, `632627a`, `a560ef9`) incorrectly drops markets where books have different lines in the same `market_key`
- Specific data example: Maikel Garcia Hits Over — Bet365 at L=0.5 while sharps at L=1.5 in same key (line-NOT-in-key design causes cross-line mismatch)

**Decisions Made:**
- Created `/VB-V3-Healthcheck` skill (`~/.claude/skills/VB-V3-Healthcheck/SKILL.md`) with full/quick/cache modes
- Deployed persistent 4-segment health pill (`● VPS ● Eve ● mkts ● picks`) with color-coded layer status
- Deployed context-aware empty-state messages (loading/no-picks/VPS-down/Eve-down each get distinct copy)
- Fixed pill click → `switchSport()` now updates URL slug + resets markets + reconnects SSE with `?sport=` param + popstate listener for back/forward

**Lessons Learned:**
- Dashboard's "Waiting for soft book + sharp book data" message was identical for every empty state (SSE disconnected, loading, no picks) — always differentiate failure modes in user-facing status
- Browser cache (Chromium) served stale HTML after VPS deploy — needed force-bypass to verify changes
- `computeEVForTheory()` returns an array, not `{picks}` — tripped up diagnostic code
- 5 of 7 MLB theories are `candidate-blend` (server-side only, not client devig) — only 2-3 traditional theories run client-side

**Action Items:**
- Fix MLB 0-picks bug: inspect vig sanity gate around lines 2300-2335 of `v3/dashboard/index.html` — the `devig()` call drops markets where Bet365 line differs from consensus sharp line
- Use browser console diagnostic: look for `[EV] dropout` log from `computeEVForTheory` (line ~2370) to confirm which gate kills the 10k markets
- Three hypotheses to test: (1) vig gate cross-line mismatch, (2) `findPair(m.market_key)` returning null from naming changes, (3) `softStale` dropping >5s old data
- Commits deployed: `35d8bfe` (health pill + empty state), `7e53b6b` (URL/SSE fix)

### Memory Flush (10:02) [Knowted admin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (10:02) [Knowted admin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (10:02) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (10:02) [Knowted admin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (10:02) [scraper]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (10:02) [superwin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (10:02) [Knowted admin]

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (12:05) [Knowted admin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (12:05) [Knowted admin]

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (12:05) [Knowted admin]

FLUSH_OK - Nothing worth saving from this session

### Session (12:06) [Knowted admin]

**Context:** Writing/refining cold outreach messages for Knowted (AI sales coaching tool) targeting high-ticket sales team operators hiring closers/setters.

**Key Exchanges:**
- User shared 6 working outreach templates that have been getting responses — sent to operators in functional medicine, marketing agencies, real estate, and consulting
- User wants a new consolidated template created from these proven messages, replacing the default Facebook/LinkedIn suggested messages which "are shit"
- Video walkthrough is longer than 60 seconds (previous messages said "60-second video" — needs correcting)

**Decisions Made:**
- Outreach strategy: lead with a research question framing ("not a pitch"), identify the specific pain of diagnosing rep underperformance, position Knowted as compressing the diagnostic-to-coaching loop
- Differentiation angle: Fathom/Zoom recordings tell you *what was said*; Knowted tells you *why* one rep converts and another doesn't (pattern detection across the team)
- CTA: offer to send a video walkthrough, low-pressure opt-out ("if not, all good")

**Lessons Learned:**
- The messages that work all follow the same skeleton: (1) context hook tied to their hiring post, (2) pain question about time-to-diagnose + time-to-fix, (3) "we're building X to compress that loop," (4) concrete contrast vs. Fathom, (5) soft CTA
- Personalizing the dollar amount of their offer and the specific role (setter vs. closer) increases relevance
- "Quick research question, not a pitch" framing disarms and gets replies

**Action Items:**
- Create a new universal outreach template (adaptable to setter/closer/industry) based on the proven structure
- Update CTA to remove "60-second" claim since the video is longer
- Replace default platform-suggested messages with the new template

### Session (12:06) [Knowted admin]

**Context:** Building and deploying cold outreach messages for Knowted via Facebook DMs to sales managers

**Key Exchanges:**
- User rejected initial 10 long-form messages (130-180 words) as "too short and too casual" — then provided their own 7 shorter, punchier variations that were actually more direct and concise
- Assistant proposed 3 additional messages (#8 case study, #9 theory hook, #10 audit offer); user confirmed #8 and #9 only, giving 9 total variations

**Decisions Made:**
- Final outreach set is 9 variations (not 10) — user's own 7 + assistant's #8 and #9
- Tone: direct, no fluff, short (50-100 words), ends with "Cheers, Luke"
- YouTube link used across all: `https://youtu.be/V9osqt07ZPs`
- Output saved as `final scripting.rotated.csv` alongside original to preserve the source file
- Rows already marked `Contacted FB = Y` (70 rows) left untouched; remaining 1524 rows got rotated messages

**Lessons Learned:**
- User's own copy was shorter and more effective than the AI-generated "longer" versions — less is more for cold DM outreach
- CSV had multi-line quoted cells that caused line-count discrepancy vs row count — verify with row count not `wc -l`

**Action Items:**
- User to sanity-check `final scripting.rotated.csv` and rename/import when ready
- Original `final scripting.csv` preserved as backup

### Session (12:06) [Knowted admin]

**Context:** Building a Facebook group scraper targeting `highticketclosersusa` to find closer-hiring posts for outreach

**Key Exchanges:**
- Debugged CDP connection issues with AdsPower browser (port rotation, hanging scripts)
- Discovered `role="article"` captures both posts AND comments — root cause of junk data (37 items, mostly "DM'ed you!" replies)
- Found `data-ad-rendering-role="story_message"` is the correct discriminator for real top-level posts — jumped from 0 to 89 real posts

**Decisions Made:**
- Selector strategy: iterate from `[data-ad-rendering-role="story_message"]` not `[role="article"]` — eliminates comment noise at the DOM level
- Multi-tier classifier: HIRING_CLOSER, HIRING_BOTH, OTHER_CLOSER, HIRING_SETTER, SEEKER, OTHER — distinguishes closer hiring from setter-only roles
- Scroll exit logic: 8-round stale streak (no new posts) triggers "feed exhausted" — user wants this bumped with more variation/randomness and target raised to 300 posts
- CSV sorted by intent priority so closer-hiring posts float to top

**Lessons Learned:**
- FB group numeric ID (`524440572541344`) and vanity slug (`highticketclosersusa`) are interchangeable — the scraper storing numeric IDs is correct, not a bug
- FB's virtualizer lazy-loads posts — aggressive fast scrolling can exhaust the feed prematurely; slower, varied scroll patterns with occasional scroll-back may coax more content
- Small false-positive leak in classifier: "looking to break into closing" phrases misclassified as HIRING_CLOSER — seeker language needs negative keyword pass

**Action Items:**
- Add scroll variation: random wait times, occasional scroll-up-and-back, longer pauses — target 300 posts
- Output clean list of poster profile URLs from hiring posts
- Consider tightening classifier to catch seeker false positives (Isis Nicole, Perpetual Pemu cases)

